
# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# Python bytecode
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/

# Python virtualenvs (bare pattern also catches symlinks, not just dirs)
.venv
.venv/

# Linked git worktrees created for agentic work (never commit their contents)
.claude/worktrees/
