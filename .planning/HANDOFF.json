{
  "version": "1.0",
  "timestamp": "2026-04-27T14:00:00Z",
  "context": "vault-importer-debugging",
  "status": "paused",
  "in_flight": {
    "task": "dry-run sweep against real vault with qwen3.6-35b-a3b classifier",
    "sweep_id": "2026-04-27T13:55:18Z",
    "report_path": "ops/sweeps/dry-run-2026-04-27T13-55-18Z.md",
    "model": "qwen3.6-35b-a3b",
    "vault_size": "201 notes in scope",
    "expected_completion": "~20-30 min from 13:55 UTC",
    "next_action": "When report file appears in vault, read it and review the proposed_moves. If quality is good → run real sweep (:vault-sweep, no dry-run). If garbage → debug further."
  },
  "guardrails_added_today": [
    "Spec-Conflict Guardrail (CLAUDE.md) — read PROJECT.md+REQUIREMENTS.md before opening AskUserQuestion; label options that deviate from validated requirements",
    "Test-Rewrite Ban (CLAUDE.md) — shipped-feature tests are read-only by default; no silent weakening",
    "Behavioral-Test-Only Rule (CLAUDE.md) — banned echo-chamber tests; tests must call the function and assert observable behavior",
    "Information-vs-Command Rule (CLAUDE.md) — operator statements are context, not directives"
  ],
  "shipped_today": [
    "260427-5kl: litellm helpers DRY consolidation (acompletion_with_profile to sentinel_shared, ResolvedModel + resolve(), strip_litellm_prefix consolidated)",
    "260427-vl1: note import + vault sweeper feature with classifier, inbox, admin gate",
    "Sweeper: TDD-added misplaced-note relocation + dry_run mode",
    "Sweeper: dry_run runs async, report written to vault file (not sync HTTP)",
    "Mistral/Llama profile bug fix: removed [INST]/[/INST] from stop_sequences (template delimiters, not stop tokens)",
    "MODEL_PREFERRED now pins all task kinds (chat/structured/fast), not just chat",
    "note_classifier: read reasoning_content fallback for qwen3 thinking-mode (LM Studio bug #1773)",
    "Chat path: temperature=0.4 pinned, system prompt rewritten to anchor friend-mode",
    "3 echo-chamber tests deleted (numpy_importable.py, two assertions in test_llm_call_shared.py)",
    "Hookify rule: blocks the if \"/\" not in <model_var> bug pattern"
  ],
  "external_state": {
    "lm_studio_cache_py_patched": "~/.lmstudio/extensions/backends/vendor/_amphibian/app-mlx-generate-mac14-arm64@22/lib/python3.11/site-packages/mlx_lm/models/cache.py — added tree_reduce to mlx.utils import. WILL BE WIPED ON LM STUDIO UPDATE. Backup at .bak-tree-reduce-fix.",
    "model_preferred_env": "qwen3.6-35b-a3b (cydonia is loaded but unused — has tree_reduce-fixable MLX bug AND infinite-digit-loop on json_schema constraint)"
  },
  "open_issues": [
    "Cydonia produces infinite-digit loops on confidence field with json_schema (model behavior, not LM Studio bug)",
    "qwen3_5_moe arch not in FAMILY_PROFILES (substring fallback to qwen2 works but should add proper entry)",
    "Sweep report counter bug: header shows 'topic_moves: 0' but section lists 200+ — topic_moves only increments on execute, not propose",
    "TODO if qwen3.6 dry-run report looks good: run real :vault-sweep to actually relocate notes"
  ],
  "next_steps_in_priority_order": [
    "1. Read ops/sweeps/dry-run-2026-04-27T13-55-18Z.md when sweep finishes",
    "2. If proposed moves look sensible: trigger live :vault-sweep (no dry-run flag) to execute",
    "3. After live sweep: review ops/sweeps/{date}.md and the relocated notes",
    "4. (Backlog) fix the topic_moves counter bug in dry-run reporting",
    "5. (Backlog) add qwen3 family entry to FAMILY_PROFILES",
    "6. (Backlog) re-test cydonia with thinking-disabled prompts when MLX patch survives — currently blocked on json_schema infinite-digits bug"
  ]
}
