# Forensic Report

**Generated:** 2026-06-11T13:31:28Z
**Problem:** The `gsd-planner` subagent reported "gsd-tools is not installed in this environment, so SDK validate/commit steps were skipped" — git commits were deferred to the orchestrator at plan-phase §13d.

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-06-11 09:20:09 -0400 — "docs(39): create phase plan" (`d8fdd16`)
- **Recent planning commits:** `a11438b` (39-CONTEXT from ADR ingest), `c0848fc` (39-VALIDATION scaffold), `d8fdd16` (3 PLAN.md + RESEARCH + PATTERNS + STATE + ROADMAP)
- **Uncommitted changes:** yes — `.planning/phases/39-extract-the-recall-module/39-VALIDATION.md` (planner filled the Nyquist scaffold after it was committed empty); plus pre-existing `.planning/REQUIREMENTS.md` and `sentinel-core/uv.lock`
- **Active worktrees:** 1 (main only — no orphans)

### Planning State
- **Current milestone:** v0.5.1 — The Second Brain
- **Current phase:** 39 — Extract the Recall Module — Planned (ready to execute)
- **Last session:** Phase 38 UAT complete (10/10 PASS)
- **Blockers:** none

### Artifact Completeness (Phase 39)
| Artifact | Present |
|----------|---------|
| 39-CONTEXT.md | yes |
| 39-RESEARCH.md | yes |
| 39-PATTERNS.md | yes |
| 39-VALIDATION.md | yes (filled; uncommitted) |
| 39-01/02/03-PLAN.md | yes (×3) |
| 39-SUMMARY.md | no — expected (phase not executed) |
| 39-VERIFICATION.md | no — expected (phase not executed) |

All planning artifacts present. SUMMARY/VERIFICATION absent because the phase is planned but not yet executed — not an anomaly.

## Anomalies Detected

### A1. Tooling-resolution mismatch: subagent invokes `gsd-tools` as a bare PATH command — Confidence: HIGH
**Evidence:**
- `command -v gsd-tools` → not found. `gsd-tools` is NOT on PATH.
- gsd-tools is installed only as a Node shim at `~/.claude/gsd-core/bin/gsd-tools.cjs` (exists, ~81K). No project-local shim — both `./gsd-core/bin/gsd-tools.cjs` and `./.claude/gsd-core/bin/gsd-tools.cjs` are absent.
- The gsd-planner agent definition (`~/.claude/agents/gsd-planner.md`) calls `gsd-tools` bare at lines 777, 785, 852, and 1089 — e.g. `gsd-tools query commit "docs($PHASE): create phase plan" --files ...`. It has Bash in its allowed tools but no resolver and no `command -v` guard.
- The orchestrator (plan-phase workflow) does NOT call bare `gsd-tools`. It embeds a multi-runtime fallback chain that resolves the shim to `~/.claude/gsd-core/bin/gsd-tools.cjs` and wraps it as `gsd_run() { node "$GSD_TOOLS" "$@"; }`.

**Interpretation:** This is not a subagent-environment quirk and not about inherited PATH/aliases. The agent and the orchestrator use two different invocation strategies for the same tool. On a global-only install (shim under `~/.claude`, not on PATH), every bare `gsd-tools` call in the agent fails, while the orchestrator's node-shim resolver succeeds. Any GSD agent that calls bare `gsd-tools` — not just gsd-planner — would hit the same wall on this machine.

### A2. Graceful degradation — no work lost — Confidence: HIGH (informational)
**Evidence:** Despite the failed commit step, the planner wrote all artifacts to disk (3 PLAN.md + filled VALIDATION.md) and explicitly handed the commit to the orchestrator, which committed 7 files at `d8fdd16`. All Phase 39 planning artifacts are present and committed (except the VALIDATION.md fill — see A3).
**Interpretation:** The planner degraded safely. The deferral pattern worked; the planning outcome is correct and complete.

### A3. Loose end: filled VALIDATION.md left uncommitted — Confidence: HIGH (low severity)
**Evidence:** `39-VALIDATION.md` was committed empty as a scaffold at `c0848fc`, then the planner populated it (Dimension 8 content, `nyquist_compliant: true`, per-task verification map). The §13d commit file list (PLAN×3 + RESEARCH + PATTERNS + STATE + ROADMAP) did not re-include VALIDATION.md, so its filled content remains uncommitted (`M` in git status).
**Interpretation:** Minor — one `git add`/commit closes it. Caused by the §13d commit scope, not by the gsd-tools failure.

**Other anomaly classes checked and NOT found:** stuck loop (commits are linear with distinct messages), abandoned work (last commit is current), crash/interruption (no orphaned worktrees, clean state), scope drift (all commits touch only Phase 39 planning files + STATE/ROADMAP), test regression (none).

## Root Cause Hypothesis

The gsd-planner agent definition invokes `gsd-tools` as a bare command that depends on it being on `$PATH`, but this machine has only the global Node-shim install (`~/.claude/gsd-core/bin/gsd-tools.cjs`), which is not on PATH. The orchestrator workflow carries its own multi-path shim resolver and therefore succeeds; the agent has no such resolver, so its `gsd-tools query commit` step fails and it correctly defers the commit to the parent. The failure is cosmetic/fragility-only — no work was lost.

## Recommended Actions

1. **(Upstream fix) Give the gsd-planner agent the orchestrator's shim resolver.** Replace the bare `gsd-tools` calls (agent lines 777, 785, 852, 1089) with the `_GSD_SHIM_NAME` / `gsd_run` fallback chain used by the workflows, or at minimum a fallback to `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs"`. This is a gsd-core defect affecting any global-only install — worth filing upstream against open-gsd/gsd-core.
2. **(Local mitigation) Put `gsd-tools` on PATH.** Add a shim/symlink `gsd-tools` -> `node ~/.claude/gsd-core/bin/gsd-tools.cjs` on PATH so every bare-command caller resolves it. Silences the issue for all agents at once.
3. **(Cleanup) Commit the filled VALIDATION.md:** `git add .planning/phases/39-extract-the-recall-module/39-VALIDATION.md && git commit -m "docs(39): fill Nyquist validation strategy"`.
4. **No recovery needed for Phase 39** — planning is complete and committed. Proceed with `/gsd-execute-phase 39` when ready.

---

*Report generated by `/gsd-forensics`. Absolute paths redacted to `~` / relative form for portability.*
