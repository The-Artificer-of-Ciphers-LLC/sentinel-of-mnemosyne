---
quick_task: 260427-vl1-note-import-vault-sweeper
type: execute
mode: quick-full
autonomous: true
files_modified:
  - shared/sentinel_shared/llm_call.py
  - modules/pathfinder/app/llm_call.py
  - modules/pathfinder/app/llm.py
  - modules/pathfinder/app/foundry.py
  - sentinel-core/pyproject.toml
  - sentinel-core/Dockerfile
  - sentinel-core/app/services/note_classifier.py
  - sentinel-core/app/services/inbox.py
  - sentinel-core/app/services/vault_sweeper.py
  - sentinel-core/app/clients/obsidian.py
  - sentinel-core/app/routes/note.py
  - sentinel-core/app/main.py
  - sentinel-core/tests/test_llm_call_shared.py
  - sentinel-core/tests/test_note_classifier.py
  - sentinel-core/tests/test_inbox.py
  - sentinel-core/tests/test_note_routes.py
  - sentinel-core/tests/test_vault_sweeper.py
  - sentinel-core/tests/test_numpy_importable.py
  - interfaces/discord/bot.py
  - tests/test_discord_admin_gate.py
must_haves:
  truths:
    - "User runs `:note <content>` in Discord, classifier picks one of 7 taxonomy slugs, and a note is created in the matching vault directory with full frontmatter (topic, confidence, created, source=note-import)."
    - "User runs `:note <topic> <content>` and the classifier is bypassed; the note is filed under the explicit topic with confidence 1.0."
    - "Low-confidence (<0.5) or `unsure` candidates are appended to `inbox/_pending-classification.md` and never silently filed."
    - "User runs `:inbox` and sees a numbered list of pending entries; `:inbox classify N <topic>` files entry N and renumbers; `:inbox discard N` removes it."
    - "Cheap pre-filter (regex openers, <20 chars, test filenames) intercepts garbage before any LLM call is issued."
    - "Non-admin users running `:vault-sweep` get a refusal; only Discord IDs in `SENTINEL_ADMIN_USER_IDS` env can start a sweep (fail-closed when env is empty)."
    - "Vault sweeper walks every `.md` under root, skips `_trash/`, `pf2e/`, `ops/sessions/`, classifies each note, embeds for de-dup, and moves duplicates (cosine ≥ 0.92) to `_trash/{YYYY-MM-DD}/` via PUT-then-DELETE — never deleting outright."
    - "Re-running `:vault-sweep` skips notes whose `sweep_pass` frontmatter matches the current pass and whose `embedding_b64` is present (idempotent)."
    - "Pathfinder module continues to import `acompletion_with_profile` and runs unchanged after the helper moves to `sentinel_shared`."
    - "Per-sweep log written to `ops/sweeps/{YYYY-MM-DD}.md` with each move's old path, new path, classifier confidence, and reason."
  artifacts:
    - path: shared/sentinel_shared/llm_call.py
      provides: "acompletion_with_profile wrapper, importable from sentinel-core and pathfinder"
      contains: "async def acompletion_with_profile"
    - path: sentinel-core/app/services/note_classifier.py
      provides: "ClassificationResult model, classify_note(), _apply_cheap_filter(), 7-slug taxonomy enum"
      contains: "TopicSlug = Literal"
    - path: sentinel-core/app/services/inbox.py
      provides: "read/parse/append/classify/discard helpers for inbox/_pending-classification.md"
      contains: "PendingEntry"
    - path: sentinel-core/app/services/vault_sweeper.py
      provides: "walk_vault, sweep_pass orchestrator, embedding-similarity de-dup, move_to_trash, sweep log writer"
      contains: "find_dup_clusters"
    - path: sentinel-core/app/routes/note.py
      provides: "POST /note/classify, GET /inbox, POST /inbox/classify, POST /inbox/discard, POST /vault/sweep/start, GET /vault/sweep/status"
      contains: "router = APIRouter"
    - path: sentinel-core/app/clients/obsidian.py
      provides: "Extended ObsidianClient: list_directory, read_note, write_note, delete_note, patch_append"
      contains: "async def list_directory"
    - path: interfaces/discord/bot.py
      provides: ":note, :inbox, :vault-sweep subcommand handlers + SENTINEL_ADMIN_USER_IDS gate"
      contains: "SENTINEL_ADMIN_USER_IDS"
  key_links:
    - from: "modules/pathfinder/app/llm.py"
      to: "shared/sentinel_shared/llm_call.py"
      via: "from sentinel_shared.llm_call import acompletion_with_profile"
      pattern: "from sentinel_shared.llm_call import acompletion_with_profile"
    - from: "sentinel-core/app/services/note_classifier.py"
      to: "shared/sentinel_shared/llm_call.py"
      via: "from sentinel_shared.llm_call import acompletion_with_profile"
      pattern: "from sentinel_shared.llm_call import acompletion_with_profile"
    - from: "interfaces/discord/bot.py"
      to: "sentinel-core /note/classify route"
      via: "_call_core HTTP POST"
      pattern: ":note|:inbox|:vault-sweep"
    - from: "sentinel-core/app/routes/note.py"
      to: "sentinel-core/app/services/note_classifier.py"
      via: "classify_note() call"
      pattern: "classify_note\\("
    - from: "sentinel-core/app/services/vault_sweeper.py"
      to: "sentinel-core/app/clients/obsidian.py"
      via: "list_directory + read_note + write_note + delete_note"
      pattern: "list_directory|write_note|delete_note"
    - from: "sentinel-core/Dockerfile"
      to: "numpy + PyYAML installed at image-build time"
      via: "RUN pip install line"
      pattern: "numpy|PyYAML"
---

<objective>
Add note-import + vault-sweeper to sentinel-core with three new Discord subcommands (`:note`, `:inbox`, `:vault-sweep`). Promote `acompletion_with_profile` from pathfinder into `sentinel_shared` so both services share one LLM-call wrapper. Implement classifier with cheap regex pre-filter, embedding-similarity de-dup for sweep, and admin-gated sweep entry point.

Purpose: Per CONTEXT.md, today there is no path that turns "Finished the sing-better course" into a structured `learning/sing-better-course-completed.md`. There is also accumulated test garbage in the vault (forward-looking; current vault is empty of garbage but the algorithm must scale). This plan delivers both — without breaking pathfinder.

Output: Eight task commits on `main`. After completion, the user can `:note ...`, `:inbox ...`, `:vault-sweep` from Discord; pathfinder behavior is unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/quick/260427-vl1-note-import-vault-sweeper/260427-vl1-CONTEXT.md
@.planning/quick/260427-vl1-note-import-vault-sweeper/260427-vl1-RESEARCH.md
@CLAUDE.md
@modules/pathfinder/app/llm_call.py
@shared/sentinel_shared/__init__.py
@shared/sentinel_shared/model_profiles.py
@sentinel-core/app/clients/obsidian.py
@sentinel-core/app/services/model_selector.py
@sentinel-core/app/main.py
@sentinel-core/pyproject.toml
@sentinel-core/Dockerfile
@modules/pathfinder/app/llm.py
@modules/pathfinder/app/foundry.py
@modules/pathfinder/app/rules.py
@interfaces/discord/bot.py

<interfaces>
<!-- Contracts the executor will use directly. No exploration needed. -->

From shared/sentinel_shared/llm_call.py (after Task 1, byte-identical to pathfinder source):
```python
async def acompletion_with_profile(
    *, model: str, messages: list[dict],
    profile: ModelProfile | None = None,
    api_base: str | None = None,
    timeout: float = 60.0, **extra: Any,
)
```

From sentinel_shared/model_profiles.py (already in repo):
```python
class ModelProfile: stop_sequences: list[str] | None
def get_profile(model_id: str, api_base: str | None = None) -> ModelProfile
```

From sentinel-core/app/services/model_selector.py (already in repo):
```python
async def select_model(rubric: str = "balanced") -> str  # use rubric="structured" for JSON-mode
```

From sentinel-core/app/clients/obsidian.py (existing methods preserved, new methods added in Task 7's prerequisite):
```python
class ObsidianClient:
    def __init__(self, http_client, base_url, api_key)
    async def check_health() -> bool
    async def get_user_context(user_id) -> str | None
    async def read_self_context(path) -> str
    async def write_session_summary(path, content) -> None  # PUT, raises on failure
    async def search_vault(query) -> list[dict]
    # New methods to add (Task 7 prerequisite):
    async def list_directory(path: str = "") -> list[str]
    async def read_note(path: str) -> str  # GET, returns "" on 404
    async def write_note(path: str, body: str) -> None  # PUT, raises
    async def delete_note(path: str) -> None  # DELETE, raises
    async def patch_append(path: str, body: str) -> None  # PATCH end-insertion
```

From interfaces/discord/bot.py:1205+:
```python
async def handle_sentask_subcommand(
    subcmd: str, args: str, user_id: str,
    attachments: list | None = None, channel=None,
) -> "str | dict"
# Existing :remember handler at L1279 is the closest analog for :note dispatch.
# `_call_core(user_id, prompt)` is the standard sentinel-core HTTP entry.
```

Frontmatter schema for filed notes (CONTEXT.md + RESEARCH §3):
```yaml
topic: <one of 7 slugs>
title_slug: <kebab, ≤60>
confidence: <0.0..1.0, 1-decimal>
created: <ISO8601 UTC>
source: note-import | vault-sweep | legacy
embedding_model: text-embedding-nomic-embed-text-v1.5  # sweep only
embedding_b64: <base64 of float32 LE>                  # sweep only
sweep_pass: <ISO8601 UTC>                              # sweep only
```

Closed-vocab taxonomy (CONTEXT.md Q1):
```python
TopicSlug = Literal["learning", "accomplishment", "journal", "reference",
                    "observation", "noise", "unsure"]
TOPIC_VAULT_PATH: dict[str, str] = {
    "learning":       "learning",
    "accomplishment": "accomplishments",
    "journal":        "journal",        # subdir per-day appended at file time
    "reference":      "references",
    "observation":    "ops/observations",
    "noise":          "",               # never filed
    "unsure":         "inbox",          # _pending-classification.md
}
```

Sweep skip-paths (RESEARCH Pitfall 5):
```python
SWEEP_SKIP_PREFIXES = ("_trash/", "pf2e/", "ops/sessions/", "ops/sweeps/", "inbox/")
```

Cheap pre-filter regex (RESEARCH §2 — frozen, do not modify):
```python
_OPENERS = re.compile(
    r"^\s*(hi|hello|hey|test|are you there|what can you do|ping|yo|sup|"
    r"thanks|thank you|ok|okay)\b", re.IGNORECASE)
_TEST_FILENAME = re.compile(r"^(test-|tmp-|untitled)", re.IGNORECASE)
```

Cosine similarity helper (RESEARCH §3 — copy from modules/pathfinder/app/rules.py):
```python
def cosine_similarity(a: np.ndarray, b: np.ndarray) -> float
def find_dup_clusters(matrix: np.ndarray, threshold: float = 0.92) -> list[list[int]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote acompletion_with_profile to sentinel_shared</name>
  <files>
    shared/sentinel_shared/llm_call.py (NEW — copy verbatim from modules/pathfinder/app/llm_call.py),
    modules/pathfinder/app/llm_call.py (DELETE),
    modules/pathfinder/app/llm.py (UPDATE import),
    modules/pathfinder/app/foundry.py (UPDATE import),
    sentinel-core/tests/test_llm_call_shared.py (NEW)
  </files>
  <behavior>
    - Test 1: `from sentinel_shared.llm_call import acompletion_with_profile` succeeds.
    - Test 2: Calling acompletion_with_profile with a profile.stop_sequences=['</end>'] passes stop=['</end>'] through to litellm.acompletion (mock).
    - Test 3: Calling with api_base="http://x" passes api_base through; calling with api_base=None omits the key.
    - Test 4: Pathfinder's `app.llm.embed_texts` and `app.foundry.*` callers still import without ModuleNotFoundError after the move.
  </behavior>
  <action>
    1. Copy `modules/pathfinder/app/llm_call.py` verbatim to `shared/sentinel_shared/llm_call.py`. Keep the docstring and the `from sentinel_shared.model_profiles import ModelProfile` import unchanged.
    2. Delete `modules/pathfinder/app/llm_call.py`.
    3. In `modules/pathfinder/app/llm.py` line 23: change `from app.llm_call import acompletion_with_profile` → `from sentinel_shared.llm_call import acompletion_with_profile`.
    4. In `modules/pathfinder/app/foundry.py` line 14: same import rewrite.
    5. Create `sentinel-core/tests/test_llm_call_shared.py` covering the four behaviors above. Use `unittest.mock.patch("litellm.acompletion")` to assert the kwargs dict shape; do NOT call live LLM. Use `pytest.mark.asyncio` (already auto via `asyncio_mode = "auto"`).
    6. Verify pathfinder still imports clean: `python -c "from app.llm import embed_texts; from app.foundry import *"` from inside `modules/pathfinder/`.
    Per pre-plan decision 1: behavior must be identical (this is a pure move + import-path rewrite). No functional changes.
    Do NOT port `ResolvedModel`/`resolve()` (pre-plan decision 2 — locked).
    Commit message: `refactor(260427-vl1): promote acompletion_with_profile to sentinel_shared`.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_llm_call_shared.py -x && cd ../modules/pathfinder && python -c "from app.llm import embed_texts; from app.foundry import build_say_messages" 2>&1 | (grep -q "Error\|Traceback" && exit 1 || exit 0)</automated>
  </verify>
  <done>shared/sentinel_shared/llm_call.py exists, modules/pathfinder/app/llm_call.py is gone, both pathfinder importers are updated, the four-behavior test file passes, pathfinder imports cleanly. Atomic commit on main.</done>
</task>

<task type="auto">
  <name>Task 2: Add numpy + PyYAML to sentinel-core (pyproject.toml + Dockerfile dual-ship)</name>
  <files>
    sentinel-core/pyproject.toml (UPDATE dependencies),
    sentinel-core/Dockerfile (UPDATE pip install RUN line),
    sentinel-core/tests/test_numpy_importable.py (NEW)
  </files>
  <action>
    Per project memory `project_dockerfile_deps`: a Python dep needs to land in BOTH pyproject.toml AND Dockerfile or the container restart-loops on ModuleNotFoundError.
    1. In `sentinel-core/pyproject.toml`, add to `dependencies` list:
       - `"numpy>=1.26,<3.0"`
       - `"PyYAML>=6.0,<7.0"`
    2. In `sentinel-core/Dockerfile`, append two backslash-continued lines to the `RUN pip install --no-cache-dir \\` block (after the `anthropic` line):
       - `"numpy>=1.26,<3.0" \\`
       - `"PyYAML>=6.0,<7.0"`
       (last line has no trailing backslash)
    3. Create `sentinel-core/tests/test_numpy_importable.py` with two tests:
       - `def test_numpy_importable(): import numpy; assert numpy.__version__`
       - `def test_yaml_importable(): import yaml; assert yaml.__version__`
       This is the regression smoke-test pattern from RESEARCH Pitfall 4.
    4. Run `docker compose build sentinel-core` to confirm the layer rebuilds clean. Do NOT skip this — failure here means downstream tasks can't ship.
    Commit message: `chore(260427-vl1): add numpy + PyYAML deps to sentinel-core (dual-ship)`.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_numpy_importable.py -x && grep -c '^[[:space:]]*"numpy' pyproject.toml | grep -qv '^0$' && grep -q "numpy" Dockerfile && grep -q "PyYAML" Dockerfile</automated>
  </verify>
  <done>Both deps appear in pyproject.toml AND Dockerfile; test_numpy_importable.py passes locally (with deps installed via `pip install -e .[dev]`); docker compose build sentinel-core succeeds. Atomic commit on main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Note classifier service + cheap pre-filter</name>
  <files>
    sentinel-core/app/services/note_classifier.py (NEW),
    sentinel-core/tests/test_note_classifier.py (NEW)
  </files>
  <behavior>
    - Test 1: _apply_cheap_filter("hello") → ("noise", 1.0). _apply_cheap_filter("") → ("noise", 1.0). _apply_cheap_filter("a" * 19) (no opener) → None. _apply_cheap_filter("ping") (single-line short, opener) → ("noise", 1.0).
    - Test 2: _apply_cheap_filter("Finished the sing-better course. Took 6 weeks.") → None (passes filter to LLM).
    - Test 3: _apply_cheap_filter("placeholder", filename="test-foo.md") with body <200 chars → ("noise", 1.0).
    - Test 4: classify_note("Finished course X", user_topic="learning") → ClassificationResult(topic="learning", confidence=1.0, ...) — bypasses LLM (assert litellm.acompletion mock NOT called).
    - Test 5: classify_note("real content here that survives filter", user_topic=None) with mocked litellm.acompletion returning JSON `{"topic":"reference","confidence":0.85,"title_slug":"some-fact","reasoning":"..."}` → returns ClassificationResult with those values.
    - Test 6: classify_note(...) with mocked LLM returning malformed JSON → coerces to ClassificationResult(topic="unsure", confidence=0.0, ...) — graceful (mirrors pathfinder classify_rule_topic salvage path).
    - Test 7: classify_note(...) with mocked LLM returning topic="garbled-not-in-vocab" → coerces to "unsure".
    - Test 8: confidence rounded to one decimal (0.853 → 0.9).
  </behavior>
  <action>
    Per CONTEXT.md decisions and RESEARCH §1+§2:
    1. Create `sentinel-core/app/services/note_classifier.py` containing:
       - `TopicSlug = Literal["learning", "accomplishment", "journal", "reference", "observation", "noise", "unsure"]`
       - `TOPIC_VAULT_PATH` dict (see interfaces above).
       - `class ClassificationResult(BaseModel)` with `topic: TopicSlug`, `confidence: float`, `title_slug: str` (max 60), `reasoning: str`.
       - `_OPENERS` and `_TEST_FILENAME` regexes — copy verbatim from RESEARCH §2 (frozen).
       - `def _apply_cheap_filter(text: str, filename: str | None = None) -> tuple[str, float] | None` — copy verbatim from RESEARCH §2.
       - `def _coerce_topic(raw: str) -> TopicSlug` — return raw if in closed vocab else "unsure" (mirrors pathfinder `coerce_topic`).
       - `def _slugify(text: str, max_len: int = 60) -> str` — kebab-case helper; lowercase, strip non-alphanumeric to single hyphens, truncate.
       - `async def classify_note(candidate_text: str, user_topic: str | None = None) -> ClassificationResult`:
         a. If `user_topic` in closed vocab → return ClassificationResult(topic=user_topic, confidence=1.0, title_slug=_slugify(candidate_text[:60]), reasoning="explicit user topic"). Bypass LLM.
         b. Run `_apply_cheap_filter(candidate_text)`. If returns ("noise", 1.0) → return that as ClassificationResult.
         c. Otherwise: build a system prompt enumerating the 7 closed-vocab slugs (mirror RULE_TOPIC_SLUGS discipline from pathfinder rules.py), call `await acompletion_with_profile(...)` (imported from sentinel_shared.llm_call) with `response_format={"type": "json_object"}`, model from `select_model("structured")`, profile from `get_profile(model_id, api_base)`. Parse the JSON response.
         d. On JSON parse failure or unknown topic → return ClassificationResult(topic="unsure", confidence=0.0, title_slug=_slugify(candidate_text[:60]), reasoning="classifier output unparseable").
         e. Round confidence to 1 decimal: `round(c, 1)`.
       - System prompt template (frozen): instruct the model to return JSON `{"topic": <slug>, "confidence": <0..1>, "title_slug": <kebab>, "reasoning": <≤2 sentences>}` and list each slug with its CONTEXT.md "When applied" description.
    2. Tests in `sentinel-core/tests/test_note_classifier.py`. Mock `litellm.acompletion` via `unittest.mock.patch`. Mock `select_model` and `get_profile` to return stable values. Cover the 8 behaviors above.
    Per CONTEXT discretion: confidence thresholds (≥0.8/0.5–0.8/<0.5) are NOT enforced inside classify_note — that's the route's job (Task 5). Classifier just returns the result.
    Commit message: `feat(260427-vl1): add note classifier service with cheap pre-filter`.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_note_classifier.py -x</automated>
  </verify>
  <done>note_classifier.py exists with the 7-slug taxonomy, cheap pre-filter, and classify_note(); 8-behavior test file passes (all green). Atomic commit on main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Inbox file format + helpers (read/parse/append/classify/discard)</name>
  <files>
    sentinel-core/app/services/inbox.py (NEW),
    sentinel-core/tests/test_inbox.py (NEW)
  </files>
  <behavior>
    - Test 1: parse_inbox(<empty body>) → [].
    - Test 2: parse_inbox(body with 2 entries) → [PendingEntry(entry_n=1, ...), PendingEntry(entry_n=2, ...)] with timestamp/topic/confidence/reasoning/candidate_text fields populated correctly.
    - Test 3: append_entry(existing_body, candidate_text="x", result=ClassificationResult(topic="unsure", confidence=0.4, ...)) → returns new body with `## Entry N+1` block appended; updated frontmatter `updated:` timestamp.
    - Test 4: remove_entry(body_with_3_entries, entry_n=2) → returns body where entry 2 is gone and old entry 3 is renumbered as entry 2.
    - Test 5: render_for_discord(entries) → returns multi-line string with numbered list, 80-char preview, suggested topic in parens (CONTEXT discretion C2).
    - Test 6: build_initial_inbox() → returns the canonical empty inbox body with frontmatter (`type: pending-classification-inbox`, `updated:`) and the editing-instructions header.
    - Test 7: All helpers are pure functions of (body, args) → new body. No I/O. (Obsidian I/O wired in Task 5.)
  </behavior>
  <action>
    Per CONTEXT decision Q3 (inbox shape) and RESEARCH §5:
    1. Create `sentinel-core/app/services/inbox.py` containing pure-function helpers (no I/O — that's Task 5/7's job):
       - `class PendingEntry(BaseModel)`: `entry_n: int`, `timestamp: str`, `topic: str`, `suggested: list[str]`, `confidence: float`, `reasoning: str`, `candidate_text: str`.
       - `INBOX_PATH = "inbox/_pending-classification.md"`.
       - `def build_initial_inbox() -> str` — returns the canonical empty body (frontmatter + header + editing instructions, see RESEARCH §5 for the exact template).
       - `def parse_inbox(body: str) -> list[PendingEntry]` — split body on `^## Entry \\d+\\s*$` (regex `re.MULTILINE`); for each section parse `- key: value` lines and the `> blockquote` body. Return list ordered by entry_n.
       - `def append_entry(body: str, candidate_text: str, result: ClassificationResult, suggested: list[str] | None = None, now: datetime | None = None) -> str` — finds max entry_n, appends new block, updates frontmatter `updated:` timestamp.
       - `def remove_entry(body: str, entry_n: int) -> str` — drops the named entry, renumbers the rest sequentially, updates `updated:` timestamp.
       - `def render_for_discord(entries: list[PendingEntry]) -> str` — numbered list, 80-char candidate preview, `(suggested: <topic>)` suffix.
       - All YAML use `yaml.safe_load` / `yaml.safe_dump(sort_keys=False, allow_unicode=True, default_flow_style=False)` — never hand-roll.
    2. Tests in `sentinel-core/tests/test_inbox.py` — pure unit tests against in-memory bodies (no Obsidian client). Cover the 7 behaviors above. Inject `now` for deterministic timestamps.
    Renumbering UX: per RESEARCH Pitfall 6, the response in Task 5 will tell the user to re-`:inbox` after a classify/discard. The remove_entry helper just renumbers — Discord-side messaging happens in Task 6.
    Commit message: `feat(260427-vl1): add inbox file helpers (parse/append/remove/render)`.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_inbox.py -x</automated>
  </verify>
  <done>inbox.py with all 6 helpers exists; 7-behavior test passes. No Obsidian I/O in this module — pure functions only. Atomic commit on main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Extend ObsidianClient + POST /note/classify route + :note Discord subcommand</name>
  <files>
    sentinel-core/app/clients/obsidian.py (UPDATE — add list_directory, read_note, write_note, delete_note, patch_append),
    sentinel-core/app/routes/note.py (NEW),
    sentinel-core/app/main.py (UPDATE — include router, init classifier),
    sentinel-core/tests/test_obsidian_client.py (UPDATE — add tests for new methods),
    sentinel-core/tests/test_note_routes.py (NEW),
    interfaces/discord/bot.py (UPDATE — add :note handler in handle_sentask_subcommand)
  </files>
  <behavior>
    - Test 1: ObsidianClient.list_directory(path) → list[str] of file/dir names; on 404 returns []; on 5xx returns [] (graceful degrade matching existing _safe_request pattern).
    - Test 2: ObsidianClient.read_note(path) returns body on 200, "" on 404, "" on error.
    - Test 3: ObsidianClient.write_note(path, body) issues PUT with text/markdown content-type and Authorization header; raises on non-2xx.
    - Test 4: ObsidianClient.delete_note(path) issues DELETE; raises on non-2xx.
    - Test 5: ObsidianClient.patch_append(path, body) sends PATCH with `Obsidian-API-Content-Insertion-Position: end` (mirrors bot.py:1303 _persist_thread_id pattern).
    - Test 6: POST /note/classify with body `{"content": "...", "topic": null}` and a mocked classifier returning high-conf "reference" → response `{"action":"filed","path":"references/<slug>-<date>.md","topic":"reference","confidence":0.9}`; ObsidianClient.write_note called once with full frontmatter (topic, title_slug, confidence, created, source=note-import).
    - Test 7: POST /note/classify with mocked classifier returning "noise" → response `{"action":"dropped","reason":"cheap-filter:noise"}`; write_note NOT called.
    - Test 8: POST /note/classify with mocked classifier returning topic="unsure" or confidence<0.5 → response `{"action":"inboxed",...}`; ObsidianClient.read_note(INBOX_PATH) + write_note(INBOX_PATH, new_body) called (or initial inbox is created if read returns "").
    - Test 9: POST /note/classify with explicit `topic=learning` → user_topic flows into classify_note; resulting note path is `learning/<slug>-<date>.md`.
    - Test 10: Discord :note dispatch — calling handle_sentask_subcommand("note", "Finished the sing-better course", user_id="123") routes to a `_call_core` HTTP call against POST /note/classify. (Or, equivalently, an aiohttp-ish call directly to the route — match existing `_call_core` semantics.) Use existing Discord test patterns from tests/test_discord_admin_gate.py (Task 8 will create the file; for this task put a minimal smoke-test inline using a dispatch-router unit test).
  </behavior>
  <action>
    1. **Extend ObsidianClient** in `sentinel-core/app/clients/obsidian.py`:
       - `async def list_directory(self, path: str = "") -> list[str]` — implementation per RESEARCH §4. Uses `_safe_request(_inner(), [], "list_directory")`.
       - `async def read_note(self, path: str) -> str` — wrap GET `/vault/{path}`; return body on 200, "" on 404, "" on error via `_safe_request`.
       - `async def write_note(self, path: str, body: str) -> None` — PUT `/vault/{path}` with `Content-Type: text/markdown`; raises on non-2xx. (Same shape as existing `write_session_summary`.)
       - `async def delete_note(self, path: str) -> None` — DELETE `/vault/{path}`; raises on non-2xx.
       - `async def patch_append(self, path: str, body: str) -> None` — PATCH with header `Obsidian-API-Content-Insertion-Position: end`; raises on non-2xx. Copy from bot.py:1303 pattern.
    2. **Create POST /note/classify route** in `sentinel-core/app/routes/note.py`:
       - Pydantic `ClassifyRequest { content: str, topic: str | None = None }` and `ClassifyResponse { action: str, path: str | None = None, topic: str | None = None, confidence: float | None = None, reason: str | None = None }`.
       - `async def classify_and_file(req, request)`:
         a. `result = await classify_note(req.content, user_topic=req.topic)`.
         b. If `result.topic == "noise"`: return `{"action":"dropped","reason":"cheap-filter:noise"}`.
         c. If `result.topic == "unsure" or result.confidence < 0.5`: read inbox via `obsidian.read_note(INBOX_PATH)`; if empty, seed with `build_initial_inbox()`; call `append_entry(...)`; `obsidian.write_note(INBOX_PATH, new_body)`; return `{"action":"inboxed","topic":result.topic,"confidence":result.confidence}`.
         d. Else (file directly): build target path via `_topic_path(result.topic, result.title_slug)` — for `journal` use `journal/{YYYY-MM-DD}/{slug}.md`; for others use `{TOPIC_VAULT_PATH[topic]}/{slug}-{YYYY-MM-DD}.md`. Build frontmatter via `yaml.safe_dump`. `obsidian.write_note(target, body)`. Return `{"action":"filed","path":target,"topic":result.topic,"confidence":result.confidence}`.
       - On filename collision (`read_note` of target returns non-empty), suffix slug with 8-hex random — never overwrite.
    3. **Wire router into app**: add `app.include_router(note_router)` in `sentinel-core/app/main.py` near existing route includes. Initialize classifier as a stateless module — no app.state singleton needed (classifier is pure functions).
    4. **Add `:note` to Discord bot** in `interfaces/discord/bot.py::handle_sentask_subcommand` (just below `:remember` at L1279):
       ```python
       if subcmd == "note":
           if not args.strip():
               return "Usage: `:note <content>` or `:note <topic> <content>`"
           # Detect explicit topic prefix: first whitespace-separated token in closed vocab
           topic, _, content = args.strip().partition(" ")
           closed_vocab = {"learning","accomplishment","journal","reference","observation","noise","unsure"}
           if topic in closed_vocab and content:
               return await _call_core_note(user_id, content=content, topic=topic)
           return await _call_core_note(user_id, content=args.strip(), topic=None)
       ```
       Add `_call_core_note(user_id, content, topic)` helper that POSTs to sentinel-core `/note/classify` (mirror existing `_call_core` HTTP plumbing — same auth header, same base URL env). Format the response into a one-line user-facing string: filed → "Filed to `{path}` ({confidence})"; inboxed → "Inboxed (low confidence). `:inbox` to review."; dropped → "Dropped as noise."
    5. **Tests**:
       - Update `sentinel-core/tests/test_obsidian_client.py` with 5 new tests (list_directory, read_note, write_note, delete_note, patch_append). Use `respx` or `httpx.MockTransport` — match the existing test style in this file.
       - Create `sentinel-core/tests/test_note_routes.py` with the 4 route tests. Use FastAPI TestClient with classifier mocked via `monkeypatch.setattr` and a fake ObsidianClient injected via dependency override.
       - Add a Discord-dispatch smoke test in `tests/test_message.py` or a new `tests/test_discord_subcommands.py` (interfaces/discord — the project test layout puts Discord tests under `interfaces/discord/tests/` if it exists; otherwise pick the closest existing pattern).
    All tests use a fake Obsidian client per the constraint — never touch live Obsidian.
    Commit message: `feat(260427-vl1): add :note subcommand + /note/classify route + ObsidianClient extensions`.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_obsidian_client.py tests/test_note_routes.py -x && grep -q '"note"' ../interfaces/discord/bot.py</automated>
  </verify>
  <done>POST /note/classify works against fake Obsidian; classifier output drives filed/inboxed/dropped action correctly; Discord :note subcommand dispatches; all tests green. Atomic commit on main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: :inbox subcommand routes + bot handler</name>
  <files>
    sentinel-core/app/routes/note.py (UPDATE — add /inbox endpoints),
    sentinel-core/tests/test_note_routes.py (UPDATE — inbox tests),
    interfaces/discord/bot.py (UPDATE — add :inbox handler)
  </files>
  <behavior>
    - Test 1: GET /inbox → returns `{"entries": [...], "rendered": "<discord-formatted string>"}`. When inbox file is empty/missing, returns `{"entries": [], "rendered": "(inbox is empty)"}`.
    - Test 2: POST /inbox/classify with `{"entry_n": 1, "topic": "reference"}` → reads inbox, finds entry 1, files via classify_note(text, user_topic=topic), removes from inbox, returns `{"action":"filed","path":"references/...","entry_n":1}`. Concurrency-safe: re-GET inbox before final PUT and abort with 409 if content-hash changed (RESEARCH §5).
    - Test 3: POST /inbox/discard with `{"entry_n": 2}` → reads inbox, removes entry 2, PUTs new body, returns `{"action":"discarded","entry_n":2}`.
    - Test 4: POST /inbox/classify with entry_n that doesn't exist → 404 `{"error":"entry not found"}`.
    - Test 5: Discord `:inbox` (no args) → dispatches GET /inbox, returns the rendered string. `:inbox classify 2 reference` → POST /inbox/classify; response includes "Re-run `:inbox` to see updated numbering." `:inbox discard 1` → POST /inbox/discard.
  </behavior>
  <action>
    1. Add to `sentinel-core/app/routes/note.py`:
       - `GET /inbox` → `read_note(INBOX_PATH)` → `parse_inbox` + `render_for_discord`.
       - `POST /inbox/classify` (body: `{entry_n, topic}`) →
         a. read inbox → parse → find entry → if missing, 404.
         b. classify_note(entry.candidate_text, user_topic=topic) for clean title_slug.
         c. write target note via obsidian.write_note (collision suffix as Task 5).
         d. compute content-hash of inbox body; re-GET inbox; if hash changed, return 409 (per RESEARCH §5 concurrency strategy).
         e. remove_entry → write_note(INBOX_PATH).
       - `POST /inbox/discard` (body: `{entry_n}`) → read → remove_entry → write. Same 409 hash-precheck.
    2. Add to Discord bot at `handle_sentask_subcommand`:
       ```python
       if subcmd == "inbox":
           parts = args.strip().split(maxsplit=2)
           if not args.strip():
               return await _call_core_inbox_list(user_id)
           verb = parts[0]
           if verb == "classify" and len(parts) >= 3:
               return await _call_core_inbox_classify(user_id, int(parts[1]), parts[2])
           if verb == "discard" and len(parts) >= 2:
               return await _call_core_inbox_discard(user_id, int(parts[1]))
           return "Usage: `:inbox` | `:inbox classify <n> <topic>` | `:inbox discard <n>`"
       ```
       Add `_call_core_inbox_*` HTTP helpers (mirror Task 5 `_call_core_note`). On classify/discard success, append " — re-run `:inbox` to see renumbered entries." to the user-visible response (RESEARCH Pitfall 6 mitigation).
    3. Add the route tests covering the 4 backend behaviors and the dispatch-routing test for the Discord verb dispatch.
    Commit message: `feat(260427-vl1): add :inbox subcommand + /inbox routes`.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_note_routes.py -x -k "inbox" && grep -q '"inbox"' ../interfaces/discord/bot.py</automated>
  </verify>
  <done>GET /inbox + POST /inbox/classify + POST /inbox/discard work against fake Obsidian; concurrency hash-precheck verified by test (409 on body change between GET and PUT); Discord `:inbox`, `:inbox classify N topic`, `:inbox discard N` all dispatch. Atomic commit on main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Vault sweeper service + admin-gated POST /vault/sweep/start + GET /vault/sweep/status</name>
  <files>
    sentinel-core/app/services/vault_sweeper.py (NEW),
    sentinel-core/app/routes/note.py (UPDATE — add sweep endpoints),
    sentinel-core/tests/test_vault_sweeper.py (NEW),
    sentinel-core/tests/test_note_routes.py (UPDATE — sweep route tests)
  </files>
  <behavior>
    - Test 1: walk_vault(client) yields every .md path under root; skips _trash/, pf2e/, ops/sessions/, ops/sweeps/, inbox/. Test with a fake client that returns nested directory listings.
    - Test 2: _should_skip(path, frontmatter, current_pass) — skips when path under SWEEP_SKIP_PREFIXES; skips when frontmatter has matching sweep_pass + topic + embedding_b64; processes otherwise.
    - Test 3: encode_embedding (float32 LE base64) + decode_embedding round-trips a list[float] of length 768. Copy from pathfinder rules.py _encode_query_embedding/_decode_query_embedding.
    - Test 4: cosine_similarity(a, b) and find_dup_clusters(matrix, 0.92) on a synthetic matrix where rows 0,1 are near-identical and row 2 is orthogonal → returns [[0,1]].
    - Test 5: Per-cluster keeper rule: keeper = max(cluster, key=lambda i: (-mtime[i], len(body[i]))). Older + longer wins.
    - Test 6: move_to_trash(client, "foo.md", reason) → write_note("_trash/{today}/foo.md", body+trash-frontmatter) then delete_note("foo.md"). On collision (read_note of trash target returns non-empty), suffix `-{8hex}.md`.
    - Test 7: Embedding endpoint failure (raise from embed_texts) → sweep continues, logs WARNING, writes topic/confidence/sweep_pass frontmatter without embedding_b64. (Pitfall 3.)
    - Test 8: Lockfile sentinel `ops/sweeps/_in-progress.md` written at start; second concurrent sweep attempt with mtime <1h returns 409. Stale lock (>1h) is taken over with WARNING log. (Pitfall 1.)
    - Test 9: POST /vault/sweep/start with no admin header/token → 403. With admin user_id from allowlist → 200, returns `{"sweep_id":"<iso-timestamp>","status":"running"}`.
    - Test 10: GET /vault/sweep/status → returns `{"sweep_id":..., "status":"idle|running|complete", "files_processed":N, "files_total":M, "duplicates_moved":K}`.
    - Test 11: Per-sweep log written to `ops/sweeps/{date}.md` listing every move (old path, new path, classifier confidence, reason).
    - Test 12: Idempotent — running sweep twice on the same vault state, second run skips already-marked notes (sweep_pass + topic + embedding_b64 present), only files containing changes are touched.
  </behavior>
  <action>
    Per CONTEXT Q5 + RESEARCH §3, §4, §7:
    1. Create `sentinel-core/app/services/vault_sweeper.py`:
       - `SWEEP_SKIP_PREFIXES = ("_trash/", "pf2e/", "ops/sessions/", "ops/sweeps/", "inbox/")`.
       - `LOCKFILE_PATH = "ops/sweeps/_in-progress.md"`; `STALE_LOCK_SECONDS = 3600`.
       - `async def walk_vault(client, root="") -> AsyncIterator[str]` — BFS over Obsidian directory listings. Skips SWEEP_SKIP_PREFIXES at queue-pop time. Yields .md paths only.
       - `def _should_skip(path: str, frontmatter: dict, current_pass: str) -> bool`.
       - Embedding helpers: `def encode_embedding(vec: list[float]) -> str` (base64 of float32 LE) and `def decode_embedding(b64: str) -> list[float]` — copy verbatim from `modules/pathfinder/app/rules.py::_encode_query_embedding/_decode_query_embedding` (do NOT hand-roll).
       - `def cosine_similarity(a, b)` — copy verbatim from `modules/pathfinder/app/rules.py::cosine_similarity` (handles zero-norm rows).
       - `def find_dup_clusters(matrix: np.ndarray, threshold: float = 0.92) -> list[list[int]]` — algorithm per RESEARCH §3 (full pairwise + connected components on sim ≥ threshold).
       - `async def move_to_trash(client, src_path, reason, sweep_at) -> str` — returns the trash path. Annotate body with trash metadata frontmatter (`original_path`, `reason`, `sweep_at`). On filename collision in `_trash/{date}/`, append `-{8hex}.md`.
       - `async def acquire_lock(client) -> bool` — read LOCKFILE; if absent, write `now_iso`. If present and mtime older than STALE_LOCK_SECONDS (parse from body), take over (log WARNING) and overwrite. If present and fresh, return False.
       - `async def release_lock(client) -> None` — delete_note(LOCKFILE).
       - `async def run_sweep(client, classifier, embedder, *, force_reclassify=False, status_callback=None) -> SweepReport`:
         a. acquire_lock → if False raise SweepInProgressError.
         b. Walk vault, collect all paths into a frozen list.
         c. For each path: read_note + parse frontmatter + body. If `_should_skip` → skip. Else apply cheap filter; if "noise", move to trash.
         d. For surviving paths: classify_note(body, user_topic=None). Write back with new frontmatter (preserving any extra fields).
         e. Embed all surviving bodies in batches via `embed_texts` (pathfinder pattern). On endpoint failure: write classification only, log WARNING, skip de-dup.
         f. Stack embeddings → find_dup_clusters → for each cluster, pick keeper, move_to_trash the rest.
         g. Append every move to `ops/sweeps/{YYYY-MM-DD}.md` (PATCH-append; create file if missing).
         h. release_lock. Return SweepReport.
       - Maintain in-process status (module-level dict or app.state) keyed by sweep_id for GET status.
    2. Add routes to `sentinel-core/app/routes/note.py`:
       - `POST /vault/sweep/start` — body `{user_id: str, force_reclassify: bool = false}`. Check `user_id` against `SENTINEL_ADMIN_USER_IDS` env (route reads env directly; in tests inject via fixture). Reject with 403 if not admin. Spawn sweep via `asyncio.create_task` and return immediately with sweep_id. Update app.state.sweep_status.
       - `GET /vault/sweep/status` — return current app.state.sweep_status.
       Note: admin-gate at the bot layer (Task 8) is the primary defense; the route also checks defense-in-depth.
    3. Tests in `sentinel-core/tests/test_vault_sweeper.py` covering behaviors 1–8, 11–12. Tests in `sentinel-core/tests/test_note_routes.py` for 9–10. All tests use a fake ObsidianClient (in-memory dict simulating GET/PUT/DELETE/PATCH). Mock `embed_texts` with deterministic vectors. Mock classify_note via monkeypatch.
    Commit message: `feat(260427-vl1): vault sweeper with embedding de-dup, idempotent skip, _trash/ moves`.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_vault_sweeper.py tests/test_note_routes.py -x -k "sweep or sweeper"</automated>
  </verify>
  <done>vault_sweeper.py implements walk + skip + classify + embed + de-dup + move-to-trash + sweep log + lockfile; all 12 behaviors covered by tests. POST /vault/sweep/start admin-gated and async; GET /vault/sweep/status returns live progress. Atomic commit on main.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: :vault-sweep subcommand + SENTINEL_ADMIN_USER_IDS gate (fail-closed)</name>
  <files>
    interfaces/discord/bot.py (UPDATE — add SENTINEL_ADMIN_USER_IDS, _is_admin, :vault-sweep handler),
    tests/test_discord_admin_gate.py (NEW — at repo root or interfaces/discord/tests/)
  </files>
  <behavior>
    - Test 1: With `SENTINEL_ADMIN_USER_IDS=""` (or unset), `_is_admin("123")` returns False (fail-closed default — RESEARCH §6).
    - Test 2: With `SENTINEL_ADMIN_USER_IDS="123,456"`, `_is_admin("123")` is True, `_is_admin("789")` is False.
    - Test 3: With `SENTINEL_ADMIN_USER_IDS="*"`, `_is_admin("any")` is True (open shortcut — RESEARCH §6).
    - Test 4: handle_sentask_subcommand("vault-sweep", "", user_id="789") with empty allowlist → returns "Admin only. Set SENTINEL_ADMIN_USER_IDS in your env to use this command."
    - Test 5: handle_sentask_subcommand("vault-sweep", "", user_id="123") with `SENTINEL_ADMIN_USER_IDS="123"` → calls POST /vault/sweep/start; returns confirmation with sweep_id.
    - Test 6: handle_sentask_subcommand("vault-sweep", "status", user_id="123") with admin → calls GET /vault/sweep/status; returns formatted progress string.
    - Test 7: handle_sentask_subcommand("vault-sweep", "force", user_id="123") with admin → POST /vault/sweep/start with `force_reclassify=true`.
  </behavior>
  <action>
    Per CONTEXT (admin-only) + RESEARCH §6:
    1. In `interfaces/discord/bot.py`, near `DISCORD_ALLOWED_CHANNELS_RAW` (around L81):
       ```python
       SENTINEL_ADMIN_USER_IDS_RAW = os.environ.get("SENTINEL_ADMIN_USER_IDS", "")
       if SENTINEL_ADMIN_USER_IDS_RAW.strip() == "*":
           ADMIN_USER_IDS = "*"  # open
       else:
           ADMIN_USER_IDS: frozenset[str] = frozenset(
               uid.strip() for uid in SENTINEL_ADMIN_USER_IDS_RAW.split(",") if uid.strip()
           )

       def _is_admin(user_id: str) -> bool:
           if ADMIN_USER_IDS == "*":
               return True
           return bool(ADMIN_USER_IDS) and user_id in ADMIN_USER_IDS
       ```
    2. In `handle_sentask_subcommand` (just below `:inbox` from Task 6):
       ```python
       if subcmd == "vault-sweep":
           if not _is_admin(user_id):
               return "Admin only. Set SENTINEL_ADMIN_USER_IDS in your env to use this command."
           verb = (args.strip().split(maxsplit=1) or [""])[0]
           if verb == "status":
               return await _call_core_sweep_status(user_id)
           force = (verb == "force")
           return await _call_core_sweep_start(user_id, force_reclassify=force)
       ```
       Add `_call_core_sweep_start` and `_call_core_sweep_status` helpers calling the Task 7 routes.
    3. Document the env var in `secrets/README.md` and `.env.example` (one-line entry each: `SENTINEL_ADMIN_USER_IDS=  # comma-sep Discord IDs, or "*" for open. Empty = no admins (fail-closed).`).
    4. Tests in `tests/test_discord_admin_gate.py` (place at the repo level matching project test layout — if `interfaces/discord/tests/` exists, use that; otherwise put under `tests/`). Use `monkeypatch.setenv("SENTINEL_ADMIN_USER_IDS", ...)` and reload the bot module via `importlib.reload`. Mock `_call_core_sweep_*` helpers. Cover all 7 behaviors.
    Per CONTEXT.md decision Q5: `:vault-sweep` does a real sweep on first invocation (no dry-run gate). The fail-closed admin allowlist is the single safety mechanism, plus the never-deletes _trash/ pattern from Task 7.
    Commit message: `feat(260427-vl1): :vault-sweep subcommand with SENTINEL_ADMIN_USER_IDS fail-closed gate`.
  </action>
  <verify>
    <automated>pytest tests/test_discord_admin_gate.py -x && grep -q "SENTINEL_ADMIN_USER_IDS" interfaces/discord/bot.py && grep -q "_is_admin" interfaces/discord/bot.py</automated>
  </verify>
  <done>SENTINEL_ADMIN_USER_IDS gate enforced at bot layer; `:vault-sweep`, `:vault-sweep status`, `:vault-sweep force` all dispatch only for admins; non-admins get the documented refusal string; .env.example + secrets/README updated. Atomic commit on main.</done>
</task>

</tasks>

<verification>
**Phase-level checks (run after all 8 tasks merged):**
1. `cd sentinel-core && pytest -x` — all sentinel-core tests pass (existing + new).
2. `cd modules/pathfinder && pytest -x` — pathfinder tests still pass after Task 1's import surgery.
3. `pytest tests/ -x` — repo-level Discord/integration tests pass.
4. `docker compose build sentinel-core` — image builds clean with new numpy + PyYAML.
5. `docker compose up -d sentinel-core discord` — services start; no ModuleNotFoundError in logs.
6. Manual smoke test (one Discord message each):
   - `:note Finished the sing-better course. Took 6 weeks.` → expect "Filed to `learning/...`".
   - `:note reference Pi is approximately 3.14159` → expect "Filed to `references/...`".
   - `:note hello` → expect "Dropped as noise."
   - `:note <ambiguous short text>` → expect "Inboxed (low confidence)" → then `:inbox` shows the entry.
   - `:inbox classify 1 reference` → entry filed; response notes renumbering.
   - As non-admin: `:vault-sweep` → "Admin only..." refusal.
   - As admin: `:vault-sweep` → returns sweep_id; `:vault-sweep status` reports progress.
7. Confirm `pathfinder` import didn't break: `docker compose logs pathfinder | grep -i "error\|traceback"` should be empty.
</verification>

<success_criteria>
- All 8 tasks merged as 8 atomic commits on `main` (per CLAUDE.md project override — direct to main, no PRs).
- All locked CONTEXT decisions implemented:
  - 7-category taxonomy enforced via `TopicSlug` Literal (Task 3).
  - Cheap regex pre-filter runs before LLM (Task 3).
  - Embedding cosine ≥ 0.92 for sweep de-dup (Task 7).
  - `inbox/_pending-classification.md` is source of truth (Task 4 + 5 + 6).
  - Auto-classification ONLY on `:note <content>` (Task 5; implicit messages keep current behavior).
  - `:vault-sweep` admin-gated via `SENTINEL_ADMIN_USER_IDS`, fail-closed (Task 8).
  - Sweeper moves to `_trash/{YYYY-MM-DD}/` — never deletes (Task 7).
  - Idempotent via `sweep_pass` frontmatter (Task 7).
- All locked pre-plan decisions honored:
  - `acompletion_with_profile` ported to `sentinel_shared`; pathfinder updated (Task 1).
  - `ResolvedModel`/`resolve()` NOT ported (sentinel-core uses `select_model` + `get_profile` directly inside Task 3).
  - `numpy` and `PyYAML` dual-shipped in pyproject + Dockerfile (Task 2).
- Pathfinder behavior identical post-port (Task 1 verify step).
- All tests use fake Obsidian client — zero live-Obsidian dependencies in the test suite.
- `must_haves.truths`, `.artifacts`, and `.key_links` are all observably true after the manual smoke test.
</success_criteria>

<output>
After completion, create `.planning/quick/260427-vl1-note-import-vault-sweeper/260427-vl1-SUMMARY.md` per the standard summary template, listing the 8 commit SHAs, files touched, observable behaviors confirmed, and any notes for the next note-import iteration (e.g. taxonomy expansion if `unsure` rate is high in practice).
</output>
