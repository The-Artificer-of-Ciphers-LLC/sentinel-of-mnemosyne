---
phase: 48-module-scaffold-shared-vault-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/sentinel_shared/obsidian.py
  - shared/sentinel_shared/graph_check.py
  - shared/tests/test_obsidian.py
  - shared/tests/test_graph_check.py
autonomous: true
requirements: [XMOD-01, MUS-05]
must_haves:
  truths:
    - "sentinel_shared exposes ObsidianClientCore + ObsidianHeadingMixin + ObsidianBinaryMixin with pf2e's exact request semantics (behavior-preserving lift, D-04)"
    - "sentinel_shared.graph_check reproduces Core's orphan rule (orphan iff no outlinks and no backlinks; resolution strictly by unique filename stem) as a pure, I/O-free function importable by any module container"
    - "shared suite is green: MockTransport round-trips prove the core methods, and the vendored orphan rule is proven on unique-stem fixtures (including that full-path targets do not resolve to bare stems)"
  artifacts:
    - shared/sentinel_shared/obsidian.py
    - shared/sentinel_shared/graph_check.py
    - shared/tests/test_obsidian.py
    - shared/tests/test_graph_check.py
  key_links:
    - "pf2e (Plan 02) and music (Plan 03/04) both import ObsidianClient* from sentinel_shared.obsidian at container build time"
    - "modules/music/tests (Plan 04) import build_graph_report/extract_wikilinks from sentinel_shared.graph_check for the MUS-05 self-check"
---

<objective>
Promote pf2e's single-copy Obsidian REST client into the shared `sentinel_shared` package as a composable core + two mixins (XMOD-01), and vendor Core's pure wikilink-orphan rule into the same package (MUS-05 foundation) so module containers — which cannot import `sentinel-core` at runtime — can prove their own zero-orphan compliance against the exact rule Core uses.

Purpose: Build the common client core NOW (validated against the 6-module PRD, D-01) rather than after a second copy accumulates; and stop the same anti-duplication problem recurring one level down for the orphan rule.
Output: `shared/sentinel_shared/obsidian.py` (3 classes), `shared/sentinel_shared/graph_check.py` (pure orphan checker), and their tests. No new external dependency — `httpx>=0.28.1` is already in `shared/pyproject.toml`. This plan is the Wave 1 foundation both pf2e cutover and the music module consume.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-module-scaffold-shared-vault-client/48-CONTEXT.md
@.planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md
@.planning/phases/48-module-scaffold-shared-vault-client/48-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vendor the pure wikilink-orphan checker into sentinel_shared.graph_check</name>
  <files>shared/sentinel_shared/graph_check.py, shared/tests/test_graph_check.py</files>
  <read_first>
    - sentinel-core/app/services/graph_analysis.py — the source of truth: `extract_wikilinks` (:36), `_slugify` (:46), `resolve_wikilink` (:56), `GraphReport` (:76), `build_graph_report(notes, hub_paths)` (:86). This is the algorithm to copy; the module copy DROPS the Core-only `hub_paths` parameter.
    - shared/sentinel_shared/similarity.py — the flat-single-purpose-module + SPOT-closing docstring convention this new file follows.
    - .planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md — Pattern 4 (full vendored module body + the 4-note hub-mesh) and "Don't Hand-Roll" (why vendor, not hand-copy).
    - .planning/phases/48-module-scaffold-shared-vault-client/48-PATTERNS.md — graph_check.py pattern block (functions to vendor verbatim).
  </read_first>
  <behavior>
    - build_graph_report over a set of mutually-linked notes with UNIQUE filename stems (e.g. `a.md` linking `[[b]]` and `b.md` linking `[[a]]`) returns `.orphans == []`.
    - A single lone note pointing only at a not-yet-written target is reported as an orphan (resolve_wikilink only creates an edge when a note whose stem matches the target already exists in the map).
    - Resolution is by FILENAME STEM, case/separator-normalized: `[[b]]` resolves to `.../b.md`, but a full-path target like `[[dir/b]]` (slashes preserved by _slugify) does NOT match a bare `b` stem — cover this explicitly so downstream callers use bare-stem link targets.
    - An unresolved wikilink contributes no edge; a self-link is excluded from a note's own outlinks; a note with no `[[...]]` yields an empty extract_wikilinks set.
  </behavior>
  <action>
    Create `shared/sentinel_shared/graph_check.py` as a flat single-purpose module beside `similarity.py`/`llm_call.py`, opening with a SPOT-closing docstring (why it exists: modules cannot import sentinel-core at runtime, so the orphan rule lives here once instead of drifting per-module). Copy `extract_wikilinks`, `_slugify`, `resolve_wikilink` (the by-stem matcher at graph_analysis.py:56-72), the `GraphReport` dataclass, and `build_graph_report` from `sentinel-core/app/services/graph_analysis.py`, dropping the Core-only `hub_paths` parameter and the `hub_count` field from `build_graph_report`/`GraphReport` (module self-checks do not classify hubs). Preserve the stem-match semantics exactly. Keep it pure: imports limited to `re`, `dataclasses`, `typing` — no sentinel-core import, no file I/O. Then create `shared/tests/test_graph_check.py` covering the four behaviors above with small purpose-built fixtures that use UNIQUE filename stems and bare-stem wikilink targets (do NOT reuse the RESEARCH Pattern 4 note bodies verbatim — they name every note `index.md` and link by full path, which does not resolve under the stem-match rule; the corrected music hub-mesh and its zero-orphan proof live in Plan 04). Include one explicit test that a full-path target `[[dir/x]]` does NOT resolve to a bare `x.md` stem.
  </action>
  <verify>
    <automated>cd shared && .venv/bin/python -m pytest -q tests/test_graph_check.py</automated>
  </verify>
  <acceptance_criteria>
    - `shared/sentinel_shared/graph_check.py` defines `build_graph_report`, `resolve_wikilink`, `extract_wikilinks`, and `GraphReport`, and `build_graph_report`'s signature takes only `notes: dict[str, str]` (no `hub_paths`).
    - `grep -n "import" shared/sentinel_shared/graph_check.py` shows no `sentinel-core`/`app.` import — the module is standalone.
    - `cd shared && .venv/bin/python -m pytest -q tests/test_graph_check.py` exits 0, including a test asserting a mutually-linked unique-stem note set yields `orphans == []`, a test asserting a lone note is an orphan, and a test asserting a full-path `[[dir/x]]` target does NOT resolve to a bare `x` stem.
  </acceptance_criteria>
  <done>sentinel_shared.graph_check exists as a pure by-stem orphan checker (no sentinel-core import, no hub_paths param) with a green test suite covering zero-orphan, lone-orphan, and full-path-non-resolution cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split pf2e's ObsidianClient into ObsidianClientCore + Heading/Binary mixins in sentinel_shared.obsidian</name>
  <files>shared/sentinel_shared/obsidian.py, shared/tests/test_obsidian.py</files>
  <read_first>
    - modules/pathfinder/app/obsidian.py — the 226-line source being split. Copy method bodies VERBATIM (D-04) from these ranges: `__init__` :21-26, `_safe_request` :28-35, `get_note` :37-51, `put_note` :53-67 (preserve the 120s timeout), `put_binary` :69-82, `get_binary` :84-102, `list_directory` :104-173 (preserve the depth-8 recursion guard), `patch_heading` :175-204, `patch_frontmatter_field` :206-226.
    - modules/pathfinder/tests/test_aliases_path_probe.py — the `httpx.MockTransport` `_make_client` helper pattern (:36-40) to mirror in the shared test.
    - shared/sentinel_shared/similarity.py — SPOT-closing docstring style.
    - .planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md — Pattern 1 (exact class skeletons + method→home table).
  </read_first>
  <behavior>
    - An ObsidianClientCore built against a MockTransport sends `Authorization: Bearer <key>` on requests when an api_key is supplied, and no auth header when the key is blank.
    - get_note returns the note body on 200 and degrades to the safe default (None) on transport error via _safe_request rather than raising.
    - put_note issues its PUT with the base_url trailing slash stripped and the 120s timeout preserved.
    - A composition subclass mixing ObsidianClientCore + ObsidianBinaryMixin + ObsidianHeadingMixin resolves __init__ to ObsidianClientCore (no mixin defines __init__) and exposes patch_heading, put_binary, get_binary alongside the core methods.
  </behavior>
  <action>
    Create `shared/sentinel_shared/obsidian.py` with a SPOT-closing docstring (promoted from pf2e, behavior-preserving lift per D-04, showing the two compositions pf2e vs music use). Define `ObsidianClientCore` carrying the HTTP plumbing (`__init__`, `_safe_request`) plus the four universally-shared methods (`get_note`, `put_note`, `list_directory`, `patch_frontmatter_field`); define `ObsidianHeadingMixin` with `patch_heading`; define `ObsidianBinaryMixin` with `put_binary`/`get_binary`. Copy each method body verbatim from the pf2e line ranges in read_first — do not redesign, retune timeouts, or alter the recursion/content-type handling. Import only `json`, `logging`, `httpx` (match the pf2e original). Then create `shared/tests/test_obsidian.py` using the MockTransport `_make_client(handler, ClientClass, ...)` helper to cover the four behaviors above, testing both `ObsidianClientCore` directly and a local full-composition subclass. Do not add any dependency to `shared/pyproject.toml` (httpx is already declared).
  </action>
  <verify>
    <automated>cd shared && .venv/bin/python -m pytest -q tests/test_obsidian.py</automated>
  </verify>
  <acceptance_criteria>
    - `shared/sentinel_shared/obsidian.py` defines exactly three classes: `ObsidianClientCore`, `ObsidianHeadingMixin`, `ObsidianBinaryMixin`.
    - `grep -c "async def " shared/sentinel_shared/obsidian.py` returns 8 (the eight promoted async methods: get_note, put_note, list_directory, patch_frontmatter_field, patch_heading, put_binary, get_binary, plus any verbatim inner helper coroutines carried over — confirm the four core methods live on ObsidianClientCore, patch_heading on the heading mixin, and the two binary methods on the binary mixin).
    - No mixin defines `__init__` (so a composition subclass resolves construction to ObsidianClientCore).
    - `cd shared && .venv/bin/python -m pytest -q tests/test_obsidian.py` exits 0.
    - `cd shared && .venv/bin/python -m pytest -q` (whole shared suite, ~35 baseline + the two new files) exits 0 with zero regressions.
  </acceptance_criteria>
  <done>sentinel_shared.obsidian exposes ObsidianClientCore + ObsidianHeadingMixin + ObsidianBinaryMixin (verbatim behavior lift, no new dependency); MockTransport tests prove auth-header handling, safe-degrade, the 120s put_note timeout, and full-composition MRO; whole shared suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module process → Obsidian Local REST API | Every `ObsidianClientCore` call crosses here carrying `Authorization: Bearer {OBSIDIAN_API_KEY}` over HTTP to `OBSIDIAN_BASE_URL` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Information Disclosure | ObsidianClientCore Bearer-token handling | high | mitigate | Copy pf2e's `_safe_request` verbatim: it logs only `operation` + the exception, never the request headers/api_key. The api_key lives only in the `_headers` dict; no code path logs or serializes it. Test asserts the header is set/omitted correctly but the key value is never emitted to logs. |
| T-48-02 | Tampering | Behavior drift during the lift (altered timeout/recursion/content-type could weaken safe-degrade) | medium | mitigate | D-04 behavior-preserving lift: bodies copied verbatim from named line ranges; MockTransport tests confirm 120s timeout preserved, safe-degrade returns default on error rather than raising. |
| T-48-SC | Tampering | package-manager installs (npm/pip/cargo) | low | accept | RESEARCH Package Legitimacy Audit: this phase installs ZERO new packages (httpx already pinned in shared/pyproject.toml). No install task exists → no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd shared && .venv/bin/python -m pytest -q` exits 0 (existing ~35 tests plus test_graph_check.py and test_obsidian.py).
- `shared/sentinel_shared/graph_check.py` and `shared/sentinel_shared/obsidian.py` both import cleanly with no sentinel-core dependency: `cd shared && .venv/bin/python -c "import sentinel_shared.obsidian, sentinel_shared.graph_check"`.
</verification>

<success_criteria>
- The shared Obsidian client (core + 2 mixins) and the vendored orphan checker exist in `sentinel_shared` and are importable at build time by any module.
- The shared test suite is green, proving MockTransport round-trips and the hub-mesh zero-orphan rule.
- Zero new external dependencies added.
</success_criteria>

## Artifacts this phase produces

This Wave-1 plan is the foundation; the full Phase-48 symbol inventory (referenced by later plans) is:

- `shared/sentinel_shared/obsidian.py` — classes `ObsidianClientCore` (get_note, put_note, list_directory, patch_frontmatter_field, _safe_request), `ObsidianHeadingMixin` (patch_heading), `ObsidianBinaryMixin` (put_binary, get_binary). [this plan]
- `shared/sentinel_shared/graph_check.py` — `build_graph_report`, `resolve_wikilink`, `extract_wikilinks`, `_slugify`, `GraphReport`. [this plan]
- `modules/pathfinder/app/obsidian.py` — rewritten to `class ObsidianClient(ObsidianClientCore, ObsidianBinaryMixin, ObsidianHeadingMixin)`. [Plan 02]
- `modules/music/` — `app/__init__.py`, `app/config.py` (Settings), `app/obsidian.py` (`class ObsidianClient(ObsidianClientCore)`), `app/main.py` (FastAPI app, `REGISTRATION_PAYLOAD`, `_register_with_retry`, `_registration_heartbeat`, `lifespan`, `GET /healthz`), `compose.yml` (`profiles: ["music"]`, service `music-module`), `Dockerfile`, `pyproject.toml`. [Plan 03]
- Root `docker-compose.yml` — additive `include: modules/music/compose.yml`. [Plan 03]
- `modules/music/app/seed.py` — `HUB_NOTES` dict + `seed_music_hub(client)`; `music/index.md` + `music/lessons/index.md` + `music/practice-log/index.md` + `music/ideas/index.md` hub-mesh vault content. [Plan 04]
- `modules/music/scripts/gen_sweep_protection_env.py` — generates `SWEEP_SKIP_PREFIXES`/`PROTECTED_NAMESPACES` from Core's committed Settings defaults + `music/`. [Plan 04]
- `sentinel-core/tests/test_env_override_matches_core_defaults.py` — drift guard (a test, not Core app code). [Plan 04]
- Test files: `shared/tests/test_obsidian.py`, `shared/tests/test_graph_check.py` [this plan]; `modules/music/tests/{__init__,conftest,test_registration,test_healthz,test_music_vault_seed}.py` [Plans 03/04]; new `modules/music/.venv` [Plan 03].

<output>
Create `.planning/phases/48-module-scaffold-shared-vault-client/48-01-SUMMARY.md` when done.
</output>
