---
phase: 48-module-scaffold-shared-vault-client
plan: 05
type: execute
wave: 4
depends_on: [48-02, 48-03, 48-04]
files_modified: []
autonomous: false
requirements: [MUS-01, MUS-02, MUS-05]
must_haves:
  truths:
    - "all four test venvs are green together (sentinel-core, modules/pathfinder, shared, modules/music) — the two unchanged suites (core, pf2e routes) show zero regressions from the shared-client cutover"
    - "the live music-module container starts, registers, and Core's module registry lists `music`; GET /healthz returns 200 (MUS-01, success criterion #1)"
    - "the 4-note music/ hub-mesh is present in the live vault and a live sweep does NOT relocate it (Pitfall 1 protection proven end-to-end)"
  artifacts: []
  key_links:
    - "docker compose --profile music up brings the module into the live stack via the Plan 03 include"
    - "the generated deploy .env override (Plan 04) is applied before the smoke so the sweep leaves music/ intact"
---

<objective>
Phase gate: prove the whole phase end-to-end on the live stack. First confirm all four test venvs are green together (no cross-suite regression from the shared-client cutover), then a human live-Docker smoke confirms the music module registers, health-checks, seeds its hub-mesh, and survives a sweep.

Purpose: Success criterion #1 (container starts + registers + registry lists `music`) and the Pitfall-1 protection are integration facts that unit tests (mocked registration, pure orphan check) cannot fully establish — this gate closes that gap before `/gsd-verify-work`.
Output: A green consolidated regression run and an operator-confirmed live smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidated 4-venv regression gate</name>
  <files>sentinel-core/tests/, modules/pathfinder/tests/, shared/tests/, modules/music/tests/</files>
  <read_first>
    - .planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md — Validation Architecture (four separate venvs, baselines: sentinel-core ~605, pf2e ~405, shared ~35+new, music new) and the per-wave-merge sampling rule.
  </read_first>
  <action>
    Run each venv's own suite and confirm all four are green together: `sentinel-core` and `modules/pathfinder` must show ZERO regressions (they are the unchanged suites — Core got no app-code changes, and pf2e only swapped its client body), while `shared` and `modules/music` include the new tests from Plans 01/03/04. Do not edit any test to force green; a red result here is a real regression to fix in the owning plan.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest -q</automated>
    <automated>cd modules/pathfinder && .venv/bin/python -m pytest -q</automated>
    <automated>cd shared && .venv/bin/python -m pytest -q</automated>
    <automated>cd modules/music && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - All four commands exit 0.
    - `sentinel-core` (~605) and `modules/pathfinder` (~405) suites show no new failures relative to their pre-phase baselines (zero regression in the unchanged suites).
    - `shared` and `modules/music` suites include and pass the new obsidian/graph_check/registration/healthz/vault-seed tests.
  </acceptance_criteria>
  <done>All four venv suites exit 0 together with zero regressions in the unchanged sentinel-core and pf2e suites.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-Docker smoke — registration, healthz, seed present, sweep-safe</name>
  <action>Human-only live verification against the deployed stack; no automated action (all automation completed in Task 1 and Plans 01-04). Follow the numbered steps in how-to-verify and report the result.</action>
  <what-built>
    The music module was scaffolded (Plan 03), cut over to the shared `sentinel_shared.ObsidianClient` (Plans 01/02), seeded a zero-orphan `music/` hub-mesh (Plan 04), and protected `music/` from the sweeper via a generated deploy `.env` override (Plan 04). This checkpoint confirms it all works on the live stack.
  </what-built>
  <how-to-verify>
    Run from the deploy checkout (the one holding `secrets/` + `.env`):
    1. Apply the sweeper protection to the deploy env BEFORE first write:
       `python3 modules/music/scripts/gen_sweep_protection_env.py >> .env`
       then confirm the two lines are present and JSON-valid, and that `security/`/`self/`/`pf2e/`/`music/` all appear in `SWEEP_SKIP_PREFIXES`.
    2. Bring up the module: `docker compose --profile music up -d` (rebuild if needed). Confirm the `music-module` container is running (probe its port with `lsof`, not `/dev/tcp`).
    3. Registration: `curl -sf -H "X-Sentinel-Key: $SENTINEL_API_KEY" http://localhost:8000/modules` — assert `music` appears in the registry JSON. Restart sentinel-core and re-check within ~30s to confirm the heartbeat re-registers.
    4. Health: `curl -sf http://localhost:8000/modules/music/healthz` returns 200 with `{"status":"ok","module":"music"}` via the Core proxy.
    5. Seed present: confirm `music/index.md`, `music/lessons/lessons-index.md`, `music/practice-log/practice-log-index.md`, `music/ideas/ideas-index.md` exist in the vault (via Obsidian REST or the vault UI).
    6. Sweeper protection (Pitfall 1): trigger a vault sweep (`:vault-sweep`) and confirm NONE of the four `music/` notes were relocated to `_trash/` — they remain in place.
  </how-to-verify>
  <resume-signal>Type "approved" if all six checks pass, or describe which check failed (container/registry/healthz/seed/sweep).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → live stack | the human runs the smoke against the deployed Core + music module and the live vault |
| deploy .env → Core sweeper | the generated override must be applied before the first sweep for `music/` to survive |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-11 | Denial of Service (data loss) | First live sweep runs before the generated override is applied → music/ hub-mesh relocated | high | mitigate | The checkpoint applies + verifies the generated override (step 1) BEFORE bring-up and the sweep check (step 6); trailing-slash `music/` confirmed present in SWEEP_SKIP_PREFIXES. |
| T-48-12 | Spoofing | Registry query without the X-Sentinel-Key would bypass Core auth | low | mitigate | The smoke uses `-H "X-Sentinel-Key: $SENTINEL_API_KEY"` for authenticated Core endpoints; healthz stays intentionally unauthenticated (pf2e precedent). |
| T-48-SC | Tampering | package-manager installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit — N/A). |
</threat_model>

<verification>
- Four-venv regression run green; unchanged suites (core, pf2e) show zero regressions.
- Operator confirms: container up, `music` in registry, /healthz 200, hub-mesh present, sweep leaves music/ intact.
</verification>

<success_criteria>
- The music-module container starts, registers (retry + heartbeat), and Core's registry lists `music` with zero Core code changes (MUS-01, criterion #1).
- The live `music/` hub-mesh is present, schema/wikilink compliant, and survives a sweep (MUS-05 + Pitfall 1).
</success_criteria>

## Artifacts this plan produces

- No new source artifacts — this is the phase verification gate (consolidated regression run + live-Docker human smoke). Produces the operator sign-off that closes Phase 48's success criteria.

<output>
Create `.planning/phases/48-module-scaffold-shared-vault-client/48-05-SUMMARY.md` when done.
</output>
