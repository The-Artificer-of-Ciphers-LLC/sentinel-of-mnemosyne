---
phase: 48-module-scaffold-shared-vault-client
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - modules/pathfinder/app/obsidian.py
autonomous: true
requirements: [XMOD-01]
must_haves:
  truths:
    - "pf2e's ObsidianClient is a pure composition subclass of the shared core + mixins — no duplicated client request logic remains anywhere in pf2e's tree (D-02, D-05)"
    - "pf2e's ~10 duck-typed consumers, the 7 FakeObsidian doubles, and the main.py import/instantiation sites keep working unchanged because the import path and constructor signature are preserved"
    - "the full modules/pathfinder test suite stays green (D-06 regression gate is an acceptance criterion, not a follow-up)"
  artifacts:
    - modules/pathfinder/app/obsidian.py
  key_links:
    - "modules/pathfinder/app/main.py:57 import + :203 lifespan instantiation resolve against the composition subclass with no edit needed"
    - "modules/pathfinder/tests/test_aliases_path_probe.py builds the client through the public ObsidianClient name with no edit needed"
---

<objective>
Cut pf2e over to the shared client from Plan 01: replace `modules/pathfinder/app/obsidian.py`'s ~200 lines of client logic with a pure composition subclass, then prove — via the full pf2e suite (D-06) — that nothing regressed. This is the strict, no-re-export-shim cutover (D-05); a file still named `obsidian.py` may only remain as a legitimate composition subclass.

Purpose: Collapse the module-to-module duplication XMOD-01 exists to eliminate, before a second copy (music) accumulates.
Output: A 6-line `modules/pathfinder/app/obsidian.py` and a green 405-test pf2e suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-module-scaffold-shared-vault-client/48-CONTEXT.md
@.planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace pf2e's obsidian.py body with a composition subclass of the shared core + mixins</name>
  <files>modules/pathfinder/app/obsidian.py</files>
  <read_first>
    - modules/pathfinder/app/obsidian.py — the 226-line file whose client logic is deleted; only a composition subclass remains.
    - shared/sentinel_shared/obsidian.py — the Plan 01 target: `ObsidianClientCore`, `ObsidianHeadingMixin`, `ObsidianBinaryMixin` (import from here).
    - modules/pathfinder/app/main.py — the two coupling sites to leave UNCHANGED: the client import at :57 and the `lifespan()` instantiation at :203 onto `app.state.obsidian_client`.
    - modules/pathfinder/tests/test_aliases_path_probe.py — verifies the client through the public `ObsidianClient` name; must keep passing unmodified.
    - .planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md — Pattern 2 (why main.py needs ZERO line changes) and Pitfall C (do not plan wasted edits against files that do not need to change).
    - .planning/phases/48-module-scaffold-shared-vault-client/48-CONTEXT.md — D-02, D-05.
  </read_first>
  <action>
    Overwrite `modules/pathfinder/app/obsidian.py` so it contains only: a short docstring stating pf2e's client is pure composition of the shared core + mixins (no client logic here), an import of `ObsidianClientCore`, `ObsidianBinaryMixin`, `ObsidianHeadingMixin` from `sentinel_shared.obsidian`, and `class ObsidianClient(ObsidianClientCore, ObsidianBinaryMixin, ObsidianHeadingMixin): pass`. Keep `ObsidianClientCore` first in the base list so the MRO resolves construction there (no mixin defines its own constructor). Do NOT edit `modules/pathfinder/app/main.py` or `modules/pathfinder/tests/test_aliases_path_probe.py` — the import path (`app.obsidian.ObsidianClient`) and constructor signature are deliberately preserved; changing them is out of scope and an anti-pattern per Pitfall C. Keep the docstring free of method signatures so no promoted method definition text lands in the file.
  </action>
  <verify>
    <automated>grep -c "async def get_note" modules/pathfinder/app/obsidian.py</automated>
    <automated>cd modules/pathfinder && .venv/bin/python -c "from app.obsidian import ObsidianClient; assert 'ObsidianClientCore' in [b.__name__ for b in ObsidianClient.__mro__]"</automated>
  </verify>
  <acceptance_criteria>
    - `modules/pathfinder/app/obsidian.py` defines exactly `class ObsidianClient(ObsidianClientCore, ObsidianBinaryMixin, ObsidianHeadingMixin)` and imports those three names from `sentinel_shared.obsidian`; it contains no `async def` method definitions of its own.
    - The dedup grep `grep -c "async def get_note" modules/pathfinder/app/obsidian.py` returns `0` (the promoted method bodies now live only in `sentinel_shared.obsidian`).
    - `git diff --stat modules/pathfinder/app/main.py modules/pathfinder/tests/test_aliases_path_probe.py` shows zero changes to those two files.
    - `ObsidianClient` is still importable from `app.obsidian` and its MRO includes `ObsidianClientCore` (verify command above exits 0).
  </acceptance_criteria>
  <done>modules/pathfinder/app/obsidian.py is a pure composition subclass importing from sentinel_shared.obsidian; no client logic remains (dedup grep 0); main.py and test_aliases_path_probe.py unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 regression gate — full pf2e suite stays green after the cutover</name>
  <files>modules/pathfinder/tests/</files>
  <read_first>
    - modules/pathfinder/tests/test_aliases_path_probe.py — highest-risk: builds the client against a MockTransport.
    - modules/pathfinder/tests/test_main.py — lifespan wiring onto app.state.obsidian_client.
    - modules/pathfinder/tests/test_registration.py — registration payload/backoff (unaffected but part of the gate).
    - .planning/phases/48-module-scaffold-shared-vault-client/48-CONTEXT.md — D-06 (regression guard is an acceptance criterion).
    - .planning/phases/48-module-scaffold-shared-vault-client/48-RESEARCH.md — Pattern 2 regression-guard command + the FakeObsidian double note (7 duck-typed fakes stay put).
  </read_first>
  <action>
    Run the entire pf2e suite and confirm it is fully green with the cutover in place. Pay explicit attention to `test_aliases_path_probe.py` (MockTransport client build), `test_main.py` (`lifespan()` / `app.state.obsidian_client`), and any test using a `FakeObsidian` double (find them with `grep -rl "FakeObsidian" modules/pathfinder/tests/`). Do not modify test files to make them pass; a failure here means the cutover changed behavior and the composition/MRO must be corrected in Task 1 instead. If an unexpected `TypeError` appears on `ObsidianClient(...)` construction, re-check that `ObsidianClientCore` is first in the base-class list.
  </action>
  <verify>
    <automated>cd modules/pathfinder && .venv/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd modules/pathfinder && .venv/bin/python -m pytest -q` exits 0 with the full suite green (~405 tests baseline; zero failures, zero errors).
    - No test file under `modules/pathfinder/tests/` was modified to achieve green (`git diff --stat modules/pathfinder/tests/` shows no changes).
    - The FakeObsidian-based tests and the lifespan wiring tests pass unchanged.
  </acceptance_criteria>
  <done>Full pf2e suite (~405) green after the cutover with no test edits; D-06 regression guard satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pf2e module process → Obsidian Local REST API | pf2e's ObsidianClient (now composed from the shared core) still carries `Authorization: Bearer {OBSIDIAN_API_KEY}` to `OBSIDIAN_BASE_URL` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-03 | Tampering | Cutover regression could silently change pf2e's write/auth semantics | high | mitigate | The full pf2e suite (D-06, Task 2) is the gate: the composition subclass inherits the identical, unchanged `_safe_request`/auth/timeout behavior from ObsidianClientCore; any drift fails an existing pf2e test (MockTransport auth-header + lifespan wiring assertions). |
| T-48-04 | Elevation of Privilege | MRO mis-order could route construction through a mixin and drop the auth-header init | low | mitigate | ObsidianClientCore is pinned first in the base list and no mixin defines `__init__`; verified by the MRO assertion in Task 1 and the full suite in Task 2. |
| T-48-SC | Tampering | package-manager installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit — N/A); no install task. |
</threat_model>

<verification>
- `modules/pathfinder/app/obsidian.py` is a composition subclass only; dedup grep returns 0.
- `modules/pathfinder/app/main.py` and `modules/pathfinder/tests/test_aliases_path_probe.py` are byte-for-byte unchanged.
- Full pf2e suite exits 0.
</verification>

<success_criteria>
- pf2e consumes the one shared `sentinel_shared.ObsidianClient` implementation; no duplicated per-module client code remains in pf2e's tree (XMOD-01 criterion #4).
- The pf2e regression suite is green with no test edits.
</success_criteria>

## Artifacts this plan produces

- `modules/pathfinder/app/obsidian.py` — rewritten to `class ObsidianClient(ObsidianClientCore, ObsidianBinaryMixin, ObsidianHeadingMixin)` (import from `sentinel_shared.obsidian`); ~200 lines of client logic deleted.

<output>
Create `.planning/phases/48-module-scaffold-shared-vault-client/48-02-SUMMARY.md` when done.
</output>
