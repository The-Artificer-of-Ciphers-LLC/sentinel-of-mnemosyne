---
phase: 27-architecture-pivot
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - sentinel-core/compose.yml
  - pi-harness/compose.yml
  - sentinel.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "docker compose config succeeds with no pi-harness service in the base graph"
    - "sentinel-core starts without depends_on pi-harness"
    - "sentinel.sh --pi flag activates pi profile"
    - "pi-harness/compose.yml has profiles: [pi] declared"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Base compose without pi-harness include"
      contains: "# Pi harness: opt-in via"
    - path: "sentinel-core/compose.yml"
      provides: "sentinel-core service without depends_on pi-harness"
    - path: "pi-harness/compose.yml"
      provides: "pi-harness service with profiles: [pi]"
    - path: "sentinel.sh"
      provides: "Startup script with --pi flag"
      contains: "--pi)"
  key_links:
    - from: "sentinel.sh --pi flag"
      to: "pi-harness/compose.yml profiles: [pi]"
      via: "docker compose --profile pi"
      pattern: "PROFILES.*pi"
---

<objective>
Remove the Pi harness from the base Docker Compose stack — atomically. This is the critical landmine task: removing the pi-harness include from docker-compose.yml and the depends_on block from sentinel-core/compose.yml must happen together. A partial removal breaks docker compose up.

Purpose: Decouple sentinel-core from pi-harness so the base stack starts cleanly without Pi. Pi becomes opt-in via `./sentinel.sh --pi up`.
Output: Four config file edits that together produce a working base stack with no Pi dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Current docker-compose.yml includes block (lines 15–19): -->
```yaml
include:
  - path: sentinel-core/compose.yml
  - path: pi-harness/compose.yml
  - path: interfaces/discord/compose.yml  # DO NOT COMMENT — restored 3x, required for Discord interface
  - path: security/pentest-agent/compose.yml
```

<!-- Current sentinel-core/compose.yml depends_on block (lines 29–31) — THE LANDMINE: -->
```yaml
    depends_on:
      pi-harness:
        condition: service_started
```

<!-- Current pi-harness/compose.yml (no profiles currently): -->
```yaml
services:
  pi-harness:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    ...
```

<!-- Current sentinel.sh case statement (lines 9–18): -->
```bash
for arg in "$@"; do
  case "$arg" in
    --discord)    PROFILES+=("discord") ;;
    --imessage)   echo "iMessage runs natively on Mac, not in Docker." && exit 1 ;;
    --pathfinder) PROFILES+=("pathfinder") ;;
    --music)      PROFILES+=("music") ;;
    --finance)    PROFILES+=("finance") ;;
    --trader)     PROFILES+=("trader") ;;
    --coder)      PROFILES+=("coder") ;;
    *)            ARGS+=("$arg") ;;
  esac
done
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Pi from base compose and wire --pi opt-in (atomic)</name>
  <files>docker-compose.yml, sentinel-core/compose.yml, pi-harness/compose.yml, sentinel.sh</files>
  <read_first>
    - docker-compose.yml — read full file before editing; note current header comment about pi-harness startup order
    - sentinel-core/compose.yml — read full file; confirm depends_on block at lines 29–31
    - pi-harness/compose.yml — read full file; confirm no profiles: key exists yet
    - sentinel.sh — read full file; confirm case statement structure before inserting --pi
  </read_first>
  <action>
Make four edits in this order. All four must be complete before verifying — partial state breaks docker compose.

**Edit 1 — docker-compose.yml:**
Remove the `- path: pi-harness/compose.yml` line from the include block.
Replace the header comment block (lines 5–14) that describes the pi-harness startup dependency with:

```
# docker-compose.yml
# Requires Docker Compose v2.20+ (Docker Desktop 4.22+)
# Add future modules and interfaces as include entries here.
# Never use `docker compose -f` stacking — use include only.
#
# Pi harness is NOT in the base stack — it is an opt-in power tool.
# Activate via: ./sentinel.sh --pi up
# Pi harness feature scope: v0.7. See ARCHITECTURE-Core.md.
```

The include block after the edit must be:
```yaml
include:
  - path: sentinel-core/compose.yml
  # Pi harness: opt-in via `./sentinel.sh --pi up`. NOT in base stack. See v0.7 scope.
  - path: interfaces/discord/compose.yml  # DO NOT COMMENT — restored 3x, required for Discord interface
  - path: security/pentest-agent/compose.yml
```

**Edit 2 — sentinel-core/compose.yml:**
Remove the entire `depends_on` block (lines 29–31):
```yaml
    depends_on:
      pi-harness:
        condition: service_started
```
Also remove the environment variable line:
```yaml
      - PI_HARNESS_URL=http://pi-harness:3000
```
If removing PI_HARNESS_URL leaves the `environment:` key with no remaining entries, remove the `environment:` key entirely. (The config.py field `pi_harness_url` has a default value and status.py handles Pi unreachability gracefully — the env var is not needed.)

**Edit 3 — pi-harness/compose.yml:**
Add `profiles: [pi]` under `pi-harness:` service name, before `build:`:
```yaml
services:
  pi-harness:
    profiles: [pi]   # opt-in only; activated via `./sentinel.sh --pi`
    build:
      context: .
      dockerfile: Dockerfile
```
The rest of the file is unchanged.

**Edit 4 — sentinel.sh:**
Insert `--pi)       PROFILES+=("pi") ;;` before the `*)` catch-all in the case statement:
```bash
    --coder)      PROFILES+=("coder") ;;
    --pi)         PROFILES+=("pi") ;;
    *)            ARGS+=("$arg") ;;
```
The `--pi` case MUST appear before `*)` or it falls through and gets passed to docker compose as an unknown flag, causing an error.
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/sentinel-of-mnemosyne && docker compose config --quiet 2>&1; echo "exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config` exits 0 with no errors or warnings about missing services
    - `grep "pi-harness" docker-compose.yml` returns only the comment line containing "opt-in via" (no active include path)
    - `grep -v "^#" docker-compose.yml | grep "pi-harness"` returns 0 lines (no active pi-harness reference)
    - `grep "depends_on" sentinel-core/compose.yml` returns 0 lines
    - `grep "PI_HARNESS_URL" sentinel-core/compose.yml` returns 0 lines
    - `grep "profiles:" pi-harness/compose.yml` contains `profiles: [pi]`
    - `grep "\-\-pi)" sentinel.sh` returns exactly 1 line with `PROFILES+=("pi")`
    - `grep "\-\-pi)" sentinel.sh` appears before `\*)` in file order (verify with grep -n)
    - `docker compose config 2>&1 | grep -i "pi-harness"` returns 0 service definitions (the pi service is not in the default resolved config)
  </acceptance_criteria>
  <done>docker compose config exits 0. No pi-harness service in base graph. sentinel.sh --pi flag present. pi-harness/compose.yml has profiles: [pi].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host shell → Docker Compose | sentinel.sh passes profile flags to docker compose; unknown flags cause compose errors |
| Base stack → Pi harness | After this plan, the trust boundary is explicit opt-in; no implicit service dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | sentinel.sh --pi flag passthrough | mitigate | --pi is consumed by case statement before `*)` catch-all; cannot reach docker compose as unknown flag |
| T-27-02-02 | Denial of Service | docker compose up without pi-harness | accept | status.py already handles Pi unreachability with try/except; no startup crash possible |
</threat_model>

<verification>
`docker compose config` exits 0 without error. Running `docker compose config 2>&1 | grep "pi-harness"` confirms pi-harness is not in the default resolved service graph (because it has `profiles: [pi]` and no profile is active by default).
</verification>

<success_criteria>
`docker compose config` exits 0. Base stack contains no pi-harness service. sentinel.sh has `--pi` flag. pi-harness/compose.yml declares `profiles: [pi]`. sentinel-core/compose.yml has no depends_on block.
</success_criteria>

<output>
After completion, create `.planning/phases/27-architecture-pivot/27-02-SUMMARY.md`
</output>
