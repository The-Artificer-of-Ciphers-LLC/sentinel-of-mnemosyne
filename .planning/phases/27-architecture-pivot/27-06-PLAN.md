---
phase: 27-architecture-pivot
plan: 06
type: execute
wave: 2
depends_on: [27-01, 27-02, 27-03, 27-04, 27-05]
files_modified:
  - sentinel.sh
  - .planning/phases/27-architecture-pivot/27-UAT.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "`docker compose down` (no flags) stops all running services including pi-harness when it was started with --pi"
    - "sentinel.sh down subcommand always passes all known profile flags so profiled containers are not orphaned"
    - "27-UAT.md Test 2 status updated from 'issue' to 'pass'"
  artifacts:
    - path: "sentinel.sh"
      provides: "All-profiles teardown on down subcommand"
      contains: "ALL_KNOWN_PROFILES"
    - path: ".planning/phases/27-architecture-pivot/27-UAT.md"
      provides: "Closed UAT gap for Test 2"
      contains: "pass"
  key_links:
    - from: "sentinel.sh down subcommand detection"
      to: "ALL_KNOWN_PROFILES loop"
      via: "if [[ \"$arg\" == \"down\" ]]; then ... PROFILE_FLAGS+=(--profile $p)"
      pattern: "ALL_KNOWN_PROFILES"
---

<objective>
Verify the sentinel.sh teardown fix (commit 305e5d9) resolves UAT Test 2, then close the gap in 27-UAT.md.

Purpose: UAT Test 2 reported that `docker compose down` without `--profile pi` left pi-harness running as an unmanaged orphan. The fix (inject all known profiles on the `down` subcommand) was committed at 305e5d9 after the UAT run. This plan confirms the fix works at runtime and updates the UAT record.

Output: UAT Test 2 re-run passes. 27-UAT.md updated — gap status changes from 'issue' to 'pass'. Phase 27 UAT closes with 9/9 passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-architecture-pivot/27-UAT.md
@sentinel.sh

<interfaces>
<!-- Current sentinel.sh teardown logic (lines 28-37, commit 305e5d9): -->
```bash
# On teardown, include all known opt-in profiles so profiled services (e.g. pi-harness)
# are stopped even when the caller didn't pass their --flag.
ALL_KNOWN_PROFILES=(pi discord pathfinder music finance trader coder)
for arg in ${ARGS[@]+"${ARGS[@]}"}; do
  if [[ "$arg" == "down" ]]; then
    for p in "${ALL_KNOWN_PROFILES[@]}"; do
      PROFILE_FLAGS+=("--profile" "$p")
    done
    break
  fi
done
```

<!-- UAT Test 2 gap (from 27-UAT.md): -->
<!-- status: issue -->
<!-- reason: "pi-harness behind profiles: [pi]; docker compose down without --profile pi does not stop it" -->
<!-- missing: "sentinel.sh down subcommand (or always pass --profile pi on down) so pi-harness is included in teardown" -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-run UAT Test 2 — pi-harness teardown</name>
  <read_first>
    - sentinel.sh — confirm ALL_KNOWN_PROFILES loop is present before running the test
  </read_first>
  <what-built>
    commit 305e5d9 added an ALL_KNOWN_PROFILES loop to sentinel.sh: when the `down` subcommand is detected, all known opt-in profile flags are injected into the docker compose command. This ensures `./sentinel.sh down` always passes `--profile pi --profile discord ...` so every profiled container (including pi-harness) is included in teardown regardless of whether `--pi` was passed at startup.
  </what-built>
  <how-to-verify>
    Step 1 — Confirm the fix is present in sentinel.sh:
    ```
    grep "ALL_KNOWN_PROFILES" sentinel.sh
    ```
    Expected: at least 1 match (the array declaration line).

    Step 2 — Start the stack with pi-harness:
    ```
    ./sentinel.sh --pi up -d
    ```
    Expected: sentinel-core and pi-harness both start.

    Step 3 — Confirm pi-harness is running:
    ```
    docker compose ps
    ```
    Expected: pi-harness service listed and running.

    Step 4 — Tear down WITHOUT the --pi flag:
    ```
    ./sentinel.sh down
    ```
    Expected: docker compose receives --profile pi (and all other profiles) automatically. pi-harness container stops and is removed.

    Step 5 — Confirm pi-harness is gone:
    ```
    docker compose ps
    ```
    Expected: NO pi-harness service listed. Output should show either an empty table or only services from the base stack (none if everything was stopped).

    Step 6 — Confirm no orphaned containers remain:
    ```
    docker ps -a --filter name=pi-harness
    ```
    Expected: empty output (no rows after the header).
  </how-to-verify>
  <acceptance_criteria>
    - `grep "ALL_KNOWN_PROFILES" sentinel.sh` returns at least 1 line
    - After `./sentinel.sh down`, `docker compose ps` shows no pi-harness row
    - After `./sentinel.sh down`, `docker ps -a --filter name=pi-harness` shows no containers
  </acceptance_criteria>
  <resume-signal>Type "pass" if all steps verified, or describe any failure with the step number and actual output</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update 27-UAT.md — close Test 2 gap</name>
  <files>.planning/phases/27-architecture-pivot/27-UAT.md</files>
  <read_first>
    - .planning/phases/27-architecture-pivot/27-UAT.md — read the full file to locate the exact YAML fields to update before editing
  </read_first>
  <action>
Edit `.planning/phases/27-architecture-pivot/27-UAT.md` to close the Test 2 gap. Make these targeted changes:

1. **Test 2 result field** — change `result: issue` to `result: pass`

2. **Test 2 note field** — add a note field after the result line:
   ```
   note: "Fixed in commit 305e5d9: sentinel.sh down subcommand now injects ALL_KNOWN_PROFILES so all profiled containers (pi-harness, discord, etc.) are included in teardown regardless of startup flags."
   ```

3. **Summary block** — update the counts:
   ```yaml
   total: 9
   passed: 9
   issues: 0
   pending: 0
   skipped: 0
   blocked: 0
   ```

4. **Gaps section** — update the second gap entry (the teardown gap):
   - Change `status: failed` to `status: resolved`
   - Add a `resolved_by` field: `resolved_by: "commit 305e5d9 — ALL_KNOWN_PROFILES loop in sentinel.sh"`

Do NOT modify any other sections or test results. Do NOT touch Test 1 (already marked fixed) or tests 3-9.
  </action>
  <verify>
    <automated>grep -c "passed: 9" .planning/phases/27-architecture-pivot/27-UAT.md && grep -c "issues: 0" .planning/phases/27-architecture-pivot/27-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "result: pass" .planning/phases/27-architecture-pivot/27-UAT.md | wc -l` returns at least 8 (tests 1, 3-9 were already pass; test 2 now pass too)
    - `grep "result: issue" .planning/phases/27-architecture-pivot/27-UAT.md` returns 0 lines
    - `grep "passed: 9" .planning/phases/27-architecture-pivot/27-UAT.md` returns 1 line
    - `grep "issues: 0" .planning/phases/27-architecture-pivot/27-UAT.md` returns 1 line
    - `grep "ALL_KNOWN_PROFILES" .planning/phases/27-architecture-pivot/27-UAT.md` returns at least 1 line (note references the fix)
    - `grep "status: resolved" .planning/phases/27-architecture-pivot/27-UAT.md` returns 1 line (teardown gap closed)
  </acceptance_criteria>
  <done>27-UAT.md shows 9/9 passed, 0 issues. Test 2 gap marked resolved. Phase 27 UAT fully closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sentinel.sh → docker compose | Script injects profile flags that determine which containers are managed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Tampering | ALL_KNOWN_PROFILES list in sentinel.sh | accept | List is hard-coded in source; adding a new profile requires a code change to sentinel.sh, which is the correct coupling. Personal local-network system. |
| T-27-06-02 | Denial of Service | down with all profiles stops services user didn't intend to stop | accept | Intentional design — teardown is a destructive operation and "stop everything" is the correct behavior. Partial teardown creates orphaned containers (the problem being fixed). |
</threat_model>

<verification>
`grep "ALL_KNOWN_PROFILES" sentinel.sh` confirms the fix is present.
`grep "passed: 9" .planning/phases/27-architecture-pivot/27-UAT.md` confirms UAT closed.
`grep "result: issue" .planning/phases/27-architecture-pivot/27-UAT.md` returns 0 (no open issues).
</verification>

<success_criteria>
Human confirms UAT Test 2 passes at runtime: `./sentinel.sh down` stops pi-harness even when started with `--pi`. 27-UAT.md updated to 9/9 passed, 0 issues. Phase 27 gap closure complete.
</success_criteria>

<output>
After completion, create `.planning/phases/27-architecture-pivot/27-06-SUMMARY.md`
</output>
