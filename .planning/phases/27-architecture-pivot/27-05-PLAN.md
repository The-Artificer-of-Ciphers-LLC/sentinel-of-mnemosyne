---
phase: 27-architecture-pivot
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ARCHITECTURE-Core.md
  - docs/PRD-Sentinel-of-Mnemosyne.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "ARCHITECTURE-Core.md describes Path B: LiteLLM-direct chat, module API gateway, Pi optional"
    - "ARCHITECTURE-Core.md contains the ASCII diagram showing the Path B runtime"
    - "ARCHITECTURE-Core.md contains no language about Pi being the brain or primary AI layer"
    - "ADR-001 in ARCHITECTURE-Core.md reflects Path B rationale (not Path A justification)"
    - "PRD §2 design principles do not describe Pi harness as the brain"
    - "PRD §3.1 system architecture diagram reflects Path B"
    - "ROADMAP.md Phase 11 goal updated to Path B module contract"
  artifacts:
    - path: "docs/ARCHITECTURE-Core.md"
      provides: "Path B canonical architecture document"
      contains: "LiteLLM-direct"
    - path: "docs/PRD-Sentinel-of-Mnemosyne.md"
      provides: "PRD with Path B AI layer description"
  key_links:
    - from: "docs/ARCHITECTURE-Core.md"
      to: "sentinel-core/app/routes/modules.py"
      via: "documents module API gateway contract"
      pattern: "POST /modules/register"
---

<objective>
Rewrite ARCHITECTURE-Core.md to Path B and update PRD §2 and §3.1 to remove Pi-as-brain language. Update ROADMAP.md Phase 11 goal. These are documentation-only changes — no runtime code is modified.

Purpose: The canonical design docs must match what was actually built. Path A docs mislead future contributors and create confusion for AI agents that read them as source of truth. The rewrite makes ARCHITECTURE-Core.md the authoritative Path B reference going forward.
Output: ARCHITECTURE-Core.md fully rewritten to Path B; PRD §2 and §3.1 updated; ROADMAP.md Phase 11 goal updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Path B system architecture diagram (from RESEARCH.md §System Architecture Diagram): -->
```
┌─────────────────────────────────────────────────┐
│            INTERFACE LAYER                      │
│   (Discord /sen, Messages — one container each) │
└────────────────────┬────────────────────────────┘
                     │  HTTP POST /message
                     │  X-Sentinel-Key header
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                 SENTINEL CORE CONTAINER                         │
│   FastAPI router · APIKeyMiddleware                             │
│   ModelRegistry · ProviderRouter · Module Registry              │
│   POST /message (chat) · POST /modules/register                 │
│   POST /modules/{name}/{path} (proxy)                           │
└──────────┬─────────────────┬───────────────────────────────────┘
           │                 │
           │ LiteLLM         │ httpx proxy (registered modules)
           ▼                 ▼
┌──────────────────┐  ┌──────────────────────┐
│  AI PROVIDER     │  │  MODULE CONTAINERS   │
│  LiteLLMProvider │  │  (v0.5+: Pathfinder, │
│  → LM Studio     │  │  Music, Finance, etc)│
│  → Claude API    │  │  Each: FastAPI        │
└──────────────────┘  │  POST /register →    │
                      │  sentinel-core       │
                      └──────────────────────┘

              ┌──────────────────────────┐
              │  OBSIDIAN VAULT (host)   │
              │  REST API plugin         │
              └──────────────────────────┘

[ Pi Harness ] — optional, only with sentinel.sh --pi flag, v0.7 scope
```

<!-- Module API contract (from CONTEXT.md locked decisions): -->
<!-- POST /modules/register — payload: {name, base_url, routes: [{path, description}]} — returns {status: "registered"} -->
<!-- POST /modules/{name}/{path} — proxies request body to module.base_url/path — 503 if unreachable, 404 if not registered -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ARCHITECTURE-Core.md to Path B</name>
  <files>docs/ARCHITECTURE-Core.md</files>
  <read_first>
    - docs/ARCHITECTURE-Core.md — read full file to understand current structure (sections, ADRs, diagrams) before rewriting
  </read_first>
  <action>
Read `docs/ARCHITECTURE-Core.md` in full, then rewrite it to reflect Path B. Preserve the document structure (sections, headings) but replace all Path A content. The new document must:

1. **§1 System Overview / Diagram** — Replace with the Path B ASCII diagram from the interfaces block above. The diagram must show: Interface Layer → Sentinel Core (with Module Registry) → AI Provider and Module Containers separately. Pi Harness appears only as a footnote: "optional, --pi flag, v0.7 scope."

2. **§2 or equivalent Design Principles section** — Remove or update any principle that names Pi as "the brain" or "primary AI layer." Replace with:
   - "LiteLLM-direct chat: POST /message calls LiteLLM → configured AI provider. No intermediate layer."
   - "Module API gateway: modules register endpoints with sentinel-core; sentinel-core proxies requests. Modules are independent containers."
   - "Pi harness is an optional power tool for advanced coding tasks. Activated via `./sentinel.sh --pi`. Scoped to v0.7."

3. **Message flow section** (§5 or equivalent) — Replace Pi-as-preferred-path with direct LiteLLM path:
   - Chat: `POST /message → APIKeyMiddleware → InjectionFilter → LiteLLMProvider → OutputScanner → response`
   - Module proxy: `POST /modules/{name}/{path} → APIKeyMiddleware → Module Registry lookup → httpx proxy → module container → response`

4. **Module API Contract section** (new section if not present) — Document the contract every module must implement:
   ```
   POST /modules/register
     Payload: { "name": str, "base_url": str, "routes": [{ "path": str, "description": str }] }
     Response: { "status": "registered" }
     Call: module container calls this at startup

   POST /modules/{name}/{path}
     sentinel-core receives, looks up module.base_url, proxies body via httpx
     Returns 503 { "error": "module unavailable" } if module unreachable
     Returns 404 if module not registered
   ```

5. **ADR-001** (or equivalent Architecture Decision Record) — Update to document Path B decision:
   - Decision: LiteLLM-direct for chat; module API gateway for extensibility; Pi demoted to optional
   - Context: Phase 25 shipped LiteLLM-direct; Phase 27 formalizes Path B as the architecture
   - Rationale: Simpler message path, modules are independently deployable containers, Pi is an advanced tool not a core dependency
   - Status: Accepted (2026-04-20)

6. **Remove** all language about:
   - Pi harness being the "brain"
   - Pi API calls being the preferred or primary AI path
   - sentinel-core as a "router" that dispatches to Pi
   - ADR text justifying Pi as primary layer

Do not remove sections entirely if they provide context — update them. The document should be complete and navigable.
  </action>
  <verify>
    <automated>grep -v "^#" docs/ARCHITECTURE-Core.md | grep -c "Pi.*brain\|brain.*Pi\|primary AI layer\|preferred.*Pi\|Pi.*preferred" 2>/dev/null; echo "Pi-as-brain references (must be 0): $?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "Pi.*brain\|brain.*Pi\|primary AI layer" docs/ARCHITECTURE-Core.md` returns 0 lines
    - `grep "LiteLLM-direct\|LiteLLM direct" docs/ARCHITECTURE-Core.md` returns at least 1 line
    - `grep "POST /modules/register" docs/ARCHITECTURE-Core.md` returns at least 1 line (module contract documented)
    - `grep "POST /modules/{name}/{path}" docs/ARCHITECTURE-Core.md` returns at least 1 line
    - `grep "optional\|--pi\|v0.7" docs/ARCHITECTURE-Core.md | grep -i "pi"` returns at least 1 line (Pi described as optional)
    - `grep "ADR\|Architecture Decision" docs/ARCHITECTURE-Core.md` returns at least 1 line (ADR present)
    - File contains the Path B ASCII diagram (grep for "INTERFACE LAYER" or "MODULE CONTAINERS")
  </acceptance_criteria>
  <done>ARCHITECTURE-Core.md rewrites to Path B. No Pi-as-brain language. Module API contract documented. ADR updated. ASCII diagram present.</done>
</task>

<task type="auto">
  <name>Task 2: Update PRD §2 + §3.1 and ROADMAP.md Phase 11</name>
  <files>docs/PRD-Sentinel-of-Mnemosyne.md</files>
  <read_first>
    - docs/PRD-Sentinel-of-Mnemosyne.md — read §2 (design principles) and §3.1 (system architecture) in full to locate exact paragraphs with Pi-as-brain language before editing
  </read_first>
  <action>
**Part A — docs/PRD-Sentinel-of-Mnemosyne.md:**

Read the file, then make targeted edits to §2 and §3.1 only. Do not restructure the PRD.

**§2 Design Principles (update "Pi harness as the brain" bullet):**

Find the bullet that reads (approximately):
> "The Pi harness as the brain. The pi-mono coding-agent is the AI execution layer."

Replace it with:
> "LiteLLM-direct as the AI layer. The Sentinel calls LiteLLM → the configured AI provider (LM Studio, Claude API, Ollama, LlamaCpp) directly. No intermediate layer. Pi harness is an optional power tool for advanced coding tasks, activated via `./sentinel.sh --pi`, scoped to v0.7."

**§3.1 System Architecture (update diagram and description):**

Find the architecture diagram that shows:
```
[ Sentinel Core Container ]   (Router, context manager, hook dispatcher)
         |
         |  Pi API call
         v
[ Pi Harness Container ]      (coding-agent — AI execution, tool use, skill dispatch)
```

Replace the diagram with the Path B ASCII diagram from the interfaces block above (the full diagram with Interface Layer, Sentinel Core, AI Provider, Module Containers, Obsidian Vault, and the Pi optional footnote).

Update any surrounding prose in §3.1 that describes Pi as the AI execution layer. The updated prose must describe:
- sentinel-core as the API gateway (chat + module proxy)
- LiteLLM as the AI execution path
- Module containers as the extensibility mechanism

**Part B — ROADMAP.md Phase 11 (requires chflags):**

ROADMAP.md has a macOS immutable flag. The edit MUST follow this exact sequence:
1. `chflags nouchg /Users/trekkie/projects/sentinel-of-mnemosyne/.planning/ROADMAP.md`
2. Edit the file: find Phase 11 goal and replace it
3. `chflags uchg /Users/trekkie/projects/sentinel-of-mnemosyne/.planning/ROADMAP.md`

**Current Phase 11 goal:**
```
**Goal**: DM co-pilot. Create and query NPCs, capture session notes, generate in-character dialogue.
```

**Replace with:**
```
**Goal**: Deliver the first module under the Path B contract. A FastAPI container that registers with sentinel-core via POST /modules/register at startup, exposes NPC management and session note endpoints, and is added to the stack via a single `docker compose --profile pathfinder` entry. This is the v0.5 reference implementation for all future modules.
```

After the chflags restore, verify the file is immutable again:
```bash
ls -lO /Users/trekkie/projects/sentinel-of-mnemosyne/.planning/ROADMAP.md | grep uchg
```
  </action>
  <verify>
    <automated>grep -c "LiteLLM-direct\|LiteLLM direct" docs/PRD-Sentinel-of-Mnemosyne.md && grep -c "Pi.*brain\|brain.*Pi\|Pi harness as the brain" docs/PRD-Sentinel-of-Mnemosyne.md | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "Pi harness as the brain\|pi-mono coding-agent is the AI execution layer" docs/PRD-Sentinel-of-Mnemosyne.md` returns 0 lines
    - `grep "LiteLLM-direct\|LiteLLM direct" docs/PRD-Sentinel-of-Mnemosyne.md` returns at least 1 line
    - `grep "MODULE CONTAINERS\|module.*register\|POST /modules" docs/PRD-Sentinel-of-Mnemosyne.md` returns at least 1 line (Path B diagram in PRD)
    - `grep "Pi API call" docs/PRD-Sentinel-of-Mnemosyne.md` returns 0 lines (old diagram removed)
    - `grep "Path B contract\|POST /modules/register" .planning/ROADMAP.md` returns at least 1 line (Phase 11 goal updated)
    - `ls -lO .planning/ROADMAP.md | grep -c uchg` returns 1 (immutable flag restored)
  </acceptance_criteria>
  <done>PRD §2 and §3.1 updated. No Pi-as-brain language remains. ROADMAP.md Phase 11 replanned under Path B. Immutable flag restored on ROADMAP.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP.md uchg flag | macOS immutable flag must be removed before edit and restored after |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | ROADMAP.md immutable flag not restored | mitigate | Task 2 action includes explicit chflags uchg restore step and verification with ls -lO. If the flag is not restored, CLAUDE.md rule violation. |
| T-27-05-02 | Information Disclosure | PRD containing contradictory architecture | mitigate | Task 2 removes Pi-as-brain language from both §2 and §3.1 in a single edit pass. |
</threat_model>

<verification>
`grep "Pi.*brain\|brain.*Pi" docs/ARCHITECTURE-Core.md docs/PRD-Sentinel-of-Mnemosyne.md` returns 0 lines. `grep "POST /modules/register" docs/ARCHITECTURE-Core.md` returns at least 1 line. `ls -lO .planning/ROADMAP.md | grep uchg` confirms immutable flag is restored. `grep "Path B contract" .planning/ROADMAP.md` confirms Phase 11 goal updated.
</verification>

<success_criteria>
ARCHITECTURE-Core.md fully describes Path B with ASCII diagram, module contract, and updated ADR. PRD §2 and §3.1 no longer name Pi as the brain or primary AI layer. ROADMAP.md Phase 11 goal describes the Path B module contract. ROADMAP.md immutable flag restored.
</success_criteria>

<output>
After completion, create `.planning/phases/27-architecture-pivot/27-05-SUMMARY.md`
</output>
