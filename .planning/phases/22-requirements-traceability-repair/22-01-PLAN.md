---
phase: 22-requirements-traceability-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "REQUIREMENTS.md exists at .planning/REQUIREMENTS.md with all v1 checkboxes correct through Phase 10"
    - "2B-01..06 requirements appear in REQUIREMENTS.md with [x] checked"
    - "STATE.md completed_phases is 10 (not 5)"
    - "08-CONTEXT.md has a superseded-by note at the top"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements registry with correct checkbox state through Phase 10"
      contains: "2B-01"
    - path: ".planning/STATE.md"
      provides: "Accurate project state"
      contains: "completed_phases: 10"
    - path: ".planning/phases/08-requirements-traceability-repair/08-CONTEXT.md"
      provides: "Phase 08 lineage note"
      contains: "Superseded by Phase 22"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "2B-01..06 section"
      via: "Knowledge Migration Tool requirements (Phase 10)"
      pattern: "2B-01"
---

<objective>
Restore REQUIREMENTS.md from its last git-committed state, extend it with 2B-01..06 requirements from Phase 10, verify checkbox accuracy through Phase 21, then fix the STATE.md completed_phases count and add a superseded-by note to 08-CONTEXT.md.

Purpose: REQUIREMENTS.md has been absent from disk since .planning/ was added to .gitignore. This is the primary requirements registry for the project. STATE.md incorrectly shows 5 completed phases when 10 have shipped.

Output: .planning/REQUIREMENTS.md (restored + extended), .planning/STATE.md (completed_phases: 10), .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md (lineage note added).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-requirements-traceability-repair/22-CONTEXT.md
@.planning/phases/08-requirements-traceability-repair/08-CONTEXT.md
@.planning/v0.1-v0.4-MILESTONE-AUDIT.md
@.planning/phases/21-production-recovery-security-pipeline-discord/21-VERIFICATION.md

<interfaces>
<!-- Key facts from git history — restore from these. -->

Last committed REQUIREMENTS.md (git show b29fe3a:.planning/REQUIREMENTS.md):
- Has [x] for CORE-01..07, MEM-01..08, IFACE-01..06, PROV-01..05, SEC-01..03
- Has [ ] for SEC-04
- Has [ ] for all future-phase requirements (PF2E, MUSIC, CODER, FIN, TRADE, COMM)
- Does NOT contain 2B-01..06 (Phase 10 requirements added after this commit)
- Traceability table bottom line: "Last updated: 2026-04-11 — Phase 7 complete"

Last committed PROJECT.md (git show c7c6a28:.planning/PROJECT.md):
- Has [x] for all Phase 1–7 items in the Active section
- Does NOT have Phase 10 item checked

Phase 21 VERIFICATION.md status: passed — so SEC-01, SEC-02, IFACE-02, IFACE-03, IFACE-04 regression is resolved.

2B requirements (Phase 10 — Knowledge Migration Tool, shipped):
- 2B-01: 27-command Discord subcommand system
- 2B-02: asyncio.gather() parallel reads 5 self/ files
- 2B-03: Thread ID persistence to ops/discord-threads.md
- 2B-04: Thread IDs reloaded on bot startup
- 2B-05: mnemosyne/self/ vault structure with stub files
- 2B-06: mnemosyne/notes/, ops/, templates/ present

Current STATE.md: completed_phases: 5 (stale — actual count is 10: phases 01, 02, 03, 04, 05, 06, 07, 10, 21, 22 counting in progress; definitively 01-07 + 10 = 8 shipped phases plus 21 = 9; if 22 itself counts = 10 — use 9 for shipped-before-this-phase or document the count per phases completed prior to 22 starting)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore and extend REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/22-requirements-traceability-repair/22-CONTEXT.md (D-01 decisions — exact IDs and flip rules)
    - .planning/phases/21-production-recovery-security-pipeline-discord/21-VERIFICATION.md (confirms Phase 21 passed — SEC-01/SEC-02/IFACE-02/03/04 regressions resolved)
    - .planning/v0.1-v0.4-MILESTONE-AUDIT.md (requirements coverage table — source of truth for 2B-01..06 descriptions)
  </read_first>
  <action>
Restore .planning/REQUIREMENTS.md by running:

```bash
git show b29fe3a:.planning/REQUIREMENTS.md > .planning/REQUIREMENTS.md
```

Then make the following targeted edits:

1. **Add 2B-01..06 section** in the ## v1 Requirements section, after the existing "### AI Security" section and before "### Pathfinder 2e Module". Insert a new section:

```markdown
### Knowledge Migration Tool (2nd Brain)

- [x] **2B-01**: 27-command Discord subcommand system operational — _SUBCOMMAND_PROMPTS (12) + _PLUGIN_PROMPTS (8) wired in bot.py; at least 20 subcommands mapped to vault interaction patterns
- [x] **2B-02**: asyncio.gather() parallel reads 5 self/ files — message.py reads identity.md, methodology.md, goals.md, relationships.md, style.md concurrently before building Pi prompt
- [x] **2B-03**: Thread ID persistence to ops/discord-threads.md — _persist_thread_id() writes channel→thread mappings to vault on each new conversation
- [x] **2B-04**: Thread IDs reloaded on bot startup — setup_hook() reads discord-threads.md and restores channel→thread mapping before accepting commands
- [x] **2B-05**: mnemosyne/self/ vault structure with stub files — identity.md, methodology.md, goals.md, relationships.md present in mnemosyne/self/
- [x] **2B-06**: mnemosyne/notes/, ops/, templates/ directories present — created by Phase 10 with correct vault layout
```

2. **Update traceability table** — append these rows before the closing coverage stats:

```
| 2B-01 | Phase 10 | Complete |
| 2B-02 | Phase 10 | Complete |
| 2B-03 | Phase 10 | Complete |
| 2B-04 | Phase 10 | Complete |
| 2B-05 | Phase 10 | Complete |
| 2B-06 | Phase 10 | Complete |
```

3. **Update coverage stats line** at the bottom — change:
   `v1 requirements: 62 total (58 original + SEC-01..04 added 2026-04-10)`
   to:
   `v1 requirements: 68 total (58 original + SEC-01..04 added 2026-04-10 + 2B-01..06 added 2026-04-11)`

4. **Update last-updated line** at the bottom:
   Change: `*Last updated: 2026-04-11 — Phase 7 complete; checkboxes and traceability table updated (Phase 08 docs repair)*`
   To: `*Last updated: 2026-04-11 — Phase 22: extended through Phase 10; added 2B-01..06; STATE.md and Nyquist matrices repaired*`

5. **Verify SEC-01, SEC-02 are [x]** — the git base already has these checked. Confirm they are present as `[x]` (not `[ ]`). Since Phase 21 verified passed, these are correct.

6. **Verify IFACE-02, IFACE-03, IFACE-04 are [x]** — same as above. The git base has them checked at Phase 6. Phase 21 resolved the regression. Leave as `[x]`.

7. **Verify SEC-04 is [ ]** — leave unchecked per D-01 (Phase 24 will wire the compose include).

Do NOT change any other checkboxes. Do NOT change any future-phase requirements.
  </action>
  <verify>
    <automated>grep -c '\[x\]' .planning/REQUIREMENTS.md && grep '2B-01' .planning/REQUIREMENTS.md && grep '2B-06' .planning/REQUIREMENTS.md && grep -c 'Complete' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` exists on disk
    - `grep '2B-01' .planning/REQUIREMENTS.md` returns a line containing `[x]`
    - `grep '2B-06' .planning/REQUIREMENTS.md` returns a line containing `[x]`
    - `grep 'SEC-04' .planning/REQUIREMENTS.md` returns a line containing `[ ]` (not `[x]`)
    - `grep '68 total' .planning/REQUIREMENTS.md` returns at least one match
    - `grep 'Phase 22' .planning/REQUIREMENTS.md` appears in the Last updated line
    - `grep -E '\[x\].*2B-0[1-6]' .planning/REQUIREMENTS.md` returns 6 lines
  </acceptance_criteria>
  <done>REQUIREMENTS.md exists on disk, contains 2B-01..06 all checked [x], SEC-04 remains [ ], coverage count updated to 68, last-updated line references Phase 22.</done>
</task>

<task type="auto">
  <name>Task 2: Fix STATE.md completed_phases count</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (current state — must read before editing)
    - .planning/phases/22-requirements-traceability-repair/22-CONTEXT.md (D-04 decision)
  </read_first>
  <action>
Read .planning/STATE.md. Make the following targeted edits:

1. Change `completed_phases: 5` → `completed_phases: 9`
   (Rationale: Phases 01, 02, 03, 04, 05, 06, 07, 10, 21 = 9 completed phases before Phase 22. Phase 22 is in progress/being documented by this plan.)

2. Change `stopped_at: Completed 02-memory-layer 02-02-PLAN.md — UAT passed, Phase 2 complete`
   → `stopped_at: Completed 21-production-recovery-security-pipeline-discord 21-01-PLAN.md — verification passed, Phase 21 complete`

3. Change `last_activity: 2026-04-11 -- Phase 21 execution started`
   → `last_activity: 2026-04-11 -- Phase 22 execution started (requirements traceability repair)`

Do not change any other fields in STATE.md.
  </action>
  <verify>
    <automated>grep 'completed_phases' .planning/STATE.md && grep 'stopped_at' .planning/STATE.md && grep 'last_activity' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'completed_phases' .planning/STATE.md` returns `completed_phases: 9`
    - `grep 'stopped_at' .planning/STATE.md` contains `21-production-recovery-security-pipeline-discord`
    - `grep 'last_activity' .planning/STATE.md` contains `Phase 22`
    - `grep 'completed_phases: 5' .planning/STATE.md` returns no matches (old value gone)
  </acceptance_criteria>
  <done>STATE.md shows completed_phases: 9, stopped_at and last_activity updated to reflect Phase 21 complete and Phase 22 in progress.</done>
</task>

<task type="auto">
  <name>Task 3: Add superseded-by note to 08-CONTEXT.md</name>
  <files>.planning/phases/08-requirements-traceability-repair/08-CONTEXT.md</files>
  <read_first>
    - .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md (current state — must read before editing)
    - .planning/phases/22-requirements-traceability-repair/22-CONTEXT.md (D-05 decision)
  </read_first>
  <action>
Read 08-CONTEXT.md. Insert the following block immediately after the frontmatter closing `---` line (before the `# Phase 08 Context:` heading):

```markdown
> **SUPERSEDED** — This phase was never executed. All work defined here was executed as Phase 22 (requirements-traceability-repair), extended through Phase 10. See `.planning/phases/22-requirements-traceability-repair/` for the executed artifacts (22-PLAN.md, 22-SUMMARY.md). Do not re-create Phase 08 artifacts.
>
> *Superseded: 2026-04-11*

```

The resulting file should have the frontmatter, then the blockquote notice, then the original content unchanged.
  </action>
  <verify>
    <automated>grep 'SUPERSEDED' .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md && grep 'Phase 22' .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'SUPERSEDED' .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md` returns at least one line
    - `grep 'Phase 22' .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md` returns at least one line
    - `grep 'Phase 08 Context' .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md` returns the original heading (content not deleted)
    - `grep '2026-04-11' .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md` returns at least one line in the superseded notice
  </acceptance_criteria>
  <done>08-CONTEXT.md has a blockquote superseded notice immediately after frontmatter, pointing to Phase 22, with the original content intact below it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No threat surface — documentation-only phase. All changes are to `.planning/` markdown files with no code execution, no external API calls, and no user-facing functionality.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | .planning/REQUIREMENTS.md | accept | Documentation file; no executable code; git history provides audit trail |
</threat_model>

<verification>
Run after all tasks complete:

```bash
# Task 1 — REQUIREMENTS.md
grep '2B-01' .planning/REQUIREMENTS.md
grep 'SEC-04.*\[ \]' .planning/REQUIREMENTS.md
grep '68 total' .planning/REQUIREMENTS.md

# Task 2 — STATE.md
grep 'completed_phases: 9' .planning/STATE.md
grep '21-production-recovery' .planning/STATE.md

# Task 3 — 08-CONTEXT.md
grep 'SUPERSEDED' .planning/phases/08-requirements-traceability-repair/08-CONTEXT.md
```

All commands must return at least one matching line.
</verification>

<success_criteria>
- REQUIREMENTS.md restored on disk with 2B-01..06 section added, all Phase 1–10 requirements checked [x] except SEC-04
- STATE.md shows completed_phases: 9
- 08-CONTEXT.md has superseded-by notice referencing Phase 22
</success_criteria>

<output>
After completion, create `.planning/phases/22-requirements-traceability-repair/22-01-SUMMARY.md`
</output>
