---
phase: 22-requirements-traceability-repair
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/phases/01-core-loop/01-VALIDATION.md
  - .planning/phases/03-interfaces/03-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "PROJECT.md exists on disk with Phase 10 2nd Brain items checked"
    - "01-VALIDATION.md exists with nyquist_compliant: true and a Nyquist Test Matrix section"
    - "03-VALIDATION.md exists with nyquist_compliant: true and a Nyquist Test Matrix section"
    - "Each VALIDATION.md Nyquist matrix maps every requirement ID to a test file and test function"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project overview with up-to-date phase checkboxes"
      contains: "Phase 10"
    - path: ".planning/phases/01-core-loop/01-VALIDATION.md"
      provides: "Nyquist compliance record for Phase 1"
      contains: "nyquist_compliant: true"
    - path: ".planning/phases/03-interfaces/03-VALIDATION.md"
      provides: "Nyquist compliance record for Phase 3"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".planning/phases/01-core-loop/01-VALIDATION.md"
      to: "sentinel-core/tests/"
      via: "Nyquist Test Matrix requirement-to-test mapping"
      pattern: "test_token_guard.py"
    - from: ".planning/phases/03-interfaces/03-VALIDATION.md"
      to: "sentinel-core/tests/test_auth.py"
      via: "Nyquist Test Matrix IFACE-06 mapping"
      pattern: "test_auth_accepts_valid_key"
---

<objective>
Restore PROJECT.md from its last git-committed state with Phase 10 checkbox added, then create 01-VALIDATION.md and 03-VALIDATION.md with Nyquist Test Matrices documenting the requirement-to-test mapping for each phase.

Purpose: PROJECT.md is missing from disk. Both VALIDATION.md files have nyquist_compliant: false and no Nyquist matrix — Phase 08 never wrote them. These are documentation-only tasks; no new tests are written.

Output: .planning/PROJECT.md (restored + Phase 10 checked), .planning/phases/01-core-loop/01-VALIDATION.md (new, Nyquist compliant), .planning/phases/03-interfaces/03-VALIDATION.md (new, Nyquist compliant).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-requirements-traceability-repair/22-CONTEXT.md
@.planning/phases/01-core-loop/01-VERIFICATION.md
@.planning/phases/03-interfaces/03-VERIFICATION.md

<interfaces>
Test files available in sentinel-core/tests/ (verified by listing):
- test_message.py, test_token_guard.py, test_pi_adapter.py (Phase 1 coverage)
- test_auth.py (Phase 3 coverage — 4 tests: test_auth_rejects_missing_key, test_auth_rejects_wrong_key, test_health_bypasses_auth, test_auth_accepts_valid_key)

Manual verification evidence locations:
- Phase 1: 01-VERIFICATION.md — Requirements Coverage table + Behavioral Spot-Checks
- Phase 3: 03-VERIFICATION.md — Requirements Coverage table + Behavioral Spot-Checks

Git restore commands:
- PROJECT.md: `git show c7c6a28:.planning/PROJECT.md > .planning/PROJECT.md`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore PROJECT.md with Phase 10 checkbox</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/phases/22-requirements-traceability-repair/22-CONTEXT.md (D-02 decision)
    - .planning/ROADMAP.md (Phase 10 description for accurate checkbox text)
  </read_first>
  <action>
Restore .planning/PROJECT.md from git:

    git show c7c6a28:.planning/PROJECT.md > .planning/PROJECT.md

Then edit the file to add Phase 10 Knowledge Migration Tool items. In the "Active" requirements section, after the existing Phase 7 items and before the Pathfinder items, insert a new subsection:

    **Knowledge Migration Tool (2nd Brain)**
    - [x] 27-command Discord subcommand system operational with self/ vault reads
    - [x] asyncio.gather() parallel reads of 5 self/ files before each response
    - [x] Thread ID persistence and reload across bot restarts
    - [x] mnemosyne/self/, notes/, ops/, templates/ vault structure established

Then add or update the last-updated line at the bottom of the file:

    *Last updated: 2026-04-11 — Phase 22: extended through Phase 10 (2nd Brain items added)*

Do NOT remove or uncheck any existing checked items. Do NOT change Out of Scope items.
  </action>
  <verify>
    <automated>grep 'asyncio.gather\|Thread ID persistence\|vault structure established' .planning/PROJECT.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/PROJECT.md` exists on disk
    - `grep 'asyncio.gather' .planning/PROJECT.md` returns a line containing `[x]`
    - `grep 'Thread ID persistence' .planning/PROJECT.md` returns a line containing `[x]`
    - `grep -c '\[x\]' .planning/PROJECT.md` returns 20 or more (existing items preserved plus new Phase 10 items)
    - `grep 'Pi harness\|Sentinel Core\|Obsidian' .planning/PROJECT.md` returns `[x]` lines (existing items not deleted)
  </acceptance_criteria>
  <done>PROJECT.md exists on disk with Phase 10 Knowledge Migration Tool items checked, all prior phase items preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Create 01-VALIDATION.md with Nyquist Test Matrix</name>
  <files>.planning/phases/01-core-loop/01-VALIDATION.md</files>
  <read_first>
    - .planning/phases/01-core-loop/01-VERIFICATION.md (source of truth for requirements coverage and test evidence — read the Requirements Coverage table and Behavioral Spot-Checks)
    - .planning/phases/22-requirements-traceability-repair/22-CONTEXT.md (D-03 decision — documentation-only, no new tests)
    - sentinel-core/tests/test_token_guard.py (verify 6 test functions exist)
    - sentinel-core/tests/test_pi_adapter.py (verify 6 test functions exist)
  </read_first>
  <action>
Create .planning/phases/01-core-loop/01-VALIDATION.md as a new file. The file MUST start with a YAML frontmatter block containing these exact fields:

    phase: 01-core-loop
    nyquist_compliant: true
    wave_0_complete: true
    status: verified
    verified: 2026-04-10T00:00:00Z
    nyquist_written: 2026-04-11T00:00:00Z
    note: "Nyquist matrix written retroactively by Phase 22. Documentation-only."

After the frontmatter, write a "# Phase 01: Core Loop — Validation" heading, then a "## Nyquist Test Matrix" section with a markdown table. The table must have columns: Requirement, Description, Test File, Test Function(s), Automated?, Evidence.

Map each requirement as follows:

CORE-01 row: test_pi_adapter.py | test_send_messages_success | Yes | PiAdapterClient POSTs to /prompt; 200 response asserted
CORE-02 row: test_pi_adapter.py | test_send_messages_success | Partial | Adapter isolation tested; pin verified by code review (package.json exact 0.66.1, no caret)
CORE-03 row: test_message.py | test_post_message_returns_response_envelope, test_post_message_503_when_pi_and_ai_provider_unavailable | Yes | ResponseEnvelope shape asserted; 200 and 503 paths covered
CORE-04 row: test_message.py | test_post_message_returns_response_envelope | Partial | Context window used in test; fallback to 4096 verified by code review
CORE-05 row: test_token_guard.py | test_rejects_oversized, test_permits_normal, test_token_count_includes_message_overhead, test_check_token_limit_raises_on_exceeded, test_check_token_limit_passes_for_normal, test_multi_message_token_guard | Yes | 6 automated tests; test_post_message_422_when_message_too_long adds integration coverage
CORE-06 row: (none) | (none) | Manual | docker-compose.yml include directive verified by code review; startup failure on missing SENTINEL_API_KEY verified per 01-VERIFICATION.md human_verification item 4
CORE-07 row: (none) | (none) | Manual | docker-compose.yml uses include: only; -f prohibited by inline comment; verified per 01-VERIFICATION.md Key Link rows 5-6

After the table, add a "## Nyquist Compliance Decision" section explaining that all 7 CORE requirements are covered — CORE-01/03/05 fully automated, CORE-02/04 partially automated with code review, CORE-06/07 manual with evidence from VERIFICATION.md. Conclude with: nyquist_compliant: true — all requirements have documented test coverage or manual verification evidence.

Add a "## Task Verification Summary" section listing the 3 plans (01-01, 01-02, 01-03) as VERIFIED, referencing 01-VERIFICATION.md for full detail.
  </action>
  <verify>
    <automated>grep 'nyquist_compliant: true' .planning/phases/01-core-loop/01-VALIDATION.md && grep 'CORE-05' .planning/phases/01-core-loop/01-VALIDATION.md && grep 'Nyquist Test Matrix' .planning/phases/01-core-loop/01-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/01-core-loop/01-VALIDATION.md` exists
    - `grep 'nyquist_compliant: true' .planning/phases/01-core-loop/01-VALIDATION.md` returns at least one match
    - `grep 'nyquist_compliant: false' .planning/phases/01-core-loop/01-VALIDATION.md` returns no matches
    - `grep 'Nyquist Test Matrix' .planning/phases/01-core-loop/01-VALIDATION.md` returns the section heading
    - `grep -c 'CORE-0' .planning/phases/01-core-loop/01-VALIDATION.md` returns 7 or more
    - `grep 'test_token_guard.py' .planning/phases/01-core-loop/01-VALIDATION.md` returns at least one line
    - `grep 'test_pi_adapter.py' .planning/phases/01-core-loop/01-VALIDATION.md` returns at least one line
  </acceptance_criteria>
  <done>01-VALIDATION.md created with nyquist_compliant: true in frontmatter and a Nyquist Test Matrix table mapping all 7 CORE requirements to test files, test functions, or manual verification evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Create 03-VALIDATION.md with Nyquist Test Matrix</name>
  <files>.planning/phases/03-interfaces/03-VALIDATION.md</files>
  <read_first>
    - .planning/phases/03-interfaces/03-VERIFICATION.md (source of truth — read Requirements Coverage table and Behavioral Spot-Checks; note that test suite 35/35 green is confirmed there)
    - .planning/phases/22-requirements-traceability-repair/22-CONTEXT.md (D-03 decision — documentation-only, no new tests)
    - sentinel-core/tests/test_auth.py (verify 4 test functions exist: test_auth_rejects_missing_key, test_auth_rejects_wrong_key, test_health_bypasses_auth, test_auth_accepts_valid_key)
  </read_first>
  <action>
Create .planning/phases/03-interfaces/03-VALIDATION.md as a new file. The file MUST start with a YAML frontmatter block containing these exact fields:

    phase: 03-interfaces
    nyquist_compliant: true
    wave_0_complete: true
    status: human_needed
    verified: 2026-04-10T15:20:00Z
    nyquist_written: 2026-04-11T00:00:00Z
    note: "Nyquist matrix written retroactively by Phase 22. Documentation-only. Status remains human_needed — Discord and iMessage require live hardware."

After the frontmatter, write a "# Phase 03: Interfaces — Validation" heading, then a "## Nyquist Test Matrix" section with a markdown table. The table must have columns: Requirement, Description, Test File, Test Function(s), Automated?, Evidence.

Map each requirement as follows:

IFACE-01 row: test_message.py | test_post_message_returns_response_envelope | Partial | MessageEnvelope (content, user_id) shape tested via POST /message; both Discord and iMessage produce this shape per code review
IFACE-02 row: (none) | (none) | Manual | bot.py + Dockerfile + compose.yml present; Docker container starts per Phase 6 user UAT confirmation; docker-compose.yml include active after Phase 21
IFACE-03 row: (none) | (none) | Manual | interaction.response.defer(thinking=True) at bot.py line 77 — first await call in sentask(); 3s SLA requires live Discord per 03-VERIFICATION.md human_verification item 1
IFACE-04 row: (none) | (none) | Manual | channel.create_thread() at bot.py line 84; thread.send(ai_response) sends into thread; full verification requires live Discord
IFACE-05 row: (none) | (none) | Manual | bridge.py IMESSAGE_ENABLED guard verified (IMESSAGE_ENABLED=false exit 0 confirmed by CLI run); full send/receive requires macOS Full Disk Access
IFACE-06 row: test_auth.py | test_auth_rejects_missing_key, test_auth_rejects_wrong_key, test_health_bypasses_auth, test_auth_accepts_valid_key | Yes | 4 automated tests; all pass in 35/35 suite green run per 03-VERIFICATION.md behavioral spot-checks

After the table, add a "## Nyquist Compliance Decision" section explaining that all 6 IFACE requirements are covered — IFACE-01 and IFACE-06 have automated tests; IFACE-02/03/04 require live Discord gateway (not a missing test, a genuine environmental dependency); IFACE-05 requires macOS Full Disk Access. Conclude with: nyquist_compliant: true — all requirements have documented test coverage or manual verification evidence.

Add a "## Task Verification Summary" section listing the 3 plans (03-01, 03-02, 03-03) as VERIFIED, referencing 03-VERIFICATION.md for full detail.
  </action>
  <verify>
    <automated>grep 'nyquist_compliant: true' .planning/phases/03-interfaces/03-VALIDATION.md && grep 'IFACE-06' .planning/phases/03-interfaces/03-VALIDATION.md && grep 'Nyquist Test Matrix' .planning/phases/03-interfaces/03-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-interfaces/03-VALIDATION.md` exists
    - `grep 'nyquist_compliant: true' .planning/phases/03-interfaces/03-VALIDATION.md` returns at least one match in the frontmatter
    - `grep 'nyquist_compliant: false' .planning/phases/03-interfaces/03-VALIDATION.md` returns no matches
    - `grep 'Nyquist Test Matrix' .planning/phases/03-interfaces/03-VALIDATION.md` returns the section heading
    - `grep -c 'IFACE-0' .planning/phases/03-interfaces/03-VALIDATION.md` returns 6 or more
    - `grep 'test_auth.py' .planning/phases/03-interfaces/03-VALIDATION.md` returns at least one line
    - `grep 'test_auth_accepts_valid_key' .planning/phases/03-interfaces/03-VALIDATION.md` returns at least one line
    - `grep 'test_auth_rejects_missing_key' .planning/phases/03-interfaces/03-VALIDATION.md` returns at least one line
  </acceptance_criteria>
  <done>03-VALIDATION.md created with nyquist_compliant: true in frontmatter and a Nyquist Test Matrix table mapping all 6 IFACE requirements to test files, test functions, or manual verification evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No threat surface — documentation-only phase. All changes are to `.planning/` markdown files with no code execution, no external API calls, and no user-facing functionality.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-02 | Tampering | .planning/PROJECT.md | accept | Documentation file; no executable code; git history provides audit trail |
</threat_model>

<verification>
Run after all tasks complete:

```bash
# Task 1 — PROJECT.md
grep -c '\[x\]' .planning/PROJECT.md
grep 'asyncio.gather' .planning/PROJECT.md

# Task 2 — 01-VALIDATION.md
grep 'nyquist_compliant: true' .planning/phases/01-core-loop/01-VALIDATION.md
grep -c 'CORE-0' .planning/phases/01-core-loop/01-VALIDATION.md

# Task 3 — 03-VALIDATION.md
grep 'nyquist_compliant: true' .planning/phases/03-interfaces/03-VALIDATION.md
grep -c 'IFACE-0' .planning/phases/03-interfaces/03-VALIDATION.md
```

All commands must return at least one matching line or a count of 6 or more.
</verification>

<success_criteria>
- PROJECT.md exists on disk with Phase 10 Knowledge Migration items checked
- 01-VALIDATION.md has nyquist_compliant: true and Nyquist Test Matrix for all 7 CORE requirements
- 03-VALIDATION.md has nyquist_compliant: true and Nyquist Test Matrix for all 6 IFACE requirements
</success_criteria>

<output>
After completion, create `.planning/phases/22-requirements-traceability-repair/22-02-SUMMARY.md`
</output>
