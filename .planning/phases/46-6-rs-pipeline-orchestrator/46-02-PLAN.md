---
phase: 46-6-rs-pipeline-orchestrator
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sentinel-core/app/services/model_resolution.py
  - sentinel-core/app/services/note_classifier.py
autonomous: true
requirements: [PIPE-02, PIPE-04, PIPE-05]

must_haves:
  truths:
    - "There is exactly ONE implementation of the LM Studio model-resolution + profile-lookup logic in the codebase; note_classifier and every six_rs LLM stage call the same helper"
    - "note_classifier's existing classify behavior is unchanged after the refactor (all pre-existing classifier tests stay green)"
  artifacts:
    - "sentinel-core/app/services/model_resolution.py exporting a public async model-resolution helper"
  key_links:
    - "note_classifier.py imports the helper from model_resolution.py instead of defining _resolve_model_for_classification locally"
    - "The helper returns the (prefixed_model_id, profile, api_base) triple the six_rs structured completions need"
---

<objective>
Promote `note_classifier._resolve_model_for_classification()` into a shared, public helper module `app/services/model_resolution.py` so that Reduce, Reweave, Rethink, and Verify's optional claim-title assist (D-05 independent structured completions) all resolve their LM Studio model + profile through ONE implementation — eliminating the 5-way drift risk RESEARCH flags in Assumption A2 (the same failure class as the carrier-allowlist drift Pitfall 2 warns about). This is pure infrastructure with no behavior change to classification; it exists so the Wave-2 six_rs stages have a stable, non-private seam to import.

Purpose: A single source of truth for structured-completion model resolution, per D-05 and RESEARCH A2.
Output: `model_resolution.py` with a public resolver; `note_classifier.py` refactored to consume it with zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-6-rs-pipeline-orchestrator/46-CONTEXT.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-RESEARCH.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-PATTERNS.md

@sentinel-core/app/services/note_classifier.py
@shared/sentinel_shared/llm_call.py
</context>

## Artifacts this plan produces

- **Module:** `sentinel-core/app/services/model_resolution.py`
- **Function:** `async def resolve_structured_model() -> tuple[str, object | None, str | None]` (public; returns `(prefixed_model_id, profile, api_base)`) — the promoted, renamed twin of `note_classifier._resolve_model_for_classification` (46-PATTERNS.md lines 201-229).
- **Refactor:** `note_classifier.py` now imports and delegates to `resolve_structured_model` (the private `_resolve_model_for_classification` becomes a thin alias or is removed, whichever keeps existing call sites/tests green).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract public model-resolution helper</name>
  <files>sentinel-core/app/services/model_resolution.py</files>
  <read_first>
    - `sentinel-core/app/services/note_classifier.py` lines 190-246 — the exact `_resolve_model_for_classification` body to move verbatim (api_base normalization, `get_loaded_models`, `select_model("structured", ...)`, `ensure_litellm_prefix`/`strip_litellm_prefix`, `get_profile`, and every try/except-warn fallback).
    - Its import block at the top of `note_classifier.py` — copy the imports the helper depends on (`settings`, `get_loaded_models`, `select_model`, `ensure_litellm_prefix`, `strip_litellm_prefix`, `get_profile`, `logger`).
  </read_first>
  <action>
    Create `model_resolution.py` housing `resolve_structured_model()` — the byte-for-byte logic of `_resolve_model_for_classification` (task kind `"structured"`, `settings.model_preferred or settings.model_name` preference, all graceful `except`-warn fallbacks preserved). Keep the returned triple identical: `(prefixed_model_id, profile, api_base)`. Do NOT change any resolution semantics — this is a move, not a redesign (D-05: reuse the classifier's exact pattern). Add a module docstring noting it is the single resolver shared by `note_classifier` and every `six_rs/*` structured completion.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -c "import asyncio; from app.services.model_resolution import resolve_structured_model; print(callable(resolve_structured_model))"</automated>
  </verify>
  <done>A single public `resolve_structured_model` exists in `model_resolution.py`, returning the classifier's exact resolution triple with all fallbacks preserved.</done>
  <acceptance_criteria>
    - `resolve_structured_model` is importable and async, returning a 3-tuple `(prefixed_model_id, profile, api_base)`.
    - Its logic matches the original resolver's fallbacks exactly (same warnings, same defaults) — verified by diffing behavior against the prior private function.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: refactor note_classifier to consume the shared helper (zero behavior change)</name>
  <files>sentinel-core/app/services/note_classifier.py</files>
  <read_first>
    - `sentinel-core/app/services/note_classifier.py` — every internal call site of `_resolve_model_for_classification` (grep within the file) so the delegation keeps `classify_note` identical.
    - Any test that patches `note_classifier._resolve_model_for_classification` (grep `tests/` for that symbol) — if a test patches it by name, keep a module-level alias so the patch target still exists.
  </read_first>
  <action>
    Replace the local `_resolve_model_for_classification` body with a delegation to `model_resolution.resolve_structured_model` (import it at module top). If any existing test patches `note_classifier._resolve_model_for_classification` by that exact name, retain a thin module-level reference (`_resolve_model_for_classification = resolve_structured_model` or a wrapper) so mock.patch targets remain valid — do NOT break the patch surface. Confirm `classify_note`'s output is unchanged.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_note_classifier.py -q</automated>
  </verify>
  <done>note_classifier delegates to the shared resolver with zero behavior change; existing classifier tests and any patch targets still resolve.</done>
  <acceptance_criteria>
    - All pre-existing `note_classifier` tests pass unchanged (no behavior regression — D-05 reuse invariant).
    - The codebase contains exactly one resolution implementation; `note_classifier` no longer duplicates it.
    - Any prior `mock.patch("app.services.note_classifier._resolve_model_for_classification")` in the suite still resolves.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sentinel-core → LM Studio/exo (`settings.lmstudio_base_url`) | The resolver queries loaded-model state over HTTP; already an established, probed boundary (no new surface introduced by moving the code). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-DRIFT | Tampering | Duplicated model-resolution logic drifting across six_rs modules | medium | mitigate | Ship ONE public `resolve_structured_model`; six_rs stages import it (this plan is the mitigation for A2/Pitfall-2-class drift) |
| T-46-LLM-DOWN | Denial of Service | Resolver called when LM Studio/exo is unloaded/404 | low | accept | Existing graceful `except`-warn fallbacks are preserved verbatim (resolver degrades to configured `MODEL_NAME`, never raises) — unchanged behavior |

No new third-party packages are introduced (RESEARCH Package Legitimacy Audit: N/A) — no `T-46-SC` supply-chain threat.
</threat_model>

<verification>
- `pytest tests/test_note_classifier.py` green (behavior parity).
- `model_resolution.resolve_structured_model` importable and callable.
- Single resolver implementation in tree (grep confirms note_classifier no longer defines the resolution body).
</verification>

<success_criteria>
- Shared resolver shipped; note_classifier delegates to it with zero behavior change.
- Wave-2 six_rs stages have a stable public import target for structured-completion model resolution (D-05).
</success_criteria>

<output>
Create `.planning/phases/46-6-rs-pipeline-orchestrator/46-02-SUMMARY.md` when done, recording the public helper signature Wave-2 stages import.
</output>
