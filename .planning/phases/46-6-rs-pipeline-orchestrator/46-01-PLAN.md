---
phase: 46-6-rs-pipeline-orchestrator
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - sentinel-core/app/services/six_rs/__init__.py
  - sentinel-core/tests/test_six_rs_reduce.py
  - sentinel-core/tests/test_six_rs_reflect.py
  - sentinel-core/tests/test_six_rs_reweave.py
  - sentinel-core/tests/test_six_rs_verify.py
  - sentinel-core/tests/test_six_rs_rethink.py
  - sentinel-core/tests/test_pipeline_orchestrator.py
  - sentinel-core/tests/test_pipeline_status_store.py
  - sentinel-core/tests/test_pipeline_routes.py
autonomous: true
requirements: [PIPE-02, PIPE-03, PIPE-04, PIPE-05, PIPE-06, PIPE-07]

must_haves:
  truths:
    - "Every PIPE-02..07 behavior has a named, collectible RED test that fails only because implementation has not landed yet"
    - "pytest collection succeeds across the whole core suite (no ImportError at collection time)"
    - "The concurrency test asserts the lock is acquired BEFORE any inbox read (Pitfall 8 ordering)"
    - "The Reduce test asserts a malformed completion is still filed as a draft, never dropped (Pitfall 6)"
  artifacts:
    - "sentinel-core/app/services/six_rs/__init__.py (empty package marker)"
    - "sentinel-core/tests/test_six_rs_{reduce,reflect,reweave,verify,rethink}.py"
    - "sentinel-core/tests/test_pipeline_orchestrator.py"
    - "sentinel-core/tests/test_pipeline_status_store.py"
    - "sentinel-core/tests/test_pipeline_routes.py"
  key_links:
    - "Function-scope imports inside each test body so collection passes before Waves 1-4 land the modules (Phase 33-01 / 45-01 precedent)"
    - "FakeVault + _ImmediateTaskRunner test doubles reused from tests/test_note_sweep_runner.py + tests/fakes/vault.py"
---

<objective>
Create the Wave 0 RED test scaffolds that pin every observable behavior this phase must deliver, following the project's established "RED stubs with function-scope imports so collection stays green" convention (STATE decisions Phase 33-01, 45-01). These tests are the executable contract Waves 1-4 implement to GREEN. Also create the empty `six_rs/` package marker so both Wave-2 stage plans can add modules without an `__init__.py` ownership conflict.

Purpose: Satisfy the Nyquist Dimension-8 requirement that every downstream task has a real `<automated>` test to turn green — no phase task ships without a pre-existing failing test.
Output: One `six_rs/__init__.py` package stub + eight RED test files covering PIPE-02..07, Pitfall 6 (Reduce files draft on malformed output), and Pitfall 8 (lock-before-inbox-read ordering).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-CONTEXT.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-RESEARCH.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-PATTERNS.md

# Test-double conventions to mirror (read these before writing any stub):
@sentinel-core/tests/test_note_sweep_runner.py
@sentinel-core/tests/fakes/vault.py
@sentinel-core/tests/test_sweep_status_store.py
@sentinel-core/tests/test_moc_maintenance.py
</context>

## Artifacts this plan produces

- **Package:** `sentinel-core/app/services/six_rs/__init__.py` (empty marker)
- **Test files (RED):**
  - `tests/test_six_rs_reduce.py` — `test_reduce_extracts_claim_and_schema_draft`, `test_reduce_malformed_completion_still_filed_as_draft` (Pitfall 6)
  - `tests/test_six_rs_reflect.py` — `test_reflect_embedding_first_hub_match`, `test_reflect_no_wikilink_from_notes_into_self` (T-46-03)
  - `tests/test_six_rs_reweave.py` — `test_reweave_append_idempotent` (PIPE-04, D-01)
  - `tests/test_six_rs_verify.py` — `test_verify_failure_requeues_with_retry_cap`, `test_verify_reuses_check_note_compliance` (PIPE-07, D-02a)
  - `tests/test_six_rs_rethink.py` — `test_rethink_triage_dispositions`, `test_rethink_tolerates_absent_tensions_dir` (PIPE-05, A3)
  - `tests/test_pipeline_orchestrator.py` — `test_ralph_mode_reduce_and_reflect` (PIPE-02), `test_pipeline_mode_full_sequence` (PIPE-03), `test_concurrent_pipeline_and_sweep_refused` (PIPE-06, Pitfall 8)
  - `tests/test_pipeline_status_store.py` — `test_status_store_round_trips_pipeline_report` (D-03a)
  - `tests/test_pipeline_routes.py` — `test_pipeline_start_admin_gated_403`, `test_pipeline_status_returns_report_shape` (PIPE-06)
- **Shared test helper:** `_ImmediateTaskRunner` (synchronous task runner; define locally in `test_pipeline_orchestrator.py` and import where needed, or duplicate the 6-line class per test module — mirror `test_note_sweep_runner.py`)

<tasks>

<task type="auto">
  <name>Task 1: six_rs package stub + six_rs stage RED tests</name>
  <files>sentinel-core/app/services/six_rs/__init__.py, sentinel-core/tests/test_six_rs_reduce.py, sentinel-core/tests/test_six_rs_reflect.py, sentinel-core/tests/test_six_rs_reweave.py, sentinel-core/tests/test_six_rs_verify.py, sentinel-core/tests/test_six_rs_rethink.py</files>
  <read_first>
    - `sentinel-core/tests/test_moc_maintenance.py` — existing `find_hub_candidate`/`attach_to_hub` fixtures against FakeVault; REUSE these, do not rewrite (RESEARCH Testing pattern).
    - `sentinel-core/tests/fakes/vault.py` — FakeVault Protocol surface (`read_note`/`write_note`/`delete_note`/`list_directory`/`acquire_sweep_lock`/`release_sweep_lock`).
    - `sentinel-core/app/services/note_schema.py` — `check_note_compliance(body, filename_slug)` return keys (`has_schema`/`has_type`/`has_claim_title`/`has_wikilink`/`failures`) that verify tests assert against.
    - 46-RESEARCH.md "Validation Architecture" → Phase Requirements → Test Map (exact test names) and Testing pattern (lines 442-465, 538-563).
  </read_first>
  <action>
    Create the empty `six_rs/__init__.py` package marker (module docstring only). Then create the five six_rs stage RED test files using FUNCTION-SCOPE imports inside each test body (e.g. `from app.services.six_rs.reduce import reduce_entry` written INSIDE the test function, per STATE Phase 33-01 precedent) so pytest collection stays green while the modules do not exist yet. Write assertions describing the intended API, not implementation:
    - reduce: assert `reduce_entry(entry_text)` yields a result carrying `claim_title`, `body`, `schema_type`; and a SECOND test that when the completion is malformed/garbage the pipeline still files a note tagged as a draft rather than dropping it (Pitfall 6 — assert the draft-file behavior at whatever seam Wave 2 exposes; may target the orchestrator helper if Reduce itself is a pure transform — leave a clear TODO marker naming the intended assertion).
    - reflect: assert embedding-first hub match returns the highest-cosine hub above the floor and calls `attach_to_hub`; and assert no wikilink is ever created FROM a `notes/` note INTO a `self/` path (T-46-03 information-disclosure guard).
    - reweave: assert a second identical reweave pass does NOT stack a duplicate `## Reweave — {date}` section (idempotent by dated marker — D-01).
    - verify: assert the failure path requeues to inbox with an incremented retry count and stops requeuing at the cap (PIPE-07, D-02b); assert the compliance decision comes from `note_schema.check_note_compliance` reuse (D-02a), not a re-implemented parser.
    - rethink: assert triage assigns one of PROMOTE/IMPLEMENT/METHODOLOGY/ARCHIVE/KEEP per item; assert it tolerates an absent/empty `ops/tensions/` directory (A3 — observations-only still triages).
    Stub `acompletion_with_profile` call sites via `unittest.mock.patch` returning canned JSON; do NOT hit a live LLM. These are RED now (imports resolve to missing symbols at call time) — that is expected and correct.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_six_rs_reduce.py tests/test_six_rs_reflect.py tests/test_six_rs_reweave.py tests/test_six_rs_verify.py tests/test_six_rs_rethink.py --collect-only -q</automated>
  </verify>
  <done>six_rs package importable; the five stage RED test files collect cleanly and fail (not error) at runtime with the RESEARCH-mandated test names.</done>
  <acceptance_criteria>
    - `six_rs/__init__.py` exists and `import app.services.six_rs` succeeds.
    - `pytest --collect-only` on all five files reports zero collection errors (function-scope imports keep collection green).
    - Running the five files (without `--collect-only`) shows the named tests FAILING (RED) — never erroring at collection, never passing.
    - Test names match the RESEARCH Test Map exactly (`test_reweave_append_idempotent`, `test_verify_failure_requeues_with_retry_cap`, `test_rethink_triage_dispositions`).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: orchestrator + route + status-store RED tests</name>
  <files>sentinel-core/tests/test_pipeline_orchestrator.py, sentinel-core/tests/test_pipeline_status_store.py, sentinel-core/tests/test_pipeline_routes.py</files>
  <read_first>
    - `sentinel-core/tests/test_note_sweep_runner.py` — `_ImmediateTaskRunner` shape, `FakeVault` seeding, `probe_*` patching (background-task test template).
    - `sentinel-core/tests/test_sweep_status_store.py` — status-store round-trip test to mirror as `test_pipeline_status_store.py`.
    - `sentinel-core/app/routes/note.py` lines 126-196 — `_is_admin_route` gate + `/vault/sweep/start` + `/vault/sweep/status` shape the route test mirrors.
    - `sentinel-core/app/services/inbox.py` `append_entry`/`INBOX_PATH` — how to seed `inbox/_pending-classification.md` into FakeVault for orchestrator tests.
  </read_first>
  <action>
    Create `test_pipeline_orchestrator.py` with a local `_ImmediateTaskRunner` (records scheduled coroutines; caller awaits `run_all()`), a `_make_vault()` helper seeding `inbox/_pending-classification.md` via `inbox.append_entry`, and function-scope imports of `pipeline_orchestrator`/`start_pipeline`. Write:
    - `test_ralph_mode_reduce_and_reflect` (PIPE-02): after a `mode="ralph"` run, assert a `notes/{slug}.md` was written for each inbox entry and the reflect/hub path was invoked; the returned/stored report shows `reduced` and `hubs_touched` counts.
    - `test_pipeline_mode_full_sequence` (PIPE-03): assert a `mode="pipeline"` run exercises Reduce, Verify, Reflect, Reweave, and a single end-of-run Rethink, with per-phase counts populated.
    - `test_concurrent_pipeline_and_sweep_refused` (PIPE-06 / Pitfall 8): pre-acquire the shared sweep lock on the FakeVault, then assert `pipeline_orchestrator.run(...)` raises `SweepInProgressError` BEFORE any `parse_inbox`/inbox read occurs (assert the inbox `read_note` was NOT called, or that the raise happens with an empty processed count) — lock acquisition strictly precedes the inbox walk.
    Create `test_pipeline_status_store.py` mirroring `test_sweep_status_store.py`: construct a small report-shaped object (or a `PipelineReport` via function-scope import) and assert `set_pipeline_status_from_report` → `get_pipeline_status()` round-trips `mode` + the per-phase counts (D-03a), and `reset_pipeline_status()` returns to idle.
    Create `test_pipeline_routes.py` using FastAPI TestClient (mirror how existing route tests seed `app.state.route_ctx`): `test_pipeline_start_admin_gated_403` asserts a non-admin `user_id` gets 403 from `POST /vault/pipeline/start`; `test_pipeline_status_returns_report_shape` asserts `GET /vault/pipeline/status` returns the report dict shape (ungated). Use function-scope imports so collection stays green pre-implementation.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_pipeline_orchestrator.py tests/test_pipeline_status_store.py tests/test_pipeline_routes.py --collect-only -q</automated>
  </verify>
  <done>Orchestrator, status-store, and route RED tests collect cleanly; the concurrency test encodes lock-before-inbox-read; the status test asserts mode + per-phase counts.</done>
  <acceptance_criteria>
    - All three files collect with zero errors.
    - The concurrency test explicitly encodes the lock-before-inbox-read ordering (Pitfall 8), not merely "raises somewhere".
    - The status-store test asserts `mode` + per-phase counts survive the round trip (D-03a).
    - The route test asserts the 403 admin gate on start and an ungated status read.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: whole-suite collection gate (no regressions introduced)</name>
  <files>sentinel-core/tests/test_pipeline_orchestrator.py</files>
  <read_first>
    - The eight files created in Tasks 1-2 (this task adds no new files; it verifies the suite still collects and the 550+ baseline is intact).
  </read_first>
  <action>
    Run the full core test suite in collect-only mode to prove the new RED scaffolds introduced no collection-time ImportError anywhere, then run the full suite normally to confirm the pre-existing 550+ baseline is still green (the new files add only RED tests, which are excluded from the "green" expectation until later waves). If any pre-existing test flips red because a stub shadowed a real module or a fixture name collided, fix the stub in place (rename/move) — do not leave a suite regression. Record the RED-count of the new files in the SUMMARY so later waves know exactly which tests must flip GREEN.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/ --collect-only -q</automated>
  </verify>
  <done>Full-suite collection is error-free, the 550+ baseline stays green, and the SUMMARY enumerates every new RED node-id later waves must flip.</done>
  <acceptance_criteria>
    - Full-suite `--collect-only` reports zero errors.
    - `cd sentinel-core && .venv/bin/python -m pytest tests/ -q` shows the pre-existing 550+ tests still passing; only the new phase-46 tests are red.
    - SUMMARY lists the exact set of new RED test node-ids that Waves 1-4 must turn green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → module under test | Tests are the only consumer here; no runtime trust boundary is crossed. FakeVault replaces the real Obsidian REST seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-TST | Tampering | RED test stubs shadowing real modules | low | mitigate | Function-scope imports only; full-suite `--collect-only` gate (Task 3) proves no real module is shadowed and the 550+ baseline stays green |

No new third-party packages are added by this phase (RESEARCH "Package Legitimacy Audit: Not applicable"), so there is no `T-46-SC` supply-chain install threat and no legitimacy checkpoint is required.
</threat_model>

<verification>
- `cd sentinel-core && .venv/bin/python -m pytest tests/ --collect-only -q` → zero collection errors.
- New phase-46 test node-ids are RED (fail at runtime), by design, until their implementing waves land.
- Pre-existing 550+ suite remains GREEN.
</verification>

<success_criteria>
- `six_rs/__init__.py` importable; eight RED test files present and collectible.
- Named tests match the RESEARCH Test Map, including the Pitfall-6 draft-on-malformed and Pitfall-8 lock-ordering cases.
- No pre-existing test regressed to red.
</success_criteria>

<output>
Create `.planning/phases/46-6-rs-pipeline-orchestrator/46-01-SUMMARY.md` when done, listing the exact RED test node-ids each later wave must turn GREEN.
</output>
