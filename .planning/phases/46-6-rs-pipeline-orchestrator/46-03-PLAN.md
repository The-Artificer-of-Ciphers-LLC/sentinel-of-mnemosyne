---
phase: 46-6-rs-pipeline-orchestrator
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - sentinel-core/app/services/inbox.py
  - sentinel-core/app/services/pipeline_status_store.py
autonomous: true
requirements: [PIPE-01, PIPE-06, PIPE-07]

must_haves:
  truths:
    - "A PendingEntry can carry a bounded retry_count that survives a render → parse round-trip"
    - "Existing inbox files with no retry_count line still parse (default 0) — no migration needed"
    - "Zero-friction capture is unchanged: append_entry still lands raw content in inbox/ with no validation gate (PIPE-01)"
    - "pipeline_status_store round-trips a PipelineReport's mode + per-phase counts, mirroring sweep_status_store"
  artifacts:
    - "sentinel-core/app/services/inbox.py with retry_count + needs_attention on PendingEntry, parsed and rendered"
    - "sentinel-core/app/services/pipeline_status_store.py (get/set/patch/reset + _new_status)"
  key_links:
    - "_parse_entry_section reads a `- retry_count: N` line with the same safe int-coercion as confidence"
    - "pipeline_status_store.set_pipeline_status_from_report reads report fields duck-typed (no PipelineReport import), exactly as sweep_status_store does"
---

<objective>
Ship the two pure/in-memory infra pieces the orchestrator needs, with no LLM and no new trust boundary: (1) extend the shipped `inbox.py` `PendingEntry` with a bounded `retry_count` (and a `needs_attention` flag) so D-02's Verify-failure requeue can count retries and stop at the cap; and (2) create `pipeline_status_store.py` as a near-verbatim clone of `sweep_status_store.py` holding the in-memory `PipelineReport` progress dict (D-03a per-phase counts, D-04's separate-status-store split). This plan also carries the PIPE-01 regression guard: capture stays frictionless — the inbox intake path is not gated or changed.

Purpose: Provide the bounded-retry queue field (D-02/D-02b) and the pull-only status store (D-03a/D-04) that Wave-3 orchestration composes.
Output: `inbox.py` additive schema change (backward compatible) + `pipeline_status_store.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-6-rs-pipeline-orchestrator/46-CONTEXT.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-RESEARCH.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-PATTERNS.md

@sentinel-core/app/services/inbox.py
@sentinel-core/app/services/sweep_status_store.py
</context>

## Artifacts this plan produces

- **Modified `inbox.py`:** `PendingEntry` gains `retry_count: int = 0` and `needs_attention: bool = False`; `_parse_entry_section` reads both (safe coercion); `_render_entry` emits both; `append_entry` accepts/threads `retry_count` (or a sibling `requeue_entry(...)` helper that increments it).
- **New `pipeline_status_store.py`:** `_PIPELINE_STATUS` dict, `get_pipeline_status()`, `set_pipeline_status_from_report(report)`, `patch_pipeline_status(**kwargs)`, `reset_pipeline_status()`, `_new_status(pipeline_id, status, mode)` — field set covers `mode` + `entries_total`/`entries_processed`/`reduced`/`hubs_touched`/`reweave_edits`/`verify_failed`/`verify_requeued`/`errors` (D-03a).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extend PendingEntry with bounded retry_count + needs_attention</name>
  <files>sentinel-core/app/services/inbox.py</files>
  <read_first>
    - `sentinel-core/app/services/inbox.py` lines 47-56 (`PendingEntry`), 84-121 (`_parse_entry_section` — copy the `confidence` safe-coercion pattern), 141-154 (`_render_entry`), 170-198 (`append_entry`).
    - 46-PATTERNS.md lines 365-435 — the exact field-read / line-emission excerpts to mirror for `retry_count`.
    - 46-RESEARCH.md "Runtime State Inventory" (lines 372-374) — confirms missing `retry_count` lines default to 0, so no migration step.
  </read_first>
  <action>
    Add `retry_count: int = 0` and `needs_attention: bool = False` to `PendingEntry`. In `_parse_entry_section`, read a `- retry_count: N` line using the SAME try/except int-coercion shape used for `confidence` (default 0 on missing/invalid) and a `- needs_attention: true/false` line (default False). In `_render_entry`, emit `- retry_count: {e.retry_count}` and `- needs_attention: {e.needs_attention}` lines alongside the existing fields. Extend `append_entry` to accept an optional `retry_count`/`needs_attention` (defaulting to 0/False so existing capture calls are unaffected — PIPE-01 frictionless capture unchanged), OR add a sibling `requeue_entry(body, candidate_text, *, retry_count, needs_attention, ...)` twin — Claude's discretion, but the requeue path must be able to set an incremented count. Do NOT introduce any validation gate on capture. Confirm a round-trip: `_render_entry` → `parse_inbox` preserves `retry_count`.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_inbox.py -q</automated>
  </verify>
  <done>PendingEntry carries retry_count + needs_attention that round-trip through render/parse; legacy entries default to 0; capture path unchanged (PIPE-01).</done>
  <acceptance_criteria>
    - `PendingEntry(retry_count=2)` renders a `- retry_count: 2` line that `parse_inbox` reads back as 2.
    - An inbox body with NO `retry_count` line parses to `retry_count=0` (backward compatible — A-confirmed no migration).
    - All pre-existing `inbox` tests stay green; `append_entry`'s default call (capture path) is unchanged (PIPE-01).
    - `needs_attention` round-trips likewise.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline_status_store cloned from sweep_status_store</name>
  <files>sentinel-core/app/services/pipeline_status_store.py</files>
  <read_first>
    - `sentinel-core/app/services/sweep_status_store.py` (whole 43-line file) — the exact dict/get/set/patch/reset shape to clone.
    - 46-PATTERNS.md lines 109-143 — the rename map (`_PIPELINE_STATUS` / `get_pipeline_status` / `set_pipeline_status_from_report` / `patch_pipeline_status` / `reset_pipeline_status`) and the explicit instruction NOT to unify the two stores (YAGNI).
    - `sentinel-core/tests/test_pipeline_status_store.py` (Wave 0) — the round-trip assertions this module must satisfy.
  </read_first>
  <action>
    Create `pipeline_status_store.py` as a near-verbatim copy of `sweep_status_store.py`, substituting the `PipelineReport` field set for the sweep fields (D-03a): `pipeline_id`, `status` (idle|running|complete|blocked|error), `mode`, `entries_total`, `entries_processed`, `reduced`, `hubs_touched`, `reweave_edits`, `verify_failed`, `verify_requeued`, `errors` (list). `set_pipeline_status_from_report(report)` reads these off `report` DUCK-TYPED (no import of PipelineReport — mirror how `set_sweep_status_from_report` avoids importing SweepReport). Add a `_new_status(pipeline_id, status, mode)` factory returning the initial running dict (the runner in Wave 3 calls it). Do NOT unify with the sweep store. Keep it a separate in-memory dict (D-04: lockfile handles exclusion, status store handles progress — orthogonal).
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_pipeline_status_store.py -q</automated>
  </verify>
  <done>pipeline_status_store round-trips a report's mode + per-phase counts (duck-typed, no PipelineReport import) and resets to idle; the Wave-0 store test is GREEN.</done>
  <acceptance_criteria>
    - `set_pipeline_status_from_report(report)` then `get_pipeline_status()` returns a dict carrying `mode` and every per-phase count (D-03a).
    - `reset_pipeline_status()` returns the store to `status="idle"` with zeroed counts.
    - No import of any `PipelineReport` type (duck-typed reads only), matching the sweep store precedent.
    - `test_pipeline_status_store.py` (Wave 0) is now GREEN.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inbox markdown file (`inbox/_pending-classification.md`) → parser | Vault content parsed into `PendingEntry`; already an established boundary. New numeric field must fail safe (bad value → 0), never raise. |
| in-process caller → pipeline_status_store | Purely in-memory, single-process; no external boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-RETRY | Denial of Service | Unbounded Verify-failure requeue loop | medium | mitigate | `retry_count` is a bounded counter; Wave-3 orchestrator stops requeuing at a named `VERIFY_RETRY_CAP` and marks `needs_attention` (D-02/D-02b). This plan provides the storage that makes the bound enforceable. |
| T-46-PARSE | Tampering | Malformed `- retry_count:` line in a hand-edited inbox file | low | mitigate | Safe int-coercion (default 0 on invalid), identical to the shipped `confidence` parse — a garbage value can never crash the parser |

No new third-party packages (RESEARCH Package Legitimacy Audit: N/A) — no `T-46-SC` supply-chain threat.
</threat_model>

<verification>
- `pytest tests/test_inbox.py tests/test_pipeline_status_store.py` green.
- Backward-compat: an inbox fixture without `retry_count` parses cleanly (retry_count=0).
- Capture path (`append_entry` default call) unchanged — PIPE-01 frictionless-capture regression guard holds.
</verification>

<success_criteria>
- `PendingEntry.retry_count`/`needs_attention` parse+render round-trip (D-02b storage ready).
- `pipeline_status_store` clones the sweep store with the D-03a per-phase field set.
- No capture-time validation introduced (PIPE-01 preserved).
</success_criteria>

<output>
Create `.planning/phases/46-6-rs-pipeline-orchestrator/46-03-SUMMARY.md` when done, recording the final `PipelineReport` field names the status store expects (so Wave-3's `PipelineReport` model matches).
</output>
