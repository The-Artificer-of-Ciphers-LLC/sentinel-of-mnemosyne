---
phase: 46-6-rs-pipeline-orchestrator
plan: 07
type: execute
wave: 4
depends_on: ["46-06"]
files_modified:
  - interfaces/discord/core_gateway.py
  - interfaces/discord/command_router.py
  - interfaces/discord/bot.py
  - interfaces/discord/tests/test_core_gateway.py
  - interfaces/discord/tests/test_command_router_module.py
autonomous: true
requirements: [PIPE-02, PIPE-03, PIPE-04, PIPE-05, PIPE-06]

must_haves:
  truths:
    - ":ralph/:pipeline/:reweave/:rethink/:refactor call the real /vault/pipeline endpoints instead of resolving to a fixed chat prompt"
    - ":pipeline status (and the other verbs' status) polls GET /vault/pipeline/status and renders the real per-phase counts"
    - ":refactor maps to the same mode='rethink' call as :rethink (D-09 synonym)"
    - "A locked/concurrent run surfaces a clear 'a vault operation is already in progress' message (D-04a), not a silent no-op"
    - "The five dead _SUBCOMMAND_PROMPTS entries are removed (Phase 45-07 precedent); :tasks and the others stay"
  artifacts:
    - "interfaces/discord/core_gateway.py — call_core_pipeline_start / call_core_pipeline_status"
    - "interfaces/discord/command_router.py — explicit branch for the five pipeline verbs + two new keyword params"
    - "interfaces/discord/bot.py — dead prompts removed, _call_core_pipeline_* wrappers, kwargs wiring"
  key_links:
    - "command_router branch mirrors the vault-sweep branch (verb-parse status vs start; admin-gated for defense-in-depth)"
    - "bot.py adds call_core_pipeline_start/status to the kwargs dict passed into discord_router_bridge.handle_subcommand"
---

<objective>
Rewire the five pipeline verbs (`:ralph`, `:pipeline`, `:reweave`, `:rethink`, `:refactor`) in the Discord interface from today's dead fixed-prompt stubs to real calls against the Wave-3 `/vault/pipeline/*` endpoints — exactly the "drop dead prompts, wire a real endpoint" move Phase 45-07 did for `:graph`/`:stats`/`:check`. Add `call_core_pipeline_start`/`call_core_pipeline_status` to `core_gateway.py`, an explicit dispatch branch (mirroring the `vault-sweep` branch, including its admin gate for defense-in-depth) to `command_router.py`, and the wrapper + kwargs wiring to `bot.py`, and REMOVE the five stale `_SUBCOMMAND_PROMPTS` entries. `:refactor` is the D-09 synonym for `:rethink` → same `mode="rethink"`. The concurrency refusal (D-04a) surfaces as a clear user-facing message.

Purpose: The user-facing command surface for PIPE-02/03/04/05 and the status-poll half of PIPE-06.
Output: The three modified Discord files + updated Discord tests, all green in the `interfaces/discord` venv.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-6-rs-pipeline-orchestrator/46-CONTEXT.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-RESEARCH.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-PATTERNS.md

@interfaces/discord/core_gateway.py
@interfaces/discord/command_router.py
@interfaces/discord/bot.py
</context>

## Artifacts this plan produces

- **`core_gateway.py`:** `async def call_core_pipeline_start(*, user_id, mode, sentinel_client) -> str` (POST `vault/pipeline/start`, timeout 120.0, log-and-return-string on error); `async def call_core_pipeline_status(*, user_id, core_url, api_key) -> str` (GET `vault/pipeline/status`, timeout 20.0, formats the per-phase counts).
- **`command_router.py`:** an explicit `if subcmd in ("ralph","pipeline","reweave","rethink","refactor")` branch; two new keyword-only params `call_core_pipeline_start`, `call_core_pipeline_status` on `handle_subcommand`.
- **`bot.py`:** `_call_core_pipeline_start(user_id, mode)` / `_call_core_pipeline_status(user_id)` wrappers; the five removed `_SUBCOMMAND_PROMPTS` keys; two new kwargs-dict entries.
- **Tests:** RED-then-GREEN coverage in `test_core_gateway.py` (pipeline gateway fns) + `test_command_router_module.py` (branch dispatch, mode mapping, status verb, concurrency message).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED Discord tests for the five rewired verbs + gateway fns</name>
  <files>interfaces/discord/tests/test_core_gateway.py, interfaces/discord/tests/test_command_router_module.py</files>
  <read_first>
    - `interfaces/discord/tests/test_core_gateway.py` — how `call_core_sweep_start`/`call_core_sweep_status` are tested (mocked `sentinel_client.post_to_module` / mocked httpx GET); mirror for the pipeline fns.
    - `interfaces/discord/tests/test_command_router_module.py` — how the existing `vault-sweep` branch is tested (stubbed `call_core_sweep_*`, asserting the right callable is invoked with the right args); mirror for the pipeline branch.
    - `interfaces/discord/tests/conftest.py` — the shared discord stubs (discord module shim) so tests import cleanly.
    - 46-PATTERNS.md lines 439-534 — the exact branch/gateway/wrapper shapes.
  </read_first>
  <action>
    Add RED tests first (test-before-impl). In `test_core_gateway.py`: assert `call_core_pipeline_start(user_id="u", mode="pipeline", sentinel_client=<mock>)` POSTs to `vault/pipeline/start` with body `{"user_id":"u","mode":"pipeline"}` and returns a "started" string carrying the `pipeline_id`; assert `call_core_pipeline_status` GETs `vault/pipeline/status` and formats the per-phase counts (reduced/hubs_touched/reweave_edits/verify_requeued) from a mocked report dict; assert both degrade to a log-and-return-string on transport error. In `test_command_router_module.py`: assert each of the five subcommands routes to `call_core_pipeline_start` with the correct `mode` (`ralph→ralph`, `pipeline→pipeline`, `reweave→reweave`, `rethink→rethink`, `refactor→rethink`); assert `:<verb> status` routes to `call_core_pipeline_status`; assert a non-admin caller is refused (mirror the vault-sweep admin-gate test); and assert the concurrency message path returns the "a vault operation is already in progress" string when the gateway reports a blocked/refused start (D-04a). Use function-scope imports / injected stubs so these collect green and are RED until Task 2/3 land.
  </action>
  <verify>
    <automated>cd interfaces/discord && .venv/bin/python -m pytest tests/test_core_gateway.py tests/test_command_router_module.py --collect-only -q</automated>
  </verify>
  <done>RED discord tests exist for both gateway fns and all five router branches (including refactor→rethink, status verb, admin refusal, and the D-04a concurrency message); they collect clean and fail pending impl.</done>
  <acceptance_criteria>
    - Both test files collect with zero errors in the discord venv.
    - The new pipeline tests are RED (fail at runtime) until Tasks 2-3 implement the code.
    - Tests encode the exact mode-mapping table including `refactor→rethink` and the `status` verb branch.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: core_gateway pipeline fns + command_router branch/signature</name>
  <files>interfaces/discord/core_gateway.py, interfaces/discord/command_router.py</files>
  <read_first>
    - `interfaces/discord/core_gateway.py` lines 78-115 — `call_core_sweep_start`/`call_core_sweep_status` verbatim templates (httpx timeouts, `X-Sentinel-Key` header, `post_to_module`, log-and-return-string error shape).
    - `interfaces/discord/command_router.py` lines 39-63 (`handle_subcommand` keyword-only signature) + 144-153 (the `vault-sweep` branch) + 179-183 (the dead `subcommand_prompts` fallback these verbs currently hit).
    - 46-06-SUMMARY.md — the exact `/vault/pipeline/status` field names to format.
  </read_first>
  <action>
    In `core_gateway.py`, add `call_core_pipeline_start(*, user_id, mode, sentinel_client)` — POST `vault/pipeline/start` with `{"user_id": user_id, "mode": mode}`, `httpx.AsyncClient(timeout=120.0)`, `sentinel_client.post_to_module(...)`, returning a "Pipeline started: `{pipeline_id}` (mode={mode}). Use `:{...} status` to check progress." string; on exception log and return a failure string. Add `call_core_pipeline_status(*, user_id, core_url, api_key)` — GET `vault/pipeline/status` with the `X-Sentinel-Key` header and `timeout=20.0`, formatting the real per-phase counts from the report dict (status, mode, entries_processed/entries_total, reduced, hubs_touched, reweave_edits, verify_requeued); on exception log and return a failure string. If the start response indicates a blocked/refused run (or the status shows `blocked`), surface the D-04a "a vault operation is already in progress" wording.
    In `command_router.py`, add `call_core_pipeline_start` and `call_core_pipeline_status` to `handle_subcommand`'s keyword-only signature, and add an explicit branch BEFORE the `subcommand_prompts` fallback: `if subcmd in ("ralph","pipeline","reweave","rethink","refactor")`. Mirror the `vault-sweep` branch — gate with `is_admin` (defense-in-depth matching the sweep precedent and the admin-gated route; a non-admin gets the "Admin only…" message), parse the verb via `args.strip().split(maxsplit=1)`, route `status` → `call_core_pipeline_status(user_id)`, else map subcmd→mode (`refactor→rethink`, others identity) and call `call_core_pipeline_start(user_id, mode=mode)`.
  </action>
  <verify>
    <automated>cd interfaces/discord && .venv/bin/python -m pytest tests/test_core_gateway.py tests/test_command_router_module.py -q</automated>
  </verify>
  <done>The gateway fns hit /vault/pipeline/* with the sweep-identical shape, and the command_router branch maps the five verbs to modes + status, admin-gated, surfacing the D-04a concurrency message.</done>
  <acceptance_criteria>
    - The five verbs dispatch to `call_core_pipeline_start` with the correct mode (`refactor→rethink`); `<verb> status` dispatches to `call_core_pipeline_status`.
    - The gateway fns hit `vault/pipeline/start` and `vault/pipeline/status` with the sweep-identical header/timeout/error shape.
    - Non-admin callers are refused; a blocked run yields the D-04a message.
    - Task-1 pipeline tests are now GREEN.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: bot.py — remove dead prompts, add wrappers, wire kwargs</name>
  <files>interfaces/discord/bot.py</files>
  <read_first>
    - `interfaces/discord/bot.py` lines 175-188 — `_SUBCOMMAND_PROMPTS` (remove ONLY the `ralph`/`pipeline`/`reweave`/`rethink`/`refactor` keys; KEEP `next`/`health`/`goals`/`reminders`/`tasks`).
    - `interfaces/discord/bot.py` lines 259-275 — `_call_core_sweep_start`/`_call_core_sweep_status` wrapper templates.
    - `interfaces/discord/bot.py` lines 544-570 — the `kwargs={...}` dict passed into `discord_router_bridge.handle_subcommand` (add the two new keys alongside `call_core_sweep_start`/`call_core_sweep_status`).
  </read_first>
  <action>
    Remove the five stale entries (`ralph`, `pipeline`, `reweave`, `rethink`, `refactor`) from `_SUBCOMMAND_PROMPTS` (Phase 45-07 "drop dead prompts" precedent) — leave `tasks` and the others intact. Add `async def _call_core_pipeline_start(user_id: str, mode: str) -> str` delegating to `core_gateway.call_core_pipeline_start(user_id=user_id, mode=mode, sentinel_client=_sentinel_client)`, and `async def _call_core_pipeline_status(user_id: str) -> str` delegating to `core_gateway.call_core_pipeline_status(user_id=user_id, core_url=SENTINEL_CORE_URL, api_key=SENTINEL_API_KEY)` (mirror the sweep wrappers exactly). Add `"call_core_pipeline_start": _call_core_pipeline_start` and `"call_core_pipeline_status": _call_core_pipeline_status` to the kwargs dict. Confirm the five verbs no longer fall through to the fixed-prompt fallback.
  </action>
  <verify>
    <automated>cd interfaces/discord && .venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <done>The five dead prompts are removed, the two pipeline wrappers are wired into the kwargs dict, and the full discord suite is green with the verbs hitting real endpoints.</done>
  <acceptance_criteria>
    - The five keys are gone from `_SUBCOMMAND_PROMPTS`; `tasks`/`next`/`health`/`goals`/`reminders` remain.
    - `_call_core_pipeline_start`/`_call_core_pipeline_status` wrappers exist and are wired into the kwargs dict.
    - The full discord suite (258+ baseline) is green in the discord venv, with the new pipeline branch tests passing.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → command_router → core_gateway HTTP → sentinel-core | User command text crosses the interface into an authenticated HTTP call carrying `X-Sentinel-Key`. |
| non-admin user → admin-gated pipeline start | The route enforces admin (T-46-01); the interface adds a defense-in-depth admin gate mirroring `vault-sweep`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01-IFACE | Elevation of Privilege | Non-admin invoking a vault-mutating pipeline start from Discord | high | mitigate | Interface branch gates with `is_admin` (defense-in-depth, mirrors the `vault-sweep` branch); the core route independently enforces `_is_admin_route` (403) — a bypass at one layer is still caught at the other |
| T-46-04-IFACE | Denial of Service | User spamming pipeline starts to force concurrent runs | medium | mitigate | Shared lock refuses concurrent runs core-side (D-04); the interface surfaces the "a vault operation is already in progress" message (D-04a) rather than silently retrying |
| T-46-LEAK | Information Disclosure | Status formatter echoing raw core internals/vault paths back to Discord | low | mitigate | `call_core_pipeline_status` formats only bounded summary keys (status/mode/counts), never raw route internals or note bodies — mirrors the Phase 45-07 bounded-formatter precedent |

No new third-party packages (RESEARCH Package Legitimacy Audit: N/A) — no `T-46-SC` supply-chain threat.
</threat_model>

<verification>
- `cd interfaces/discord && .venv/bin/python -m pytest tests/ -q` green (258+ baseline + new pipeline branch tests).
- The five verbs route to `/vault/pipeline/*`; `:refactor` maps to `mode="rethink"`.
- Dead `_SUBCOMMAND_PROMPTS` entries removed; concurrency message (D-04a) surfaced.
</verification>

<success_criteria>
- The pipeline command surface calls real endpoints (PIPE-02/03/04/05) with pollable status (PIPE-06).
- Defense-in-depth admin gate + clear concurrency message (D-04a).
- No dead fixed-prompt fallbacks remain for the five verbs.
</success_criteria>

<output>
Create `.planning/phases/46-6-rs-pipeline-orchestrator/46-07-SUMMARY.md` when done, noting the final user-facing command help strings and the status-formatter field set.
</output>
