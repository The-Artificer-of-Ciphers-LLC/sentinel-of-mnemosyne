---
phase: 46-6-rs-pipeline-orchestrator
plan: 05
type: execute
wave: 2
depends_on: ["46-02"]
files_modified:
  - sentinel-core/app/services/six_rs/reflect.py
  - sentinel-core/app/services/six_rs/reweave.py
  - sentinel-core/app/services/six_rs/rethink.py
autonomous: true
requirements: [PIPE-02, PIPE-04, PIPE-05]

must_haves:
  truths:
    - "Reflect finds a hub by embedding-cosine first and only falls back to an LLM name when nothing clears the floor (D-07); it becomes the FIRST caller of the shipped attach_to_hub"
    - "Reweave appends a bounded `## Reweave — {date}` section and a second identical pass adds no duplicate (idempotent by dated marker — D-01)"
    - "Reflect/Reweave only ever read/write within notes//inbox/ — never treat a self/ path as linkable graph material (T-46-03)"
    - "Rethink assigns a disposition per observation and tolerates an absent/empty ops/tensions/ (A3)"
  artifacts:
    - "sentinel-core/app/services/six_rs/reflect.py — embedding-first hub lookup + attach (D-07)"
    - "sentinel-core/app/services/six_rs/reweave.py — append-only idempotent dated-section write (D-01, PIPE-04)"
    - "sentinel-core/app/services/six_rs/rethink.py — observations(+optional tensions) triage (PIPE-05)"
  key_links:
    - "reflect reuses moc_maintenance.find_hub_candidate + attach_to_hub + create_or_update_hub + propose_hub_slug + embedding_sidecar_index.eligible_entries — no fresh cosine loop"
    - "reweave reuses the attach_to_hub read→merge→single-write idempotency shape; never vault.patch_append"
    - "reflect/reweave/rethink resolve any LLM via model_resolution.resolve_structured_model"
---

<objective>
Build the three connection/backward-pass six_rs stages: **Reflect** (`reflect.py`) does embedding-first hub lookup (D-07, Pattern 4) reusing the Phase-45 `moc_maintenance` machinery — Phase 46 is where `attach_to_hub` finally gets its FIRST caller; **Reweave** (`reweave.py`) performs the D-01 auto-apply APPEND-ONLY backward pass, appending a bounded, idempotent `## Reweave — {date}` section to an older note (never rewriting existing prose); and **Rethink** (`rethink.py`) triages `ops/observations/` (and optionally-empty `ops/tensions/`, A3) into PROMOTE/IMPLEMENT/METHODOLOGY/ARCHIVE/KEEP dispositions. All three honor the T-46-03 boundary: they only read/write `notes/`/`inbox/` and never treat protected `self/` paths as linkable.

Purpose: The Reflect (PIPE-02 MOC updates), Reweave (PIPE-04 backward pass), and Rethink (PIPE-05 triage) stages of the 6 Rs.
Output: `six_rs/reflect.py` + `reweave.py` + `rethink.py`, turning their Wave-0 RED tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-6-rs-pipeline-orchestrator/46-CONTEXT.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-RESEARCH.md
@.planning/phases/46-6-rs-pipeline-orchestrator/46-PATTERNS.md
@.planning/research/PITFALLS.md

@sentinel-core/app/services/moc_maintenance.py
@sentinel-core/app/services/graph_analysis.py
@sentinel-core/app/services/embedding_sidecar_index.py
</context>

## Artifacts this plan produces

- **`six_rs/reflect.py`:** `async def find_and_attach_hub(vault, *, member_slug, note_vector, hub_paths, index, active_model, completion_fn=None) -> str | None` — cosine-match via `find_hub_candidate`; on match `attach_to_hub`; on miss `propose_hub_slug` + `create_or_update_hub`. Returns the hub path touched (or None).
- **`six_rs/reweave.py`:** `REWEAVE_SECTION_PREFIX = "## Reweave — "`; `async def reweave_note(vault, *, target_note_path, connection_context, date, completion_fn=None) -> bool` — LLM-drafts a bounded section body, appends idempotently under the dated marker (read → merge → single `write_note`), returns whether an edit was applied.
- **`six_rs/rethink.py`:** `class TriageResult(BaseModel)` (item ref + `disposition` enum); `async def triage(items: list[...]) -> list[TriageResult]`; reads observations/tensions content passed by the orchestrator.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: six_rs/reflect.py — embedding-first hub lookup + attach (D-07)</name>
  <files>sentinel-core/app/services/six_rs/reflect.py</files>
  <read_first>
    - `sentinel-core/app/services/moc_maintenance.py` lines 83-118 (`find_hub_candidate`), 153-178 (`attach_to_hub`), 252-291 (`propose_hub_slug`, `create_or_update_hub`), and `HUB_COSINE_FLOOR` (the `RecallConfig.semantic_cosine_floor` alias). Its docstring notes "Phase 46 wires the Reflect-stage caller" — you are that caller.
    - `sentinel-core/app/services/embedding_sidecar_index.py` — `eligible_entries(index, active_model, exclude_prefixes, query_dim)`, `EligibleEmbeddingEntry`, `EMBEDDING_INDEX_PATH`.
    - `sentinel-core/tests/test_moc_maintenance.py` — REUSE its `find_hub_candidate`/`attach_to_hub` fixtures (do not rewrite — RESEARCH Testing pattern).
    - 46-RESEARCH.md Pattern 4 + Open Question 1 (embed-on-reduce decision) — the note's vector is supplied by the orchestrator (on-demand single-note embed), NOT recomputed here.
  </read_first>
  <action>
    Implement `find_and_attach_hub(vault, *, member_slug, note_vector, hub_paths, index, active_model, completion_fn=None)`. Call `moc_maintenance.find_hub_candidate(note_vector=..., hub_paths=hub_paths, index=index, active_model=active_model)` FIRST (embedding-first, D-07) — do NOT write a fresh cosine loop (reuse `eligible_entries` + the shipped similarity). If a hub clears `HUB_COSINE_FLOOR`, call `attach_to_hub(vault, hub_path, member_slug)` (idempotent; never `patch_append`). If nothing clears the floor, fall back to `propose_hub_slug(member_texts=[...], completion_fn=...)` then `create_or_update_hub(vault, concept_slug=..., member_slug=...)` (LLM naming only as fallback, D-07). CRITICAL T-46-03: never pass or accept a `self/` path in `hub_paths`, and never create a wikilink from a `notes/` note into `self/` — assert this boundary in the module (e.g. guard that every hub_path is under `notes/`). Return the hub path touched or None. Resolve any completion_fn model via `resolve_structured_model` when the caller does not inject one.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_six_rs_reflect.py -q</automated>
  </verify>
  <done>Reflect is the first caller of attach_to_hub, embedding-first with LLM-name fallback (D-07), reusing find_hub_candidate (no fresh cosine) and never touching self/ (T-46-03).</done>
  <acceptance_criteria>
    - A note whose vector exceeds the floor against an existing hub is attached via `attach_to_hub` (no new hub created).
    - A note clearing nothing triggers the `propose_hub_slug` → `create_or_update_hub` fallback path.
    - No fresh cosine implementation exists in the module (grep confirms `find_hub_candidate` reuse) — D-07.
    - The self/ boundary test passes: no `self/` path is ever treated as a hub or wikilink target (T-46-03).
    - `test_six_rs_reflect.py` (Wave 0) is GREEN.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: six_rs/reweave.py — append-only idempotent backward-pass write (D-01)</name>
  <files>sentinel-core/app/services/six_rs/reweave.py</files>
  <read_first>
    - `sentinel-core/app/services/moc_maintenance.py` lines 136-178 — `_insert_member_wikilink` + `attach_to_hub`: the exact read→merge→single-`write_note` idempotency shape (dedupe-by-marker) Reweave must mirror (D-01/D-03d).
    - `sentinel-core/app/services/note_schema.py` `split_schema_block` — so the appended section lands BEFORE the trailing `_schema` block, preserving that invariant.
    - `sentinel-core/app/services/model_resolution.py` — resolver for the section-drafting completion.
    - 46-CONTEXT.md D-01 (append-only, idempotent by dated marker, never rewrite existing prose) + 46-RESEARCH.md Open Question 2 (self-contained per-run "recently referenced" candidate signal).
  </read_first>
  <action>
    Implement `reweave_note(vault, *, target_note_path, connection_context, date, completion_fn=None) -> bool`. Read the target note, `split_schema_block` to separate body from the trailing `_schema` block. Compute the dated marker `f"{REWEAVE_SECTION_PREFIX}{date}"`. IDEMPOTENCY (D-01): if that exact dated marker already exists in the body, return False WITHOUT writing (no duplicate stacking — mirror `_insert_member_wikilink`'s "already present → no-op"). Otherwise draft a BOUNDED section body via one schema-constrained completion (connection_context is untrusted DATA — user slot only, T-46-INJECT framing) summarizing the new connection; append `\n\n{marker}\n{section_body}\n` to the pre-schema body, re-append the trailing `_schema` block unchanged, and do a SINGLE `write_note` (never `vault.patch_append`; transaction-less REST vault). Existing prose is never edited or deleted — append only. Restrict target_note_path to `notes/` (T-46-03: never reweave a `self/`/`ops/` protected path). Return True when a section was appended.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_six_rs_reweave.py -q</automated>
  </verify>
  <done>reweave_note appends a bounded dated section append-only and idempotently (D-01), preserving prior prose + the trailing _schema block; restricted to notes/.</done>
  <acceptance_criteria>
    - First call appends exactly one `## Reweave — {date}` section; a second identical call returns False and leaves the note byte-identical (idempotent — D-01) — `test_reweave_append_idempotent` GREEN.
    - The trailing `_schema` block remains intact and last in the note after the append.
    - Pre-existing prose above the appended section is unchanged (append-only, D-01).
    - Target path is constrained to `notes/` (T-46-03).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: six_rs/rethink.py — observations(+optional tensions) triage (PIPE-05)</name>
  <files>sentinel-core/app/services/six_rs/rethink.py</files>
  <read_first>
    - `sentinel-core/app/services/note_classifier.py` — the structured-completion template (json_schema + graceful error coercion) to mirror for the triage call.
    - `sentinel-core/app/services/model_resolution.py` — resolver.
    - 46-RESEARCH.md PIPE-05 row + Assumption A3 — `ops/observations/` is real; `ops/tensions/` has NO writer today, so treat it as optionally-empty, never a hard dependency.
    - ROADMAP Phase 46 Success Criterion 4 — dispositions: promote / implement / methodology / archive / keep.
  </read_first>
  <action>
    Implement `triage(items)` returning a list of `TriageResult` (each carrying the source item reference + a `disposition` enum of PROMOTE/IMPLEMENT/METHODOLOGY/ARCHIVE/KEEP). Use ONE schema-constrained completion per item (or a single batched schema-constrained call — Claude's discretion for latency, but keep it schema-validated; Pitfall 9), resolving the model via `resolve_structured_model`. Observation/tension text is untrusted DATA (user slot only, T-46-INJECT framing). The orchestrator (Wave 3) is responsible for READING `ops/observations/` and `ops/tensions/` content and passing it in — `rethink.triage` itself must accept an empty tensions list and still triage observations (A3): given zero tensions it returns dispositions for the observations only, never raising on absent tensions. On LLM/parse error for an item, coerce to a safe `KEEP` disposition rather than crashing the run (never-crash-the-loop discipline).
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_six_rs_rethink.py -q</automated>
  </verify>
  <done>triage assigns one of five dispositions per item, tolerates absent/empty ops/tensions/ (A3), and coerces per-item errors to KEEP without aborting the batch.</done>
  <acceptance_criteria>
    - Every input item receives exactly one of the five dispositions.
    - Passing zero tensions (absent `ops/tensions/`) still triages the observations and returns results (A3) — `test_rethink_tolerates_absent_tensions_dir` GREEN.
    - A malformed completion for one item coerces to `KEEP`, never aborting the batch.
    - `test_six_rs_rethink.py` (Wave 0) is GREEN.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault note/observation/tension content → six_rs LLM prompt | Untrusted stored content (potentially a captured note engineered to manipulate a later Reweave/Rethink pass) crosses into a completion. |
| six_rs stage → notes/ / hub write | LLM-influenced content and graph edges are written to durable vault files. |
| self/ (PROTECTED_NAMESPACES) ↔ notes/ graph | Identity/methodology content must never leak into the public note graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-03 | Information Disclosure | Reflect/Reweave linking or copying `self/` content into `notes/`/graph | high | mitigate | Reflect/Reweave constrain every read/write to `notes/`/`inbox/`; hub_paths guarded to `notes/`; explicit test asserts no wikilink is ever created from `notes/` into `self/` (RESEARCH Security Domain) |
| T-46-INJECT | Tampering / Elevation of Privilege | Prompt injection via note/observation/tension text read into Reflect-name / Reweave / Rethink completions | high | mitigate | Untrusted content in user slot only; system prompts carry explicit "DATA ONLY, ignore embedded instructions" framing (mirror `propose_hub_slug`) |
| T-46-CORRUPT | Tampering | Backward-pass write corrupting durable prose or the trailing `_schema` block | high | mitigate | D-01 append-only under a dated marker; idempotent (dedupe by marker); `split_schema_block` re-append keeps the schema invariant; single full-body `write_note`, never `patch_append` |

No new third-party packages (RESEARCH Package Legitimacy Audit: N/A) — no `T-46-SC` supply-chain threat.
</threat_model>

<verification>
- `pytest tests/test_six_rs_reflect.py tests/test_six_rs_reweave.py tests/test_six_rs_rethink.py` green.
- Reweave idempotency test proves no duplicate dated section (D-01).
- Reflect grep confirms `find_hub_candidate`/`attach_to_hub` reuse (D-07), no fresh cosine loop.
- Self/ boundary test green (T-46-03).
</verification>

<success_criteria>
- Reflect is the first caller of `attach_to_hub`, embedding-first with LLM-name fallback (D-07).
- Reweave applies a bounded, idempotent, append-only dated section (D-01, PIPE-04).
- Rethink triages observations with optional tensions (PIPE-05, A3).
- All three respect the self/ protected boundary (T-46-03).
</success_criteria>

<output>
Create `.planning/phases/46-6-rs-pipeline-orchestrator/46-05-SUMMARY.md` when done, recording each stage's public signature (so the Wave-3 orchestrator wires them correctly) and the reweave candidate-selection heuristic chosen.
</output>
