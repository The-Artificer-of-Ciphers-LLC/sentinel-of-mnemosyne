---
phase: 47-migration-cutover-hardening
plan: 07
type: execute
wave: 6
depends_on: ["47-06"]
files_modified:
  - .planning/v0.6.0-REGRESSION-LEDGER.md
autonomous: false
requirements: [MIG-01, MIG-03]
must_haves:
  truths:
    - "A real cross-directory move keeps its [[wikilink]] resolving in the live Obsidian app (D-03 step 0, empirically confirmed before trusting verify-then-trust)."
    - "The live :migrate --dry-run reveals the ACTUAL flat-7 directory names (reference/ vs references/) and per-directory counts before any write (Pitfall E)."
    - "After the live :migrate run, every flat-7 note is under notes/ or ops/ (none grandfathered), the pre/post :graph diff shows zero new orphans, and rollback did NOT fire."
    - "A Phase-47 boundary check-in row is appended to the v0.6.0 regression ledger recording the outcome (MIG-03)."
  artifacts:
    - .planning/v0.6.0-REGRESSION-LEDGER.md
  key_links:
    - "Live run executes from the deploy checkout with Obsidian REST reachable; dry-run precedes the live run."
    - "Ledger §4 gains the Phase-47 boundary row after the live run + post-verify pass."
---

<objective>
Execute the actual data cutover against the live vault and record the boundary result. First empirically confirm (once, live) that a same-title cross-directory move preserves `[[wikilink]]` resolution in the real Obsidian app (D-03 step 0 / RESEARCH.md Assumption A2 / PITFALLS.md Pitfall 7). Then run `:migrate --dry-run` live to resolve the real flat-7 directory names + counts (Pitfall E) and review the planned moves. Then run the live `:migrate`, verify the post-conditions (no grandfathering, zero new orphans, no rollback), and append the Phase-47 boundary check-in row to the v0.6.0 regression ledger.

Purpose: MIG-01's success criterion is that every EXISTING flat-7 note is actually backfilled — that is a live data operation, not a code change. It is human-gated because it is a destructive, vault-wide mutation requiring the live stack + running Obsidian (per deploy topology, the live stack runs from the deploy checkout with secrets; the dev checkout cannot reach the live vault). MIG-03's boundary check-in records the outcome.
Output: A fully-migrated live vault (no flat-7 content remaining) + a new boundary row in `.planning/v0.6.0-REGRESSION-LEDGER.md` §4.
</objective>

## Artifacts This Phase Produces (this plan)

- `.planning/v0.6.0-REGRESSION-LEDGER.md` — a new §4 Phase-47 boundary check-in row (append-only; do not edit prior rows).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v0.6.0-REGRESSION-LEDGER.md
@.planning/phases/47-migration-cutover-hardening/47-CONTEXT.md
@.planning/phases/47-migration-cutover-hardening/47-VALIDATION.md
@.planning/phases/47-migration-cutover-hardening/47-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Empirical live wikilink survival test (D-03 step 0) + live :migrate --dry-run review</name>
  <what-built>
    The `:migrate` command with a `--dry-run` flag (Plans 03-05) and the verify-then-trust ops-bound track (D-03) that assumes same-title cross-directory moves preserve wikilinks. This checkpoint empirically confirms that assumption against the LIVE Obsidian app and previews the real cutover.
  </what-built>
  <how-to-verify>
    Run these against the LIVE stack (the deploy checkout with secrets/.env and Obsidian REST reachable — NOT the dev checkout):
    1. Empirical wikilink test (D-03 step 0 / Assumption A2): pick one existing note that has at least one inbound `[[wikilink]]`. Move it via the Obsidian REST API to a different directory KEEPING its title/filename stem unchanged. Open the Obsidian app and confirm the inbound `[[link]]`'s backlink still resolves in the backlink pane. Then run `:graph` and confirm the dangling-link count is UNCHANGED. (If the link breaks, STOP — verify-then-trust is invalid and the plan must switch the ops track to active rewrite; report this.)
    2. Live dry-run (Pitfall E): run `:migrate --dry-run`. Confirm the report lists the ACTUAL source directories it found (resolving `reference/` vs `references/` and `accomplishment/` vs `accomplishments/`) with a non-zero per-directory count for each populated flat-7 directory, and enumerates every planned move, `_schema` staple, and backlink rewrite with ZERO writes performed.
    Report: (a) the empirical wikilink result (resolved / broke), (b) the actual directory names + counts from the dry-run, (c) any planned move that looks wrong.
  </how-to-verify>
  <resume-signal>Type "approved" if the wikilink resolved live AND the dry-run report looks correct (real dirs + counts + sane plan), or describe the issue (broken link / wrong dir / zero counts) to block the live run.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live :migrate run + post-verify (no grandfathering, zero new orphans, no rollback)</name>
  <what-built>
    The transactional `:migrate` live run (Plans 03-04): two-track backfill (ops-bound direct moves + notes-bound Reduce), active backlink rewrite, embedding sidecar-key preservation, the pre/post `:graph` zero-new-orphans hard gate, and atomic rollback on any hard failure.
  </what-built>
  <how-to-verify>
    Against the LIVE stack, after the dry-run was approved:
    1. Run the live `:migrate` (the non-dry confirmation form). Wait for it to reach a terminal status via `GET /vault/migrate/status` (complete / error / blocked).
    2. Confirm status is `complete` and `rolled_back` is false (no rollback fired). If it rolled back, capture the reported trigger (hard exception / new orphan / ops backlink shortfall) and report — the vault is back at its exact pre-migration state, safe to retry after the cause is fixed.
    3. Confirm NO grandfathering: the flat-7 directories (`learning/`, `references/`, `journal/`, `accomplishments/`) contain zero remaining notes; every migrated note now lives under `notes/{claim-slug}.md` (with `_schema` + ≥1 wikilink) or `ops/journal/{YYYY-MM-DD}/` / `ops/accomplishments/`.
    4. Confirm wikilink integrity: the post-migration `:graph` dangling-link count shows zero NEW orphans versus the pre-migration baseline; spot-check that a previously-inbound `[[link]]` to a Reduce'd note now resolves to its new claim title.
    5. Spot-check embeddings: a moved ops-bound note is still returned by semantic recall under its new path (sidecar key preserved, not re-embedded).
  </how-to-verify>
  <resume-signal>Type "approved" with the observed counts (notes migrated to notes/ vs ops/, remaining flat-7 = 0, new orphans = 0), or describe the failure (rollback fired / notes left behind / new orphans / recall gap).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Append the Phase-47 boundary check-in row to the v0.6.0 regression ledger (MIG-03)</name>
  <read_first>
    - .planning/v0.6.0-REGRESSION-LEDGER.md (§4 Phase Boundary Check-ins table format — append a new row, do not edit prior rows; §3 D-05 accepted-transient which this boundary confirms self-healed)
    - .planning/phases/47-migration-cutover-hardening/47-06-SUMMARY.md (the recorded suite counts + MEM-0x status from Plan 06)
  </read_first>
  <files>.planning/v0.6.0-REGRESSION-LEDGER.md</files>
  <action>
    Append one new row to the §4 "Phase Boundary Check-ins" table (append-only — do not modify the Phase 44 row) recording: Phase 47, the plan id, today's date, the full-suite result (the exact sentinel-core + discord collected/passed/skipped/failed counts captured in Plan 06), the MEM-0x status ("All green"), and a Notes cell recording the live cutover outcome (notes migrated to `notes/` vs `ops/`, remaining flat-7 = 0, new orphans = 0, rollback did not fire) AND confirming the §3 D-05 accepted transient self-healed — the old top-level `journal/`/`accomplishments/`/`learning/`/`references/` notes are now physically under `ops/`/`notes/`, no compensating recency shim was ever introduced (D-05a). Keep the row format identical to the existing Phase 44 row.
  </action>
  <verify>
    <automated>grep -Eq '^\| 47 ' .planning/v0.6.0-REGRESSION-LEDGER.md && grep -q 'MEM-09' .planning/v0.6.0-REGRESSION-LEDGER.md && echo LEDGER-ROW-APPENDED</automated>
  </verify>
  <acceptance_criteria>
    - A new §4 row beginning `| 47 ` exists recording the full-suite counts (both venvs), MEM-0x "All green", and the live cutover outcome (flat-7 = 0, new orphans = 0, no rollback).
    - The Notes cell confirms the D-05/D-05a accepted transient self-healed and no recency shim was introduced.
    - The prior Phase 44 row is unchanged (append-only preserved).
  </acceptance_criteria>
  <done>The v0.6.0 regression ledger has a Phase-47 boundary check-in row recording a green boundary + the completed live cutover; MIG-03 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → live `:migrate` against the production vault | Destructive, vault-wide, one-shot data mutation on live content |
| Live Obsidian app ↔ REST-driven move | REST move must preserve the app's own backlink resolution (empirically unverified until this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02 | DoS/Integrity | Live migration leaving the vault half-migrated | high | mitigate | Atomic rollback (Plan 04) restores exact pre-state on any hard trigger; the dry-run + empirical wikilink test gate the live run; blocking human checkpoints before any live write |
| T-47-06 | Tampering/Integrity | REST-driven move silently breaking live Obsidian backlinks (Assumption A2) | high | mitigate | Empirical single-note live test (D-03 step 0) BEFORE the full batch; if the link breaks, the ops track must switch to active rewrite (checkpoint blocks the run) |
| T-47-01 | Spoofing/Elevation | Live `:migrate` invocation | high | mitigate | Admin-gated at route + bot layers (Plan 05); the live run is operator-driven from the secured deploy checkout |
| T-47-SC | Tampering (supply chain) | package installs | low | accept | Zero new third-party packages; live-ops + doc-append only |
</threat_model>

<verification>
The two blocking human-verify checkpoints gate the live cutover: (1) empirical live wikilink survival + dry-run review, (2) live run + post-verify (no grandfathering, zero new orphans, no rollback, recall intact). Then the ledger row append is verified by grep for the new `| 47 ` boundary row.
</verification>

<success_criteria>
The live vault is fully migrated — every flat-7 note is under `notes/` or `ops/`, none grandfathered, zero new orphans, embeddings + wikilinks intact, rollback did not fire — and the v0.6.0 regression ledger has a Phase-47 boundary check-in row recording a green boundary and the completed cutover.
</success_criteria>

<output>
Create `.planning/phases/47-migration-cutover-hardening/47-07-SUMMARY.md` when done.
</output>
