---
phase: 47-migration-cutover-hardening
plan: 05
type: execute
wave: 4
depends_on: ["47-04"]
files_modified:
  - sentinel-core/app/routes/migration.py
  - interfaces/discord/command_router.py
  - interfaces/discord/core_gateway.py
  - sentinel-core/tests/test_migration_routes.py
autonomous: true
requirements: [MIG-01, MIG-02]
must_haves:
  truths:
    - "POST /vault/migrate/start is admin-gated by _is_admin_route() verbatim; a non-admin request returns 403 (V4 access control, T-47-01)."
    - "The Discord :migrate command dispatches admin-gated with a --dry-run flag pass-through, mirroring :vault-sweep."
    - "GET /vault/migrate/status returns the migration_status_store shape."
  artifacts:
    - sentinel-core/app/routes/migration.py
  key_links:
    - "POST /vault/migrate/start → _is_admin_route() → migration_orchestrator.start_migration()."
    - ":migrate dispatch → core_gateway.call_core_migrate_start(dry_run, user_id) → POST /vault/migrate/start."
---

<objective>
Build the invocation surface for the migration: the admin-gated `POST /vault/migrate/start` (with a `dry_run` flag) and `GET /vault/migrate/status` routes in `sentinel-core`, and the Discord `:migrate [--dry-run]` command dispatch + `core_gateway` HTTP wrappers. Turn the Plan 01 route tests GREEN. Every primitive is a verbatim mirror of the existing `:vault-sweep` surface.

Purpose: Nothing can invoke the orchestrator (not even the live dry-run) until this surface exists. Defense-in-depth admin gating (route layer + Discord bot layer) is the T-47-01 mitigation for a destructive, vault-wide mutation — identical to the established `:vault-sweep`/`:pipeline` precedent.
Output: `routes/migration.py` + Discord `:migrate` dispatch + `core_gateway` wrappers; route tests GREEN.
</objective>

## Artifacts This Phase Produces (this plan)

- `sentinel-core/app/routes/migration.py` — `POST /vault/migrate/start` (`MigrateStartRequest{user_id, dry_run}`), `GET /vault/migrate/status`.
- `interfaces/discord/command_router.py` (MODIFIED) — `:migrate` subcommand dispatch with a `--dry-run` flag.
- `interfaces/discord/core_gateway.py` (MODIFIED) — `call_core_migrate_start(dry_run, user_id)`, `call_core_migrate_status()`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-migration-cutover-hardening/47-PATTERNS.md
@.planning/phases/47-migration-cutover-hardening/47-RESEARCH.md
@sentinel-core/app/routes/note.py
@sentinel-core/app/services/migration_orchestrator.py
@sentinel-core/app/services/migration_status_store.py
@interfaces/discord/command_router.py
@interfaces/discord/core_gateway.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin-gated /vault/migrate/start + /vault/migrate/status routes</name>
  <read_first>
    - sentinel-core/tests/test_migration_routes.py (the RED contract from Plan 01)
    - sentinel-core/app/routes/note.py (lines 126-138 `_is_admin_route`; 135-196 `/vault/sweep/start`, `/vault/sweep/status`, `SweepStartRequest` — the exact analog incl. router registration)
    - sentinel-core/app/services/migration_orchestrator.py (Plan 03/04 — `start_migration` signature)
    - sentinel-core/app/services/migration_status_store.py (Plan 03 — `get_status`)
    - .planning/phases/47-migration-cutover-hardening/47-PATTERNS.md (section "routes/migration.py")
  </read_first>
  <files>sentinel-core/app/routes/migration.py</files>
  <behavior>
    - POST /vault/migrate/start with a non-admin user_id → HTTP 403 "admin only".
    - POST /vault/migrate/start with an admin user_id and dry_run=True → 200 ack {migration_id,status:"running",mode} and a scheduled background task.
    - GET /vault/migrate/status → the migration_status_store shape.
  </behavior>
  <action>
    Create `routes/migration.py` mirroring `note.py`'s sweep routes. Define `MigrateStartRequest` with `user_id: str` and `dry_run: bool` (mirror `SweepStartRequest.dry_run`). `POST /vault/migrate/start`: call `_is_admin_route(req.user_id)` first and `raise HTTPException(status_code=403, detail="admin only")` when it returns False (import/reuse the SAME `_is_admin_route` from `note.py`, do not fork it); resolve the vault via `get_route_context(request)`; call `migration_orchestrator.start_migration(vault=vault, dry_run=req.dry_run)` and return its ack. `GET /vault/migrate/status`: return `migration_status_store.get_status()`. Register the router the same way the sweep router is registered in the app. Do NOT add the embedding/classifier "safe_to_mutate" probe from the sweep route — migration reuses the pipeline orchestrator's own internal calls (per PATTERNS.md note).
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_routes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_migrate_start_requires_admin` passes: a non-admin `user_id` → 403 (T-47-01 mitigation proven).
    - `test_migrate_start_dry_run` passes: admin + `dry_run=True` → 200 ack with `migration_id` and `status:"running"`.
    - `test_migrate_status_shape` passes: `GET /vault/migrate/status` returns the status-store shape.
    - `routes/migration.py` reuses `note.py`'s `_is_admin_route` (grep confirms the same symbol, not a re-implemented gate).
  </acceptance_criteria>
  <done>routes/migration.py exists and is registered; the admin-gated start + status routes pass the Plan 01 route tests.</done>
</task>

<task type="auto">
  <name>Task 2: Discord :migrate dispatch + core_gateway HTTP wrappers</name>
  <read_first>
    - interfaces/discord/command_router.py (the existing `:vault-sweep`/`:pipeline` dispatch entries — grep for `"vault-sweep"`/`"pipeline"` string literals in the subcommand table)
    - interfaces/discord/core_gateway.py (the existing `call_core_sweep_start`/`call_core_pipeline_start`-style HTTP wrappers — the exact analog)
    - .planning/phases/47-migration-cutover-hardening/47-PATTERNS.md (sections "command_router.py", "core_gateway.py")
    - interfaces/discord/bot.py (check ONLY for a stale fixed-prompt `:migrate` entry to remove; do not add new bot logic)
  </read_first>
  <files>interfaces/discord/command_router.py, interfaces/discord/core_gateway.py</files>
  <action>
    In `core_gateway.py`, add `call_core_migrate_start(dry_run: bool, user_id: str)` and `call_core_migrate_status()` mirroring the existing `call_core_sweep_start`/`call_core_pipeline_start` wrappers exactly — same request/response shape, same error handling, POSTing to `/vault/migrate/start` (body `{user_id, dry_run}`) and GETting `/vault/migrate/status`. In `command_router.py`, add a `:migrate` subcommand entry mirroring the `:vault-sweep` dispatch: parse a `--dry-run` flag (default to dry-run OR require an explicit confirmation flag for a live run — mirror how `:vault-sweep` distinguishes dry vs live), apply the SAME admin gate the `:vault-sweep` entry uses (defense-in-depth at the bot layer, per "Task 8 also gates at bot"), and call `call_core_migrate_start(dry_run, user_id)` / `call_core_migrate_status()`. If `bot.py` holds a stale fixed-prompt `:migrate` entry, remove it; otherwise leave `bot.py` untouched.
  </action>
  <verify>
    <automated>cd interfaces/discord && .venv/bin/python -m pytest tests/ -q -k "router or gateway or migrate or command" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `core_gateway.py` exposes `call_core_migrate_start(dry_run, user_id)` and `call_core_migrate_status()` mirroring the sweep wrappers (same shape + error handling).
    - `command_router.py` has a `:migrate` dispatch entry that is admin-gated at the bot layer and passes a `--dry-run` flag through; live vs dry is distinguished the same way `:vault-sweep` does.
    - The discord suite still collects and passes (326 baseline, no shrink): `cd interfaces/discord && .venv/bin/python -m pytest tests/ -q`.
    - No stale fixed-prompt `:migrate` entry remains in `bot.py`.
  </acceptance_criteria>
  <done>The Discord :migrate command dispatches admin-gated with a --dry-run flag through core_gateway to the new routes; the discord suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → :migrate command → POST /vault/migrate/start | Untrusted actor triggers a destructive, vault-wide mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Spoofing/Elevation | POST /vault/migrate/start + :migrate dispatch | high | mitigate | `_is_admin_route()` reused verbatim at the route layer AND the Discord bot layer (defense-in-depth); non-admin → 403; `test_migrate_start_requires_admin` proves it |
| T-47-02 | DoS | Accidental live migration | high | mitigate | `:migrate` defaults to/requires an explicit non-dry confirmation flag before a live run (mirrors `:vault-sweep`); dry-run is the safe default surface |
| T-47-SC | Tampering (supply chain) | package installs | low | accept | Zero new third-party packages; reuses FastAPI route + existing discord HTTP client patterns |
</threat_model>

<verification>
Run `cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_routes.py -q` (GREEN) and `cd interfaces/discord && .venv/bin/python -m pytest tests/ -q` (326, no shrink). Confirm the route reuses `note.py`'s `_is_admin_route` and the Discord dispatch mirrors `:vault-sweep`.
</verification>

<success_criteria>
The migration is invokable: an admin-gated `POST /vault/migrate/start` (dry_run flag) + `GET /vault/migrate/status` route and a Discord `:migrate [--dry-run]` command wired through `core_gateway`. Non-admin invocation is rejected 403. Route tests GREEN; both suites stay green.
</success_criteria>

<output>
Create `.planning/phases/47-migration-cutover-hardening/47-05-SUMMARY.md` when done.
</output>
