---
phase: 47-migration-cutover-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - sentinel-core/tests/test_migration_rollback_ledger.py
  - sentinel-core/tests/test_ops_backlink_scan.py
  - sentinel-core/tests/test_migration_orchestrator.py
  - sentinel-core/tests/test_migration_routes.py
autonomous: true
requirements: [MIG-01, MIG-02]
must_haves:
  truths:
    - "Every downstream implementation task has a real RED test to turn GREEN (no MISSING gates)."
    - "The orchestrator integration tests assert FULL post-conditions (flat-7 original gone AND notes/{slug} exists), never a shallow did-not-raise check."
    - "The compliance/verification gate is exercised for real — never mocked (Phase 46 cold-start anti-pattern)."
  artifacts:
    - sentinel-core/tests/test_migration_rollback_ledger.py
    - sentinel-core/tests/test_ops_backlink_scan.py
    - sentinel-core/tests/test_migration_orchestrator.py
    - sentinel-core/tests/test_migration_routes.py
  key_links:
    - "Each test file imports the exact module path + symbol names the implementation plans will create (interface pinned here)."
---

<objective>
Author the four Wave 0 test files as RED characterization tests (Nyquist discipline) that pin the exact contract for every new Phase 47 module BEFORE any implementation exists. These tests fail on import/attribute-error today and become the `<automated>` gate every later task turns GREEN.

Purpose: Nyquist feedback-sampling requires a real automated gate per task. Authoring the contract first also prevents interface drift across the six-plan dependency chain and forbids the Phase 46 "mock the gate" anti-pattern (per [[phase46-pipeline-coldstart-gap]]).
Output: Four test files under `sentinel-core/tests/`, all currently RED, defining the interfaces of `migration_rollback_ledger`, `ops_backlink_scan`, `migration_orchestrator`, and `routes/migration`.
</objective>

## Artifacts This Phase Produces

New symbols created across Phase 47 (this plan authors the tests that pin their contracts):

- `sentinel-core/app/services/migration_rollback_ledger.py` — `RollbackLedger` (methods `record_restore_original`, `record_ops_move`, `record_backlink_rewrite`, `record_inbox_write`, async `replay`)
- `sentinel-core/app/services/ops_backlink_scan.py` — async `scan_for_title_refs(vault, title) -> int`
- `sentinel-core/app/services/migration_status_store.py` — `get_status`, `patch_status`, `set_status`, `new_status`
- `sentinel-core/app/services/migration_orchestrator.py` — async `run(vault, *, dry_run, ...) -> MigrationReport`, async `start_migration(...)`, `MigrationReport`
- `sentinel-core/app/routes/migration.py` — `POST /vault/migrate/start`, `GET /vault/migrate/status`
- `interfaces/discord/command_router.py` (MODIFIED) — `:migrate` dispatch (with `--dry-run` flag)
- `interfaces/discord/core_gateway.py` (MODIFIED) — `call_core_migrate_start`, `call_core_migrate_status`
- Tests: `test_migration_rollback_ledger.py`, `test_ops_backlink_scan.py`, `test_migration_orchestrator.py`, `test_migration_routes.py`
- Ledger check-in row appended to `.planning/v0.6.0-REGRESSION-LEDGER.md` §4

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-migration-cutover-hardening/47-RESEARCH.md
@.planning/phases/47-migration-cutover-hardening/47-PATTERNS.md
@.planning/phases/47-migration-cutover-hardening/47-VALIDATION.md
@sentinel-core/tests/test_graph_analysis.py
@sentinel-core/tests/test_links_sidecar_index.py
@sentinel-core/tests/test_pipeline_orchestrator.py
@sentinel-core/tests/test_note_routes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED unit tests for the rollback ledger and ops backlink scan</name>
  <read_first>
    - sentinel-core/tests/test_graph_analysis.py (pure-computation unit-test style — the analog for the ledger test)
    - sentinel-core/tests/test_links_sidecar_index.py (fake-vault-with-canned-find() fixture style — the analog for the scan test)
    - .planning/phases/47-migration-cutover-hardening/47-PATTERNS.md (sections "migration_rollback_ledger.py" and "ops_backlink_scan.py" — pinned interfaces + `detach_from_hub` idempotency precedent)
    - sentinel-core/app/services/graph_analysis.py (`extract_wikilinks`, `_slugify` — the reusable extraction/normalization primitives the scan mirrors)
  </read_first>
  <files>sentinel-core/tests/test_migration_rollback_ledger.py, sentinel-core/tests/test_ops_backlink_scan.py</files>
  <action>
    Write `test_migration_rollback_ledger.py` importing `RollbackLedger` from `app.services.migration_rollback_ledger`. Cover D-02/D-02a: `test_replay_restores_byte_identical_prestate` (record a `record_ops_move(src,dst,sidecar_key_moved=True)` + `record_restore_original(src, body)` + `record_backlink_rewrite(note_path, before_body)` against a fake in-memory vault, mutate the vault, then `await ledger.replay(vault)` and assert every touched path's body is byte-identical to its pre-mutation snapshot); `test_replay_reverse_order` (assert inverse ops execute last-in-first-out); `test_replay_is_idempotent` (calling `replay` twice yields the same restored state — mirrors `detach_from_hub`'s no-op-if-absent discipline). Use a minimal FakeVault holding a `dict[str,str]` of path→body with async `read_note`/`write_note`/`delete_note`/`relocate`.
    Write `test_ops_backlink_scan.py` importing `scan_for_title_refs` from `app.services.ops_backlink_scan`. Cover Pattern 3: `test_scan_counts_title_refs` (FakeVault whose async `find(query)` returns canned hits for `"[[Old Title]]"`, assert the returned int equals the hit count); `test_scan_is_not_notes_scoped` (assert the scan surfaces a reference located under `ops/journal/...`, proving it is vault-wide and NOT `notes/`-scoped); `test_scan_diff_detects_new_dangling` (assert a pre-count vs a smaller post-count is what the caller uses to detect a broken backlink). Do NOT import or touch `graph_analysis`/`links_sidecar_index` in the scan test — the helper is standalone and vault-wide by design.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_rollback_ledger.py tests/test_ops_backlink_scan.py -q 2>&1 | grep -Eq 'ModuleNotFoundError|ImportError|No module named|error' && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - Both files collect and FAIL RED with an import/attribute error (the modules do not exist yet) — this is the expected Wave 0 state, not a defect.
    - `RollbackLedger` is referenced with methods `record_restore_original`, `record_ops_move`, `record_backlink_rewrite`, `record_inbox_write`, and async `replay(vault)`.
    - `scan_for_title_refs(vault, title)` is referenced as an async function returning an int; the scan test asserts an `ops/`-located hit is found (not-notes-scoped).
    - Neither test mocks a compliance/verification gate; the ledger test asserts byte-identical restoration, not "did not raise".
  </acceptance_criteria>
  <done>test_migration_rollback_ledger.py and test_ops_backlink_scan.py exist, collect, and fail RED with a missing-module error; the pinned symbol names match the Artifacts list.</done>
</task>

<task type="auto">
  <name>Task 2: RED integration tests for the migration orchestrator</name>
  <read_first>
    - sentinel-core/tests/test_pipeline_orchestrator.py (async fixture + fake-vault + FULL post-condition assertion style)
    - sentinel-core/tests/test_vault_sweeper.py (relocate/sidecar assertion patterns)
    - .planning/phases/47-migration-cutover-hardening/47-RESEARCH.md (§Summary corrections 1-3, §Open Questions Q1 rollback-trigger, Patterns 1-4)
    - .planning/phases/47-migration-cutover-hardening/47-VALIDATION.md (per-task map rows 47-01-01, 47-01-02, 47-02-01)
  </read_first>
  <files>sentinel-core/tests/test_migration_orchestrator.py</files>
  <action>
    Write `test_migration_orchestrator.py` importing `run` and `MigrationReport` from `app.services.migration_orchestrator`. Pin these named tests (all RED until Plans 03/04):
    `test_full_backfill_no_grandfathering` (MIG-01) — seed a FakeVault with flat-7 files under `learning/`, `references/`, `journal/`, `accomplishments/`; run `await run(vault, dry_run=False)`; assert EVERY flat-7 original path is gone AND each ops-bound file now exists under `ops/journal/{YYYY-MM-DD}/` or `ops/accomplishments/`, AND each notes-bound file produced a `notes/{claim-slug}.md` with a `_schema` block and ≥1 wikilink (Pitfall A warning sign: assert `notes/{slug}` exists, NOT `inbox/{name}` exists).
    `test_embedding_and_wikilink_preservation` (MIG-02) — seed an ops-bound file with `embedding_b64` frontmatter and a matching key in `ops/sweeps/embedding-index.json`; after run, assert the sidecar key was renamed old-path→new-path with the embedding VALUE unchanged (not re-embedded), and assert a pre-existing `[[old-title]]` reference still resolves.
    `test_dry_run_writes_nothing` (D-02) — run with `dry_run=True`; assert the FakeVault recorded zero `write_note`/`delete_note`/`relocate` mutations and the returned report enumerates the planned moves.
    `test_hard_failure_triggers_atomic_rollback` (D-02/D-02a) — inject a hard failure (FakeVault `relocate` raises on the Nth call); assert the vault is restored byte-identical to its pre-run snapshot and `report.rolled_back is True`.
    `test_verify_failed_entry_does_not_rollback` (Open Question Q1) — stub the reused pipeline to complete with a non-empty `report.verify_failed` (a dead-lettered entry) but NO hard exception; assert already-successful ops-bound moves are RETAINED (not unwound) and `report.rolled_back is False`. Never mock the real compliance gate to force a pass — drive a genuine dead-letter outcome.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_orchestrator.py -q 2>&1 | grep -Eq 'ModuleNotFoundError|ImportError|No module named|error' && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - File collects and fails RED (module `app.services.migration_orchestrator` absent).
    - The five named tests exist with the assertions described; `test_full_backfill_no_grandfathering` asserts `notes/{slug}` existence and flat-7 absence (never `inbox/` existence).
    - `test_verify_failed_entry_does_not_rollback` and `test_hard_failure_triggers_atomic_rollback` encode the locked rollback-trigger split (hard exception / orphan-diff → rollback; dead-letter → no rollback).
    - No test mocks the six_rs/pipeline compliance gate to fake a GREEN.
  </acceptance_criteria>
  <done>test_migration_orchestrator.py exists, collects, fails RED; the five named contract tests pin MIG-01/MIG-02 post-conditions and the rollback-trigger split.</done>
</task>

<task type="auto">
  <name>Task 3: RED admin-gated route tests for /vault/migrate/*</name>
  <read_first>
    - sentinel-core/tests/test_note_routes.py (sweep-route tests: admin-gate 403 rejection + dry-run vs live request shape — the exact analog)
    - sentinel-core/app/routes/note.py (lines 126-196 — `_is_admin_route`, `/vault/sweep/start`, `/vault/sweep/status` shapes)
    - .planning/phases/47-migration-cutover-hardening/47-PATTERNS.md (section "routes/migration.py")
  </read_first>
  <files>sentinel-core/tests/test_migration_routes.py</files>
  <action>
    Write `test_migration_routes.py` mirroring `test_note_routes.py`'s sweep-route tests against the (not-yet-existing) `POST /vault/migrate/start` and `GET /vault/migrate/status` routes. Pin: `test_migrate_start_requires_admin` (a non-admin `user_id` returns HTTP 403 "admin only" — V4 access control, threat T-47-01); `test_migrate_start_dry_run` (an admin `user_id` with `dry_run=True` returns 200 with a `migration_id`/`status:"running"` ack and schedules a background task); `test_migrate_status_shape` (`GET /vault/migrate/status` returns the status-store shape with a `status` field in the running/complete/blocked/error vocabulary). Use the existing test client/app fixture pattern from `test_note_routes.py`; set/patch `SENTINEL_ADMIN_USER_IDS` the same way the sweep-route tests do.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_routes.py -q 2>&1 | grep -Eq 'ModuleNotFoundError|ImportError|No module named|404|error|failed' && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - File collects and fails RED (route module absent / route returns 404).
    - `test_migrate_start_requires_admin` asserts a non-admin request → 403 (the T-47-01 mitigation contract).
    - Admin/dry-run and status-shape tests reference the exact route paths `POST /vault/migrate/start` and `GET /vault/migrate/status`.
    - Test reuses the `test_note_routes.py` admin-gate env-var setup, no new auth scaffolding invented.
  </acceptance_criteria>
  <done>test_migration_routes.py exists, collects, fails RED; the admin-gate-403 contract for the destructive migration route is pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → POST /vault/migrate/start | Untrusted actor triggers a destructive, vault-wide mutation |
| Migration process → vault REST + sidecar indexes | Partial writes can leave inconsistent state |
| Vault note body → six_rs.reduce LLM prompt | Untrusted content enters an LLM completion (notes-bound track) |

## STRIDE Threat Register (this plan: pins the test contracts that PROVE the mitigations)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Spoofing/Elevation | POST /vault/migrate/start + :migrate dispatch | high | mitigate | `test_migrate_start_requires_admin` pins the non-admin→403 contract that `_is_admin_route()` must satisfy (implemented Plan 05) |
| T-47-02 | DoS/Tampering | migration_orchestrator.run (crash mid-run) | high | mitigate | `test_hard_failure_triggers_atomic_rollback` pins byte-identical restoration (implemented Plans 02/04) |
| T-47-03 | Tampering/Integrity | embedding-index.json sidecar key | high | mitigate | `test_embedding_and_wikilink_preservation` pins sidecar-key rename with unchanged value (implemented Plan 03) |
| T-47-SC | Tampering (supply chain) | package installs | low | accept | Zero new third-party packages this phase (RESEARCH.md Package Legitimacy Audit: N/A); no install tasks in any Phase 47 plan |
</threat_model>

<verification>
Run `cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_rollback_ledger.py tests/test_ops_backlink_scan.py tests/test_migration_orchestrator.py tests/test_migration_routes.py -q`. All four files MUST collect and fail RED with missing-module/404 errors — this is the intended Wave 0 outcome. Confirm none of them mock a compliance/verification gate.
</verification>

<success_criteria>
Four RED test files exist, each pinning the exact symbol names/route paths later plans implement. No `<automated>MISSING>` gates remain for any Phase 47 implementation task — every downstream task has a real test to turn GREEN.
</success_criteria>

<output>
Create `.planning/phases/47-migration-cutover-hardening/47-01-SUMMARY.md` when done.
</output>
