---
phase: 47-migration-cutover-hardening
plan: 06
type: execute
wave: 5
depends_on: ["47-05"]
files_modified: []
autonomous: true
requirements: [MIG-03, MIG-04]
must_haves:
  truths:
    - "The full sentinel-core (590) + discord (326) suites pass — no shrinking test count (D-05 hard gate, MIG-04)."
    - "Every MEM-01..MEM-09 characterization test is green (the hot regression gate)."
    - "The restored command surface still dispatches every :command (Pathfinder + Recall + the 27-command surface intact)."
  artifacts: []
  key_links:
    - "The suite/MEM-0x/command-surface gate is the pre-cutover safety gate: the migration machinery is proven safe BEFORE it runs live."
---

<objective>
Verify the D-05 boundary hardening gates on the completed migration machinery, BEFORE the live cutover: the full sentinel-core (590 collected) and discord (326 collected) suites are green with no shrinking count (MIG-04), every MEM-01..MEM-09 characterization test is green, and the restored command surface still dispatches every `:command` (MIG-03 pre-check). This is a hard gate, not a report — a red MEM-0x row or a shrunk suite is a phase-gate failure.

Purpose: You never run a destructive, vault-wide migration on red code. This plan proves the newly-built `:migrate` machinery introduced zero regression to Pathfinder, Recall/semantic-recall/retention, or the 27-command surface — the exact "core gutted again" failure mode MIG-03/the v0.6.0 regression ledger exist to prevent.
Output: A green boundary verification (no file changes) that unblocks the live cutover (Plan 07).
</objective>

## Artifacts This Phase Produces (this plan)

None — this is a verification-only gate. (The ledger check-in row is appended in Plan 07, after the live cutover records its outcome.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/v0.6.0-REGRESSION-LEDGER.md
@.planning/phases/47-migration-cutover-hardening/47-VALIDATION.md
@.planning/phases/47-migration-cutover-hardening/47-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite hard gate — both venvs green, no shrinking count (MIG-04)</name>
  <read_first>
    - .planning/v0.6.0-REGRESSION-LEDGER.md (§2 Full-Suite Baseline Gate + §4 the last check-in row's recorded counts — the baseline to compare against)
    - .planning/phases/47-migration-cutover-hardening/47-VALIDATION.md (Full suite command + Sampling Rate)
  </read_first>
  <files>(verification only — no files modified)</files>
  <action>
    Run the full suite in BOTH venvs (there is no combined command): `cd sentinel-core && .venv/bin/python -m pytest tests/ -q` (expect ≥590 collected including the four new Phase 47 test files) and `cd interfaces/discord && .venv/bin/python -m pytest tests/ -q` (expect ≥326 collected). Compare the collected/passed counts against the last recorded ledger baseline (§4). Any RED test, any collection error, or a SHRINKING collected count vs the prior boundary is a hard failure — diagnose and fix in place (never delete/skip a test to make the suite green; rewriting an assertion is acceptable ONLY when it tracks an intentionally-changed locked decision). Record the exact counts (collected/passed/skipped/failed) for both venvs for the Plan 07 ledger check-in.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/ -q && cd ../interfaces/discord && .venv/bin/python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `sentinel-core` suite: ≥590 collected, 0 failed (12 skipped tolerated per baseline), no collection errors.
    - `interfaces/discord` suite: ≥326 collected, 0 failed, no collection errors.
    - Collected count does not shrink versus the last `.planning/v0.6.0-REGRESSION-LEDGER.md` §4 boundary row.
    - Exact counts for both venvs are captured for the Plan 07 ledger check-in.
  </acceptance_criteria>
  <done>Both suites are green with no shrinking count; the counts are recorded for the boundary check-in.</done>
</task>

<task type="auto">
  <name>Task 2: MEM-0x hot gate + command-surface smoke (MIG-03 pre-check)</name>
  <read_first>
    - .planning/v0.6.0-REGRESSION-LEDGER.md (§1 The Hot Gate MEM-01..MEM-09 contracts + §3 the D-05 accepted transient)
    - .planning/phases/47-migration-cutover-hardening/47-CONTEXT.md (D-05, D-05a — reconcile against the ledger, do not silently diverge)
  </read_first>
  <files>(verification only — no files modified)</files>
  <action>
    Run the MEM-0x characterization subset `cd sentinel-core && .venv/bin/python -m pytest tests/ -k "mem0 or recall or recency" -q` and confirm every MEM-01..MEM-09 contract is green. Confirm the command-surface is intact: run the discord command-dispatch tests and confirm every `:command` (the restored 27-command surface, including the new `:migrate`) still dispatches. Reconcile against the ledger's §3 D-05 accepted transient (D-05a): note that once the live cutover moves the old top-level `journal/`/`accomplishments/`/`learning/`/`references/` notes under `ops/`, the accepted recency-weighting transient self-heals — do NOT introduce any compensating shim (per the locked D-05 decision). Capture the MEM-0x status string for the Plan 07 ledger check-in.
  </action>
  <verify>
    <automated>grep -q 'MEM-09' .planning/v0.6.0-REGRESSION-LEDGER.md && cd sentinel-core && .venv/bin/python -m pytest tests/ -k "mem0 or recall or recency" -q</automated>
  </verify>
  <acceptance_criteria>
    - Every MEM-01..MEM-09 characterization test is green (the hot gate).
    - The command-surface dispatch tests pass — every `:command` including `:migrate` dispatches.
    - No compensating recency-weighting shim was introduced (D-05a) — grep confirms no reintroduced carrier-prefix allowlist in `recall.py`.
    - The MEM-0x status string is captured for the boundary check-in.
  </acceptance_criteria>
  <done>The MEM-0x hot gate and the command-surface smoke are green; the boundary status is captured; no shim reintroduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration machinery → existing core (Recall, Pathfinder, command surface) | New code could silently regress the hot MEM-0x gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-05 | Tampering | Silent core regression from the migration code | high | mitigate | The MEM-01..09 hot gate + full-suite no-shrink gate MUST be green before the live cutover; a red row is a phase-gate failure, fixed in place |
| T-47-02 | DoS | Running a live migration on red code | high | mitigate | This gate is a hard prerequisite (Plan 07 depends on this plan) — the destructive run is blocked until the machinery is proven green |
| T-47-SC | Tampering (supply chain) | package installs | low | accept | Zero new third-party packages; verification-only plan |
</threat_model>

<verification>
Run both full suites (`sentinel-core` then `interfaces/discord`) and the MEM-0x subset. All green; no shrinking count; MEM-01..09 green; every `:command` dispatches. Record the counts and MEM-0x status for the Plan 07 boundary check-in.
</verification>

<success_criteria>
The migration machinery is proven safe: full sentinel-core (≥590) + discord (≥326) suites green with no shrink, MEM-01..09 hot gate green, command surface intact, no recency shim reintroduced. The live cutover (Plan 07) is unblocked.
</success_criteria>

<output>
Create `.planning/phases/47-migration-cutover-hardening/47-06-SUMMARY.md` when done.
</output>
