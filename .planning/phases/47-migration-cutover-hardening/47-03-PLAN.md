---
phase: 47-migration-cutover-hardening
plan: 03
type: execute
wave: 2
depends_on: ["47-02"]
files_modified:
  - sentinel-core/app/services/migration_status_store.py
  - sentinel-core/app/services/migration_orchestrator.py
  - sentinel-core/tests/test_migration_orchestrator.py
autonomous: true
requirements: [MIG-01, MIG-02]
must_haves:
  truths:
    - "The migration acquires the shared sweep lock FIRST and always releases it in a finally (concurrency guard, Pitfall 8)."
    - "--dry-run prints every planned move/staple/rewrite with ZERO writes and probes both singular/plural flat-7 directory spellings."
    - "Each ops-bound file is relocated frontmatter-preserving AND its embedding sidecar key is renamed in the SAME rollback-tracked transaction (D-04, no re-embed)."
    - "The ops-bound backlink scan runs pre/post per moved file and finds zero new dangling refs (D-03 verify-then-trust)."
  artifacts:
    - sentinel-core/app/services/migration_status_store.py
    - sentinel-core/app/services/migration_orchestrator.py
  key_links:
    - "run() acquires vault.acquire_sweep_lock() before any read; releases in finally."
    - "relocate() + embedding sidecar-key patch recorded together via RollbackLedger.record_ops_move(sidecar_key_moved=True)."
    - "dry-run directory probe resolves reference/ vs references/ and accomplishment/ vs accomplishments/ empirically."
---

<objective>
Build the migration orchestrator's spine — the shared-lock/try-finally/dry-run/background-task skeleton and the status store — and implement Track A (ops-bound direct moves) end to end: `journal/` → `ops/journal/{YYYY-MM-DD}/` and `accomplishments/` → `ops/accomplishments/`, each a frontmatter-preserving `relocate()` plus an embedding-sidecar-key patch, both recorded in the rollback ledger, gated by the ops-scoped backlink scan.

Purpose: Track A is the lower-risk track (D-01 rationale) and the natural first build. It exercises the rollback ledger and the ops backlink scan from Plan 02 against real vault primitives, and establishes the run()/start_migration() shape the notes-bound track (Plan 04) extends. Implements the D-04 "no re-embed for direct moves" guarantee via RESEARCH.md correction 2 (sidecar-key patch), and the D-03 ops-bound verify-then-trust via correction 3 (ops backlink scan, NOT `:graph`).
Output: `migration_status_store.py` + `migration_orchestrator.py` (skeleton + Track A), turning the ops-bound portion of the orchestrator tests GREEN.
</objective>

## Artifacts This Phase Produces (this plan)

- `sentinel-core/app/services/migration_status_store.py` — `get_status`, `patch_status`, `set_status`, `new_status` (mirrors `pipeline_status_store.py`).
- `sentinel-core/app/services/migration_orchestrator.py` — async `run(vault, *, dry_run, embedder=None, settings=None, status_callback=None) -> MigrationReport`; async `start_migration(*, vault, dry_run, embedder=None, settings=None, task_runner=None) -> dict`; `MigrationReport` dataclass; internal `_discover_flat7`, `_move_ops_bound`. (Track B `_enqueue_notes_bound` is added in Plan 04.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/47-migration-cutover-hardening/47-RESEARCH.md
@.planning/phases/47-migration-cutover-hardening/47-PATTERNS.md
@sentinel-core/app/services/pipeline_orchestrator.py
@sentinel-core/app/services/vault_sweeper.py
@sentinel-core/app/services/embedding_sidecar_index.py
@sentinel-core/app/services/note_classifier.py
@sentinel-core/app/vault.py
@sentinel-core/app/services/task_runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Status store + orchestrator skeleton (lock, dry-run, background task, report)</name>
  <read_first>
    - sentinel-core/app/services/pipeline_orchestrator.py (lines 486-534 `run()` lock/try/finally shape; 540-577 `start_pipeline()` background-task-and-status shape; the sibling `pipeline_status_store.py` referenced by `patch_pipeline_status`/`_set_pipeline_status`)
    - sentinel-core/app/services/note_classifier.py (lines 36-100 `TOPIC_VAULT_PATH` — the current PARA routing table; source of the ops-bound destination prefixes)
    - sentinel-core/app/services/vault_sweeper.py (lines 71-80 `SWEEP_SKIP_PREFIXES`; run-sweep lock usage as a second reuse precedent)
    - sentinel-core/app/vault.py (lines 692-720 `acquire_sweep_lock`/`release_sweep_lock`; `find`/`list_under`/`read_note` signatures)
    - .planning/phases/47-migration-cutover-hardening/47-PATTERNS.md (sections "migration_orchestrator.py", "migration_status_store.py")
  </read_first>
  <files>sentinel-core/app/services/migration_status_store.py, sentinel-core/app/services/migration_orchestrator.py</files>
  <behavior>
    - run(vault, dry_run=True) writes NOTHING and returns a MigrationReport whose planned-moves list enumerates every discovered flat-7 file with its computed destination.
    - run() acquires acquire_sweep_lock() first; SweepInProgressError → status "blocked" (re-raised distinctly); generic exception → status "error"; release_sweep_lock() always runs in finally.
    - _discover_flat7 probes BOTH `reference`/`references` and `accomplishment`/`accomplishments` via list_under and reports actual per-directory counts (Pitfall E).
    - start_migration seeds status "running", schedules the background task via AsyncioTaskRunner, returns an immediate {migration_id,status,mode} ack.
  </behavior>
  <action>
    Implement `migration_status_store.py` mirroring `pipeline_status_store.py` exactly: an in-memory status dict with `new_status(migration_id, status, mode)`, `patch_status(**kwargs)`, `set_status(report)`, `get_status()`, using the running/complete/blocked/error vocabulary.
    Implement `migration_orchestrator.py` skeleton: `MigrationReport` dataclass (fields `migration_id`, `status`, `dry_run`, `planned`, `ops_moved`, `notes_backfilled`, `verify_failed`, `new_orphans`, `rolled_back`, `errors`). `async def run(vault, *, dry_run, embedder=None, settings=None, status_callback=None)` following `pipeline_orchestrator.run`'s lock/try/except/finally shape verbatim (acquire `vault.acquire_sweep_lock()` first, `release_sweep_lock()` in finally, distinct `SweepInProgressError`→blocked path). `_discover_flat7(vault)` uses `vault.list_under(prefix)` for each candidate flat-7 directory, probing both singular and plural spellings for reference(s)/accomplishment(s), and derives destinations from `note_classifier.TOPIC_VAULT_PATH` (ops-bound → `ops/journal/{YYYY-MM-DD}/`, `ops/accomplishments/`). When `dry_run` is True, populate `report.planned` from discovery and RETURN before any mutation (assert-zero-writes contract). `async def start_migration(...)` mirrors `start_pipeline` (seed running status, `AsyncioTaskRunner().schedule(...)`, return ack). Leave a clearly-marked call site where Track A and Track B will run inside the try block; this plan wires Track A (next task), Track B lands in Plan 04.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_orchestrator.py::test_dry_run_writes_nothing -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_dry_run_writes_nothing` passes: a `dry_run=True` run records zero `write_note`/`delete_note`/`relocate` calls and `report.planned` enumerates discovered files with destinations.
    - `run()` acquires the sweep lock before any vault read and releases it in a `finally`; `SweepInProgressError` maps to status "blocked" distinct from generic "error".
    - `_discover_flat7` probes both `reference`/`references` and `accomplishment`/`accomplishments` (grep the module for both spellings).
    - `migration_status_store` exposes the same get/patch/set/new surface as `pipeline_status_store` with the running/complete/blocked/error vocabulary.
  </acceptance_criteria>
  <done>Status store + orchestrator skeleton exist; dry-run test GREEN; lock/finally + dual-spelling directory probe in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Track A — ops-bound relocate + sidecar-key patch + rollback + backlink scan gate</name>
  <read_first>
    - sentinel-core/tests/test_migration_orchestrator.py::test_embedding_and_wikilink_preservation (the RED contract)
    - sentinel-core/app/vault.py (lines 631-690 `relocate` — protected-namespace guard ordering, provenance frontmatter, collision suffix)
    - sentinel-core/app/services/embedding_sidecar_index.py (lines 22,44-60 `EMBEDDING_INDEX_PATH`, `decode_index_body`, `encode_index_body`)
    - sentinel-core/app/services/migration_rollback_ledger.py (Plan 02 — `record_ops_move(src, dst, sidecar_key_moved=True)`)
    - sentinel-core/app/services/ops_backlink_scan.py (Plan 02 — `scan_for_title_refs`)
    - .planning/phases/47-migration-cutover-hardening/47-RESEARCH.md (Patterns 2 & 3, Pitfall C)
  </read_first>
  <files>sentinel-core/app/services/migration_orchestrator.py</files>
  <behavior>
    - For each ops-bound file: pre-move `scan_for_title_refs(vault, title)`; `relocate(src, dst)`; rename the embedding-index key old-path→new-path (value unchanged, NO re-embed); record both in one RollbackLedger.record_ops_move entry; post-move `scan_for_title_refs` and assert the count is unchanged (else rewrite the dangling ref or mark for rollback).
    - The embedding sidecar entry survives with its original embedding_b64 value under the new path.
  </behavior>
  <action>
    Implement `_move_ops_bound(vault, src, dst, rollback, ledger_backlinks)` and wire it into `run()`'s Track A loop (ops-bound FIRST per D-01 default). Steps per file: read the note title (filename stem); `pre = await scan_for_title_refs(vault, title)`; `actual_dst = await vault.relocate(src, dst)`; read `EMBEDDING_INDEX_PATH`, if `src` key present rename it to `actual_dst` with the value untouched via `decode_index_body`/`encode_index_body` and write it back; `rollback.record_ops_move(src, actual_dst, sidecar_key_moved=<bool>)` (the SAME entry covers both the file move and the sidecar key — T-47-03; when the `src` key is absent from the sidecar the note simply had no embedding — leave it for the normal post-migration sweep per D-04a, `sidecar_key_moved=False`); `post = await scan_for_title_refs(vault, title)`; if `post < pre` treat as a broken ops-bound backlink — this feeds the D-03a-equivalent ops-track gate (a shortfall marks the run for rollback in Plan 04's trigger logic). Append each moved file to `report.ops_moved`. Honor `relocate()`'s protected-namespace guards (do not bypass them). Do NOT wait for the next sweep to fix the sidecar — patch it inline (Pitfall C / D-04 "no re-embed").
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_orchestrator.py::test_embedding_and_wikilink_preservation -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_embedding_and_wikilink_preservation` passes for the ops-bound path: the moved note's sidecar key is renamed old→new with the `embedding_b64` value unchanged (no re-embed), and a pre-existing `[[old-title]]` reference still resolves (pre/post scan count unchanged).
    - The sidecar-key patch and the relocate are recorded in a single `record_ops_move` ledger entry (verified by a rollback assertion in the shared test fixture).
    - `scan_for_title_refs` runs pre and post per ops-bound file; a post<pre shortfall is captured on the report (not silently ignored).
    - No code path waits for `vault_sweeper` to reconcile the sidecar key.
  </acceptance_criteria>
  <done>Track A moves ops-bound files frontmatter-preserving with an inline sidecar-key patch, rollback-recorded, backlink-scan-gated; the embedding/wikilink preservation test is GREEN for the ops track.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord user → migration run | Destructive vault-wide mutation (lock-guarded) |
| Migration process → vault REST + embedding sidecar | relocate + sidecar key must move atomically together |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-02 | DoS/Tampering | Concurrent migration + partial move | high | mitigate | `acquire_sweep_lock()` first, `release_sweep_lock()` in finally (shared with sweeper+pipeline); every move recorded via RollbackLedger for replay |
| T-47-03 | Tampering/Integrity | embedding-index.json sidecar key | high | mitigate | sidecar-key patch inline after each relocate, recorded in the SAME `record_ops_move` entry so rollback restores path+key together |
| T-47-SC | Tampering (supply chain) | package installs | low | accept | Zero new third-party packages; reuses existing vault/sidecar/task-runner services |
</threat_model>

<verification>
Run `cd sentinel-core && .venv/bin/python -m pytest tests/test_migration_orchestrator.py::test_dry_run_writes_nothing tests/test_migration_orchestrator.py::test_embedding_and_wikilink_preservation -q` (GREEN). Then the quick-run subset `tests/test_pipeline_orchestrator.py tests/test_vault_sweeper.py tests/test_graph_analysis.py tests/test_links_sidecar_index.py -q` for collateral safety.
</verification>

<success_criteria>
Orchestrator skeleton (lock/finally/dry-run/background-task) + status store exist; Track A moves every ops-bound file frontmatter-preserving with an inline sidecar-key patch recorded in the rollback ledger and gated by the ops backlink scan; dry-run writes nothing and probes both directory spellings; ops-track preservation test GREEN.
</success_criteria>

<output>
Create `.planning/phases/47-migration-cutover-hardening/47-03-SUMMARY.md` when done.
</output>
