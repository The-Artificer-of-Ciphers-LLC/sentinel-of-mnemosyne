---
phase: 25-v0-40-pre-beta-refactoring-eliminate-duplicates-complete-stu
plan: 07
type: standard
wave: 3
depends_on:
  - 25-05-PLAN.md
  - 25-06-PLAN.md
files_modified:
  - docs/ARCHITECTURE-Core.md
  - docs/obsidian-lifebook-design.md
  - sentinel-core/app/models.py
autonomous: true
requirements:
  - IFACE-01
  - MEM-03
  - CORE-03

must_haves:
  truths:
    - "ARCHITECTURE-Core.md Message Envelope section matches models.py (content, user_id required; source, channel_id optional)"
    - "ARCHITECTURE-Core.md Pi harness port says 3000 (not 8765)"
    - "ARCHITECTURE-Core.md session path says ops/sessions/ (not core/sessions/)"
    - "ARCHITECTURE-Core.md endpoints list includes GET /status and GET /context/{user_id}"
    - "obsidian-lifebook-design.md get_self_context section shows 5 files (identity.md, methodology.md, goals.md, relationships.md, reminders.md)"
    - "sentinel-core/app/models.py MessageEnvelope has source and channel_id as optional fields"
    - "Existing interfaces still work — no existing POST /message calls break (source and channel_id are optional, default None)"
  artifacts:
    - path: "docs/ARCHITECTURE-Core.md"
      provides: "Updated architecture doc — CONTRA-01 through CONTRA-04 resolved"
    - path: "docs/obsidian-lifebook-design.md"
      provides: "Updated design doc — CONTRA-03 resolved (5 self/ files)"
    - path: "sentinel-core/app/models.py"
      provides: "MessageEnvelope with source and channel_id optional fields"
  key_links:
    - from: "sentinel-core/app/models.py MessageEnvelope"
      to: "docs/ARCHITECTURE-Core.md envelope spec"
      via: "Both show: content (required), user_id (required), source (optional), channel_id (optional)"
      pattern: "source.*None.*channel_id.*None"
    - from: "docs/ARCHITECTURE-Core.md port"
      to: "pi-harness/compose.yml port"
      via: "Both say 3000"
      pattern: "3000"
---

<objective>
Cluster D: Doc sync. Resolves all four architecture contradictions (CONTRA-01 through CONTRA-04) by making docs match code and expanding the MessageEnvelope Pydantic model per D-03.

Implements: RD-10 (synchronize architecture docs with code), CONTRA-01 through CONTRA-04 fixes, D-03 (MessageEnvelope expansion).

This plan depends on Plan 05 (routes/status.py must exist before ARCHITECTURE-Core.md can document it) and Plan 06 (SEC-04 checked — nothing architectural changes here, but wave ordering keeps doc sync last).

Purpose: Documentation that doesn't match code is a maintenance liability and a source of confusion for any future contributor or AI agent reading the architecture docs. These are surgical doc edits — no logic changes except the Pydantic model expansion.

Output: ARCHITECTURE-Core.md updated for CONTRA-01 through CONTRA-04 + new endpoints. obsidian-lifebook-design.md updated for CONTRA-03. models.py expanded with two optional fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-v0-40-pre-beta-refactoring-eliminate-duplicates-complete-stu/25-CONTEXT.md
@V040-REFACTORING-DIRECTIVE.md

<interfaces>
<!-- What the docs currently say vs. what they should say after this plan. -->

CONTRA-01: Message Envelope mismatch
  Code (current): MessageEnvelope has content: str, user_id: str = "default"
  Doc (current): Shows 8 fields including id, source, channel_id, timestamp, attachments, metadata
  Resolution (D-03): Expand model to add source: str | None = None and channel_id: str | None = None
  Doc update: Show 4 fields (content required, user_id required, source optional, channel_id optional)
  Leave id/timestamp/attachments/metadata documented as "reserved for future interface expansion — not currently in use"

CONTRA-02: Pi harness port mismatch
  Code (pi-harness/compose.yml): port 3000
  Doc (ARCHITECTURE-Core.md): port 8765
  Resolution: Change 8765 to 3000 in doc

CONTRA-03: Context read paths mismatch
  Code (routes/message.py _SELF_PATHS): 5 files — identity.md, methodology.md, goals.md, relationships.md, ops/reminders.md
  Doc (obsidian-lifebook-design.md): Says "get_self_context() concatenates 3 files: identity.md, goals.md, relationships.md"
  Resolution: Update obsidian-lifebook-design.md to show 5 files

CONTRA-04: Session summary path mismatch
  Code (routes/message.py): path = f"ops/sessions/{date_str}/{user_id}-{time_str}.md"
  Doc (ARCHITECTURE-Core.md): /core/sessions/{YYYY-MM-DD}/{user_id}-{HH-MM-SS}.md
  Resolution: Change core/sessions/ to ops/sessions/ in doc

New routes to document (from Plan 05 / RD-05):
  GET /status — system status, requires X-Sentinel-Key
  GET /context/{user_id} — debug context, requires X-Sentinel-Key

Current MessageEnvelope in sentinel-core/app/models.py:
```python
class MessageEnvelope(BaseModel):
    content: str
    user_id: str = "default"
```

Target MessageEnvelope (D-03):
```python
class MessageEnvelope(BaseModel):
    content: str
    user_id: str = "default"
    source: str | None = None          # Interface identifier (e.g., "discord", "imessage")
    channel_id: str | None = None      # Interface-specific channel identifier
    # id, timestamp, attachments, metadata: reserved for future expansion — not currently in use
```

All existing callers of POST /message pass only content and user_id — adding optional fields is backward-compatible. No existing tests break.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| models.py model expansion | MessageEnvelope gains two new optional fields — could affect validation or serialization behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-01 | Tampering | MessageEnvelope source field injection | mitigate | source: str | None is a plain string field with no special processing — it is stored in the envelope but not executed. Pydantic v2 validates type but does not sanitize. If source is forwarded to logs or Obsidian writes, it should pass through InjectionFilter (current post_message route already filters content; source/channel_id are metadata not user message content — accept residual risk for v0.40). |
| T-25-07-02 | Information Disclosure | Doc update reveals internal paths | accept | ARCHITECTURE-Core.md is a developer-facing internal document in the repo. Revealing ops/sessions/ vault path is appropriate for developer documentation. |
| T-25-07-03 | Tampering | Doc changes alter agent behavior | accept | AI agents reading these docs will get correct information after the update — this is the intended outcome, not a risk. |
</threat_model>

<tasks>

<task type="auto" id="25-07-01" priority="required">
  <name>Task 1: Expand MessageEnvelope in models.py (D-03) and update ARCHITECTURE-Core.md (CONTRA-01, 02, 04 + new routes)</name>
  <files>
    sentinel-core/app/models.py
    docs/ARCHITECTURE-Core.md
  </files>
  <action>
    Step 1 — Read sentinel-core/app/models.py to confirm current MessageEnvelope state.

    Step 2 — Edit sentinel-core/app/models.py MessageEnvelope class:
    Add two optional fields after user_id, using D-03 exactly:
    ```python
    class MessageEnvelope(BaseModel):
        content: str
        user_id: str = "default"
        source: str | None = None          # Interface identifier (e.g., "discord", "imessage")
        channel_id: str | None = None      # Interface-specific channel identifier
        # id, timestamp, attachments, metadata: reserved for future expansion — not currently in use
    ```
    Do NOT change ResponseEnvelope or any other model.

    Step 3 — Run sentinel-core tests to confirm no regression from the model change:
    `cd sentinel-core && .venv/bin/python -m pytest tests/ -x -q`
    All tests must pass (source and channel_id are optional — no existing test sends them, all pass None implicitly).

    Step 4 — Read docs/ARCHITECTURE-Core.md to locate the sections that need changes.

    Step 5 — Edit docs/ARCHITECTURE-Core.md with these four surgical changes:

    CONTRA-01 fix — Update the Message Envelope section:
    Replace the 8-field JSON example with a description that matches the actual 4-field model:
    ```json
    {
      "content": "user message text",
      "user_id": "user-identifier",
      "source": "discord",
      "channel_id": "channel-identifier"
    }
    ```
    Add a note: "content and user_id are required. source and channel_id are optional (default null) — interfaces may populate them for routing context. Fields id, timestamp, attachments, and metadata are reserved for future interface expansion and are not currently in use."

    CONTRA-02 fix — Find "8765" or "port 8765" in the Pi harness section and change it to 3000.

    CONTRA-04 fix — Find "core/sessions/" and change it to "ops/sessions/".

    New routes — In the REST API routes table/section, add:
    - GET /status | Yes (X-Sentinel-Key) | routes/status.py | System-wide service reachability
    - GET /context/{user_id} | Yes (X-Sentinel-Key) | routes/status.py | Debug: show context for user

    Step 6 — Verify all four changes landed:
    `grep "8765" docs/ARCHITECTURE-Core.md` — must return 0 results
    `grep "core/sessions" docs/ARCHITECTURE-Core.md` — must return 0 results
    `grep "3000" docs/ARCHITECTURE-Core.md` — must return at least 1 result
    `grep "ops/sessions" docs/ARCHITECTURE-Core.md` — must return at least 1 result
    `grep "/status\|/context" docs/ARCHITECTURE-Core.md` — must return matches for both new routes
    `grep "source.*None\|channel_id.*None" sentinel-core/app/models.py` — must return matches
  </action>
  <verify>
    <automated>grep -c "8765" /Users/trekkie/projects/sentinel-of-mnemosyne/docs/ARCHITECTURE-Core.md && grep "source" /Users/trekkie/projects/sentinel-of-mnemosyne/sentinel-core/app/models.py && cd /Users/trekkie/projects/sentinel-of-mnemosyne/sentinel-core && .venv/bin/python -m pytest tests/ -x -q</automated>
  </verify>
  <done>MessageEnvelope has source and channel_id as optional fields. ARCHITECTURE-Core.md updated: port is 3000, session path is ops/sessions/, envelope section shows 4-field model, /status and /context/{user_id} in route table. sentinel-core tests still GREEN. grep "8765" docs/ARCHITECTURE-Core.md returns 0.</done>
</task>

<task type="auto" id="25-07-02" priority="required">
  <name>Task 2: Update obsidian-lifebook-design.md (CONTRA-03) and run final acceptance checks</name>
  <files>
    docs/obsidian-lifebook-design.md
  </files>
  <action>
    Step 1 — Read docs/obsidian-lifebook-design.md to locate the get_self_context section.

    Step 2 — Edit docs/obsidian-lifebook-design.md:

    CONTRA-03 fix — Find the section describing "get_self_context() concatenates 3 files" and update it to show 5 files:
    - self/identity.md
    - self/methodology.md
    - self/goals.md
    - self/relationships.md
    - ops/reminders.md

    Update any count ("3 files") to say "5 files". Update any list that only shows 3 to show all 5.

    Step 3 — Verify CONTRA-03 fix:
    `grep -n "3 files\|3 file" docs/obsidian-lifebook-design.md` — must return 0 results (no more "3 files" claim)
    `grep "methodology\|reminders" docs/obsidian-lifebook-design.md` — must return matches (the two added files)

    Step 4 — Run the complete Phase 25 acceptance criteria checks from V040-REFACTORING-DIRECTIVE.md §10:

    AC-1: Zero duplicates
    `grep -rn "def call_core" interfaces/`
    Must return 0 results.

    `grep -rn "NotImplementedError" sentinel-core/app/`
    Must return 0 results.

    AC-3: Zero RED tests
    `cd sentinel-core && .venv/bin/python -m pytest tests/ -q`
    Must exit 0.

    AC-4: Zero architecture contradictions
    `grep "8765" docs/ARCHITECTURE-Core.md` — must return 0
    `grep "core/sessions" docs/ARCHITECTURE-Core.md` — must return 0
    `grep "3 files" docs/obsidian-lifebook-design.md` — must return 0

    AC-5: Route registry match (4 routes in sentinel-core)
    `grep -rn "@router\.\|@app\.get\|@app\.post" sentinel-core/app/ | grep -v "test_\|.pyc"`
    Must show exactly: POST /message, GET /health, GET /status, GET /context/{user_id}

    AC-7: Shared library
    `grep -rn "SentinelCoreClient" interfaces/discord/bot.py interfaces/imessage/bridge.py`
    Must return 2 matches.

    AC-8: SEC-04 complete
    `python -m pytest security/pentest/jailbreak_baseline.py -q`
    Must exit 0.
    `grep "\[x\].*SEC-04" .planning/REQUIREMENTS.md`
    Must return 1 match.

    Step 5 — Record any failures. If all pass, Phase 25 is ready for verification.
  </action>
  <verify>
    <automated>grep -c "3 files\|3 file" /Users/trekkie/projects/sentinel-of-mnemosyne/docs/obsidian-lifebook-design.md && grep "methodology" /Users/trekkie/projects/sentinel-of-mnemosyne/docs/obsidian-lifebook-design.md && grep -rn "def call_core" /Users/trekkie/projects/sentinel-of-mnemosyne/interfaces/ | wc -l</automated>
  </verify>
  <done>obsidian-lifebook-design.md shows 5 self/ files (added methodology.md and reminders.md). No "3 files" claim remains. All Phase 25 §10 acceptance criteria pass. CONTRA-01 through CONTRA-04 all resolved. Zero call_core() functions. Zero NotImplementedError in app/. SEC-04 checked.</done>
</task>

</tasks>

<verification>
Final cross-phase acceptance criteria check (from V040-REFACTORING-DIRECTIVE.md §10):

1. grep -rn "def call_core" interfaces/
   Must return 0.

2. grep -rn "NotImplementedError" sentinel-core/app/
   Must return 0.

3. cd sentinel-core && .venv/bin/python -m pytest tests/ -q
   Must exit 0.

4. cd pi-harness && npx vitest run
   Must exit 0.

5. grep "8765" docs/ARCHITECTURE-Core.md
   Must return 0.

6. grep "core/sessions" docs/ARCHITECTURE-Core.md
   Must return 0.

7. grep "source" sentinel-core/app/models.py
   Must show optional field.

8. grep "\[x\].*SEC-04" .planning/REQUIREMENTS.md
   Must return 1 match.

9. python -m pytest security/pentest/jailbreak_baseline.py -q
   Must exit 0.

10. ls security/JAILBREAK-BASELINE.md
    Must exist.
</verification>

<success_criteria>
- MessageEnvelope has source: str | None = None and channel_id: str | None = None (D-03)
- ARCHITECTURE-Core.md: port 8765 → 3000 (CONTRA-02), core/sessions → ops/sessions (CONTRA-04), envelope shows 4 fields (CONTRA-01), /status and /context/{user_id} in route table
- obsidian-lifebook-design.md: get_self_context shows 5 files including methodology.md and ops/reminders.md (CONTRA-03)
- grep "8765" docs/ARCHITECTURE-Core.md returns 0
- grep "core/sessions" docs/ARCHITECTURE-Core.md returns 0
- sentinel-core pytest suite exits 0 (no regressions from model expansion)
- All four CONTRA items resolved
- All Phase 25 §10 acceptance criteria verified as passing
</success_criteria>

<output>
After completion, create `.planning/phases/25-v0-40-pre-beta-refactoring-eliminate-duplicates-complete-stu/25-07-SUMMARY.md`
</output>
