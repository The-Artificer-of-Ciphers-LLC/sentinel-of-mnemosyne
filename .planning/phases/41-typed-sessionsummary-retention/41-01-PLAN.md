---
phase: 41-typed-sessionsummary-retention
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - sentinel-core/app/services/recall.py
  - sentinel-core/tests/test_recall.py
autonomous: true
requirements: [MEM-08, MEM-06, MEM-09]
must_haves:
  truths:
    - "recall.py defines a frozen SessionSummary value type with date/user_id/time/user_msg/sentinel_msg/path/body fields"
    - "recall.py defines a frozen RetentionPolicy value type with hot_limit and hot_window_days fields (defaults 3 / 2)"
    - "recency_weight(date_str, now, half_life_days) returns 1.0 for a same-day date and 0.5 at exactly half_life_days old; exponential decay blend, tunable half-life default ~7 days, recency does NOT hard-override relevance (D-01)"
    - "recency_weight fails open (returns 1.0) on an unparseable or non-string date"
  artifacts:
    - path: "sentinel-core/app/services/recall.py"
      provides: "SessionSummary + RetentionPolicy frozen dataclasses and the recency_weight pure helper"
      contains: "class SessionSummary"
    - path: "sentinel-core/tests/test_recall.py"
      provides: "recency_weight curve + SessionSummary/RetentionPolicy construction tests"
      contains: "recency_weight"
  key_links:
    - from: "sentinel-core/app/vault.py"
      to: "SessionSummary"
      via: "TYPE_CHECKING import from app.services.recall"
      pattern: "SessionSummary"
    - from: "recency_weight"
      to: "SessionSummary.date"
      via: "date_str argument is the SessionSummary.date string"
      pattern: "recency_weight\\("
---

<objective>
Introduce the typed value contracts this phase is built on: the frozen `SessionSummary`
and `RetentionPolicy` dataclasses, and the pure `recency_weight` helper — all in
`recall.py` beside their existing analogs (`SearchResult`, `RecallConfig`, `_rrf_merge`).
This is the interface-first foundation: every later plan imports these symbols.

Purpose: MEM-08 needs a typed session value; MEM-06 needs a tunable retention policy
object; MEM-09 needs a pure recency-decay function. Defining all three first lets the
Vault retype (Plan 02), the config wiring (Plan 03), and the Recall integration
(Plan 04) all build against fixed contracts instead of exploring for them.

Output: `SessionSummary`, `RetentionPolicy`, `recency_weight` exported from
`app/services/recall.py`, with the `recency_weight` curve pinned by RED→GREEN tests.

Resolves OQ2 (REMOVE `RecallConfig.recent_session_limit` — its job moves to
`RetentionPolicy.hot_limit`, one source of truth) and OQ3 (`RetentionPolicy` is a
SEPARATE injected value, NOT a `RecallConfig` field) by establishing the standalone
`RetentionPolicy` type here; the wiring lands in Plans 03/04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/adr/0005-typed-session-summary.md
@.planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md
@.planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md
@.planning/phases/41-typed-sessionsummary-retention/41-CONTEXT.md
</context>

<artifacts_this_phase_produces>
This plan creates the following NEW symbols (excluded from cross-phase drift verification — they did not exist before Phase 41):

- `SessionSummary` — `@dataclass(frozen=True)` in `app/services/recall.py`. Fields: `date: str` (YYYY-MM-DD), `user_id: str`, `time: str` (HH-MM-SS), `user_msg: str`, `sentinel_msg: str`, `path: str`, `body: str` (full raw markdown).
- `RetentionPolicy` — `@dataclass(frozen=True)` in `app/services/recall.py`. Fields: `hot_limit: int = 3`, `hot_window_days: int = 2`.
- `recency_weight(date_str: str, *, now: datetime, half_life_days: float = 7.0) -> float` — module-level pure function in `app/services/recall.py`.

Phase-wide artifacts produced by later plans (listed here so the whole phase surface is in one place):
- `app/vault.py`: retyped `get_recent_sessions(user_id, policy: RetentionPolicy) -> list[SessionSummary]` (Plan 02).
- `tests/fakes/vault.py`: retyped `get_recent_sessions` + `read_recent_sessions` alias (Plan 02).
- `app/config.py`: `Settings.retention_hot_limit: int = 3`, `Settings.retention_hot_window_days: int = 2` (env `RETENTION_HOT_LIMIT` / `RETENTION_HOT_WINDOW_DAYS`) (Plan 03).
- `app/services/recall.py`: `RecalledContext.sessions: list[SessionSummary]` (retype, Plan 04); `Recall._policy` injected attribute (Plan 04); `_CARRIER_NAMESPACE_PREFIXES` = the full conversation-carrier set journal/ + learning/ + accomplishments/ + references/ (Plan 04).
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for recency_weight curve and value-type construction</name>
  <files>sentinel-core/tests/test_recall.py</files>
  <read_first>
    - sentinel-core/tests/test_recall.py — existing test style: each test constructs a real `Recall` against `FakeVault`; imports at lines 16-17 (`from app.services.recall import Recall, RecallConfig, RecalledContext, SearchResult`). Add `SessionSummary, RetentionPolicy, recency_weight` to that import.
    - sentinel-core/app/services/recall.py:117-219 — the `SearchResult` / `RecallConfig` frozen-dataclass pattern to mirror.
    - .planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md §"Pattern 3" — the recommended curve `0.5 ** (age_days / half_life_days)`, half-life 7 days, fail-open on unparseable date.
    - .agents/skills/test-rigor (if present) and .agents/skills/error-handling-patterns/SKILL.md — behavioral-test rigor; tests must CALL the function and assert observable results, never grep source.
  </read_first>
  <behavior>
    - recency_weight: a date equal to `now`'s date → 1.0 (within float tolerance).
    - recency_weight: a date exactly `half_life_days` old → 0.5 (within float tolerance).
    - recency_weight: a date exactly `2*half_life_days` old → 0.25 (within float tolerance).
    - recency_weight: a future date (date > now) → clamped to 1.0 (age floored at 0, no >1.0 weight).
    - recency_weight: an unparseable string ("not-a-date") → 1.0 (fail-open, no exception).
    - recency_weight: a non-string / None date → 1.0 (fail-open, no exception).
    - SessionSummary: constructs with all 7 fields and is frozen (assigning a field raises FrozenInstanceError / dataclasses.FrozenInstanceError).
    - RetentionPolicy: `RetentionPolicy()` yields hot_limit=3, hot_window_days=2; explicit values override.
  </behavior>
  <action>
    Write the failing tests FIRST in `tests/test_recall.py`. Add `SessionSummary, RetentionPolicy, recency_weight` to the existing `from app.services.recall import ...` line.
    Name the tests so the RESEARCH §Validation test-map `-k` selectors resolve: `test_recency_weight_curve` (covers same-day=1.0, 7d=0.5, 14d=0.25, future-clamped), `test_recency_weight_failopen_on_bad_date` (unparseable string and None both return 1.0), `test_session_summary_is_frozen_value` (construct all 7 fields `SessionSummary(date, user_id, time, user_msg, sentinel_msg, path, body)`; assert a field reassignment raises), and `test_retention_policy_defaults` (`RetentionPolicy()` == hot_limit 3 / hot_window_days 2 and explicit override works).
    Use `datetime(..., tzinfo=timezone.utc)` for the `now=` argument and pass `half_life_days=7.0` explicitly in the curve test so the constant is pinned by the test, not assumed. Use `pytest.approx(0.5, abs=1e-9)` for curve assertions. RUN the suite and confirm these new tests FAIL with ImportError / NameError (symbols not yet defined) — that is the RED state. Do NOT touch `recall.py` in this task.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_recall.py -k "recency_weight or session_summary or retention_policy" -q -rfE 2>&1 | tail -40 | grep -E "^(FAILED|ERROR) tests/test_recall.py::(test_recency_weight_curve|test_recency_weight_failopen_on_bad_date|test_session_summary_is_frozen_value|test_retention_policy_defaults)\b" | sort -u | wc -l | grep -qx "4" && echo RED-CONFIRMED-4-NEW-TESTS-FAIL</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_recall.py` imports `SessionSummary, RetentionPolicy, recency_weight` from `app.services.recall`.
    - Four new test functions exist: `test_recency_weight_curve`, `test_recency_weight_failopen_on_bad_date`, `test_session_summary_is_frozen_value`, `test_retention_policy_defaults`.
    - The RED verify confirms EXACTLY these four named tests appear as `FAILED`/`ERROR` (ImportError/NameError on the missing symbols counts as ERROR on each collected test) — a pre-existing failure elsewhere in the file cannot fake the RED, because the gate counts only these four test names. RED confirmed.
    - No production code changed in this task.
  </acceptance_criteria>
  <done>The four new tests exist and fail because the symbols are undefined (RED), confirmed by the named-test failed-count gate.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — define SessionSummary, RetentionPolicy, recency_weight</name>
  <files>sentinel-core/app/services/recall.py</files>
  <read_first>
    - sentinel-core/app/services/recall.py:12-37 — imports block. `from dataclasses import dataclass, field` already present (line 18). `datetime`/`timezone` are NOT yet imported in this module — add `from datetime import datetime, timezone` near the stdlib imports.
    - sentinel-core/app/services/recall.py:117-219 — place `SessionSummary` and `RetentionPolicy` in the "Value types" block beside `SearchResult` / `RecallConfig`, mirroring `@dataclass(frozen=True)` with a docstring.
    - sentinel-core/app/services/recall.py:566-597 — `_rrf_merge`, the module-level pure-function neighbor where `recency_weight` belongs.
    - sentinel-core/app/services/message_processing.py:181-203 — `_build_session_summary` writer: this is the authoritative shape `SessionSummary` fields describe (frontmatter `timestamp`/`user_id`/`model`; body `## User` / `## Sentinel`; path `ops/sessions/{date}/{user_id}-{time}.md`). The parser that POPULATES SessionSummary lives in Plan 02; here only define the type.
  </read_first>
  <action>
    Add `from datetime import datetime, timezone` to the imports.
    Define `@dataclass(frozen=True) class SessionSummary` with exactly these typed fields in order: `date: str`, `user_id: str`, `time: str`, `user_msg: str`, `sentinel_msg: str`, `path: str`, `body: str`, plus a one-line docstring noting `date` is "YYYY-MM-DD" and `body` is the full raw markdown (back-compat carrier).
    Define `@dataclass(frozen=True) class RetentionPolicy` with `hot_limit: int = 3` and `hot_window_days: int = 2`, docstring noting these replace `RecallConfig.recent_session_limit` (OQ2) and the inline today+yesterday window (`vault.py:288-291`).
    Define module-level `recency_weight(date_str: str, *, now: datetime, half_life_days: float = 7.0) -> float` beside `_rrf_merge`: parse `date_str` with `datetime.strptime(date_str, "%Y-%m-%d").replace(tzinfo=timezone.utc)`; on `(ValueError, TypeError)` return `1.0` (fail-open per Security V5); compute `age_days = max(0.0, (now - d).total_seconds() / 86400.0)` (clamps future dates to weight 1.0); return `0.5 ** (age_days / half_life_days)`. Keep it pure — no I/O, no `self`, no module-global reads. Do NOT yet remove `RecallConfig.recent_session_limit` (its consumer `_hot_sessions` is rewired in Plan 04; removing it now would break the existing `_hot_sessions` call at recall.py:658-659 — leave that for the lockstep in Plan 04).
    RUN the Task 1 tests; they must now PASS (GREEN). Do NOT add behavior beyond what the tests pin.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_recall.py -k "recency_weight or session_summary or retention_policy" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `recall.py` defines `@dataclass(frozen=True) class SessionSummary` with fields date, user_id, time, user_msg, sentinel_msg, path, body.
    - `recall.py` defines `@dataclass(frozen=True) class RetentionPolicy` with hot_limit (default 3) and hot_window_days (default 2).
    - `recall.py` defines module-level `recency_weight(date_str, *, now, half_life_days=7.0)`.
    - `recall.py` imports `from datetime import datetime, timezone`.
    - The four Task 1 tests pass (GREEN). The full `tests/test_recall.py` still imports cleanly.
    - `RecallConfig.recent_session_limit` is NOT yet removed (deferred to Plan 04 lockstep).
  </acceptance_criteria>
  <done>SessionSummary, RetentionPolicy, recency_weight exist; the RED tests now pass.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR — confirm no circular import and full recall suite stays green</name>
  <files>sentinel-core/app/services/recall.py</files>
  <read_first>
    - sentinel-core/app/services/recall.py:33-34 — `if TYPE_CHECKING: from app.vault import Vault`. Plan 02 will make `vault.py` import `SessionSummary`/`RetentionPolicy` from `recall.py`; this task confirms the cycle stays type-only and import order is clean from `recall.py`'s side.
  </read_first>
  <action>
    No new behavior. Confirm `recall.py` imports cleanly standalone (`python -c "import app.services.recall"` from the package root succeeds with no ImportError). Verify the new value types are placed in the "Value types" section and `recency_weight` next to `_rrf_merge`, and that the module docstring / section comments still read coherently. Run the full `tests/test_recall.py` to confirm the existing tests (which still use the old `list[str]` session contract) remain green — Plan 01 must NOT change any existing test behavior or the `_hot_sessions`/`RecalledContext.sessions` runtime contract.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run python -c "import app.services.recall" && uv run pytest tests/test_recall.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `import app.services.recall` succeeds with no ImportError.
    - `uv run pytest tests/test_recall.py -q` exits 0 (all existing + new tests green).
    - No existing test was modified or weakened in this plan.
  </acceptance_criteria>
  <done>recall.py imports cleanly; the full recall test file is green; the value-type foundation is in place for Plans 02-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault markdown → SessionSummary.date | A `date` string read from a vault note path/frontmatter flows into `recency_weight` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01 | Denial of Service | `recency_weight(date_str)` parsing a malformed/hostile date from vault content | mitigate | `recency_weight` catches `(ValueError, TypeError)` and returns 1.0 (fail-open); `test_recency_weight_failopen_on_bad_date` pins this — a bad date cannot raise into the recall path |
| T-41-02 | Tampering | crafted far-future `date` to force max recency weight and dominate ranking | accept | weight is capped at 1.0 via `max(0.0, age)` (future → age 0 → weight 1.0); recency is a blend multiplier, never a hard override (D-01), so a single boosted item cannot evict a strongly-relevant result. Personal single-user vault; no external write path to session dates. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (RESEARCH §Package Legitimacy Audit: not applicable). All imports (`dataclasses`, `datetime`, existing recall deps) are pre-existing. No legitimacy gate required. |
</threat_model>

<verification>
- `uv run pytest tests/test_recall.py -q` is green.
- `recency_weight` curve is pinned by an executable test (today=1.0, 7d=0.5, 14d=0.25), not assumed.
- No external package installed; no existing test weakened.
</verification>

<success_criteria>
- SessionSummary, RetentionPolicy, recency_weight exist in `recall.py` and import cleanly.
- The recency curve and fail-open behavior are verified by behavioral tests that call the function.
- OQ2 (remove recent_session_limit — staged; type exists, removal in Plan 04) and OQ3 (separate RetentionPolicy type) are established by the standalone `RetentionPolicy` defined here.
</success_criteria>

<output>
Create `.planning/phases/41-typed-sessionsummary-retention/41-01-SUMMARY.md` when done.
</output>
