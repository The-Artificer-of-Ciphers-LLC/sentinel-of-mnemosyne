---
phase: 41-typed-sessionsummary-retention
plan: 05
type: execute
wave: 4
depends_on: ["41-01", "41-02", "41-04"]
files_modified:
  - sentinel-core/app/services/message_processing.py
  - sentinel-core/app/routes/status.py
  - sentinel-core/tests/test_message.py
  - sentinel-core/tests/test_status.py
  - sentinel-core/tests/test_integration_obsidian_llm.py
  - sentinel-core/tests/test_auth.py
autonomous: true
requirements: [MEM-08, MEM-06]
must_haves:
  truths:
    - "message_processing.py joins SessionSummary fields (s.body), not raw dataclasses — no TypeError on str.join"
    - "Typed session context still flows through injection_filter.wrap_context (injection boundary not bypassed)"
    - "status.py serializes SessionSummary fields explicitly to JSON (no raw dataclasses in JSONResponse); recent_sessions_count still works"
    - "All ~19 get_recent_sessions mock sites are aligned to the typed contract: empty-list mocks keep [] (signature-only); content-returning mocks construct SessionSummary values"
    - "test_integration_obsidian_llm.py:166 asserts the new policy= call shape (not limit=3); the KNOWN_SESSION content assertion at line 171 is preserved/strengthened"
    - "The full test suite is green (phase integration gate)"
  artifacts:
    - path: "sentinel-core/app/services/message_processing.py"
      provides: "typed-session consumer joining s.body through wrap_context"
      contains: "recalled.sessions"
    - path: "sentinel-core/app/routes/status.py"
      provides: "explicit SessionSummary JSON serialization"
      contains: "recalled.sessions"
  key_links:
    - from: "message_processing.py"
      to: "injection_filter.wrap_context"
      via: "joined typed-session context still passes through the injection boundary"
      pattern: "wrap_context"
    - from: "status.py"
      to: "JSONResponse sessions list"
      via: "explicit [{...} for s in recalled.sessions] comprehension"
      pattern: "recalled\\.sessions"
---

<objective>
Update the two live `RecalledContext.sessions` consumers and all remaining test mock
sites in lockstep with the operator-approved `list[str]` → `list[SessionSummary]` type
change. This is the lockstep that prevents the two `TypeError` breakages (Pitfall 2) and
keeps the ~19 `get_recent_sessions` mock sites honest against the typed contract.

Purpose: MEM-08 end-to-end — once sessions are typed, the presentation consumers
(`message_processing.py` join, `status.py` JSON) must read fields instead of strings,
and the shipped tests that pin the old contract must be retyped (not weakened). MEM-06
surfaces in `test_integration_obsidian_llm.py:166` where the call shape changes from
`limit=3` to `policy=`.

Output: typed-session-aware consumers, a green full suite, and an explicit lockstep
record (per Test-Rewrite Ban: these are operator-approved type-change lockstep updates
and assertion STRENGTHENINGS, never silent weakenings).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/adr/0005-typed-session-summary.md
@.planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md
@.planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md
@.planning/phases/41-typed-sessionsummary-retention/41-01-SUMMARY.md
@.planning/phases/41-typed-sessionsummary-retention/41-02-SUMMARY.md
@.planning/phases/41-typed-sessionsummary-retention/41-04-SUMMARY.md
</context>

<artifacts_this_phase_produces>
RETYPED consumers + test stubs introduced by this plan (excluded from cross-phase drift verification):

- `app/services/message_processing.py`: the `recalled.sessions` join (line 108-110) now reads `SessionSummary.body`.
- `app/routes/status.py`: the `sessions` JSON value (line 55) is now an explicit `[{...} for s in recalled.sessions]` comprehension over SessionSummary fields.
- `tests/test_message.py`: the inline fake's `get_recent_sessions` (line 1143) retyped to `(user_id, policy)`; ~17 `return_value = []` sites are signature-compatible (empty list satisfies both contracts).
- `tests/test_status.py` / `tests/test_integration_obsidian_llm.py`: content-returning mocks reconstructed as `SessionSummary(...)`; the line-166 assertion updated to the `policy=` call shape.

Consumed: `SessionSummary` (Plan 01), typed `get_recent_sessions` (Plan 02), typed `RecalledContext.sessions` (Plan 04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Retype the two RecalledContext.sessions consumers (message_processing + status)</name>
  <files>sentinel-core/app/services/message_processing.py, sentinel-core/app/routes/status.py</files>
  <read_first>
    - sentinel-core/app/services/message_processing.py:104-125 — the recalled-context assembly: line 108-110 does `"\n---\n".join(recalled.sessions)` (breaks on dataclasses); line 114-116 wraps the joined context in `self._injection_filter.wrap_context(...)` (the injection boundary that must NOT be bypassed — RESEARCH Security: Tampering/prompt-injection).
    - sentinel-core/app/services/message_processing.py:181-203 — `_build_session_summary` writer: do NOT change the written shape (it is Plan 02's parser contract).
    - sentinel-core/app/routes/status.py:50-58 — the JSONResponse: line 55 `"sessions": recalled.sessions` (now dataclasses, not JSON-serializable); line 56 is the EXACT serialization idiom to copy (`[{"path": r.path, "score": r.score} for r in recalled.warm]`); line 57 `len(recalled.sessions)` still works unchanged.
    - sentinel-core/app/services/recall.py (Plan 04 result) — `SessionSummary` fields (date, user_id, time, user_msg, sentinel_msg, path, body) available for the join/serialization.
    - .planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md §"Pitfall 2" + §"Pattern" — join `s.body`; serialize explicit fields.
  </read_first>
  <action>
    In `message_processing.py:108-110`: change `"\n---\n".join(recalled.sessions)` to join the per-session bodies — `"\n---\n".join(s.body for s in recalled.sessions)`. Keep the surrounding `"Recent session history:\n" + ...` prefix and the `if recalled.sessions:` guard. Critically, leave lines 112-116 intact so the joined context still flows through `self._budget.truncate(...)` and `self._injection_filter.wrap_context(...)` — the typed sessions MUST still pass through the injection boundary (do not bypass `wrap_context`).
    In `status.py:55`: replace `"sessions": recalled.sessions,` with an explicit comprehension mirroring the line-56 `warm` idiom: `"sessions": [{"date": s.date, "user_id": s.user_id, "time": s.time, "user_msg": s.user_msg, "sentinel_msg": s.sentinel_msg, "path": s.path} for s in recalled.sessions],`. Leave `"recent_sessions_count": len(recalled.sessions)` (line 57) unchanged.
    Do not change any other behavior; do not touch the warm serialization on line 56.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run python -c "import app.services.message_processing, app.routes.status"</automated>
  </verify>
  <acceptance_criteria>
    - `message_processing.py` joins `s.body` over `recalled.sessions` (no raw-dataclass `join`); the joined context still passes through `truncate` + `wrap_context`.
    - `status.py` serializes an explicit dict per SessionSummary (date/user_id/time/user_msg/sentinel_msg/path); `recent_sessions_count` unchanged.
    - Both modules import cleanly.
  </acceptance_criteria>
  <done>The two consumers read typed SessionSummary fields; the injection boundary is preserved; status JSON is well-formed.</done>
</task>

<task type="auto">
  <name>Task 2: Lockstep-align the ~19 get_recent_sessions mock sites (empty-list signature + content-returning reconstruction)</name>
  <files>sentinel-core/tests/test_message.py, sentinel-core/tests/test_status.py, sentinel-core/tests/test_integration_obsidian_llm.py, sentinel-core/tests/test_auth.py</files>
  <read_first>
    - sentinel-core/tests/test_message.py — ~17 `get_recent_sessions.return_value = []` sites (lines 30, 251, 262, 276, 369, 416, 438, 686, 706, 775, 830, 879, 927, 972, 1038, 1088) and the inline fake class method at lines 1143-1144 (`async def get_recent_sessions(self, user_id, limit=3) -> list:`). The `[]` AsyncMock sites are type-agnostic (empty list satisfies `list[SessionSummary]`) — they need NO change unless they pass `limit=` to the call. The inline fake method signature must change to `(self, user_id: str, policy=None)` to match the Protocol.
    - sentinel-core/tests/test_auth.py:66 — `mock_obsidian.get_recent_sessions.return_value = []` — type-agnostic, leave as-is (verify it still passes).
    - sentinel-core/tests/test_status.py:21 — `m.get_recent_sessions = AsyncMock(return_value=["session1"])` — content-returning; reconstruct as `AsyncMock(return_value=[SessionSummary(date=..., user_id=..., time=..., user_msg=..., sentinel_msg=..., path=..., body="session1")])` so the status route serializes real typed fields. Strengthen the downstream assertion to check the serialized `sessions[0]` dict has the expected fields (do not loosen).
    - sentinel-core/tests/test_integration_obsidian_llm.py:26 (`KNOWN_SESSION` markdown), :38 (`AsyncMock(return_value=[KNOWN_SESSION])`), :166 (`assert_called_once_with("test-user-123", limit=3)`), :171 (asserts `KNOWN_SESSION in all_content`), :196 (`return_value=[]`). Wrap `KNOWN_SESSION` in a `SessionSummary(..., body=KNOWN_SESSION, user_msg=..., sentinel_msg=...)` at line 38 so the join in message_processing surfaces it; update line 166 to `assert_called_once_with("test-user-123", policy=...)` matching the new call shape (the call now passes `policy=self._policy`); keep/strengthen line 171's `KNOWN_SESSION in all_content` content assertion (since the consumer now joins `s.body`, the KNOWN_SESSION text must still appear — preserve this shipped-behavior assertion).
    - .planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md §"Test Mock Pattern" + CLAUDE.md "Test-Rewrite Ban" / "Behavioral-Test-Only Rule" — these are LOCKSTEP retypes to the operator-approved contract; STRENGTHEN content assertions, never weaken or stub around the typed contract.
  </read_first>
  <action>
    Import `SessionSummary` (and `RetentionPolicy` where a call-shape assertion needs it) from `app.services.recall` in test_status.py and test_integration_obsidian_llm.py.
    test_message.py: leave the ~17 `return_value = []` AsyncMock sites unchanged (empty list is contract-compatible). Change ONLY the inline fake class method (line 1143) signature to `async def get_recent_sessions(self, user_id: str, policy=None) -> list:` returning `self.get_recent_sessions_value` (default `[]`); if any test sets `get_recent_sessions_value` to string content, convert that seed to a `SessionSummary`.
    test_auth.py:66: leave as-is; confirm green.
    test_status.py:21: reconstruct the `["session1"]` mock as a single `SessionSummary(...)` with `body="session1"` (and plausible date/user_id/time/user_msg/sentinel_msg/path); strengthen the assertion on the response `sessions` payload to check the serialized dict fields.
    test_integration_obsidian_llm.py: at line 38 wrap `KNOWN_SESSION` in a `SessionSummary(..., body=KNOWN_SESSION)`; at line 166 update the assertion to the `policy=` call shape (`assert_called_once_with("test-user-123", policy=<the policy instance or mock.ANY for the policy arg>)` — prefer asserting the user_id positional and that a `policy` kwarg of type RetentionPolicy was passed, e.g. via inspecting `call_args.kwargs["policy"]`); KEEP the line-171 `KNOWN_SESSION in all_content` assertion intact (it now passes because message_processing joins `s.body == KNOWN_SESSION`). Line 196 `return_value=[]` is contract-compatible — leave it.
    Record in the SUMMARY: every changed site classified as (a) signature-only lockstep (empty-list / inline-fake signature) or (b) content reconstruction + assertion strengthening — none are weakenings. Explicitly state that test_recall.py:118 strengthening was done in Plan 04 and the KNOWN_SESSION content assertion (line 171) and the limit/policy call-shape assertion (line 166) protect shipped MEM behavior and were updated in lockstep with the operator-approved type change, not loosened.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_message.py tests/test_status.py tests/test_integration_obsidian_llm.py tests/test_auth.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The inline fake in test_message.py:1143 has signature `(self, user_id: str, policy=None)`.
    - test_status.py and test_integration_obsidian_llm.py construct `SessionSummary(...)` values for content-returning mocks; imports present.
    - test_integration_obsidian_llm.py:166 asserts the `policy=` call shape (not `limit=3`); line 171's `KNOWN_SESSION in all_content` assertion is preserved.
    - No assertion was loosened (e.g. no `== ["session1"]` → `is not None`); changes are signature alignment or content reconstruction + strengthening.
    - `uv run pytest tests/test_message.py tests/test_status.py tests/test_integration_obsidian_llm.py tests/test_auth.py -q` exits 0.
  </acceptance_criteria>
  <done>All consumer-side test mock sites match the typed contract; content assertions are preserved/strengthened; the targeted test files are green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase integration gate — full suite green + lockstep record</name>
  <files>sentinel-core/tests/test_message.py</files>
  <read_first>
    - .planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md §"Validation Architecture" §"Sampling Rate" — the phase-gate command `cd sentinel-core && uv run pytest -q` must be green before verify-work.
    - CLAUDE.md "Test-Rewrite Ban" / "Behavioral-Test-Only Rule" — the SUMMARY must record the lockstep classification.
  </read_first>
  <action>
    Run the FULL test suite (`cd sentinel-core && uv run pytest -q`). If any test outside the enumerated sites fails because of the type change, that is an additional in-lockstep site the research blast-radius scan missed — align it the same way (empty-list = signature-only; content = reconstruct + strengthen). Do NOT weaken any assertion to make a failure pass; if a failure is NOT a type-change lockstep (i.e. a genuine regression), STOP and surface it (AI Deferral Ban — do not skip/xfail). In the SUMMARY, record the final lockstep tally: count of signature-only sites, count of content-reconstruction sites, and an explicit statement that no shipped-feature assertion was weakened (only strengthened or retyped in lockstep with the operator-approved `list[SessionSummary]` change).
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd sentinel-core && uv run pytest -q` exits 0 (full suite green — phase integration gate).
    - The SUMMARY records the lockstep tally and an explicit no-weakening statement.
    - No test was skipped/xfailed to pass; any genuine regression was surfaced, not suppressed.
  </acceptance_criteria>
  <done>The full Sentinel Core test suite is green; the typed-session change is integrated end-to-end; the lockstep is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recalled session body → LLM message list | Typed session content is joined and injected into the prompt; must remain inside the injection_filter boundary |
| recalled session → status JSON | Typed session fields serialized into the /status HTTP response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-12 | Tampering (prompt injection) | typed session body joined into the prompt bypassing `wrap_context` | mitigate | The join change is INSIDE the existing `truncate` → `wrap_context` path (message_processing.py:112-116 left intact); typed sessions still flow through the injection boundary. Verified by the existing message-pipeline tests staying green. |
| T-41-13 | Information Disclosure | status JSON leaking unintended SessionSummary internals | accept | `/status` is an authenticated operator route (`X-Sentinel-Key`); it serializes only the session fields already present in the hot tier (the operator's own sessions). No new field beyond what the operator already sees. test_status assertions pin the payload shape. |
| T-41-14 | DoS | a non-string `s.body` breaking `str.join` | mitigate | `SessionSummary.body` is typed `str` (frozen dataclass, Plan 01); the parser (Plan 02) always sets `body` to a string (defaults to "" on malformed input). `join` over `str` fields cannot raise TypeError. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit n/a). No legitimacy gate required. |
</threat_model>

<verification>
- `cd sentinel-core && uv run pytest -q` is green (full phase gate).
- The session context still passes through `wrap_context` (injection boundary preserved).
- status JSON serializes explicit SessionSummary fields; `recent_sessions_count` intact.
- Every mock-site change is a signature alignment or a content reconstruction + assertion strengthening — no weakening; recorded in the SUMMARY.
</verification>

<success_criteria>
- MEM-08: the two `RecalledContext.sessions` consumers read typed fields end-to-end; the full suite is green.
- MEM-06: the `policy=` call-shape change is reflected in the integration assertion.
- Test-Rewrite Ban honored: lockstep retypes + strengthenings only, recorded explicitly.
</success_criteria>

<output>
Create `.planning/phases/41-typed-sessionsummary-retention/41-05-SUMMARY.md` when done.
</output>
