---
phase: 41-typed-sessionsummary-retention
plan: 04
type: tdd
wave: 3
depends_on: ["41-01", "41-02", "41-03"]
files_modified:
  - sentinel-core/app/services/recall.py
  - sentinel-core/tests/test_recall.py
autonomous: true
requirements: [MEM-08, MEM-09, MEM-07, MEM-06]
must_haves:
  truths:
    - "RecalledContext.sessions is annotated list[SessionSummary] and carries typed values end-to-end"
    - "Recall.__init__ accepts an injected RetentionPolicy (policy=) and _hot_sessions calls get_recent_sessions(user_id, policy=self._policy)"
    - "_hot_sessions returns sessions ordered by recency_weight on SessionSummary.date (more recent first), a blend not a hard relevance override"
    - "Warm-tier recency (place b) multiplies the RRF score of carrier-namespace results (path startswith journal/) by recency_weight using the note's own date; self/ and ops/ are never weighted (D-02)"
    - "A session older than hot_window_days surfaces via RecalledContext.warm through the journal carrier (MEM-07), and the ops/ exclusion is NOT relaxed (criterion 4)"
    - "RecallConfig.recent_session_limit is removed; hot_limit now lives only on RetentionPolicy (OQ2, one source of truth)"
    - "OQ1 is locked: a 'session-derived warm result' is a warm SearchResult whose path is in the journal/ carrier namespace, dated from its own path"
    - "The inbox/ MEM-07 hole (low-confidence/unsure content is inbox'd into a skip-listed dir and never embedded) is an explicitly recorded document-and-accept decision, characterized by a test, not silently omitted"
  artifacts:
    - path: "sentinel-core/app/services/recall.py"
      provides: "typed RecalledContext.sessions, policy-injected Recall, recency-ordered _hot_sessions, warm-tier recency for carrier notes"
      contains: "list[SessionSummary]"
    - path: "sentinel-core/tests/test_recall.py"
      provides: "recency hot-order, warm-merge carrier-only, excludes-self, old-session-warm, retention-window, inbox-gap characterization tests"
      contains: "recency"
  key_links:
    - from: "Recall._hot_sessions"
      to: "recency_weight(s.date)"
      via: "sorted by recency weight, most-recent first"
      pattern: "recency_weight\\("
    - from: "Recall._warm_search"
      to: "recency_weight on journal/ carrier results"
      via: "RRF-score multiplier gated to the journal/ namespace, never self/"
      pattern: "journal/"
    - from: "Recall.__init__"
      to: "self._policy (RetentionPolicy)"
      via: "injected policy passed to get_recent_sessions"
      pattern: "policy"
---

<objective>
Integrate the typed/recency machinery into the `Recall` class: retype
`RecalledContext.sessions` to `list[SessionSummary]`, inject the `RetentionPolicy`
into `Recall`, make `_hot_sessions` typed + recency-ordered (MEM-09 place a), apply
warm-tier recency to journal carrier notes (MEM-09 place b), and verify the old-session
warm reachability (MEM-07). Remove `RecallConfig.recent_session_limit` (OQ2).

Purpose: this is where MEM-08 (typed sessions in `RecalledContext`), MEM-09 (recency in
both places, episodic-only), MEM-07 (old session reachable via warm), and the MEM-06
policy injection converge inside the Recall module — above the Vault seam (ADR-0003).

Output: a fully typed, recency-weighted, policy-driven `Recall`, with behavioral tests
pinning the hot ordering, the carrier-only warm weighting, the never-weight-self rule,
the old-session-warm path, and a characterization test for the documented inbox/ gap.

Locks OQ1 (carrier-namespace = `journal/`, dated from path), completes OQ2 (remove
`recent_session_limit`) and OQ3 (separate injected policy). Records the inbox/ MEM-07
hole as a document-and-accept decision (D-06 mandate; AI Deferral Ban).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/adr/0005-typed-session-summary.md
@.planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md
@.planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md
@.planning/phases/41-typed-sessionsummary-retention/41-CONTEXT.md
@.planning/phases/41-typed-sessionsummary-retention/41-01-SUMMARY.md
@.planning/phases/41-typed-sessionsummary-retention/41-02-SUMMARY.md
@.planning/phases/41-typed-sessionsummary-retention/41-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW / RETYPED symbols introduced by this plan (excluded from cross-phase drift verification):

- `app/services/recall.py`: `RecalledContext.sessions: list[SessionSummary]` (was `list[str]`).
- `app/services/recall.py`: `Recall.__init__(..., policy: RetentionPolicy | None = None)` and `self._policy` attribute (default `RetentionPolicy()` when None).
- `app/services/recall.py`: `Recall._hot_sessions(user_id) -> list[SessionSummary]` (typed + recency-ordered).
- `app/services/recall.py`: warm-tier recency application inside `_warm_search` (RRF-score multiplier on `journal/`-namespace results) — and a `_CARRIER_NAMESPACE_PREFIXES = ("journal/",)` module constant (OQ1).
- REMOVED: `RecallConfig.recent_session_limit` (OQ2). `_hot_sessions` no longer reads it.

Consumed (from earlier plans): `SessionSummary`, `RetentionPolicy`, `recency_weight` (Plan 01); typed `get_recent_sessions(user_id, policy)` (Plan 02); `settings.retention_hot_*` → injected policy (Plan 03).
DOWNSTREAM (Plan 05): the two `RecalledContext.sessions` consumers (`message_processing.py`, `status.py`) are retyped there.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — failing tests for typed sessions, recency hot-order, warm carrier weighting, self-exclusion, old-session-warm, retention window, and the inbox gap</name>
  <files>sentinel-core/tests/test_recall.py</files>
  <read_first>
    - sentinel-core/tests/test_recall.py:1-30 — fixture style (real `Recall` against `FakeVault`; imports at 16-17; `make_fixture_index` helper at 27 for SemanticRecall tests).
    - sentinel-core/tests/test_recall.py:118-127 — `test_assemble_returns_sessions`: line 121 seeds `"ops/sessions/2026-06-11/trekkie-12-00-00.md": "session body here"`; line 127 asserts `any("session body here" in s for s in result.sessions)`. This MUST be STRENGTHENED to assert on `SessionSummary` fields (e.g. `result.sessions[0].body` contains the text, and `.user_id == "trekkie"`), per the Test-Rewrite Ban — never loosened.
    - sentinel-core/tests/test_recall.py:142 — `test_warm_excludes_self_and_ops_prefixes` (criterion 4 guard): keep GREEN and unchanged.
    - sentinel-core/tests/test_recall.py:264 — existing `vault.get_recent_sessions = raising_sessions` graceful-degrade test; confirm the assemble-coerces-to-[] envelope still holds with the typed return.
    - sentinel-core/app/services/recall.py:117-219 (value types), :566-597 (`_rrf_merge`), :643-725 (`_hot_self`/`_hot_sessions`/`_warm_search`), :731-771 (`assemble` + RecalledContext build).
    - .planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md §"Open Questions" (OQ1: carrier namespace = journal/, dated from path), §"Pitfall 1" (inbox gap — document & accept, characterize with a test), §"Validation Architecture" test-map (the `-k` selectors to use).
    - .agents/skills/test-rigor (if present) — strict behavioral assertions; CALL assemble / _hot_sessions, assert observable results (ordering, RecalledContext.warm contents, SessionSummary fields), never grep source.
  </read_first>
  <behavior>
    - Typed sessions: `assemble` returns `RecalledContext.sessions` as `list[SessionSummary]`; the seeded `ops/sessions/2026-06-11/trekkie-12-00-00.md` parses to a SessionSummary whose `.body` contains the seeded text and `.user_id == "trekkie"` (strengthen test_assemble_returns_sessions).
    - Recency hot order (place a): given two parseable sessions for the same user, one dated today and one dated 10 days ago, `_hot_sessions` (or assemble's sessions list) returns the today session BEFORE the older one. (`-k recency_order`)
    - Recency hot order is a BLEND not a hard override: a strongly-relevant older session is still PRESENT in the list (not dropped) — recency reorders, it does not filter. Assert the older session is still in the returned list.
    - recency_weight curve already pinned in Plan 01 — do not duplicate.
    - Warm carrier weighting (place b): seed two warm candidates, both in `journal/` namespace, equal RRF rank, one dated today and one dated 10 days ago in its path; after `_warm_search`, the today carrier note ranks above the older one. (`-k recency_warm`)
    - Never-weight-self (D-02): a `self/`-namespace (or non-journal) warm result's score is NOT multiplied by recency_weight — its relative order is unchanged by date. (`-k recency_excludes_self`)
    - Old-session-warm reachability (MEM-07): a session whose corresponding journal carrier note is older than `hot_window_days` (so it is NOT in the hot list) is surfaced via `RecalledContext.warm`. (`-k old_session_warm`)
    - ops/ NOT relaxed (criterion 4): the existing `test_warm_excludes_self_and_ops_prefixes` stays green — add an assertion (or rely on it) that an `ops/sessions/` path never appears in `RecalledContext.warm`.
    - Retention window tunability (MEM-06): constructing `Recall` with `RetentionPolicy(hot_limit=1, hot_window_days=...)` returns at most 1 hot session; a different `hot_limit` returns a different count. (`-k retention`)
    - inbox gap characterization (Pitfall 1): a low-confidence turn whose content lands in `inbox/` (skip-listed) is NOT recoverable via warm recall — write a test that ASSERTS the boundary (inbox content absent from `RecalledContext.warm`), documenting the accepted gap. This test characterizes the boundary; it must NOT force-close it. (`-k inbox_gap`)
  </behavior>
  <action>
    Write ALL the above as failing tests FIRST in `tests/test_recall.py`, using FakeVault and the existing fixture style. Add `recency_weight` to imports if not already there. Name tests so the RESEARCH `-k` selectors resolve: `test_assemble_returns_sessions` (STRENGTHEN the existing one — assert SessionSummary fields), `test_recency_order_hot`, `test_recency_order_is_blend_not_filter`, `test_recency_warm_carrier_only`, `test_recency_excludes_self`, `test_old_session_warm_reachable`, `test_retention_window_tunable`, `test_inbox_gap_not_recalled`.
    For the warm tests, seed FakeVault `notes` with `journal/{date}/...` carrier notes and a fixture embedding index (use the existing `make_fixture_index` helper at line 27) or the keyword path so `_warm_search` returns them; pin dates in the PATH (`journal/2026-06-02/...`) so OQ1's path-derived date applies. For `test_recency_excludes_self`, seed a `self/`-prefixed warm candidate and assert its ordering is independent of date.
    For `test_inbox_gap_not_recalled`, seed an `inbox/_inbox.md` note and assert it never appears in `RecalledContext.warm` (it is in `sweep_skip_prefixes` so it is never embedded — characterize, don't fix).
    Inject the policy where needed: construct `Recall(vault=..., config=..., semantic_strategy=..., policy=RetentionPolicy(hot_limit=..., hot_window_days=...))`. RUN the suite; the new/strengthened tests FAIL because the integration isn't wired yet (RED). Do NOT modify production code in this task.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_recall.py -k "recency or retention or old_session_warm or inbox_gap or assemble_returns_sessions" -q 2>&1 | grep -qiE "error|failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - The eight named tests exist in `tests/test_recall.py` and CALL `Recall.assemble` / `_hot_sessions` (not source-grep).
    - `test_assemble_returns_sessions` asserts on `SessionSummary` fields (`.body`, `.user_id`), strengthened from the old string assertion.
    - `test_recency_excludes_self` asserts a self/non-journal result's order is date-independent.
    - `test_inbox_gap_not_recalled` asserts inbox content is absent from `RecalledContext.warm`.
    - Running the `-k` selector shows the new tests FAIL (RED); no production code changed.
  </acceptance_criteria>
  <done>The eight behavioral tests exist and fail because the Recall integration is not yet wired (RED).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — retype RecalledContext.sessions, inject policy, recency-order _hot_sessions, weight warm carrier notes, remove recent_session_limit</name>
  <files>sentinel-core/app/services/recall.py</files>
  <read_first>
    - sentinel-core/app/services/recall.py:144-145 — `RecalledContext.sessions: list[str]` annotation + docstring to retype to `list[SessionSummary]`.
    - sentinel-core/app/services/recall.py:160-219 — `RecallConfig`; line 172 `recent_session_limit: int = 3` to REMOVE (OQ2). Confirm via grep no test passes `recent_session_limit=` by name (RESEARCH OQ2 says none do — verify `test_recall.py:355,381` construct `RecallConfig()` with other fields only).
    - sentinel-core/app/services/recall.py:615-630 — `Recall.__init__`; add a `policy: RetentionPolicy | None = None` keyword param and set `self._policy = policy or RetentionPolicy()`.
    - sentinel-core/app/services/recall.py:656-660 — `_hot_sessions`: change return type to `list[SessionSummary]`, call `await self._vault.get_recent_sessions(user_id, policy=self._policy)` (drop the `limit=self._config.recent_session_limit` arg), then `sorted(sessions, key=lambda s: recency_weight(s.date, now=datetime.now(timezone.utc)), reverse=True)`.
    - sentinel-core/app/services/recall.py:662-725 — `_warm_search`: after `_rrf_merge` produces `merged` (line 701) and bodies are read (713-723), apply place (b): for each result whose `path` startswith a carrier-namespace prefix (`journal/`), multiply its score by `recency_weight(<date parsed from path>, now=...)`; leave non-carrier results untouched (D-02). Re-sort by the adjusted score (descending, tie-break on path) before returning, OR apply the multiplier on `merged` before the body-read so the final order reflects recency. Add a `_CARRIER_NAMESPACE_PREFIXES = ("journal/",)` module constant near `_WARM_TIER_EXCLUDE_PREFIXES` (line 53) and a tiny `_path_date(path)` helper that extracts `journal/{date}/...` → `date` (fail-open: unparseable → recency_weight returns 1.0, so the multiplier is a no-op).
    - sentinel-core/app/services/recall.py:741-771 — `assemble`: the `sessions: list[str]` local annotation (756) → `list[SessionSummary]`; the `return_exceptions=True` envelope already coerces a failed `_hot_sessions` to `[]` — keep that fail-open behavior.
    - .planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md §"recency_weight pure helper" and §"_hot_sessions retyping" — the exact hook points; do NOT modify `_rrf_merge`'s internal formula (multiply its OUTPUT).
  </read_first>
  <action>
    Retype `RecalledContext.sessions` to `list[SessionSummary]` and update its docstring.
    Remove `RecallConfig.recent_session_limit` (line 172) and its docstring (OQ2).
    Add `policy: RetentionPolicy | None = None` to `Recall.__init__`; set `self._policy = policy or RetentionPolicy()`.
    Rewrite `_hot_sessions` to return `list[SessionSummary]`, call `get_recent_sessions(user_id, policy=self._policy)`, and return the list sorted by `recency_weight(s.date, now=datetime.now(timezone.utc))` descending (most-recent first) — a BLEND: do not drop any session, only reorder.
    Add `_CARRIER_NAMESPACE_PREFIXES = ("journal/",)` and `_path_date(path)` near line 53. In `_warm_search`, apply place (b): multiply the RRF score of any survivor whose `path` is in `_CARRIER_NAMESPACE_PREFIXES` by `recency_weight(_path_date(path), now=datetime.now(timezone.utc))`; leave all other survivors (including `self/`) untouched; re-sort by adjusted score before returning. Do NOT change `_rrf_merge` itself, and do NOT touch `_WARM_TIER_EXCLUDE_PREFIXES` / the `ops/` exclusion.
    Update the `assemble` local `sessions` annotation to `list[SessionSummary]`; keep the exception-coercion fail-open envelope.
    RUN the Task-1 tests; iterate until GREEN. Keep `test_warm_excludes_self_and_ops_prefixes` green (do not relax exclusions).
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_recall.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `RecalledContext.sessions` annotation is `list[SessionSummary]`.
    - `RecallConfig.recent_session_limit` is removed; grep for `recent_session_limit` in `recall.py` returns nothing.
    - `Recall.__init__` accepts `policy=`; `_hot_sessions` calls `get_recent_sessions(user_id, policy=self._policy)` and sorts by `recency_weight`.
    - `_warm_search` multiplies only `journal/`-namespace survivor scores by `recency_weight`; `self/` and `ops/` results are untouched; `_rrf_merge` formula unchanged; `_WARM_TIER_EXCLUDE_PREFIXES` unchanged.
    - All eight Task-1 tests pass; `test_warm_excludes_self_and_ops_prefixes` stays green.
    - `uv run pytest tests/test_recall.py -q` exits 0.
  </acceptance_criteria>
  <done>Recall is typed, policy-injected, recency-ordered in both places (episodic-only), and the recall test file is green.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR + record the OQ1/OQ2/OQ3 + inbox-gap decisions in the SUMMARY</name>
  <files>sentinel-core/app/services/recall.py, sentinel-core/tests/test_recall.py</files>
  <read_first>
    - sentinel-core/app/services/recall.py — the whole `_warm_search` + `_hot_sessions` region after Task 2, to confirm the recency application reads cleanly and the carrier-namespace gating is obvious to a future reader (comment it: "place (b), D-03, episodic-only, OQ1: carrier = journal/").
    - .planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md §"Pitfall 1" — the inbox document-and-accept rationale to transcribe into the SUMMARY as the explicit recorded decision (D-06 / AI Deferral Ban).
  </read_first>
  <action>
    No behavior change. Add brief inline comments at the two recency application sites naming the decision IDs (D-03 place a / place b, D-02 episodic-only, OQ1 carrier namespace) so the gating is self-documenting. Confirm the warm-tier recency does not accidentally weight `self/` (the `_CARRIER_NAMESPACE_PREFIXES` gate must be a positive allowlist of `journal/`, NOT a negation of the exclude list). Run the full `recall` test file once more.
    In `41-04-SUMMARY.md`, RECORD explicitly (these are the phase's open-decision resolutions — they must be durable, not silent): OQ1 = "session-derived warm result" is a warm SearchResult whose path startswith `journal/`, dated from its path; OQ2 = `RecallConfig.recent_session_limit` REMOVED (hot_limit lives only on RetentionPolicy); OQ3 = RetentionPolicy injected as a separate object into Recall; and the inbox/ MEM-07 hole = DOCUMENT-AND-ACCEPT (low-confidence/`unsure` turns are deliberately noise-quarantined in `inbox/` per `sweep_skip_prefixes`; forcing them into recall would reintroduce the noise the exclusion exists to suppress; characterized by `test_inbox_gap_not_recalled`). This satisfies the D-06 mandate and the AI Deferral Ban — the gap is accepted by explicit operator-visible record, not omitted.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_recall.py -q && ! grep -n "recent_session_limit" app/services/recall.py</automated>
  </verify>
  <acceptance_criteria>
    - The two recency sites carry comments naming D-03 place a/b, D-02, and OQ1.
    - The carrier gate is a positive `journal/` allowlist (not a negation of the exclude list).
    - `grep recent_session_limit app/services/recall.py` returns nothing (exit 1 — the `!` makes the verify pass).
    - `41-04-SUMMARY.md` records OQ1, OQ2, OQ3 resolutions AND the inbox document-and-accept decision.
    - `uv run pytest tests/test_recall.py -q` exits 0.
  </acceptance_criteria>
  <done>The recency wiring is self-documenting; the three open questions and the inbox gap are recorded as durable decisions; recall tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed SessionSummary → RecalledContext → MessageProcessor | Recalled session data crosses the Recall interface as typed values, later wrapped by injection_filter in the consumer (Plan 05) |
| journal carrier note date (from path) → recency multiplier | A path-derived date drives the warm-tier score multiplier |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-08 | Information Disclosure | recency weighting accidentally applied to `self/` or authored notes, surfacing/ranking them by episodic recency (violates D-02 validated boundary) | mitigate | Warm-tier recency is a POSITIVE allowlist gated to `_CARRIER_NAMESPACE_PREFIXES = ("journal/",)`; `self/` and `ops/` are never multiplied. Pinned by `test_recency_excludes_self`. |
| T-41-09 | Information Disclosure | relaxing the `ops/` exclusion to reach old sessions, leaking the Sentinel's own operational notes into warm recall | mitigate | `_WARM_TIER_EXCLUDE_PREFIXES` and `RecallConfig.exclude_prefixes` are UNCHANGED (criterion 4); old-session reachability comes only from the `journal/` carrier (D-05). Pinned by `test_warm_excludes_self_and_ops_prefixes` (kept green) + `test_old_session_warm_reachable`. |
| T-41-10 | Denial of Service | a malformed SessionSummary.date or carrier path crashing `_hot_sessions` / `_warm_search` and taking down recall | mitigate | `recency_weight` and `_path_date` fail open (return weight 1.0 / no-op multiplier); `assemble`'s `return_exceptions=True` envelope coerces a failed tier to `[]`. Pinned by the existing `raising_sessions` graceful-degrade test (test_recall.py:264). |
| T-41-11 | Information Disclosure | the inbox/ MEM-07 gap silently dropping low-confidence turns without an operator-visible record | mitigate | Documented-and-accepted decision recorded in 41-04-SUMMARY (D-06 mandate); characterized by `test_inbox_gap_not_recalled` — explicit, not silent. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit n/a). No legitimacy gate required. |
</threat_model>

<verification>
- `uv run pytest tests/test_recall.py -q` is green (all new + existing tests, including the criterion-4 exclusion guard).
- `grep recent_session_limit app/services/recall.py` returns nothing (OQ2 complete).
- Warm recency is a positive `journal/` allowlist; `self/`/`ops/` never weighted; exclusion list unchanged.
- The inbox gap is recorded as a document-and-accept decision in the SUMMARY.
</verification>

<success_criteria>
- MEM-08: `RecalledContext.sessions` carries `list[SessionSummary]`.
- MEM-09: recency applied in BOTH places (hot order + warm carrier merge), episodic-only, never self.
- MEM-07: an old session is reachable via `RecalledContext.warm`; `ops/` exclusion intact.
- MEM-06: the injected RetentionPolicy drives hot_limit/window; recent_session_limit removed.
- OQ1/OQ2/OQ3 + inbox gap resolved and durably recorded.
</success_criteria>

<output>
Create `.planning/phases/41-typed-sessionsummary-retention/41-04-SUMMARY.md` when done.
</output>
