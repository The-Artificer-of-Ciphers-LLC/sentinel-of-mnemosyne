---
phase: 41-typed-sessionsummary-retention
plan: 03
type: tdd
wave: 2
depends_on: ["41-01"]
files_modified:
  - sentinel-core/app/config.py
  - sentinel-core/app/composition.py
  - sentinel-core/tests/test_config.py
autonomous: true
requirements: [MEM-06]
must_haves:
  truths:
    - "Settings exposes retention_hot_limit (default 3) and retention_hot_window_days (default 2)"
    - "Env vars RETENTION_HOT_LIMIT and RETENTION_HOT_WINDOW_DAYS override those defaults"
    - "composition.py builds a RetentionPolicy from settings.retention_hot_limit / settings.retention_hot_window_days and injects it into Recall"
  artifacts:
    - path: "sentinel-core/app/config.py"
      provides: "env-overridable retention_hot_limit / retention_hot_window_days Settings fields"
      contains: "retention_hot_window_days"
    - path: "sentinel-core/app/composition.py"
      provides: "RetentionPolicy construction from settings, injected into Recall"
      contains: "RetentionPolicy"
    - path: "sentinel-core/tests/test_config.py"
      provides: "default + env-override tests for the retention settings"
      contains: "RETENTION_HOT"
  key_links:
    - from: "sentinel-core/app/composition.py"
      to: "Recall(... policy=RetentionPolicy(...))"
      via: "settings.retention_hot_* threaded into a RetentionPolicy injected at the composition root"
      pattern: "RetentionPolicy\\("
    - from: "Settings.retention_hot_window_days"
      to: "RETENTION_HOT_WINDOW_DAYS env var"
      via: "pydantic-settings env load (same idiom as sweep_skip_prefixes)"
      pattern: "retention_hot_window_days"
---

<objective>
Make `RetentionPolicy` operator-tunable WITHOUT a redeploy: add two env-overridable
`Settings` fields (mirroring the `sweep_skip_prefixes` idiom) and wire a
`RetentionPolicy` built from them into `Recall` at the composition root.

Purpose: MEM-06 (D-04) — the recent-session window becomes a knob the operator can
widen via `RETENTION_HOT_LIMIT` / `RETENTION_HOT_WINDOW_DAYS`. This is env config, NOT
vault-file tuning (vault-file RecallConfig tuning stays deferred). Defaults (3 / 2)
preserve current behavior exactly.

Output: `Settings.retention_hot_limit` / `Settings.retention_hot_window_days`, a
`RetentionPolicy` constructed from them in `composition.py` and injected into `Recall`,
plus default + env-override tests.

Resolves OQ3 at the wiring layer: `RetentionPolicy` is injected as a SEPARATE object
into `Recall` (not threaded through `RecallConfig`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/adr/0005-typed-session-summary.md
@.planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md
@.planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md
@.planning/phases/41-typed-sessionsummary-retention/41-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (excluded from cross-phase drift verification):

- `app/config.py`: `Settings.retention_hot_limit: int = 3` (env `RETENTION_HOT_LIMIT`).
- `app/config.py`: `Settings.retention_hot_window_days: int = 2` (env `RETENTION_HOT_WINDOW_DAYS`).
- `app/composition.py`: a `RetentionPolicy(...)` instance built from settings and passed into `Recall(...)`.

Consumed (created by Plan 01): `RetentionPolicy` from `app.services.recall`.
NOTE: `Recall.__init__` does not yet accept a `policy=` parameter — that constructor
change lands in Plan 04 (Wave 3). This plan adds the Settings fields and STAGES the
composition wiring; the `Recall(... policy=...)` injection is finalized when Plan 04
adds the `policy` parameter. See Task 2 action for the exact handoff.
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED→GREEN — env-overridable retention Settings fields</name>
  <files>sentinel-core/app/config.py, sentinel-core/tests/test_config.py</files>
  <read_first>
    - sentinel-core/app/config.py:26 — `class Settings(BaseSettings)`; :102-117 — the `sweep_skip_prefixes` field (the env-overridable idiom to mirror); :131-135 — `protected_namespaces`; :137 — `model_config = {"env_file": ".env", ...}`.
    - .planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md §"app/config.py" and §"Env-overridable Settings field" — code default + env override + inline doc comment.
    - .planning/phases/41-typed-sessionsummary-retention/41-RESEARCH.md §"Validation Architecture" — `test_config.py` does not exist yet; create it. MEM-06 env-override test: `uv run pytest tests/test_config.py -k retention -x`.
    - .agents/skills/test-rigor (if present) — env-override tests must construct `Settings` with the env var set (e.g. via `monkeypatch.setenv` then instantiate `Settings()`) and assert the field VALUE changed, not grep source.
  </read_first>
  <behavior>
    - Default: `Settings()` with no env override → `retention_hot_limit == 3`, `retention_hot_window_days == 2`.
    - Env override: `RETENTION_HOT_LIMIT=5` and `RETENTION_HOT_WINDOW_DAYS=4` in the environment → `Settings()` yields `retention_hot_limit == 5`, `retention_hot_window_days == 4`.
    - Type coercion: the env string "5" is coerced to int 5 by pydantic-settings (not the string "5").
  </behavior>
  <action>
    Write the test FIRST. Create `tests/test_config.py` with `test_retention_defaults` (instantiate `Settings()` with retention env vars unset — use `monkeypatch.delenv(..., raising=False)` for both vars to isolate from the ambient environment — assert 3 and 2) and `test_retention_env_override` (`monkeypatch.setenv("RETENTION_HOT_LIMIT", "5")`, `monkeypatch.setenv("RETENTION_HOT_WINDOW_DAYS", "4")`, instantiate a fresh `Settings()`, assert `retention_hot_limit == 5` and `retention_hot_window_days == 4` AND `isinstance(..., int)`). RUN it — it fails (fields don't exist yet): RED.
    Then add to `Settings` (config.py), beside `sweep_skip_prefixes`, mirroring its inline-doc style: `retention_hot_limit: int = 3  # env: RETENTION_HOT_LIMIT` and `retention_hot_window_days: int = 2  # env: RETENTION_HOT_WINDOW_DAYS`, with a short comment noting these are the MEM-06 hot-window knobs (D-04) and the defaults preserve the today+yesterday / top-3 behavior. pydantic-settings auto-maps the uppercased field name to the env var (same as the existing fields) — no alias needed. RUN the tests: GREEN.
    Do NOT add a vault-file tuning path (deferred). Do NOT change any other Settings field.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_config.py -k retention -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_config.py` exists with `test_retention_defaults` and `test_retention_env_override`, both calling `Settings()` and asserting field values.
    - `Settings.retention_hot_limit` default is 3; `Settings.retention_hot_window_days` default is 2.
    - With `RETENTION_HOT_LIMIT=5` / `RETENTION_HOT_WINDOW_DAYS=4` set, `Settings()` yields ints 5 and 4.
    - `uv run pytest tests/test_config.py -k retention -x` exits 0.
  </acceptance_criteria>
  <done>The retention knobs exist on Settings, override from env, and are verified by behavioral tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RetentionPolicy from settings into the composition root</name>
  <files>sentinel-core/app/composition.py</files>
  <read_first>
    - sentinel-core/app/composition.py:26 — `from app.services.recall import Recall, RecallConfig, SemanticRecall`; add `RetentionPolicy` to this import.
    - sentinel-core/app/composition.py:322-335 — the `if recall is None:` block that builds `_config = RecallConfig()`, `_semantic = SemanticRecall(...)`, and `recall = Recall(vault=vault, config=_config, semantic_strategy=_semantic)`. `settings` is already in scope here.
    - .planning/phases/41-typed-sessionsummary-retention/41-PATTERNS.md §"app/composition.py" — the exact wiring idiom.
    - IMPORTANT HANDOFF: `Recall.__init__` does NOT yet accept `policy=` (Plan 04 adds it). Construct the `RetentionPolicy` here now and pass it via `policy=` in the same edit; Plan 04 (Wave 3, depends on this plan) adds the matching `policy` parameter to `Recall.__init__`. This plan and Plan 04 are in different waves (2 then 3) and Plan 04 depends on 41-03, so the parameter will exist before this wiring runs against a constructed app. To keep THIS plan's verification green standalone, the Task 2 verify only checks that composition.py imports cleanly and the RetentionPolicy is constructed — it does NOT boot the full app (which would require Plan 04's constructor change). If a clean standalone boot is needed, gate the `policy=` kwarg so it is only passed when `Recall.__init__` accepts it; prefer the simpler approach: construct the policy and pass `policy=_policy`, and let Plan 04's wave complete the constructor — the phase-gate full suite (run after Wave 3) is the integration check.
  </read_first>
  <action>
    Add `RetentionPolicy` to the `from app.services.recall import ...` line (composition.py:26). In the `if recall is None:` block (composition.py:322-335), after `_config = RecallConfig()`, add `_policy = RetentionPolicy(hot_limit=settings.retention_hot_limit, hot_window_days=settings.retention_hot_window_days)`. Pass it into the `Recall(...)` constructor as `policy=_policy` (alongside the existing `vault=`, `config=`, `semantic_strategy=` kwargs). Do NOT thread it through `RecallConfig` (OQ3: separate injected object). `settings` is already in scope — do not re-read config.
    Because `Recall.__init__` gains its `policy` parameter in Plan 04 (which depends on this plan and runs in the next wave), this edit and Plan 04's constructor change form the lockstep. Verify here only that the module imports and the RetentionPolicy is constructed from the settings fields; the booted-app integration is covered by the phase-gate full suite after Wave 3.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run python -c "import app.composition; from app.services.recall import RetentionPolicy; print(RetentionPolicy(hot_limit=1, hot_window_days=1))"</automated>
  </verify>
  <acceptance_criteria>
    - `composition.py` imports `RetentionPolicy` from `app.services.recall`.
    - A `RetentionPolicy(hot_limit=settings.retention_hot_limit, hot_window_days=settings.retention_hot_window_days)` is constructed in the `if recall is None:` block.
    - It is passed to `Recall(...)` as `policy=_policy` (NOT threaded through `RecallConfig`).
    - `import app.composition` succeeds.
  </acceptance_criteria>
  <done>The composition root builds a RetentionPolicy from the env-overridable settings and injects it into Recall (constructor parameter finalized in Plan 04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var → Settings.retention_* | Operator-supplied env values configure the hot-window retention policy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06 | Tampering / DoS | a hostile/typo env value (`RETENTION_HOT_LIMIT=-1` or a huge number) widening the recall window unboundedly and surfacing data that should age out, or causing oversized reads | mitigate | pydantic-settings coerces to `int` (rejects non-ints at startup with a clear ValidationError). Defaults preserve current behavior. Note in SUMMARY: the operator controls this env (local-network single-operator deployment per CLAUDE.md); a negative value yields a Python empty-slice (`[:-1]` / empty range) → degrades to fewer/no sessions, never an unbounded read. Document this as the in-scope retention surface; an explicit lower-bound clamp is a candidate follow-up but not required at ASVS L1 for an operator-controlled env. |
| T-41-07 | Information Disclosure | widening `hot_window_days` surfacing older operational/session data into recall beyond intent | accept | This is the INTENDED MEM-06 capability (operator widens the window deliberately). The `ops/` exclusion (criterion 4) still gates what is searchable; widening the hot window only extends how many days of the user's OWN `ops/sessions/{user_id}-*` notes are read — same user, same namespace. No cross-namespace leak. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH §Package Legitimacy Audit n/a). No legitimacy gate required. |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py -k retention -x` is green (defaults + env override).
- `import app.composition` succeeds and constructs a RetentionPolicy from settings.
- No vault-file tuning path added (stays deferred); no other Settings field changed.
</verification>

<success_criteria>
- MEM-06: the hot-window limit/days are env-overridable via `RETENTION_HOT_LIMIT` / `RETENTION_HOT_WINDOW_DAYS`, defaulting to current behavior.
- OQ3 resolved at the wiring layer: RetentionPolicy is a separate injected object, not a RecallConfig field.
- The composition root threads settings → RetentionPolicy → Recall.
</success_criteria>

<output>
Create `.planning/phases/41-typed-sessionsummary-retention/41-03-SUMMARY.md` when done.
</output>
