---
phase: 38-pf2e-multi-step-onboarding-dialog
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - interfaces/discord/tests/test_dialog_router.py
autonomous: true
requirements: [PVL-01]
must_haves:
  truths:
    - "RED tests for dialog_router cover hit/miss conditions: prefix detection, channel type detection, draft existence, fall-through to None"
    - "RED tests prove the router is a transparent pass-through on miss (returns None) and a hijacker on hit (returns dialog response)"
    - "Tests assert the AI / command_router is NEVER reached on hit"
  artifacts:
    - path: "interfaces/discord/tests/test_dialog_router.py"
      provides: "RED behavioral tests for maybe_consume_as_answer hit/miss matrix"
      contains: "from dialog_router import"
  key_links:
    - from: "Wave 0 RED tests"
      to: "Wave 2 implementation (38-05)"
      via: "function-scope ImportError until dialog_router ships"
      pattern: "from dialog_router import"
---

<objective>
Wave 0 RED tests (TDD) for the new `interfaces/discord/dialog_router.py` module — the pre-router gate that consumes plain-text replies inside draft-bearing threads (D-01, D-02). Tests lock the four hit conditions and the must-fall-through-on-miss contract before any implementation lands.

Purpose: ensure `command_router.py` stays unmodified (D-03) and `bot.py:on_message` stays unmodified (D-04) by proving the new gate has surgical hit conditions: NO `:` prefix AND channel is a `discord.Thread` AND a draft exists at the canonical path.

Output: one new test file with all assertions failing as ImportError.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-SPEC.md
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-RESEARCH.md
@interfaces/discord/discord_router_bridge.py
@interfaces/discord/command_router.py
@interfaces/discord/tests/conftest.py
@interfaces/discord/tests/test_pathfinder_player_adapter.py

<interfaces>
Function under test: `dialog_router.maybe_consume_as_answer` (NEW MODULE — does not exist yet).

Expected public surface (locked by 38-CONTEXT.md D-01, D-02):

```python
async def maybe_consume_as_answer(
    *,
    user_id: str,
    message: str,
    channel,                    # discord.Thread or other
    sentinel_client,
    http_client,
) -> str | None
    # Returns the bot's response text on hit (consumed as answer);
    # Returns None on miss (router falls through to command_router unchanged).
```

Hit conditions (ALL must hold):
1. message does NOT start with ":" (raw user prefix check; ignore leading whitespace per command_router.py:8-34 conventions)
2. channel is an instance of `discord.Thread`
3. A draft exists at `mnemosyne/pf2e/players/_drafts/{channel.id}-{user_id}.md` (HTTP GET returns 200)

Miss → return None. The bridge falls through to `command_router.route_message` unmodified.

This module CALLS `pathfinder_player_dialog.consume_as_answer` on hit. The test stubs `pathfinder_player_dialog.consume_as_answer` via monkeypatch.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED tests for hit/miss matrix (D-01, D-02)</name>
  <files>interfaces/discord/tests/test_dialog_router.py</files>
  <behavior>
    Test cases:

    - test_miss_when_message_has_colon_prefix: channel = MagicMock(spec=discord.Thread, id=42); http.get returns 200 (draft exists); message = ":pf player note hi". `maybe_consume_as_answer(...)` returns None. Stub `pathfinder_player_dialog.consume_as_answer` via monkeypatch and assert it was NOT called.
    - test_miss_when_message_has_leading_whitespace_then_colon: message = "  :pf player ask foo" — also miss (return None). Locks "raw user prefix check ignores leading whitespace" alignment with command_router.py.
    - test_miss_when_channel_is_not_thread: channel = MagicMock(spec=discord.TextChannel) (NOT a Thread); message = "Kaela"; http.get would return 200 if called. Result: None. http.get MUST NOT have been called (early-out before the listing — D-02 ordering).
    - test_miss_when_draft_does_not_exist: channel is a Thread, message has no colon, but http.get returns 404. Result: None. Stub consume_as_answer MUST NOT be called.
    - test_hit_invokes_consume_as_answer: channel = MagicMock(spec=discord.Thread, id=42); message = "Kaela"; http.get returns 200 with a valid draft body. Stub `pathfinder_player_dialog.consume_as_answer = AsyncMock(return_value="next question")`. Assert:
        * Return value is exactly "next question".
        * consume_as_answer was called once with kwargs `thread=channel, user_id=<the user_id passed in>, message_text="Kaela", sentinel_client=<as passed>, http_client=<as passed>`.
    - test_hit_uses_thread_id_in_draft_path_lookup: same setup as above but channel.id=999. Assert http.get called with URL ending `/vault/mnemosyne/pf2e/players/_drafts/999-u-1.md`. (Locks the draft-path scheme used for the GET.)
    - test_empty_message_is_miss: message = "" (empty / whitespace-only). Result: None. consume_as_answer NOT called. (Avoids treating Discord embed-only messages as answers.)
    - test_obsidian_get_error_falls_through: http.get raises a network error (httpx.RequestError). Router treats this as miss (return None) so command_router still gets a chance, instead of crashing the message handler. consume_as_answer NOT called. (Defensive — Pitfall: never silently swallow Obsidian errors mid-answer, but the GATE before commit is non-fatal.)

    Function-scope imports per test:
        from dialog_router import maybe_consume_as_answer
  </behavior>
  <action>
    Create `interfaces/discord/tests/test_dialog_router.py` with the 8 RED tests.

    Header pattern (file-level imports — stable types only, NO `dialog_router` symbol at file scope):
        from __future__ import annotations
        from unittest.mock import AsyncMock, MagicMock
        import discord  # provided by conftest stub
        import pathfinder_player_dialog as ppd_module  # imported lazily — will fail until 38-04 ships

    To avoid the file-level `pathfinder_player_dialog` import failing during collection (it doesn't exist yet either), use lazy import inside each test body via `monkeypatch.setattr` after a try/except `import pathfinder_player_dialog`. Recommended pattern: use `importlib.import_module` inside the test body and skip with `pytest.skip("pending pathfinder_player_dialog impl")` ONLY if the lazy import fails — but the test still asserts ImportError on `dialog_router`, which is the actual RED state.

    Better pattern (matching test_pathfinder_player_adapter.py): keep ALL `dialog_router`/`pathfinder_player_dialog` imports inside the test bodies. Use `monkeypatch.setattr("pathfinder_player_dialog.consume_as_answer", AsyncMock(...))` after the lazy `import pathfinder_player_dialog` line. This way: tests fail with ImportError on `dialog_router` (RED), and once `dialog_router` ships, the monkeypatch resolves cleanly.

    Each test asserts on observable behaviour: return value (str or None), call args of `http.get` and `consume_as_answer` mock, and absence of unexpected calls. NO source-grep, NO `assert True`, NO sole `assert_called`.

    Module does not exist — all 8 tests fail with ImportError. RED state confirmed.
  </action>
  <verify>
    <automated>cd interfaces/discord && pytest tests/test_dialog_router.py 2>&1 | tail -10</automated>
  </verify>
  <done>File exists; 8 RED tests collect without error; all 8 fail with ImportError on `dialog_router`. RED state confirmed.</done>
</task>

</tasks>

<verification>
- File `interfaces/discord/tests/test_dialog_router.py` exists.
- 8 RED tests collect; all fail with ImportError or ModuleNotFoundError on `dialog_router`.
- No production code added (RED-only).
- Existing tests in `interfaces/discord/tests/` continue to pass.
- Behavioral-Test-Only Rule honored — every assertion observable.
</verification>

<success_criteria>
- 1 new test file with 8 RED behavioral tests
- Hit/miss matrix covers all 4 conditions from D-02
- Tests prove the gate is a no-op on miss (preserves command_router as the canonical router)
- AI Deferral Ban honored
- Test-Rewrite Ban honored
</success_criteria>

<output>
After completion, create `.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-02-SUMMARY.md`
</output>
