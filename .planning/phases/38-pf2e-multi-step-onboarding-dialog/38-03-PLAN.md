---
phase: 38-pf2e-multi-step-onboarding-dialog
plan: 03
type: tdd
wave: 0
depends_on: []
files_modified:
  - interfaces/discord/tests/test_pathfinder_player_adapter.py
autonomous: true
requirements: [PVL-01]
must_haves:
  truths:
    - "RED tests for PlayerStartCommand no-args branch assert thread creation is invoked (instead of usage string)"
    - "RED tests for PlayerStartCommand pipe-syntax branch are pinned as REGRESSION — ship in this plan, kept GREEN forever"
    - "RED tests for new PlayerCancelCommand cover 'no draft', 'draft present in-thread', 'cancel from non-thread channel single-draft', 'cancel from non-thread channel MULTI-draft (D-17)' and 'dispatch registration' branches"
    - "RED tests for mid-dialog rejection guard cover all 7 non-start/non-cancel verbs (note, ask, npc, recall, todo, style, canonize), with single-draft and multi-draft cases"
  artifacts:
    - path: "interfaces/discord/tests/test_pathfinder_player_adapter.py"
      provides: "Existing 12 tests preserved; new RED tests for no-args branch, cancel verb (incl. multi-draft cancel symmetry per D-17), rejection guard, and pipe-syntax regression locks"
      contains: "test_pipe_syntax_regression_unchanged"
  key_links:
    - from: "Wave 0 RED tests"
      to: "Wave 3 implementation (38-06, 38-07)"
      via: "function-scope ImportError or AssertionError until adapter additions land"
      pattern: "from pathfinder_player_adapter import"
---

<objective>
Wave 0 RED tests (TDD) extending `interfaces/discord/tests/test_pathfinder_player_adapter.py` to cover:

1. **No-args branch of `PlayerStartCommand`** (D-15): when `request.rest` is empty, the adapter MUST delegate to `pathfinder_player_dialog.start_dialog(...)` instead of returning the usage string.
2. **New `PlayerCancelCommand`** (D-16, D-17, SPEC Req 6) — including the multi-draft symmetry case where cancel issued from a non-dialog channel archives ALL of the user's in-flight dialog threads.
3. **Mid-dialog rejection guard** (D-05, D-07, D-08, SPEC Req 5): seven non-start/non-cancel `:pf player <verb>` invocations must be rejected (no route call) when a draft exists for the user.
4. **Pipe-syntax regression locks** (Constraint: pre-existing pipe-syntax MUST keep working): explicit RED-now-but-already-GREEN regression tests so any future change that breaks the pipe path fails CI.

This is a TDD plan because the tests assert on behaviour that does not yet exist (no-args branch, cancel command, guard) — function-scope imports of `PlayerCancelCommand` will fail until 38-06 ships. The pipe-syntax regression tests in this plan will pass immediately on commit (already-shipped behaviour) but are explicitly framed as REGRESSION CONTRACTS.

Output: extended test file with new RED tests; existing 12 tests untouched (Test-Rewrite Ban).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-SPEC.md
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-RESEARCH.md
@interfaces/discord/tests/test_pathfinder_player_adapter.py
@interfaces/discord/tests/conftest.py
@interfaces/discord/pathfinder_player_adapter.py
@interfaces/discord/pathfinder_dispatch.py
@interfaces/discord/pathfinder_types.py

<interfaces>
Existing public surface (DO NOT MODIFY):
- `PlayerStartCommand` (pathfinder_player_adapter.py:30-71) — pipe-syntax handler
- `_USAGE` (pathfinder_player_adapter.py:23-27) — existing usage-string tuple, preserved byte-for-byte
- `PlayerNoteCommand`, `PlayerAskCommand`, `PlayerNpcCommand`, `PlayerRecallCommand`, `PlayerTodoCommand`, `PlayerStyleCommand`, `PlayerCanonizeCommand` — Phase 37 verb handlers
- `_VALID_STYLE_PRESETS` (line 22) — to be reused
- `bot` singleton (bot.py:708) — used by `PlayerCancelCommand` to resolve `bot.get_channel(thread_id)` from non-thread channels (D-17)

New public surface (locked by 38-CONTEXT.md D-15, D-16, D-17, and SPEC Req 5/6):

```python
# Modified — adds no-args branch
class PlayerStartCommand(PathfinderCommand):
    async def handle(self, request: PathfinderRequest) -> PathfinderResponse:
        # if request.rest is empty:
        #   - if channel is a discord.Thread AND a draft exists for (channel.id, user_id):
        #       call pathfinder_player_dialog.resume_dialog(...)
        #   - else:
        #       call pathfinder_player_dialog.start_dialog(...) (creates thread + draft + posts Q1)
        # else: existing pipe-syntax flow (UNCHANGED)

# NEW — registered in pathfinder_dispatch.py
class PlayerCancelCommand(PathfinderCommand):
    async def handle(self, request: PathfinderRequest) -> PathfinderResponse:
        # If channel is a Thread: cancel_dialog(thread=channel, ...).
        # Else (non-thread channel): list _drafts/, filter to *-{user_id}.md, enumerate ALL thread_ids
        #   - 0 drafts: return "No onboarding dialog in progress."
        #   - N>=1 drafts: iterate ALL of them, call cancel_dialog for each via bot.get_channel(tid).
        #     Failures of individual archives are aggregated, not raised. (D-17)

# NEW helper used by the seven non-start/non-cancel verbs
async def reject_if_draft_open(request: PathfinderRequest) -> PathfinderResponse | None:
    # GET _drafts/ directory listing
    # filter for *-{user_id}.md
    # if any match → return PathfinderResponse with text:
    #     "You have an onboarding dialog open in <#thread_id> [and <#thread_id2>...]. Reply there to continue, or run :pf player cancel to abort."
    # else → return None (caller proceeds with normal flow)
```

The seven verbs that must consult `reject_if_draft_open` BEFORE their normal logic: note, ask, npc, recall, todo, style, canonize.

Test infrastructure:
- Function-scope imports per existing convention (16 imports across 12 tests)
- `AsyncMock` for `request.sentinel_client.post_to_module` and `request.http_client`
- Discord stubs from `interfaces/discord/tests/conftest.py`
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: RED tests for PlayerStartCommand no-args branch + pipe-syntax regression locks</name>
  <files>interfaces/discord/tests/test_pathfinder_player_adapter.py</files>
  <behavior>
    Append to existing test file (do NOT modify existing 12 tests — Test-Rewrite Ban).

    NEW (RED — fail until 38-06 wires the no-args branch):
    - test_player_start_no_args_creates_thread_and_draft: channel = MagicMock(spec=discord.TextChannel); request = PathfinderRequest(noun="player", verb="start", rest="", parts=["player","start"], user_id="u-1", channel=channel, ...); http.get returns 404 (no existing draft). Monkeypatch `pathfinder_player_dialog.start_dialog` to AsyncMock(return_value=fake_thread). Call `await PlayerStartCommand().handle(request)`. Assert:
        * `pathfinder_player_dialog.start_dialog` called once with `invoking_channel=channel`, `user_id="u-1"`, `display_name="<value>"`, `http_client=request.http_client`. The `display_name` MUST be the value of `request.author_display_name` (new field on PathfinderRequest — see 38-04 Task 2 / 38-06 Task 1). Set `author_display_name="Trekkie"` on the test request and assert `start_dialog` was called with `display_name="Trekkie"`.
        * `request.sentinel_client.post_to_module` NOT called (start_dialog handles the route call after answers, not on creation).
        * Response kind is "text" and content references the new thread (mention syntax `<#{fake_thread.id}>` or similar).
    - test_player_start_no_args_in_thread_with_existing_draft_resumes: channel = MagicMock(spec=discord.Thread, id=42); http.get returns 200 with draft body containing `step: preferred_name`. Monkeypatch `pathfinder_player_dialog.resume_dialog = AsyncMock(return_value="What is your preferred name?")`. Call handle. Assert:
        * `pathfinder_player_dialog.resume_dialog` called once with `thread=channel`, `user_id="u-1"`.
        * `pathfinder_player_dialog.start_dialog` NOT called (no new thread).
        * Response content equals "What is your preferred name?" (or similar).
        * SPEC Req 7 acceptance: existing answers (character_name) NOT mutated — `http.put` MUST NOT be called by the adapter.

    REGRESSION (RED-as-already-GREEN — pipe-syntax MUST keep working forever):
    - test_pipe_syntax_regression_three_part_call_unchanged: request.rest = "Aria | Ari | Tactician"; channel = anything. Existing behaviour: `request.sentinel_client.post_to_module` called once with payload {"user_id": "u-1", "character_name": "Aria", "preferred_name": "Ari", "style_preset": "Tactician"}. NO call to `pathfinder_player_dialog.start_dialog` or `resume_dialog`. Response content matches existing format `"Player onboarded as `Ari` (Tactician). Profile: ..."`.
    - test_pipe_syntax_regression_invalid_preset_returns_text: request.rest = "Aria | Ari | Wizard". Response content contains "Invalid style preset" and lists the four valid presets. NO call to start_dialog or resume_dialog. NO call to post_to_module. (Locks Constraint: pipe-syntax stays strict.)
    - test_pipe_syntax_regression_no_thread_created: with pipe-syntax args, monkeypatch `start_dialog` and `resume_dialog` to AsyncMocks; assert NEITHER is called. (SPEC Acceptance: `:pf player start a | b | c` produces same profile.md, no thread created.)
    - test_pipe_syntax_regression_payload_byte_for_byte_matches_dialog_completion_payload: assert payload dict has exactly the four keys `{user_id, character_name, preferred_name, style_preset}` and nothing else — locks the contract that pipe-syntax and dialog-completion produce IDENTICAL payloads (RESEARCH Q8).

    Function-scope imports per test:
        from pathfinder_player_adapter import PlayerStartCommand
        from pathfinder_types import PathfinderRequest
        # for monkeypatch of pathfinder_player_dialog: lazy `import pathfinder_player_dialog` inside test body
  </behavior>
  <action>
    Edit `interfaces/discord/tests/test_pathfinder_player_adapter.py` — APPEND tests at end of file. Do NOT touch the existing 12 tests. Use the same `_make_request` helper if one exists; otherwise build PathfinderRequest with kwargs as the existing tests do (verified pattern at lines 27, 44, 71, etc.).

    For the no-args tests (RED): they will fail because the no-args branch currently returns the `_USAGE` string (existing behaviour at pathfinder_player_adapter.py:38-42, where `_USAGE` is the constant defined at lines 23-27). Tests assert on `start_dialog` / `resume_dialog` being called — these assertions fail until 38-06 lands. RED state confirmed.

    For the pipe-syntax regression tests: they pass NOW (existing behaviour is correct). They are committed as REGRESSION CONTRACTS — any future change that breaks pipe-syntax fails these tests. Frame the docstrings explicitly: `"""REGRESSION: pipe-syntax MUST stay functional through Phase 38 per SPEC Constraint."""`

    Total new tests in this task: 6 (2 RED for no-args, 4 regression locks).
  </action>
  <verify>
    <automated>cd interfaces/discord && pytest tests/test_pathfinder_player_adapter.py 2>&1 | tail -10</automated>
  </verify>
  <done>2 new tests fail (RED, no-args branch not yet implemented); 4 new pipe-syntax regression tests pass GREEN. 12 existing tests still pass — no regression. File diff is purely additive.</done>
</task>

<task type="tdd">
  <name>Task 2: RED tests for PlayerCancelCommand incl. multi-draft symmetry (SPEC Req 6, D-10, D-16, D-17)</name>
  <files>interfaces/discord/tests/test_pathfinder_player_adapter.py</files>
  <behavior>
    Append:

    - test_player_cancel_with_no_draft_returns_no_progress_text: channel = MagicMock(spec=discord.Thread, id=42); request rest=""; http.get on draft path returns 404. Monkeypatch `pathfinder_player_dialog.cancel_dialog = AsyncMock(return_value="No onboarding dialog in progress.")`. Call `await PlayerCancelCommand().handle(request)`. Assert response.kind == "text" and content == "No onboarding dialog in progress." (verbatim per SPEC Req 6).
    - test_player_cancel_with_draft_delegates_to_cancel_dialog: channel = MagicMock(spec=discord.Thread, id=42); http.get returns 200 with a draft body. Monkeypatch `cancel_dialog` to AsyncMock(return_value="Onboarding cancelled. Run `:pf player start` to begin again."). Assert:
        * `cancel_dialog` called once with `thread=channel`, `user_id="u-1"`, `http_client=request.http_client`.
        * Response.content matches the cancel ack text.
    - test_player_cancel_from_non_thread_channel_single_draft_archives_remote_thread: channel = MagicMock(spec=discord.TextChannel) (NOT a Thread); user has exactly ONE draft in thread_id=999. The `_drafts/` directory listing returns `["999-u-1.md"]`. Monkeypatch `bot.get_channel` to return MagicMock(spec=discord.Thread, id=999). The adapter MUST resolve thread 999 via `bot.get_channel(999)` and pass it to `cancel_dialog`. Assert:
        * `cancel_dialog` called once with `thread=<the resolved Thread mock>`, NOT `thread=channel`.
        * `bot.get_channel(999)` invoked exactly once.
        * Response.content contains "Cancelled the onboarding dialog." (single-draft case per D-17).
    - test_player_cancel_from_non_thread_channel_with_two_drafts_archives_both: channel = MagicMock(spec=discord.TextChannel); `_drafts/` directory listing returns `["111-u-1.md", "222-u-1.md", "333-u-2.md"]` (two for user u-1, one for user u-2). Build a fake `SENTINEL_THREAD_IDS = {111, 222, 333, 999}` set and monkeypatch the adapter's reference to it. Monkeypatch `bot.get_channel` to return distinct MagicMock(spec=discord.Thread) per id with `.edit = AsyncMock()`. Track the http_client.delete call args (the adapter delegates to `cancel_dialog`, which calls `delete_draft` → http_client.delete). Call `await PlayerCancelCommand().handle(request)` (user u-1). Assert:
        * `cancel_dialog` called TWICE (or `bot.get_channel` called twice with 111 and 222 — accept either as proof of the loop), NOT for thread 333 (different user).
        * Both draft files deleted: http_client.delete called with URLs containing `111-u-1.md` AND `222-u-1.md`. NOT called with `333-u-2.md`.
        * Both threads' `.edit(archived=True)` awaited (for the resolved thread mocks for 111 and 222).
        * Both thread_ids 111 and 222 removed from `SENTINEL_THREAD_IDS` (use the fake set passed in; assert 111 not in set AND 222 not in set; 333 and 999 still present).
        * Response.content contains "Cancelled 2 onboarding dialogs." (multi-draft format per D-17).
        * NO "pick one" / "Run :pf player cancel from inside the thread" text — that branch is forbidden per D-17.
    - test_player_cancel_multi_draft_one_archive_failure_still_completes_others: channel = MagicMock(spec=discord.TextChannel); listing returns `["111-u-1.md", "222-u-1.md"]`. `bot.get_channel(111)` returns a Thread whose `.edit` AsyncMock raises `discord.HTTPException`. `bot.get_channel(222)` returns a healthy Thread. Assert:
        * Both draft files still deleted (the loop did not abort on 111's archive failure — D-17 step 3).
        * Thread 222's `.edit(archived=True)` was awaited.
        * Response.content contains "Cancelled 2 onboarding dialogs." or includes a diagnostic mentioning `<#111>` for the failed archive — either is acceptable per D-17 (operator's discretion on diagnostic phrasing), but the loop MUST complete.
    - test_player_cancel_registered_in_dispatch: import `pathfinder_dispatch`; assert `pathfinder_dispatch.COMMANDS["player"]["cancel"]` is an instance of `PlayerCancelCommand`. (D-16 — registration is part of the contract.)

    Function-scope imports per test:
        from pathfinder_player_adapter import PlayerCancelCommand
  </behavior>
  <action>
    Append 6 RED tests. The first five fail with ImportError on `PlayerCancelCommand` (the class doesn't exist yet); the sixth fails on the dispatch lookup (`KeyError: 'cancel'`). All RED until 38-06 ships.

    For tests 3-5 (cancel from non-thread channel): mock `bot.get_channel` via monkeypatch on the `bot` module-level singleton import (per `bot.py:708` — `bot = SentinelBot()`). Document in the test docstrings:
      - Test 3: "Locks D-10 + D-17: cancel from outside the dialog thread MUST archive the remote thread."
      - Test 4: "Locks D-17 multi-draft symmetry: cancel from outside ANY dialog thread archives ALL of the user's open dialogs. NO 'pick one' branch."
      - Test 5: "Locks D-17 step 3: per-thread archive failures aggregate, do not abort the loop."

    For the directory listing fixture: use the response shape locked by RESEARCH §Pitfall 5 (dual-shape parse) — the existing `_list_user_draft_thread_ids` helper introduced in 38-06 Task 2 will handle both shapes; the test should use the array-of-strings shape for clarity.
  </action>
  <verify>
    <automated>cd interfaces/discord && pytest tests/test_pathfinder_player_adapter.py -k "player_cancel" 2>&1 | tail -10</automated>
  </verify>
  <done>6 new RED tests fail with ImportError on PlayerCancelCommand or KeyError on dispatch lookup. RED state confirmed.</done>
</task>

<task type="tdd">
  <name>Task 3: RED tests for mid-dialog rejection guard (SPEC Req 5, D-05/07/08)</name>
  <files>interfaces/discord/tests/test_pathfinder_player_adapter.py</files>
  <behavior>
    Append. ONE parametrised test per verb is acceptable; explicit per-verb tests are clearer. Choose explicit:

    For each verb in [note, ask, npc, recall, todo, style, canonize]:
    - test_<verb>_blocked_when_draft_open: http.get on `_drafts/` directory returns 200 with body listing `["999-u-1.md"]`. Set request.rest to a valid arg for the verb (e.g. for note: "I trust Varek"). Call `Player<Verb>Command().handle(request)`. Assert:
        * `request.sentinel_client.post_to_module` NOT called (no route hit — the verb's normal handler MUST short-circuit).
        * Response.content contains "<#999>" (Discord channel mention syntax — D-07).
        * Response.content contains ":pf player cancel".
        * Response.content contains "onboarding".

    Plus three multi-case tests:
    - test_multi_draft_rejection_lists_all_thread_links: directory listing returns ["111-u-1.md", "222-u-1.md", "333-u-2.md"]. Call PlayerNoteCommand for user u-1. Assert response.content contains BOTH "<#111>" AND "<#222>", but NOT "<#333>" (different user — filter by suffix per D-05). post_to_module NOT called.
    - test_no_draft_passes_through_to_normal_verb: directory listing returns 200 with `["333-u-2.md"]` (other user's draft only). PlayerNoteCommand for u-1. post_to_module IS called normally (existing behaviour preserved). Locks: rejection only fires for THIS user.
    - test_drafts_dir_404_passes_through: directory listing returns 404 (no _drafts/ dir yet). Verb proceeds normally. Pitfall 4 / RESEARCH §"Code Examples" line 217 `_list_drafts` returning [] on 404 → no rejection.

    Function-scope imports per test:
        from pathfinder_player_adapter import PlayerNoteCommand, PlayerAskCommand, PlayerNpcCommand, PlayerRecallCommand, PlayerTodoCommand, PlayerStyleCommand, PlayerCanonizeCommand

    Verb-specific request.rest fixtures (so the existing handler would be valid if NOT blocked):
      - note: "I trust Varek"
      - ask: "What's the AC of a goblin?"
      - npc: "Varek trustworthy"
      - recall: "Varek"
      - todo: "buy rope"
      - style: "list"
      - canonize: "green q-1 The rule is..."
  </behavior>
  <action>
    Append 7 + 3 = 10 RED tests. The 7 per-verb tests fail because the rejection guard does not exist yet — the existing handlers call post_to_module unconditionally, so `post_to_module NOT called` fails RED. The 3 multi-case tests likewise fail.

    For the directory listing mock: build the http.get response body to mirror the dual-shape defensive parse from RESEARCH §Pitfall 5 — write at least one test where the response is the array shape `["111-u-1.md"]` and one where it's `{"files": [{"path": "111-u-1.md"}]}` to lock both shapes are handled (Pitfall 5).

    Total new tests in this task: 10. RED state: all 10 fail because the guard doesn't exist.
  </action>
  <verify>
    <automated>cd interfaces/discord && pytest tests/test_pathfinder_player_adapter.py 2>&1 | tail -15</automated>
  </verify>
  <done>10 new RED tests fail (guard not implemented). 12 existing tests still pass. 8 no-args + cancel RED tests from prior tasks still RED. 4 pipe-syntax regression tests still GREEN.</done>
</task>

</tasks>

<verification>
- File `interfaces/discord/tests/test_pathfinder_player_adapter.py` extended with 22 new tests (6 + 6 + 10).
- Existing 12 tests are byte-for-byte unchanged (Test-Rewrite Ban).
- 4 pipe-syntax regression tests pass GREEN (already-shipped behaviour).
- 18 RED tests fail (no-args branch, cancel command incl. multi-draft symmetry per D-17, rejection guard).
- No production code added (RED-only — implementation in 38-06 and 38-07).
- Behavioral-Test-Only Rule honored — every assertion is observable and bug-detecting.
- Function-scope import convention applied to every new test.
</verification>

<success_criteria>
- 22 new tests committed; 18 RED + 4 GREEN regression locks.
- SPEC Requirements 1, 5, 6, 7 each have at least one new RED test.
- Pipe-syntax preservation (SPEC Constraint) has 4 explicit regression contracts.
- All 7 non-start/non-cancel verbs have a rejection-guard test.
- Multi-draft (D-08) and dual-shape directory response (Pitfall 5) explicitly tested for the rejection path.
- Multi-draft cancel symmetry (D-17) has explicit single-draft, two-draft, and partial-failure RED tests.
</success_criteria>

<output>
After completion, create `.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-03-SUMMARY.md`
</output>
