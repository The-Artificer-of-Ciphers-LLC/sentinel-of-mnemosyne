---
phase: 38-pf2e-multi-step-onboarding-dialog
plan: 09
type: auto
wave: 6
depends_on: [38-08]
files_modified:
  - docs/USER-GUIDE.md
  - .planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md
autonomous: true
requirements: [PVL-01]
must_haves:
  truths:
    - "USER-GUIDE.md gains a 'Multi-Step Onboarding Dialog' subsection under PF2E Player Commands documenting the new no-args flow, cancel verb, and pipe-syntax preservation"
    - "38-CONTEXT.md gains an 'Architecture Map' appended section showing the dialog_router → pathfinder_player_dialog → /player/onboard pipeline"
    - "Full discord interface test suite passes (regression sweep)"
    - "Full pathfinder module test suite passes (no Phase 37 regressions)"
    - "SPEC Acceptance Criterion 10 verified manually via git-log inspection — RED test files committed BEFORE production module commits"
  artifacts:
    - path: "docs/USER-GUIDE.md"
      provides: "Multi-step onboarding dialog walkthrough for users + cancel command reference"
    - path: ".planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md"
      provides: "Architecture Map section appended"
  key_links:
    - from: "Phase 38 closeout"
      to: "/gsd-verify-work readiness"
      via: "all SPEC acceptance criteria GREEN, docs updated, no orphan tests, no behaviour drift, criterion-10 git-log verified"
      pattern: "PVL-01"
---

<objective>
Phase 38 closeout: documentation update, architecture map append, and end-to-end regression sweep across both deployables (discord interface + pathfinder module). Includes the manual git-log verification of SPEC Acceptance Criterion 10. Mark Phase 38 ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-SPEC.md
@.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md
@docs/USER-GUIDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update USER-GUIDE.md with multi-step onboarding flow</name>
  <files>docs/USER-GUIDE.md</files>
  <behavior>
    USER-GUIDE.md gains a subsection (under the existing "PF2E Player Commands" section, or a new section if that one doesn't yet exist) titled `### Multi-Step Onboarding Dialog`. Content covers:
      - `:pf player start` with no args: starts a thread, asks three questions, posts the answer back to your profile.
      - `:pf player cancel` to abort.
      - Pipe-syntax `:pf player start a | b | c` is still supported as the one-shot form.
      - Mid-dialog rejection: other `:pf player <verb>` commands are blocked while a dialog is open; a link to the open thread is provided.
      - Restart safety: drafts persist until completion or cancel.
  </behavior>
  <action>
    Read `docs/USER-GUIDE.md`. Locate the "PF2E Player Commands" section (created in Phase 37). Append the new subsection at the end of that section. If no section exists, create it with the verb table from Phase 37 first, then append the dialog subsection — but DO NOT rewrite content from Phase 37 (Test-Rewrite-Ban analogue for docs).

    Verify the section heading is present after the edit.
  </action>
  <verify>
    <automated>grep -c "^### Multi-Step Onboarding Dialog" docs/USER-GUIDE.md</automated>
  </verify>
  <done>Subsection committed to USER-GUIDE.md.</done>
</task>

<task type="auto">
  <name>Task 2: Append Architecture Map to 38-CONTEXT.md</name>
  <files>.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md</files>
  <behavior>
    `38-CONTEXT.md` gains an `## Architecture Map` section appended at the bottom listing:
    - New modules: `pathfinder_player_dialog.py`, `dialog_router.py`
    - Modified modules: `pathfinder_player_adapter.py` (no-args branch + cancel + reject_if_draft_open), `pathfinder_dispatch.py` (cancel registration + author_display_name plumbing), `pathfinder_bridge.py` (author_display_name forwarding), `pathfinder_types.py` (author_display_name field), `bot.py` (_pf_dispatch + on_message author_display_name population), `discord_router_bridge.py` (pre-router gate)
    - Untouched: `command_router.py`, `bot.py:on_message` thread guard, `modules/pathfinder/app/routes/player.py`
    - Pipeline diagram: bridge → dialog_router → pathfinder_player_dialog → /player/onboard
    - Vault layout addition: `mnemosyne/pf2e/players/_drafts/{thread_id}-{user_id}.md`
    - Test files map: each SPEC requirement → test file(s) covering it
  </behavior>
  <action>
    Edit `.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md` — append `## Architecture Map` section at the end of the file. Preserve existing sections verbatim.
  </action>
  <verify>
    <automated>grep -c "^## Architecture Map" .planning/phases/38-pf2e-multi-step-onboarding-dialog/38-CONTEXT.md</automated>
  </verify>
  <done>Architecture Map section appended.</done>
</task>

<task type="auto">
  <name>Task 3: Full regression sweep + SPEC criterion 10 git-log verification</name>
  <files>(no file changes — verification only)</files>
  <behavior>
    Run the full discord interface suite and the full pathfinder module suite. Both must complete with zero failures and zero new warnings beyond the pre-Phase-38 baseline.

    Additionally, verify SPEC Acceptance Criterion 10 manually via git log: every Wave-0 RED test file MUST appear in a commit BEFORE the production module file that makes its tests pass. This is the property a fragile `pytest --collect-only` substring grep cannot verify.
  </behavior>
  <action>
    Execute:
    1. `cd interfaces/discord && pytest tests/ -x` — full suite, expect all GREEN.
    2. `cd modules/pathfinder && pytest tests/ -x` — full suite, expect all GREEN (Phase 37 regression check; Phase 38 made zero changes here per RESEARCH Q8).
    3. Confirm `git diff modules/pathfinder/` is empty (no inadvertent backend edits).
    4. Confirm `git diff interfaces/discord/command_router.py` is empty (D-03).
    5. Confirm bot.py changes are limited to: additive `author_display_name` kwarg on `_pf_dispatch` + the on_message callsite populating it from `message.author.display_name`. The `on_message` thread guard at line 668 MUST be unchanged (D-04).

    6. **SPEC Acceptance Criterion 10 — manual git-log verification:**

       Run:
       ```bash
       git log --diff-filter=A --format='%H %ai %s' -- \
         'interfaces/discord/tests/test_pathfinder_player_dialog.py' \
         'interfaces/discord/tests/test_dialog_router.py' \
         'interfaces/discord/tests/test_phase38_integration.py'

       git log --diff-filter=A --format='%H %ai %s' -- \
         'interfaces/discord/pathfinder_player_dialog.py' \
         'interfaces/discord/dialog_router.py'
       ```

       Verify, by inspecting commit timestamps and SHAs:
       - The commit that ADDED `test_pathfinder_player_dialog.py` predates the commit that ADDED `pathfinder_player_dialog.py`.
       - The commit that ADDED `test_dialog_router.py` predates the commit that ADDED `dialog_router.py`.
       - The commit subject lines for the test-file-add commits indicate Wave-0 / RED / TDD intent (e.g. `test(38-01): RED tests for ...`).

       Print a one-line PASS / FAIL line per file pair. If any RED test file was committed AT OR AFTER its production counterpart, FAIL the task and surface to the operator per AI Deferral Ban — do NOT mark Phase 38 done with criterion 10 unverified.

    If any test fails or criterion 10 ordering is violated: STOP. Surface the specific failure to the operator per CLAUDE.md AI Deferral Ban. Do NOT mark the phase done.
  </action>
  <verify>
    <automated>cd interfaces/discord && pytest tests/ -x 2>&1 | tail -3 && cd ../../modules/pathfinder && pytest tests/ -x 2>&1 | tail -3 && git -C "$OLDPWD/../.." diff --stat interfaces/discord/command_router.py modules/pathfinder/ && git -C "$OLDPWD/../.." log --diff-filter=A --format='%h %ai %s' -- interfaces/discord/tests/test_pathfinder_player_dialog.py interfaces/discord/tests/test_dialog_router.py interfaces/discord/pathfinder_player_dialog.py interfaces/discord/dialog_router.py</automated>
  </verify>
  <done>All discord tests GREEN. All pathfinder tests GREEN. command_router.py and modules/pathfinder/ both have empty diffs. RED test commits precede production module commits for every test/module pair (criterion 10 verified).</done>
</task>

</tasks>

<verification>
- USER-GUIDE.md updated.
- 38-CONTEXT.md gains Architecture Map.
- Full discord interface suite GREEN.
- Full pathfinder module suite GREEN.
- command_router.py byte-for-byte unchanged.
- modules/pathfinder/ byte-for-byte unchanged.
- bot.py changes limited to additive `author_display_name` kwarg on `_pf_dispatch` + on_message callsite population. Thread guard at bot.py:668 unchanged.
- All 10 SPEC acceptance criteria covered: 1..9 by passing tests in test_phase38_integration.py, 10 by git-log verification in this task.
- Phase 38 ready for `/gsd-verify-work`.
</verification>

<success_criteria>
- 2 docs files updated; zero code changes
- Full regression sweep passes both deployables
- D-01..D-17 all honored
- SPEC Constraints honored (pipe-syntax preserved byte-for-byte, on_message thread guard untouched, command_router untouched, /player/onboard untouched)
- AI Deferral Ban + Test-Rewrite Ban + Behavioral-Test-Only Rule + Spec-Conflict Guardrail all honored
- SPEC Acceptance Criterion 10 verified by git-log inspection (RED tests precede production)
- Phase 38 ready for verification
</success_criteria>

<output>
After completion, create `.planning/phases/38-pf2e-multi-step-onboarding-dialog/38-09-SUMMARY.md`
</output>
