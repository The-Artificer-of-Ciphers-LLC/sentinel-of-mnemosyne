---
phase: 43-embeddings-through-sentinel
plan: 05
type: execute
wave: 3
depends_on: [43-01, 43-02, 43-03, 43-04]
files_modified: []
autonomous: false
requirements: [EMB-03, EMB-04]
user_setup:
  - service: lm-studio
    why: "Serves POST /v1/embeddings for text-embedding-nomic-embed-text-v1.5 (768-dim) on port 1234; EMB-02/EMB-03/EMB-04 cannot be verified end-to-end until it is running with the nomic model loaded (mirrors the operator-gated verification pattern from exo-model-notfound-502.md)"
    env_vars:
      - name: EMBEDDING_BASE_URL
        source: "Live .env (deploy checkout) — set to http://host.docker.internal:1234/v1 so both containers point embeddings at LM Studio, independent of the chat provider"
    dashboard_config:
      - task: "Load text-embedding-nomic-embed-text-v1.5 and start the LM Studio local server on port 1234"
        location: "LM Studio app → Local Server (or `lms load text-embedding-nomic-embed-text-v1.5`)"
must_haves:
  truths:
    - "with LM Studio serving nomic on 1234 and the config fix live, a container restart makes sentinel-core /health report embedding_model_loaded: true (SC-2/EMB-02)"
    - ":pf rule semantic retrieval builds its rules index and returns relevant rules with no 503 degradation while the backend is up (SC-3/EMB-03)"
    - "core's Phase-40 semantic recall returns non-empty warm-tier hits from the rebuilt sidecar index after cutover — no silent empty-index degradation (SC-4/EMB-04)"
  artifacts:
    - "no code artifacts — this is the cutover + live-verification gate; the re-sweep is the existing startup rebuild triggered by a container restart (D-09)"
  key_links:
    - "docker compose restart sentinel-core pathfinder -> initialize_startup() _startup_rebuild() + pf2e _build_rules_index_safely() -> both indexes repopulated against LM Studio"
    - "/health embedding_model_loaded -> probe_embedding_model_loaded(settings.embedding_base_url) (the 43-01 fix)"
---

<objective>
Perform the one-time cutover and verify the three inherently-live success criteria that cannot be automated without a running LM Studio: the core embedding probe reports loaded, `:pf rule` retrieval works end-to-end, and core semantic recall returns warm hits from the rebuilt sidecar. Per RESEARCH and CONTEXT, the cutover mechanism (D-09) is NOT new code — it is the existing non-blocking startup rebuild plus a `docker compose restart`, which the owner performs himself.

Purpose: Close EMB-03 and EMB-04 with the operator-gated live verification that Waves 1-2 could not automate. This mirrors the exact "code/config ships, full verification is gated on an operator loading a model" pattern already documented for the exo cutover.
Output: Confirmed live behavior (health probe, `:pf rule`, semantic recall) and a phase-regression-green gate. No code changes.

Locked-decision coverage: D-09 (forced re-embed/re-sweep on cutover = restart both containers; existing mechanism, no new trigger). Verifies the end-to-end payoff of D-01/D-02/D-03/D-04/D-06/D-07/D-08.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-embeddings-through-sentinel/43-RESEARCH.md
@scripts/uat_rules.py
</context>

<artifacts_this_plan_produces>
- No source artifacts. Produces: the verified live-cutover state for EMB-03/EMB-04 and a phase-regression-green record for the phase gate.
</artifacts_this_plan_produces>

<tasks>

<task type="auto">
  <name>Task 1: Phase regression gate — full suites green in both containers</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - 43-RESEARCH.md "Validation Architecture" → Sampling Rate ("Phase gate: full suite green in both containers") and the quick/full run commands.
  </read_first>
  <action>
    Run the full offline test suite in both services and confirm green before the live cutover: `cd sentinel-core && pytest` and `cd modules/pathfinder && pytest`. Also run `cd /Users/trekkie/projects/sentinel-of-mnemosyne && pytest shared/tests` if present. Any failure or warning must be root-caused and fixed inline (no-defer) before proceeding to the human cutover — a red suite here means a Wave 1-2 change regressed and the cutover must not be verified against broken code. Record the pass counts in the SUMMARY.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest -q 2>&1 | tail -5 ; cd /Users/trekkie/projects/sentinel-of-mnemosyne/modules/pathfinder && pytest -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd sentinel-core && pytest` exits 0.
    - `cd modules/pathfinder && pytest` exits 0.
    - No unresolved warnings/errors are waved off; any surfaced failure was root-caused and fixed in this phase.
  </acceptance_criteria>
  <done>Both containers' full pytest suites exit 0 with no waved-off warnings before the live cutover proceeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator cutover — load nomic, set EMBEDDING_BASE_URL, restart, confirm /health</name>
  <action>Human-gated cutover (no code changes): the operator loads the nomic embedding model in LM Studio on port 1234, sets `EMBEDDING_BASE_URL=http://host.docker.internal:1234/v1` in the live `.env`, restarts both containers to fire the existing startup rebuild (D-09), then confirms `/health` reports `embedding_model_loaded: true`. This exercises the 43-01 base_url fix against the real backend.</action>
  <what-built>
    sentinel-core's embeddings client + readiness probe now resolve from the dedicated `embedding_*` settings (43-01), so once LM Studio serves nomic on port 1234 and the containers restart, the existing non-blocking startup rebuild (`initialize_startup()` → `_startup_rebuild()` → `rebuild_embedding_index()`) repopulates the core sidecar index and pf2e's `_build_rules_index_safely()` rebuilds its rules index — the D-09 cutover, with no new trigger code.
  </what-built>
  <how-to-verify>
    1. Ensure LM Studio is running with `text-embedding-nomic-embed-text-v1.5` loaded and its local server listening on port 1234 (or `lms load text-embedding-nomic-embed-text-v1.5`).
    2. In the live deploy checkout's `.env`, set `EMBEDDING_BASE_URL=http://host.docker.internal:1234/v1` (leave the chat `EXO_*`/`LMSTUDIO_*` values as-is — embeddings are independent per D-04).
    3. Restart both services: `docker compose restart sentinel-core pathfinder` (the forced re-sweep, D-09).
    4. Confirm the backend is reachable directly: `LIVE_TEST=1 python scripts/uat_rules.py` runs `test_lm_studio_embeddings_reachable` green and reports a `dim=` (expect 768; note the value if it differs — see RESEARCH Assumption A1).
    5. Confirm core sees the model: `curl -s -H "X-Sentinel-Key: $SENTINEL_API_KEY" http://<core-host>/health` shows `embedding_model_loaded: true`.
  </how-to-verify>
  <resume-signal>Type "approved" if `embedding_model_loaded: true` and the reachability UAT is green (note the reported dim), or describe what failed (wrong port, model not loaded, health still false).</resume-signal>
  <done>`/health` reports `embedding_model_loaded: true` and `test_lm_studio_embeddings_reachable` is green with the reported dim noted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live verify EMB-03 (:pf rule) and EMB-04 (semantic recall)</name>
  <action>Human verification (no code changes): with the backend live and both indexes rebuilt, confirm the phase's end-to-end payoff — `:pf rule` returns relevant sourced rules with no 503, and a paraphrase `/message` returns non-empty semantic warm-tier hits with no dimension-mismatch degrade.</action>
  <what-built>
    With the backend live and both indexes rebuilt, pf2e's `:pf rule` now obtains its vectors through core's `POST /embeddings` (43-02/43-03) and core's Phase-40 semantic recall reads the freshly-rebuilt, dimension-consistent sidecar index (43-01/43-04). This is the end-to-end payoff of the phase.
  </what-built>
  <how-to-verify>
    1. EMB-03 — run a `:pf rule` query with a known Remaster mechanic (or `LIVE_TEST=1 python scripts/uat_rules.py`'s `test_http_rule_flows`); confirm it returns a relevant, sourced ruling with NO 503 "embeddings degraded" response while the backend is up.
    2. EMB-04 — send a normal `/message` whose answer depends on a vault note (a paraphrase/near-synonym query per SC-1 of Phase 40); confirm `RecalledContext.warm` returns non-empty semantic hits (not an empty index / keyword-only degrade).
    3. Confirm no dimension-mismatch spam: the core log shows the sidecar populated for the active nomic model (no "all N entries mismatch active model — degrading to keyword-only" warning).
  </how-to-verify>
  <resume-signal>Type "approved" if `:pf rule` returns relevant rules with no 503 AND semantic recall returns warm hits, or describe the failure (empty index, 503 while backend up, dimension-mismatch degrade).</resume-signal>
  <done>`:pf rule` returns relevant rules with no 503 while the backend is up, and core semantic recall returns non-empty warm-tier hits with no dimension-mismatch degrade.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator action (LM Studio load + `.env` edit + container restart) → live system | the human-gated cutover crosses into the running deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-05-01 | Information Disclosure | live `/health` + startup logs during cutover | low | accept | `/health` reports only the `embedding_model_loaded` boolean; the 43-01 secret-handling controls keep `embedding_api_key`/`embedding_base_url` out of logs; no new surface added by this verification plan |
| T-43-05-SC | Supply Chain | pip installs | low | accept | N/A — no new packages; this plan changes no code |
</threat_model>

<verification>
- Task 1 automated: both containers' full pytest suites exit 0.
- Human-verified (operator, LM Studio required): `/health` `embedding_model_loaded: true`; `:pf rule` returns relevant rules with no 503; core semantic recall returns non-empty warm hits.
</verification>

<success_criteria>
- EMB-03: `:pf rule` semantic retrieval works end-to-end with no 503 while the backend is up.
- EMB-04: core semantic recall produces/reads embeddings against the same backend with no silent empty-index degradation; dimension guard prevents stale cosine.
- Phase regression suites green in both `sentinel-core` and `modules/pathfinder`.
</success_criteria>

<output>
Create `.planning/phases/43-embeddings-through-sentinel/43-05-SUMMARY.md` when done.
</output>
