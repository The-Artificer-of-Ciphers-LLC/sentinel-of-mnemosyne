---
phase: 45-note-quality-schema-graph-analysis
plan: 05
type: execute
wave: 3
depends_on: [45-02]
files_modified:
  - sentinel-core/app/services/moc_maintenance.py
  - sentinel-core/tests/test_moc_maintenance.py
autonomous: true
requirements: [NOTE-02]
must_haves:
  truths:
    - "Hub matching reuses the embedding sidecar + shared cosine + the existing recall semantic_cosine_floor 0.50 (D-03) — no new embedding call and no new threshold."
    - "A hub materializes only on the 2nd topically-similar member (min-cluster-size 2, D-03a); a lone note that clears no hub is hub-pending (returns None, reported as orphan, D-03b)."
    - "attach_to_hub reads the full hub body, splits off the trailing _schema block, inserts the member wikilink under a stable marker, and re-appends the block — the block stays the LAST thing in the file (RESEARCH landmine); it never uses patch_append."
    - "Hub creation is idempotent, keyed on the deterministic notes/{concept-slug}.md path (D-03d); a repeat member is a no-op, never a duplicate wikilink."
  artifacts:
    - sentinel-core/app/services/moc_maintenance.py
    - sentinel-core/tests/test_moc_maintenance.py
  key_links:
    - "attach_to_hub depends on note_schema.split_schema_block (02) to preserve the trailing block; hub naming reuses acompletion_with_profile with an untrusted-input posture."
---

<objective>
Build `moc_maintenance.py`: the lazy MOC/hub machinery (NOTE-02, SC-2). It finds the nearest existing
hub by reusing the sweeper's embedding index + the shared cosine helper + the existing recall floor 0.50
(D-03), materializes a hub only on the 2nd clustered member (D-03a) holding singletons hub-pending
(D-03b), and performs the idempotent, trailing-`_schema`-preserving hub write (D-03d + the RESEARCH
landmine). Phase 45 ships and unit-tests this machinery; Phase 46 wires the pipeline caller (D-02) — so
this plan builds no write-path trigger, only the reusable functions.

Purpose: NOTE-02's lazy, append-never-duplicate hub notes under the transaction-less REST vault.
Output: `sentinel-core/app/services/moc_maintenance.py`, `sentinel-core/tests/test_moc_maintenance.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-CONTEXT.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md
@sentinel-core/app/services/embedding_sidecar_index.py
@sentinel-core/app/services/recall.py
@sentinel-core/app/services/note_classifier.py
@sentinel-core/app/services/note_schema.py
@sentinel-core/app/vault.py
@sentinel-core/tests/fakes/vault.py
</context>

## Artifacts this phase produces

Modules: note_schema.py (02), graph_analysis.py (03), links_sidecar_index.py (04),
moc_maintenance.py (this plan); routes graph.py + main.py (06); discord (07); sidecar ops/graph/links-index.json (04).
**This plan delivers:** `moc_maintenance.py` + its tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: find_hub_candidate — embedding-first lookup reusing recall floor 0.50 (D-03) + min-cluster-size 2 decision (D-03a/b)</name>
  <files>sentinel-core/app/services/moc_maintenance.py, sentinel-core/tests/test_moc_maintenance.py</files>
  <read_first>
    - sentinel-core/app/services/embedding_sidecar_index.py (eligible_entries: decode/filter/dimension-mismatch guard)
    - sentinel-core/app/services/recall.py (RecallConfig.semantic_cosine_floor = 0.50 at ~line 285; the search() cosine-floor loop ~564-578)
    - shared/sentinel_shared/similarity.py (cosine_similarity — the cross-package SPOT helper)
    - .planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md (Pattern 3 hub-matching example; Don't Hand-Roll table)
  </read_first>
  <behavior>
    - find_hub_candidate returns the hub path with the highest cosine that clears the floor, or None when no hub clears it (hub-pending -> orphan, D-03b).
    - The floor value is the imported recall semantic_cosine_floor (0.50), not a locally re-declared literal.
    - Candidate entries are read through eligible_entries so the dimension-mismatch guard is preserved (a stored-dim != active-model-dim entry is hard-skipped).
    - A cluster-size decision helper reports that a hub should materialize only when a 2nd clearing member exists (min-cluster-size 2, D-03a).
  </behavior>
  <action>
    Create app/services/moc_maintenance.py. Define MIN_CLUSTER_SIZE = 2 and import the cosine floor from
    recall's RecallConfig rather than declaring a new threshold (D-03: reuse the shipped 0.50). Implement
    find_hub_candidate(*, note_vector, hub_paths, index, active_model) -> str | None that obtains candidate
    entries via embedding_sidecar_index.eligible_entries (preserving its dimension-mismatch guard), restricts
    to entries whose path is in hub_paths, scores each with sentinel_shared.similarity.cosine_similarity, and
    returns the best path clearing the floor or None. Add a small should_materialize_hub decision (given the
    count of members that clear the floor for a nascent topic, materialize on the 2nd) encoding D-03a. Do not
    add a second cosine implementation and do not add a fresh embedding call.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_moc_maintenance.py -q -k "candidate or cluster"</automated>
  </verify>
  <done>find_hub_candidate reuses eligible_entries + shared cosine + recall floor 0.50 (None => hub-pending); should_materialize_hub fires on the 2nd member (D-03a).</done>
  <acceptance_criteria>
    - A note clearing the floor against one hub returns that hub path; a note clearing nothing returns None (hub-pending).
    - The floor used equals recall's semantic_cosine_floor (asserted by test), not a duplicated literal.
    - should_materialize_hub is False for the 1st clearing member and True for the 2nd (D-03a).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: attach_to_hub — idempotent, trailing-_schema-preserving read-modify-write (D-03d + RESEARCH landmine)</name>
  <files>sentinel-core/app/services/moc_maintenance.py, sentinel-core/tests/test_moc_maintenance.py</files>
  <read_first>
    - .planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md (Common Pitfall 1 + Anti-Patterns: NEVER patch_append a hub; split-off-block -> mutate -> re-append; warning-sign test asserts body ends with the fence)
    - sentinel-core/app/services/note_schema.py (split_schema_block returns (pre-block body, raw block))
    - sentinel-core/app/vault.py (read_note/write_note full-body PUT; patch_append — the primitive to AVOID here)
  </read_first>
  <behavior>
    - hub_path_for_slug(concept_slug) returns the deterministic notes/{concept-slug}.md path (D-03d idempotency key).
    - attach_to_hub reads the full hub body, splits off any trailing _schema block, inserts the member wikilink under a stable member-section marker only if not already present, then re-appends the trailing block, and writes the merged body in a single write_note; it never calls patch_append.
    - Re-attaching the same member is a no-op (append-never-duplicate); the trailing block remains the terminal content.
  </behavior>
  <action>
    Define HUB_MEMBER_MARKER (a stable member-section heading constant) and hub_path_for_slug(concept_slug) ->
    str yielding notes/{concept-slug}.md. Implement async attach_to_hub(vault, hub_path, member_slug) that:
    reads the full hub body; uses note_schema.split_schema_block to separate the pre-block body from the raw
    trailing block; ensures the member-section marker exists in the pre-block body; inserts the member wikilink
    under that marker only when the same wikilink is not already present (idempotent, append-never-duplicate);
    re-appends the raw trailing block (if any) so it stays last; and performs a single write_note of the merged
    body. It must never call vault.patch_append. Add tests seeding a hub whose body already ends in a trailing
    _schema block, attaching two members, and asserting the body (rstripped) still ends with the fence close and
    each member appears exactly once — this is the concrete guard for the phase's highest-risk behavior and it
    activates the Wave-0 trailing-block fixture.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_moc_maintenance.py -q -k "attach or idempotent"</automated>
  </verify>
  <done>attach_to_hub keeps the trailing block last, never duplicates a member, never calls patch_append, and activates the Wave-0 trailing-block fixture.</done>
  <acceptance_criteria>
    - After attaching a 2nd member, the hub body still ends (rstripped) with the fenced-block close — the trailing block is preserved as last content.
    - Re-attaching an existing member does not duplicate the wikilink.
    - No path in moc_maintenance.py invokes patch_append (member append goes through read/split/write only).
    - The Wave-0 test `tests/test_p45_invariants.py -k trailing` now RUNS and passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: create_or_update_hub orchestration + constrained concept-slug LLM fallback (D-03c/d, untrusted-input posture)</name>
  <files>sentinel-core/app/services/moc_maintenance.py, sentinel-core/tests/test_moc_maintenance.py</files>
  <read_first>
    - sentinel-core/app/services/note_classifier.py (acompletion_with_profile(response_format=json_schema) structured-completion pattern ~290-320; candidate_text untrusted-input posture ~284-287)
    - .planning/phases/45-note-quality-schema-graph-analysis/45-CONTEXT.md (D-03c concept-slug title; D-03d deterministic path + read-then-append-or-create)
  </read_first>
  <behavior>
    - propose_hub_slug returns a short concept slug (noun phrase) via structured completion; note text passed to the model is treated as untrusted data, never as instructions.
    - create_or_update_hub reads the deterministic hub path first; if present it delegates to attach_to_hub (merge), if absent it writes a fresh hub note with the member-section marker and a trailing _schema block (type hub) already in place.
    - The operation is idempotent under the transaction-less REST vault — identity is the deterministic path, not a lock.
  </behavior>
  <action>
    Add async propose_hub_slug(*, member_texts, completion_fn) that calls acompletion_with_profile with a
    json_schema response format constraining the output to a short concept slug (D-03c), placing all vault-derived
    text in the untrusted user slot exactly as note_classifier does with candidate_text (never as system
    directives). Implement async create_or_update_hub(vault, *, concept_slug, member_slug, completion_fn=None)
    that derives the path via hub_path_for_slug, reads it, and either merges via attach_to_hub (existing hub) or
    writes a fresh hub body containing the member-section marker, the first member wikilink, and a trailing
    _schema block declaring type hub. Fresh-create and repeat-call must both converge to the same content
    (idempotent by deterministic path, D-03d). Unit-test with a stubbed completion_fn (no live LLM) covering
    create-then-append and the injection-posture assertion (vault text is not placed in a system/instruction slot).
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_moc_maintenance.py -q</automated>
  </verify>
  <done>create_or_update_hub is idempotent on the deterministic notes/{slug}.md path; propose_hub_slug is json_schema-constrained with an untrusted-input posture (D-03c/d).</done>
  <acceptance_criteria>
    - First call on a missing hub creates notes/{slug}.md with marker + member wikilink + trailing type-hub _schema block.
    - Second call with a new member appends under the marker and keeps the trailing block last; second call with the same member is a no-op.
    - propose_hub_slug uses a json_schema-constrained completion and places vault-derived text only in the untrusted user slot (asserted with a stubbed completion_fn).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault note text -> hub-naming LLM | Member note bodies fed to the concept-slug completion are untrusted and may attempt prompt injection |
| moc_maintenance -> vault write | Full-body PUT to a hub note under a transaction-less REST vault |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-INJ | Tampering / Elevation of Privilege | propose_hub_slug LLM fallback | high | mitigate | Vault text placed only in the untrusted user slot, never system directives — mirrors note_classifier candidate_text posture; json_schema-constrained output |
| T-45-BLOCK | Tampering | trailing _schema block corruption on hub append | high | mitigate | No patch_append; read/split/mutate/re-append/single-write preserves terminal block; asserted by fence-close test + Wave-0 fixture |
| T-45-DUP | Tampering | duplicate member wikilinks / hub sprawl | medium | mitigate | Idempotent deterministic-path identity + presence check before insert (D-03d) |
</threat_model>

<verification>
- `cd sentinel-core && .venv/bin/python -m pytest tests/test_moc_maintenance.py tests/test_p45_invariants.py -q` → moc tests green; Wave-0 trailing-block fixture now runs and passes.
- `cd sentinel-core && .venv/bin/python -m pytest tests/ -q` → 473+ passed floor holds (no pipeline caller wired; machinery only).
</verification>

<success_criteria>
- moc_maintenance.py exposes find_hub_candidate, should_materialize_hub, hub_path_for_slug, attach_to_hub, create_or_update_hub, propose_hub_slug, MIN_CLUSTER_SIZE, HUB_MEMBER_MARKER.
- Reuses recall floor 0.50 + shared cosine + eligible_entries (no new embed, no new threshold).
- Idempotent, trailing-block-preserving hub writes; injection-safe naming; Wave-0 fixture activated.
</success_criteria>

<output>
Create `.planning/phases/45-note-quality-schema-graph-analysis/45-05-SUMMARY.md` when done.
</output>
