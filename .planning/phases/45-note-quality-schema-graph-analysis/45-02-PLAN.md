---
phase: 45-note-quality-schema-graph-analysis
plan: 02
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - sentinel-core/app/services/note_schema.py
  - sentinel-core/tests/test_note_schema.py
autonomous: true
requirements: [NOTE-01]
must_haves:
  truths:
    - "note_schema.parse_schema_block reads the trailing fenced _schema block from the END of the body (regex anchored at end-of-string), never the leading provenance frontmatter (D-01)."
    - "note_schema.has_claim_title is a purely structural check (H1 present, not a bare filename slug, >1 word) with zero LLM calls (D-05)."
    - "note_schema.check_note_compliance reports missing _schema / claim-title / wikilink deterministically and never raises on malformed input (self-heals to a FAIL entry)."
  artifacts:
    - sentinel-core/app/services/note_schema.py
    - sentinel-core/tests/test_note_schema.py
  key_links:
    - "split_schema_block returns (pre-block-body, raw-block-or-None) — the exact seam Plan 05 needs to mutate a hub body without disturbing the trailing block."
    - "Inner YAML is parsed with yaml.safe_load only (never yaml.load/eval), matching markdown_frontmatter.py."
---

<objective>
Build `note_schema.py`: the parser/validator for the trailing ```_schema fenced footer block (D-01),
plus the structural claim-title check (D-05) and the wikilink-presence + per-note compliance helpers
that back `:check` (SC-1, SC-4). This is a pure content-parsing module with no I/O of its own; it is
called by the routes (06) and by moc_maintenance (05).

Purpose: NOTE-01's durable-standard inspection. The block is anchored at the END of the body and kept
strictly disjoint from the LEADING provenance frontmatter that markdown_frontmatter.py owns.
Output: `sentinel-core/app/services/note_schema.py`, `sentinel-core/tests/test_note_schema.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-CONTEXT.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md
@sentinel-core/app/markdown_frontmatter.py
@sentinel-core/tests/fakes/vault.py
</context>

## Artifacts this phase produces

Modules: note_schema.py (this plan), graph_analysis.py (03), links_sidecar_index.py (04),
moc_maintenance.py (05); routes graph.py + main.py (06); discord gateway/router/bot (07);
sidecar path ops/graph/links-index.json (04). **This plan delivers:** `note_schema.py` + its tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trailing _schema block parse + split (D-01, regex-from-end)</name>
  <files>sentinel-core/app/services/note_schema.py, sentinel-core/tests/test_note_schema.py</files>
  <read_first>
    - sentinel-core/app/markdown_frontmatter.py (leading-anchor regex `^---...---` with re.DOTALL and yaml.safe_load/safe_dump — the pattern to MIRROR but anchor oppositely)
    - .planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md (Pattern 1 + Code Examples: end-anchored regex after rstrip; yaml.safe_load inner; None on malformed)
  </read_first>
  <behavior>
    - parse_schema_block(body) returns a dict for a well-formed trailing block (type + hub keys).
    - parse_schema_block returns None when the block is absent, malformed, non-dict, or when yaml parsing raises.
    - A stray earlier fenced block with the same info-string does NOT win — only the terminal block matches (end-of-string anchor after rstrip).
    - split_schema_block(body) returns (body_without_trailing_block, raw_block_text_or_None), preserving all pre-block content byte-for-byte.
  </behavior>
  <action>
    Create app/services/note_schema.py. Add module-level compiled regex matching a fenced info-string
    block whose language tag is the schema tag, anchored at end-of-string (use re.DOTALL and an end anchor
    after rstripping the body) so only the terminal block matches. Implement parse_schema_block(body: str)
    returning the inner content parsed with yaml.safe_load and coerced to dict-or-None, catching every parse
    exception and degrading to None (never raising). Implement split_schema_block(body: str) returning a
    tuple of (pre-block body, raw block text or None) so callers can mutate the pre-block body and re-append
    the block unchanged. Use yaml.safe_load exclusively — never an unsafe loader — matching
    markdown_frontmatter.py. Keep this module free of any leading-frontmatter parsing; it owns ONLY the
    trailing block.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_note_schema.py -q -k "parse or split"</automated>
  </verify>
  <done>parse_schema_block + split_schema_block parse the terminal block, round-trip the split, and return None on malformed input without raising.</done>
  <acceptance_criteria>
    - Present/absent/malformed cases covered; malformed returns None, never raises.
    - A note whose prose contains an earlier same-tag fenced block still parses only the terminal block.
    - split_schema_block round-trips: pre-block body + re-appended raw block reproduces the original (modulo trailing whitespace).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structural claim-title + wikilink presence + per-note compliance (D-05, no LLM)</name>
  <files>sentinel-core/app/services/note_schema.py, sentinel-core/tests/test_note_schema.py</files>
  <read_first>
    - .planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md (Code Examples: has_claim_title H1 regex + normalized-slug comparison; Pitfall 4 honest-narrow heuristic)
    - .planning/phases/45-note-quality-schema-graph-analysis/45-CONTEXT.md (D-05: structural only, no LLM calls)
  </read_first>
  <behavior>
    - has_claim_title(body, filename_slug) is True when an H1 exists, its normalized text is not equal to the normalized filename slug, and it has more than one word; False otherwise.
    - has_wikilink(body) is True when at least one wikilink target is present, else False.
    - check_note_compliance(body, filename_slug) returns a dict with booleans (has_schema, has_type, has_claim_title, has_wikilink) and a failures list; it makes zero network/LLM/embedding calls and never raises.
  </behavior>
  <action>
    Add has_claim_title(body: str, filename_slug: str) -> bool implementing D-05's structural-only rule:
    an H1 line exists (multiline H1 regex), its normalized form (lowercased, spaces and underscores folded
    to hyphens) differs from the normalized filename slug, and the title has more than one word. Document in
    a code comment that this is deliberately structural-only and that LLM-judged title quality is the deferred
    :check --deep pass — but express that limitation by concept, do not embed any literal that a later negative
    grep targets. Add has_wikilink(body: str) -> bool. Add check_note_compliance(body: str, filename_slug: str)
    -> dict aggregating parse_schema_block presence + type key + has_claim_title + has_wikilink into a
    structured result with an explicit failures list, catching any internal error and recording it as a FAIL
    rather than raising. No import of any LLM/embedding/httpx symbol may appear in this module.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_note_schema.py -q</automated>
  </verify>
  <done>has_claim_title, has_wikilink, and check_note_compliance are deterministic, structural-only, and provably free of LLM/httpx imports (D-05).</done>
  <acceptance_criteria>
    - has_claim_title accepts a real multi-word claim H1, rejects a bare-slug H1 and an H1-less note.
    - has_wikilink true/false cases pass.
    - check_note_compliance returns deterministic structured output and a test asserts the module imports no LLM/httpx symbol (determinism gate for SC-4).
    - The Wave-0 wikilink/importorskip tests are unaffected (this module does not define resolve_wikilink).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vault note body -> note_schema parser | Note bodies are untrusted user/vault content; the _schema block and inner YAML may be malformed or hostile |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-YAML | Tampering | parse_schema_block inner YAML | high | mitigate | yaml.safe_load only (never yaml.load/eval) — matches markdown_frontmatter.py; arbitrary object construction is rejected |
| T-45-DOS1 | Denial of Service | malformed block / body crashing the read path | medium | mitigate | parse_schema_block and check_note_compliance catch all parse exceptions and degrade to None / FAIL entry, never propagate |
| T-45-DET | Elevation of Privilege | hidden LLM dependency in a "cheap" :check path | medium | mitigate | Module imports no LLM/embedding/httpx symbol; a unit test asserts this, enforcing D-05's no-LLM contract |
</threat_model>

<verification>
- `cd sentinel-core && .venv/bin/python -m pytest tests/test_note_schema.py -q` → all green.
- `cd sentinel-core && .venv/bin/python -m pytest tests/ -q` → 473+ passed floor holds (additive module, no existing test touched).
</verification>

<success_criteria>
- note_schema.py exposes parse_schema_block, split_schema_block, has_claim_title, has_wikilink, check_note_compliance.
- Trailing block is end-anchored and disjoint from leading frontmatter; malformed input never raises.
- Zero LLM/embedding/network imports; asserted by test.
</success_criteria>

<output>
Create `.planning/phases/45-note-quality-schema-graph-analysis/45-02-SUMMARY.md` when done.
</output>
