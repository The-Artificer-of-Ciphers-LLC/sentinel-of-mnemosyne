---
phase: 45-note-quality-schema-graph-analysis
plan: 06
type: execute
wave: 4
depends_on: [45-02, 45-03, 45-04]
files_modified:
  - sentinel-core/app/routes/graph.py
  - sentinel-core/app/main.py
  - sentinel-core/tests/test_graph_routes.py
autonomous: true
requirements: [NOTE-03]
must_haves:
  truths:
    - "GET /vault/graph, /vault/stats, /vault/check exist, respond with Pydantic-modeled JSON, and are NOT admin-gated (unlike /vault/sweep/start) — read-only, non-destructive (research Anti-Patterns, A3)."
    - "Each route calls rebuild_links_index_if_stale (D-04a hybrid freshness) before computing, so it never does a full vault walk when the sidecar is fresh."
    - "/vault/check reports per-note missing _schema / claim-title / wikilink via note_schema.check_note_compliance with zero LLM calls (D-05)."
    - "Routes are registered in main.py alongside the existing routers without altering existing route behavior."
  artifacts:
    - sentinel-core/app/routes/graph.py
    - sentinel-core/tests/test_graph_routes.py
  key_links:
    - "Routes read the sidecar index (04), derive hub_paths from schema.type==hub, call build_graph_report (03) and check_note_compliance (02); no model-readiness probe in the read path."
---

<objective>
Expose the graph machinery over HTTP: a new `app/routes/graph.py` with `GET /vault/graph`,
`GET /vault/stats`, and `GET /vault/check`, registered in `main.py`. They mirror the `/vault/sweep/*`
route shape but deliberately carry NO admin gate and NO model-readiness probe — the sidecar rebuild is
pure Python and non-destructive to user content (research Anti-Patterns; Assumption A3). This closes
SC-3 and SC-4 on the API side.

Purpose: NOTE-03 — the user-facing read-only inspection endpoints backing `:graph`/`:stats`/`:check`.
Output: `sentinel-core/app/routes/graph.py`, registration in `sentinel-core/app/main.py`,
`sentinel-core/tests/test_graph_routes.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-CONTEXT.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md
@sentinel-core/app/routes/note.py
@sentinel-core/app/state.py
@sentinel-core/app/main.py
@sentinel-core/tests/test_note_routes.py
</context>

## Artifacts this phase produces

Modules: note_schema.py (02), graph_analysis.py (03), links_sidecar_index.py (04), moc_maintenance.py (05);
routes graph.py + main.py (this plan); discord (07); sidecar ops/graph/links-index.json (04).
**This plan delivers:** `app/routes/graph.py`, its main.py registration, and `test_graph_routes.py`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: graph.py routes — /vault/graph + /vault/stats + /vault/check (no admin gate, no model probe)</name>
  <files>sentinel-core/app/routes/graph.py, sentinel-core/tests/test_graph_routes.py</files>
  <read_first>
    - sentinel-core/app/routes/note.py (APIRouter, get_route_context, request/response Pydantic models, the /vault/sweep/* shape and _is_admin_route — which these routes must NOT copy)
    - sentinel-core/app/state.py (RouteContext.vault, get_route_context)
    - sentinel-core/app/services/links_sidecar_index.py (rebuild_links_index_if_stale, LINKS_INDEX_PATH, entry shape: wikilinks + schema)
    - sentinel-core/app/services/graph_analysis.py (build_graph_report, GraphReport) and app/services/note_schema.py (check_note_compliance)
  </read_first>
  <behavior>
    - GET /vault/graph returns note_count, orphans, backlinks summary, hub_count, link_density as a Pydantic-modeled JSON body, computed from the sidecar index after a hybrid freshness check.
    - GET /vault/stats returns note_count, hub_count, orphan_count, average notes-per-hub, and link_density from the same GraphReport computation (one shared read).
    - GET /vault/check returns per-note failures (missing _schema / claim-title / wikilink) plus compliant_count, using check_note_compliance with zero LLM calls.
    - None of the three routes call _is_admin_route or any model-readiness probe.
  </behavior>
  <action>
    Create app/routes/graph.py with its own APIRouter, mirroring note.py's use of get_route_context to obtain
    the vault. In each handler, call links_sidecar_index.rebuild_links_index_if_stale(ctx.vault) for D-04a hybrid
    freshness, derive the notes map and the hub_paths set (entries whose parsed schema type is hub) from the
    returned index, and: for /vault/graph and /vault/stats call graph_analysis.build_graph_report once and render
    two Pydantic response models (graph-shaped and stats-shaped); for /vault/check iterate the index entries and
    call note_schema.check_note_compliance per note, returning FAIL/compliant counts. Define Pydantic response
    models mirroring note.py's ClassifyResponse style. Do NOT add an admin gate and do NOT add a model-readiness
    probe — these are read-only and non-destructive (research Anti-Patterns / A3). If a stale index was served
    because a vault operation holds the lock, surface that as a caveat field in the response rather than failing.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_graph_routes.py -q</automated>
  </verify>
  <done>/vault/graph, /vault/stats, /vault/check return 200 with modeled JSON for any (non-admin) caller, invoke hybrid freshness, and /vault/check makes zero LLM calls.</done>
  <acceptance_criteria>
    - TestClient GETs to /vault/graph, /vault/stats, /vault/check each return 200 with the modeled JSON against a seeded FakeVault (mirroring test_note_routes.py's app-construction pattern).
    - A non-admin caller receives a normal 200 from all three routes (a test asserts no admin gate — parity with the sweep-route admin test but inverted).
    - /vault/check output is produced with zero LLM/embedding calls (asserted).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register graph router in main.py</name>
  <files>sentinel-core/app/main.py</files>
  <read_first>
    - sentinel-core/app/main.py (router imports at lines ~30-36; include_router calls at ~99-104)
  </read_first>
  <action>
    In main.py, import the new graph router (mirroring `from app.routes.note import router as note_router`)
    and add a single include_router call alongside the existing ones. Make no other change to main.py — the
    edit is purely additive so existing route registration and tests are untouched.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_graph_routes.py tests/test_composition.py -q</automated>
  </verify>
  <done>The graph router is registered once in main.py additively; existing route/composition tests and the 473 baseline stay green.</done>
  <acceptance_criteria>
    - The graph router is included exactly once; the three routes are reachable on the composed app.
    - No existing router registration is modified; existing route/composition tests stay green.
    - `cd sentinel-core && .venv/bin/python -m pytest tests/ -q` holds the 473-passed floor.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> /vault/graph|stats|check | Requests cross the X-Sentinel-Key auth boundary already enforced for all /vault/* routes |
| route -> sidecar/vault reads | Read-only computation over untrusted vault content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-ACL | Elevation of Privilege | deliberately un-gated read routes | medium | accept | Non-destructive read-only sidecar rebuild; admin-gating would break the commands for non-admins with no security benefit (A3) — accepted, distinct from the destructive sweep route which stays gated |
| T-45-VAL | Tampering | untrusted vault content in responses | medium | mitigate | Pydantic response models bound the output shape; check_note_compliance/parse_schema_block self-heal, so malformed content yields FAIL entries, not 500s |
| T-45-AUTH | Spoofing | unauthenticated access | low | mitigate | Routes inherit the existing X-Sentinel-Key requirement covering all /vault/* endpoints; no new auth surface |
</threat_model>

<verification>
- `cd sentinel-core && .venv/bin/python -m pytest tests/test_graph_routes.py -q` → all green (200s, no admin gate, no-LLM check).
- `cd sentinel-core && .venv/bin/python -m pytest tests/ -q` → 473+ passed floor holds.
</verification>

<success_criteria>
- graph.py exposes GET /vault/graph, /vault/stats, /vault/check with Pydantic response models; registered in main.py.
- No admin gate, no model-readiness probe; hybrid freshness invoked per call; /vault/check is LLM-free.
- Baseline green; existing routes untouched.
</success_criteria>

<output>
Create `.planning/phases/45-note-quality-schema-graph-analysis/45-06-SUMMARY.md` when done.
</output>
