---
phase: 45-note-quality-schema-graph-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentinel-core/tests/test_p45_invariants.py
autonomous: true
requirements: [NOTE-01, NOTE-02, NOTE-03]
must_haves:
  truths:
    - "The no-notes/-write-path premise (D-02 inspect-only) is locked by a live characterizing test that fails if note_classifier ever routes learning/reference to notes/."
    - "The wikilink -> path resolution rule (filename-stem, research Open Question 2) is pinned by a fixture test that activates the moment graph_analysis lands."
    - "The trailing _schema-block invariant (RESEARCH landmine) is pinned by a fixture test that activates the moment moc_maintenance lands."
  artifacts:
    - sentinel-core/tests/test_p45_invariants.py
  key_links:
    - "importorskip guards keep the two feature-dependent invariants SKIPPED (green suite) until their module exists, then auto-activate as GREEN gates for waves 2/3."
---

<objective>
Install the three cross-cutting characterizing / fixture tests VALIDATION.md lists as Wave 0
BEFORE any feature module is built, so the feature waves have a live Nyquist feedback signal.

This is the Wave 0 scaffold required by the phase's validation contract. One test characterizes
existing behavior and passes immediately; two are fixtures that `importorskip` their not-yet-built
module (so the suite stays green at the 473-passed / 12-skipped baseline) and auto-activate into
hard GREEN gates when waves 2/3 land the modules.

Purpose: lock the read-mostly / inspect-only premise (D-02) and pin the two design rules RESEARCH
could not verify against a live vault (wikilink resolution; trailing-block preservation) so a
regression in a later wave is caught within ~60s.
Output: `sentinel-core/tests/test_p45_invariants.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-CONTEXT.md
@.planning/phases/45-note-quality-schema-graph-analysis/45-VALIDATION.md
@sentinel-core/app/services/note_classifier.py
@sentinel-core/tests/fakes/vault.py
</context>

## Artifacts this phase produces

New modules (`sentinel-core/app/services/`): `note_schema.py` (02), `graph_analysis.py` (03),
`links_sidecar_index.py` (04), `moc_maintenance.py` (05).
New route file `sentinel-core/app/routes/graph.py` + registration in `sentinel-core/app/main.py` (06):
`GET /vault/graph`, `GET /vault/stats`, `GET /vault/check`.
New Discord gateway fns `call_core_graph` / `call_core_stats` / `call_core_check` in
`interfaces/discord/core_gateway.py`, rewired `:graph`/`:stats`/`:check` branches in
`command_router.py`, edited `bot.py` (07).
New in-vault sidecar file path `ops/graph/links-index.json` (built by 04).
New tests: `test_p45_invariants.py` (this plan), `test_note_schema.py` (02), `test_graph_analysis.py` (03),
`test_links_sidecar_index.py` (04), `test_moc_maintenance.py` (05), `test_graph_routes.py` (06),
and `test_command_router_module.py` / `test_core_gateway.py` additions (07).
**This plan delivers:** `sentinel-core/tests/test_p45_invariants.py`.

<tasks>

<task type="auto">
  <name>Task 1: Characterizing test — note_classifier keeps learning/reference in inbox/ (locks D-02 inspect-only)</name>
  <files>sentinel-core/tests/test_p45_invariants.py</files>
  <read_first>
    - sentinel-core/app/services/note_classifier.py (TOPIC_VAULT_PATH dict, lines ~60-67 — learning/reference/unsure all map to "inbox")
    - sentinel-core/tests/test_note_classifier.py (existing import + style conventions for this module)
  </read_first>
  <action>
    Create sentinel-core/tests/test_p45_invariants.py. Add a live (un-guarded) test that imports
    TOPIC_VAULT_PATH from app.services.note_classifier and asserts learning and reference both resolve
    to the inbox root (not any notes root), and that no value in TOPIC_VAULT_PATH points at a notes
    directory. This characterizes the premise D-02 depends on — there is no notes/ write path for
    Phase 45 to enforce against, which is what makes inspect-only correct. Add a module docstring
    stating this file holds Phase 45 cross-cutting invariants and that guarded tests self-activate when
    their module lands. Do NOT modify note_classifier.py or any existing test file.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_p45_invariants.py -q -k classifier</automated>
  </verify>
  <done>Live classifier-routing invariant test exists and passes, locking the no-notes/-write-path premise (D-02).</done>
  <acceptance_criteria>
    - Test passes now against the current tree (learning -> inbox, reference -> inbox, no notes root in the map).
    - If a future edit routes learning/reference to notes/, this test fails — the guard is real.
    - No existing test file or production module is modified.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fixture test — wikilink -> path resolution rule is filename-stem (pins research Open Question 2)</name>
  <files>sentinel-core/tests/test_p45_invariants.py</files>
  <read_first>
    - .planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md (Open Question 2; Code Examples backlink matching: candidate endswith "/{target}.md")
    - sentinel-core/tests/fakes/vault.py (FakeVault: dict-backed notes, read_note/write_note/list_under)
  </read_first>
  <action>
    In test_p45_invariants.py add a test that uses `pytest.importorskip("app.services.graph_analysis")`
    at the top of the test body so it SKIPS until Plan 03 lands. It calls the module's `resolve_wikilink`
    resolver against a hand-seeded set of note paths containing a hub plus two members, asserting a bare
    `[[Member One]]` target resolves to the flat notes path whose filename stem equals the target and
    returns None for a target with no matching stem. This pins the flat-notes filename-stem rule (Pattern 3
    guarantees unique stems) that RESEARCH could not verify against a live Obsidian instance. The exact
    resolver contract is `resolve_wikilink(target: str, note_paths) -> str | None`, which Plan 03 provides.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_p45_invariants.py -q -k wikilink</automated>
  </verify>
  <done>Wikilink -> path fixture is installed and skips cleanly (graph_analysis absent), ready to auto-activate at Plan 03.</done>
  <acceptance_criteria>
    - Test is collected and SKIPS cleanly now (graph_analysis absent) — it does not error the suite.
    - After Plan 03 lands graph_analysis.resolve_wikilink, this test runs and must pass (filename-stem match, None on miss).
    - Full suite still reports the 473-passed floor with the skip counted, never a collection error.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fixture test — attaching a 2nd hub member preserves the trailing _schema block position (RESEARCH landmine)</name>
  <files>sentinel-core/tests/test_p45_invariants.py</files>
  <read_first>
    - .planning/phases/45-note-quality-schema-graph-analysis/45-RESEARCH.md (Common Pitfall 1 + Anti-Patterns: blind patch_append pushes the wikilink after the trailing block)
    - sentinel-core/tests/fakes/vault.py (FakeVault async read_note/write_note)
  </read_first>
  <action>
    In test_p45_invariants.py add an async test guarded by `pytest.importorskip("app.services.moc_maintenance")`
    so it SKIPS until Plan 05 lands. Seed a FakeVault with a hub note at a deterministic notes path whose body
    ends in a trailing fenced _schema block (type hub). Call the module's `attach_to_hub(vault, hub_path, member_slug)`
    to add a second member, then assert the resulting hub body, after rstrip, still ends with the fenced block
    close marker (three backticks) — proving the member wikilink was inserted BEFORE the trailing block, not
    appended after it. Also assert the member wikilink text is present exactly once. This characterizes the
    single highest-risk behavior in the phase.
  </action>
  <verify>
    <automated>cd sentinel-core && .venv/bin/python -m pytest tests/test_p45_invariants.py -q -k trailing</automated>
  </verify>
  <done>Trailing-_schema-block invariant fixture is installed and skips cleanly (moc_maintenance absent), ready to auto-activate at Plan 05.</done>
  <acceptance_criteria>
    - Test is collected and SKIPS cleanly now (moc_maintenance absent).
    - After Plan 05 lands moc_maintenance.attach_to_hub, this test runs and must pass (block stays terminal; member appears once).
    - Full suite reports 473-passed floor with skips counted; no collection error introduced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> FakeVault | In-memory dict double; no live vault, no network, no untrusted external input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Tampering | test scaffold masking real regressions | low | mitigate | Guarded tests use `importorskip` (skip), never a bare early-return/pass — a genuine skip is visible in pytest output, not a silent green |
| T-45-02 | Denial of Service | collection error breaks whole suite | medium | mitigate | `importorskip` at test-body top guarantees clean skip, never an ImportError at collection time that would red the 473 baseline |
</threat_model>

<verification>
- `cd sentinel-core && .venv/bin/python -m pytest tests/test_p45_invariants.py -q` → 1 passed, 2 skipped.
- `cd sentinel-core && .venv/bin/python -m pytest tests/ -q` → 473+ passed, 12+ skipped, zero errors (baseline intact, two new skips added).
</verification>

<success_criteria>
- test_p45_invariants.py exists with exactly three tests: one live characterizing (classifier routing) + two importorskip fixtures (wikilink resolution, trailing-block preservation).
- No production module or existing test is modified.
- Full suite green with the two new skips; no collection errors.
</success_criteria>

<output>
Create `.planning/phases/45-note-quality-schema-graph-analysis/45-01-SUMMARY.md` when done.
</output>
