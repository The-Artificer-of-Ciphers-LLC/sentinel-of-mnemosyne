- 2026-05-07 (plan 37-12): test_foundry.py::test_roll_event_accepted/test_notify_dispatched/test_llm_fallback fail with `NameError: name 'get_profile' is not defined` at modules/pathfinder/app/routes/foundry.py:106. Pre-existing on main (verified via git stash). Out of scope for plan 37-12; surface to operator for separate fix.
- 2026-05-07 (plan 37-14): test_registration.py::test_registration_payload_has_16_routes asserts the legacy Phase 35 route count (16) but REGISTRATION_PAYLOAD now lists 29 routes after Phases 36 + 37 added /npcs/, /npcs/{slug}/foundry-actor, /player/* (8 routes), /foundry/messages/import. Stale assertion — pre-existing on main (verified via git stash). Out of scope for plan 37-14; needs an operator-authorized test update (Test-Rewrite Ban applies — surface for explicit consent).
