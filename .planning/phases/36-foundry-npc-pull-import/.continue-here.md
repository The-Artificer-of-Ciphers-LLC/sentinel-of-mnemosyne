---
context: phase
phase: 36-foundry-npc-pull-import
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-27T00:13:59Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: ROADMAP.md bash protection — never use `cp $backup .planning/ROADMAP.md` in bash. Use Edit/Write tools to update ROADMAP.md in-place. The PreToolUse hook blocks any bash cp that overwrites this file.
- [ ] CONSTRAINT: Worktree `worktree-agent-a0ab92b421918d07a` is STILL LIVE — do not delete or create a new Phase 36 worktree. Smoke test must happen before merging.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| ROADMAP.md bash cp blocked | Worktree merge template includes `cp $ROADMAP_BACKUP .planning/ROADMAP.md`. Blocked by PreToolUse hook. | blocking | Skip the ROADMAP bash cp block entirely. Use `git merge` directly, then verify ROADMAP.md is intact with `ls -la`. |
| %2F slug test gives 404 not 400 | Starlette decodes %2F→/ before routing → 404, not 400. | advisory | Use `INVALID_SLUG` (uppercase/underscore) to exercise the [a-z0-9-] guard instead. |
| python3 -m pytest fails | System Python lacks rapidfuzz and other module deps. | advisory | Always run `uv run python3 -m pytest` inside `modules/pathfinder/`. |

<current_state>
Phase 36 is 2/3 plans complete. Plans 01 (RED stubs) and 02 (Python backend) are merged to main. Plan 03 (JS frontend) is at a human-verify checkpoint — worktree is live with 2 committed tasks, waiting for Foundry smoke test.

**2026-04-26 session added quick-task fixes (not Phase 36):**
- Discord /sen try/except guard (thinking indicator always resolves on error)
- pf2e keyword fast-path classifier + garble sanity gate + poisoned cache deleted
- Vault search score threshold 0.5 (2nd brain cross-domain hallucination fix)
- Model profile library + stop sequences wired into all litellm call sites (both containers rebuilt)
</current_state>

<completed_work>

- Plan 36-01 COMPLETE: 7 RED TDD stubs in `modules/pathfinder/tests/test_npcs.py` (commit f5dcd58)
- Plan 36-02 COMPLETE: `routes/npcs.py` + `main.py` wiring, 7 tests GREEN (commit 540c5f1)
- Plan 36-03 Task 1: `SentinelNpcImporter` class + `renderActorDirectory` hook (commit 0ee8b70 in worktree)
- Plan 36-03 Task 2: `module.json` bumped to 1.1.0, `sentinel-connector.zip` regenerated (commit df26f7e in worktree)
</completed_work>

<remaining_work>

- **IMMEDIATE**: Human Foundry smoke test (6 steps below) — type `approved` to unblock
- Merge `worktree-agent-a0ab92b421918d07a` → main (no-ff, skip ROADMAP bash cp)
- Create `36-03-SUMMARY.md`
- Run `/gsd-verify-work` for Phase 36
- Mark Phase 36 complete in ROADMAP.md (Edit tool only)
- Update STATE.md milestone progress
</remaining_work>

<decisions_made>

- FVT-04f uses `INVALID_SLUG` not `..%2Fetc%2Fpasswd` — Starlette 404 vs 400 routing quirk
- Tests run via `uv run python3 -m pytest` — rapidfuzz in uv venv only
- Worktree merge skips ROADMAP.md bash backup/restore — hook blocks it
- resolve_model prefix guard → `startswith("openai/")` — family slashes in IDs fooled old guard
- Vault search score threshold = 0.5
- model_profiles.py lives separately in both containers (no shared volume)
</decisions_made>

<blockers>

- **Human smoke test required** before merging worktree — needs running Foundry + live Sentinel stack
</blockers>

## Foundry Smoke Test (6 Steps)

1. Install `sentinel-connector.zip` into Foundry (Add-on Modules → Install Module → File)
2. Enable the module in your world
3. Open the Actors directory
4. Confirm **"Import from Sentinel"** button appears in the directory header
5. Click it — a dialog should list NPCs from the pf2e module
6. Select one NPC and import — confirm actor appears in Foundry with correct data

Type **`approved`** to proceed with merge, or describe any failures.

## Required Reading (in order)

1. `.planning/phases/36-foundry-npc-pull-import/36-03-PLAN.md` — full Wave 2 JS plan
2. `.planning/HANDOFF.json` — structured state

## Infrastructure State

- `sentinel-core` — Up, healthy. Stop sequences `["<|im_end|>", "<|endoftext|>"]` active for qwen2.5-coder-14b.
- `pf2e-module` — Up, healthy. Keyword fast-path + sanity gate active.
- `discord` — Up, connected to Gateway.
- Worktree `worktree-agent-a0ab92b421918d07a` — **LOCKED, LIVE** at `.claude/worktrees/agent-a0ab92b421918d07a`

<context>
2026-04-26 session was entirely quick tasks and hot fixes — no Phase 36 execution. Phase state is exactly as the previous session left it: worktree live, smoke test pending. All today's fixes are committed and running in rebuilt containers.
</context>

<next_action>
Start with: Run the 6-step Foundry smoke test. Type 'approved' on success. On approval: `git merge worktree-agent-a0ab92b421918d07a --no-ff --no-edit -m "chore: merge phase 36-03 worktree"` — then create 36-03-SUMMARY.md, run verifier, close phase.
</next_action>
