---
context: phase
phase: 36-foundry-npc-pull-import
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-26T09:45:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.

- [ ] CONSTRAINT: ROADMAP.md bash protection — never use `cp $backup .planning/ROADMAP.md` in bash. Use Edit/Write tools to update ROADMAP.md in-place. The PreToolUse hook blocks any bash cp that overwrites this file.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| ROADMAP.md bash cp blocked | Worktree merge template includes `cp $ROADMAP_BACKUP .planning/ROADMAP.md`. This is blocked by the PreToolUse hook. Executors are told not to touch ROADMAP.md, so just skip the restore step entirely. | blocking | Before any worktree merge, remove the ROADMAP backup/restore bash block. Use `git merge` directly, then verify ROADMAP.md is intact with `ls -la`. |
| %2F slug test gives 404 not 400 | Starlette decodes %2F→/ before routing. `/npcs/..%2Fetc%2Fpasswd/foundry-actor` becomes a path non-match → 404, not the expected 400 from the slugify guard. | advisory | Test path-traversal guard with non-slash invalid chars (e.g. UPPERCASE, underscore, dot). The guard `slugify(slug) != slug → 400` works for all chars outside [a-z0-9-]. |
| python3 -m pytest fails | System Python lacks rapidfuzz and other module deps. Tests fail with ModuleNotFoundError. | advisory | Always run `uv run python3 -m pytest` inside `modules/pathfinder/`. |

<current_state>
Phase 36 execution is 2/3 plans complete. Wave 0 (RED tests) and Wave 1 (Python backend) are merged to main. Wave 2 (Foundry JS) is at a human-verify checkpoint.

Worktree `worktree-agent-a0ab92b421918d07a` is still live at `.claude/worktrees/agent-a0ab92b421918d07a` with 2 committed tasks. The updated `sentinel-connector.zip` is ready.

Waiting for human to run the Foundry smoke test and type "approved".
</current_state>

<completed_work>

- **Plan 36-01 (Wave 0):** `modules/pathfinder/tests/test_npcs.py` — 7 RED test stubs for FVT-04a..f + REGISTRATION_PAYLOAD guard. Merged to main (commit f5dcd58).
- **Plan 36-02 (Wave 1):** `app/routes/npcs.py` created; `app/main.py` updated (CORS GET fix, imports, REGISTRATION_PAYLOAD, lifespan wiring, router registration). All 7 tests GREEN. Merged to main (commit 540c5f1).
- **Plan 36-03 Task 1:** `sentinel-connector.js` extended with `SentinelNpcImporter` ApplicationV2 class + `renderActorDirectory` hook. Syntax check passes. (commit 0ee8b70, in worktree)
- **Plan 36-03 Task 2:** `module.json` bumped to 1.1.0. `sentinel-connector.zip` regenerated. (commit df26f7e, in worktree)
</completed_work>

<remaining_work>

- **Plan 36-03 Task 3:** Human verify checkpoint — Foundry smoke test (6 steps below)
- **After approval:** Merge worktree → main, create 36-03-SUMMARY.md, run phase verifier, mark phase 36 complete in ROADMAP.md
</remaining_work>

<decisions_made>

- FVT-04f test: use `INVALID_SLUG` (uppercase+underscore) instead of `..%2Fetc%2Fpasswd` — Starlette path normalization makes %2F-encoded traversal untestable via httpx
- Test runner: always `uv run python3 -m pytest` in `modules/pathfinder/`
- Worktree merge: skip ROADMAP.md bash backup/restore (hook-protected; executor didn't modify it)
</decisions_made>

<blockers>

- Human Foundry smoke test required before plan 36-03 can complete
</blockers>

## Required Reading (in order)
1. `.planning/phases/36-foundry-npc-pull-import/36-03-PLAN.md` — full plan including human checkpoint spec
2. `.planning/phases/36-foundry-npc-pull-import/36-02-SUMMARY.md` — Wave 1 completion + FVT-04f deviation note

## Infrastructure State
- **Worktree:** `.claude/worktrees/agent-a0ab92b421918d07a` — branch `worktree-agent-a0ab92b421918d07a`, locked, 2 committed tasks
- **Main branch:** HEAD is 540c5f1 (Wave 1 merged)
- **Test suite:** `uv run python3 -m pytest tests/test_npcs.py` → 7 passed

## Human Checkpoint — Foundry Smoke Test

**Pre-conditions:** `docker compose --profile pf2e up`, Obsidian running, ≥1 NPC in `mnemosyne/pf2e/npcs/`.

**Step 1 — Backend smoke:**
```bash
curl -s -H "X-Sentinel-Key: YOUR_KEY" http://localhost:8000/modules/pathfinder/npcs/ | python3 -m json.tool
```
Expected: JSON array with `{name, slug, level, ancestry}`.

**Step 2 — Install module:** Use zip at `modules/pathfinder/foundry-client/sentinel-connector.zip` or manifest URL. Enable in world.

**Step 3 — Import button visible:** Actors tab → "Import from Sentinel" button in header row.

**Step 4 — Full import flow:** Click → dialog with NPC `<select>` → Import → actor created.

**Step 5 — Duplicate handling:** Re-import same NPC → `Dialog.confirm` → Yes updates, No creates copy.

**Step 6 — Offline handling:** Stop Sentinel → dialog shows "No NPCs found. Is Sentinel running?" + Retry.

Type **"approved"** when all steps pass, or describe failures.

## On "approved" — Resume Steps

1. Merge worktree (skip ROADMAP bash cp):
   ```bash
   git merge worktree-agent-a0ab92b421918d07a --no-ff --no-edit -m "chore: merge executor worktree (worktree-agent-a0ab92b421918d07a)"
   git worktree remove .claude/worktrees/agent-a0ab92b421918d07a --force --force
   git branch -D worktree-agent-a0ab92b421918d07a
   ```
2. Verify ROADMAP.md still exists: `ls -la .planning/ROADMAP.md`
3. Create `.planning/phases/36-foundry-npc-pull-import/36-03-SUMMARY.md`
4. Run phase verifier: spawn `gsd-verifier` agent for phase 36
5. Update ROADMAP.md phase 36 status → complete (use Edit tool, not bash cp)
6. Update STATE.md

<context>
Clean TDD execution: Wave 0 RED → Wave 1 GREEN → Wave 2 JS frontend. The only notable issue was that %2F-encoded path traversal can't be tested through the httpx+Starlette stack because Starlette normalizes %2F to / before routing (the guard still works in production — any traversal slug either fails routing as 404 or fails slugify as 400). Wave 2 tasks are committed and syntactically valid; just waiting on the human smoke test.
</context>

<next_action>
Start with: Present the human checkpoint above to the user. Wait for "approved" or failure description. Then merge the worktree using the steps in "On approved" above — skipping all ROADMAP.md bash cp operations.
</next_action>
