---
phase: 44-vault-namespace-taxonomy-foundation
plan: 03
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - sentinel-core/app/services/recall.py
  - sentinel-core/app/services/note_intake.py
  - sentinel-core/app/services/message_processing.py
  - sentinel-core/app/routes/message.py
  - sentinel-core/tests/test_message.py
autonomous: true
requirements: [VAULT-02, VAULT-04]
must_haves:
  truths:
    - "_WARM_TIER_EXCLUDE_PREFIXES no longer survives as a second, drifting duplicate of RecallConfig.exclude_prefixes — its consumers reference the single source of truth (which already includes inbox/)"
    - "note_intake.classify_and_apply no longer redirects a chat-classified note to a journal path when its destination is excluded — after D-03 the redirect target (ops/journal) is itself excluded, so the 'guaranteed searchable' contract is structurally unsatisfiable and is retired"
    - "routes/message.py _safe_file_chat_note no longer promises a warm-searchable redirect; chat-derived notes file to their classified destination and are recalled via the session/episodic path (same accepted latency trade-off as :capture/:seed, D-02)"
    - "the two affected test_message.py tests assert the new, actually-true post-D-03 behavior rather than passing against a stale precondition"
    - "cd sentinel-core && pytest stays green at 471 collected"
  artifacts:
    - "sentinel-core/app/services/recall.py — _WARM_TIER_EXCLUDE_PREFIXES reconciled (deleted or repointed to RecallConfig.exclude_prefixes)"
    - "sentinel-core/app/services/note_intake.py — searchable_only redirect block removed; stale import dropped"
    - "sentinel-core/app/services/message_processing.py — re-export reconciled"
    - "sentinel-core/app/routes/message.py — _safe_file_chat_note redirect retired"
    - "tests/test_message.py — test_chat_note_path_passes_warm_tier_exclusion_filter + test_observation_topic_chat_note_redirected_to_searchable_path rewritten"
  key_links:
    - "This closes the SECOND dual-source-of-truth (D-03b) that lives right next to the one plan 44-01 fixed — Pitfall 3 root cause"
    - "Depends on 44-01: the redirect only becomes structurally unsatisfiable AFTER the D-03 reroute makes every classifier topic resolve to an ops/- or inbox/-prefixed destination"
    - "Decision recorded explicitly (mirrors D-05 framing): retire the redirect, accept the D-02 latency window for chat-auto-filed learning/reference content"
---

<objective>
Resolve the Pitfall 3 consequence of the D-03 reroute (landed in 44-01): `message.py`/`note_intake.py`'s "guaranteed searchable" chat-note redirect is now structurally unsatisfiable — after the reroute every classifier topic resolves to an excluded destination, including the `journal` redirect target itself (now `ops/journal/...`). Retire the futile redirect (D-06) and reconcile the second, stale warm-tier exclusion list (`_WARM_TIER_EXCLUDE_PREFIXES`, which is missing `inbox/`) with the single source of truth `RecallConfig.exclude_prefixes`. Update the two `test_message.py` tests that currently keep passing against the stale precondition so they assert real post-reroute behavior.

Purpose: make the warm-tier-exclusion check reflect reality and remove a redirect that can no longer deliver its promise, recording the accepted trade-off explicitly rather than leaving it as a silent side effect.
Output: reconciled exclusion list, retired redirect, updated tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-CONTEXT.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-RESEARCH.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-PATTERNS.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reconcile _WARM_TIER_EXCLUDE_PREFIXES with RecallConfig.exclude_prefixes (D-03b)</name>
  <files>sentinel-core/app/services/recall.py, sentinel-core/app/services/message_processing.py, sentinel-core/app/services/note_intake.py</files>
  <read_first>
    - 44-RESEARCH.md "Additional seams discovered this session" (the _WARM_TIER_EXCLUDE_PREFIXES second-duplicate finding: recall.py definition, message_processing.py re-export, note_intake.py + test consumers) and Common Pitfall #3
    - 44-PATTERNS.md recall.py section (reconciliation target: delete _WARM_TIER_EXCLUDE_PREFIXES and repoint consumers to RecallConfig.exclude_prefixes; prefer deletion)
    - sentinel-core/app/services/recall.py — _WARM_TIER_EXCLUDE_PREFIXES and RecallConfig.exclude_prefixes
    - sentinel-core/app/services/message_processing.py — the _WARM_TIER_EXCLUDE_PREFIXES re-export
    - sentinel-core/app/services/note_intake.py — the _WARM_TIER_EXCLUDE_PREFIXES import and its use in the searchable_only guard
  </read_first>
  <behavior>
    - The warm-tier exclusion prefixes used anywhere in the codebase are a single value that includes inbox/ (i.e. matches RecallConfig.exclude_prefixes) — no code path still consults a tuple missing inbox/
    - No import or re-export of a stale _WARM_TIER_EXCLUDE_PREFIXES tuple survives (either deleted with consumers repointed, or aliased to RecallConfig.exclude_prefixes)
  </behavior>
  <action>
    Eliminate the second dual-source-of-truth: delete _WARM_TIER_EXCLUDE_PREFIXES from recall.py and repoint its consumers (the message_processing.py re-export and the note_intake.py import) at the single canonical value, RecallConfig.exclude_prefixes (which already includes ops/, _trash/, self/, inbox/). If a named symbol must stay importable for external stability, alias it to the RecallConfig default rather than keeping a second literal tuple. This is the same structural fix as plan 44-01's carrier removal, applied to the sibling drift the research found next to it. Do this reconciliation together with the redirect retirement in Task 2 (the note_intake searchable_only guard is the primary consumer) so no wave leaves a NameError.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_recall.py tests/test_message.py -q</automated>
  </verify>
  <acceptance_criteria>
    - No code path consults a warm-tier exclusion set missing inbox/
    - message_processing.py and note_intake.py resolve their exclusion prefixes from RecallConfig.exclude_prefixes (or a same-valued alias)
    - Suite green after the reconciliation (no NameError/import breakage)
  </acceptance_criteria>
  <done>_WARM_TIER_EXCLUDE_PREFIXES no longer survives as a stale duplicate; all consumers resolve warm-tier exclusion from RecallConfig.exclude_prefixes (includes inbox/); suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire the _safe_file_chat_note searchable-only redirect + update the two message tests (D-06)</name>
  <files>sentinel-core/app/services/note_intake.py, sentinel-core/app/routes/message.py, sentinel-core/tests/test_message.py</files>
  <read_first>
    - 44-CONTEXT.md D-06 (retire _safe_file_chat_note redirect + fix the stale test)
    - 44-RESEARCH.md Common Pitfall #3 (the redirect target is now excluded; the stale test keeps passing against _WARM_TIER_EXCLUDE_PREFIXES) and Open Question #1 (recommendation: retire, accept D-02 latency, record explicitly)
    - 44-PATTERNS.md note_intake.py section (_safe_file_chat_note / searchable_only redirect block to retire, with the current logic excerpt)
    - sentinel-core/app/services/note_intake.py — classify_and_apply searchable_only guard
    - sentinel-core/app/routes/message.py — _safe_file_chat_note, _queue_chat_note_write
    - sentinel-core/tests/test_message.py — test_chat_note_path_passes_warm_tier_exclusion_filter, test_observation_topic_chat_note_redirected_to_searchable_path
  </read_first>
  <behavior>
    - A chat message classified as learning/reference files to its classified destination (inbox/) with no redirect to a journal path; the note is recalled via the session/episodic path, not force-routed to a warm location
    - test_chat_note_path_passes_warm_tier_exclusion_filter and test_observation_topic_chat_note_redirected_to_searchable_path assert the new behavior (no journal redirect; destination is the classified path) rather than the retired guarantee
    - Chat-note filing still succeeds (no exception) and still embeds via the D-02 sweeper path on the next cycle — the accepted latency trade-off is the same one D-02/D-05 already accept
  </behavior>
  <action>
    Remove the searchable_only redirect block in note_intake.classify_and_apply (the branch that, when the resolved destination is warm-tier-excluded, redirects to a journal path) — after the D-03 reroute the journal target is itself excluded, so the redirect can no longer produce a searchable result for any topic. Retire the corresponding "guaranteed searchable" promise in routes/message.py's _safe_file_chat_note: chat-derived notes file to their classified destination (episodic ops/journal/ or staged inbox/) and are recalled via the session/episodic path. Drop the now-unused _WARM_TIER_EXCLUDE_PREFIXES import from note_intake.py (reconciled in Task 1). Keep the rest of the chat-note write flow (_queue_chat_note_write background write) intact.

    Rewrite the two affected test_message.py tests to assert the new, actually-true behavior (no journal redirect; the written path is the classified destination) instead of continuing to pass against the stale _WARM_TIER_EXCLUDE_PREFIXES precondition. Record the decision in the SUMMARY (mirrors D-05's "accepted, not silent" framing): the redirect is retired and chat-auto-filed learning/reference content follows the same one-sweep-cycle embedding latency as :capture/:seed.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_message.py -k "chat_note or searchable or redirect" -q</automated>
  </verify>
  <acceptance_criteria>
    - The searchable_only journal-redirect branch is gone from note_intake.classify_and_apply
    - _safe_file_chat_note no longer promises a warm-searchable redirect
    - Both rewritten test_message.py tests pass asserting the classified-destination behavior
    - Chat-note filing still succeeds; no NameError from the dropped import
  </acceptance_criteria>
  <done>The searchable_only journal-redirect is removed from note_intake.classify_and_apply and _safe_file_chat_note; both test_message.py tests assert the new classified-destination behavior; decision recorded in the SUMMARY.</done>
</task>

</tasks>

<artifacts_produced>
New symbols / files this plan produces: none new — this plan removes/reconciles.
Symbols removed: recall._WARM_TIER_EXCLUDE_PREFIXES (deleted or aliased to RecallConfig.exclude_prefixes); the searchable_only journal-redirect branch in note_intake.classify_and_apply; the "guaranteed searchable" redirect in routes/message.py _safe_file_chat_note.
Tests rewritten: tests/test_message.py::test_chat_note_path_passes_warm_tier_exclusion_filter, ::test_observation_topic_chat_note_redirected_to_searchable_path.
No new external packages. No new modules.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat message content -> classified vault destination | Auto-classified chat notes now file to their true destination (inbox/ or ops/journal/) with no redirect; no new external input |
| warm-tier exclusion check -> recall | The exclusion set is now single-sourced and includes inbox/ — the check reflects reality |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-03-01 | Information Disclosure | warm-tier exclusion single-sourcing | low | mitigate | Reconciling to RecallConfig.exclude_prefixes makes the exclusion set STRICTER (adds inbox/), never looser; self/ and ops/ remain excluded. No content becomes newly reachable. |
| T-44-03-02 | Repudiation / silent behavior change | retired chat-note redirect | low | mitigate | Decision recorded explicitly in the SUMMARY (mirrors D-05): chat-auto-filed learning/reference content follows the accepted D-02 embedding-latency window rather than a now-impossible warm redirect. Tests assert the new behavior so it cannot silently drift back. |
| T-44-03-SC | Tampering | pip installs | n/a | accept | No package installs (pure internal refactor). |

Block-on: HIGH. No high-severity threats — this plan tightens the exclusion set and removes a dead code path; it adds no external input, auth surface, or new write path.
</threat_model>

<verification>
- Per task: keyword-scoped pytest in each <verify>.
- Plan boundary (SC-5 hard gate): `cd sentinel-core && pytest` stays green at 471 collected; MEM-01..MEM-09 unaffected.
- Confirm no stale exclusion tuple remains anywhere by running the full suite (a NameError or a passing-but-stale test would surface).
</verification>

<success_criteria>
- Second dual-source-of-truth (_WARM_TIER_EXCLUDE_PREFIXES) eliminated; consumers single-sourced
- Chat-note searchable redirect retired; the two tests assert real behavior
- Decision recorded explicitly in the SUMMARY (accepted trade-off, not silent)
- Full suite green (471)
</success_criteria>

<output>
Create `.planning/phases/44-vault-namespace-taxonomy-foundation/44-03-SUMMARY.md` when done.
</output>
