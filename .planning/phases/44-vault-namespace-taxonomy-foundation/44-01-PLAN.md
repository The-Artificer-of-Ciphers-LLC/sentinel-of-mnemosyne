---
phase: 44-vault-namespace-taxonomy-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentinel-core/app/services/note_classifier.py
  - sentinel-core/app/services/note_intake.py
  - sentinel-core/app/services/vault_sweep_plan.py
  - sentinel-core/app/services/recall.py
  - sentinel-core/tests/test_note_classifier.py
  - sentinel-core/tests/test_vault_sweep_plan.py
  - sentinel-core/tests/test_recall.py
  - .planning/v0.6.0-REGRESSION-LEDGER.md
autonomous: true
requirements: [VAULT-02, VAULT-03]
must_haves:
  truths:
    - "New learning/reference captures route to inbox/; new journal captures route to ops/journal/{YYYY-MM-DD}/; new accomplishment captures route to ops/accomplishments/ (per D-03 AFTER table)"
    - "topic_dir_for('journal', today=D) returns 'ops/journal/D' derived from TOPIC_VAULT_PATH['journal'], not a hardcoded 'journal/' literal (Pitfall 1 closed)"
    - "note_intake._topic_target_path derives the journal path from the dict base, not a hardcoded 'journal/' literal (Pitfall 1 closed, second site)"
    - "vault_sweep_plan.is_in_topic_dir no longer conflates ops/journal, ops/accomplishments, ops/observations into one 'ops/' family (Pitfall 2 closed)"
    - "Warm-tier recency weighting no longer applies via _CARRIER_NAMESPACE_PREFIXES; only typed Session summaries are recency-ordered (D-01, MEM-09 end state)"
    - "cd sentinel-core && pytest stays green at 471 collected; MEM-01..MEM-09 characterization tests unaffected"
  artifacts:
    - "sentinel-core/app/services/note_classifier.py — TOPIC_VAULT_PATH rerouted + topic_dir_for journal-derive"
    - "sentinel-core/app/services/note_intake.py — _topic_target_path journal-derive"
    - "sentinel-core/app/services/vault_sweep_plan.py — is_in_topic_dir taxonomy-aware"
    - "sentinel-core/app/services/recall.py — _CARRIER_NAMESPACE_PREFIXES + reweight block + _path_date removed"
    - ".planning/v0.6.0-REGRESSION-LEDGER.md — MEM-01..09 contract + D-05 accepted transient"
    - "tests/test_note_classifier.py::test_topic_dir_for_journal_derives_from_dict"
    - "tests/test_vault_sweep_plan.py::test_is_in_topic_dir_does_not_conflate_ops_subdirs"
    - "tests/test_recall.py::test_recency_applies_only_to_session_summaries"
  key_links:
    - "recall.py no longer maintains a duplicate hand-kept taxonomy tuple — Pitfall 2 dual-source-of-truth root cause structurally closed (D-03b)"
    - "TOPIC_VAULT_PATH['journal'] value actually reaches the computed journal path — both journal f-string literals derive from `base`"
    - "recency_weight() itself is KEPT (still used by _hot_sessions session sort, MEM-09 place a)"
---

<objective>
Land the PARA taxonomy reroute (VAULT-02, D-03) and its matching recall carrier-allowlist removal (VAULT-03, D-01) together, in one plan, so the full suite is green at the plan boundary with no silent-regression window. This is the atomic core of Phase 44: the two research-surfaced hazards that would otherwise make the reroute a silent no-op (the hardcoded `journal/` literals, Pitfall 1) or silently break the sweeper (the `is_in_topic_dir` family-root collapse, Pitfall 2) are fixed in the SAME plan as the routing-table edit. The v0.6.0 regression ledger is established here as the standing MEM-01..09 + accepted-transient (D-05) contract for every later phase boundary.

Purpose: close VAULT-02 and VAULT-03 as a single trap-and-fix unit — the taxonomy move and the carrier-allowlist collapse never exist in separate commits.
Output: rerouted classifier, taxonomy-aware sweep-plan, carrier-free recall recency path, characterization tests, and the regression ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-CONTEXT.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-RESEARCH.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PARA reroute table + both journal literals + is_in_topic_dir family fix (D-03, Pitfall 1, Pitfall 2)</name>
  <files>sentinel-core/app/services/note_classifier.py, sentinel-core/app/services/note_intake.py, sentinel-core/app/services/vault_sweep_plan.py, sentinel-core/tests/test_note_classifier.py, sentinel-core/tests/test_vault_sweep_plan.py</files>
  <read_first>
    - 44-RESEARCH.md "Common Pitfalls" (Pitfall 1 + Pitfall 2) and "Code Examples" (the two journal-literal before/after and the is_in_topic_dir fix)
    - 44-PATTERNS.md sections for vault_sweep_plan.py (is_in_topic_dir fix excerpt) and note_intake.py (journal-literal fix excerpt)
    - .planning/research/ARCHITECTURE.md "Taxonomy routing — before and after" (the AFTER table = D-03 verbatim)
    - sentinel-core/app/services/note_classifier.py (TOPIC_VAULT_PATH, topic_dir_for, closed-vocab TopicSlug)
    - sentinel-core/app/services/note_intake.py (_topic_target_path)
    - sentinel-core/app/services/vault_sweep_plan.py (is_in_topic_dir, propose_topic_move)
  </read_first>
  <behavior>
    - test_topic_dir_for_journal_derives_from_dict: topic_dir_for("journal", today="2026-07-06") == "ops/journal/2026-07-06" (derives from the dict value; no such test exists today)
    - topic_dir_for("accomplishment") == "ops/accomplishments"; topic_dir_for("observation") == "ops/observations" (unchanged); topic_dir_for("learning") == "inbox"; topic_dir_for("reference") == "inbox"
    - _topic_target_path for topic="journal" produces "ops/journal/{today}/{slug}.md" (derived from base, not "journal/...")
    - test_is_in_topic_dir_does_not_conflate_ops_subdirs: is_in_topic_dir("ops/observations/x.md", "ops/accomplishments") is False; is_in_topic_dir("ops/accomplishments/x.md", "ops/accomplishments") is True; is_in_topic_dir("ops/journal/2026-07-06/x.md", topic_dir_for("journal", today="2026-06-01")) is True (nested-date family, any day matches)
    - Rewrite test_plan_topic_move_skips_existing_topic_family and test_plan_topic_move_describes_destination_and_reason so their src/dst expectations use the new ops/-prefixed destinations (accomplishments/a.md -> ops/accomplishments/a.md)
    - Closed 7-slug vocabulary (TopicSlug) is unchanged — only routing changes (D-03a)
  </behavior>
  <action>
    Reroute TOPIC_VAULT_PATH in note_classifier.py to the D-03 AFTER table: learning and reference both map to the inbox destination; journal maps to "ops/journal"; accomplishment maps to "ops/accomplishments"; observation stays "ops/observations"; noise stays "" (never filed); unsure stays the inbox pending-classification queue. Do NOT touch the TopicSlug closed vocabulary — this is a routing-only change (D-03a).

    In the SAME edit, fix Pitfall 1 at BOTH hardcoded call sites: in note_classifier.topic_dir_for, the journal branch must build its per-day path from the fetched `base` variable (TOPIC_VAULT_PATH["journal"]) instead of a literal top-level segment; in note_intake._topic_target_path, the journal branch must likewise build from `base`. Editing the dict alone is a proven no-op for the journal topic — both f-string templates must derive from the dict value in this commit (Execution-Ordering Hazard #1: same task/commit, both edits or neither).

    Fix Pitfall 2 in vault_sweep_plan.is_in_topic_dir: replace the single-segment family-root derivation with a taxonomy-aware form — when the topic dir is the nested-date ops/journal family, treat the ops/journal prefix as the family root so any day matches; otherwise use the full topic dir plus a trailing slash as an exact-match family root; then test the path against that family root. This stops journal/accomplishment/observation from collapsing to one indistinguishable ops/ family. Use the fix shape shown in 44-PATTERNS.md.

    Add the two new Wave-0 characterization tests named in the behavior block, and rewrite the two existing vault_sweep_plan tests whose destination expectations moved under ops/. Follow the existing sibling-test conventions in each test file (direct function-call assertions for the pure helpers; no new mocking layer). Do not weaken or delete any assertion to make it pass — a shrinking or silenced suite is a phase-gate failure (Pitfall 1 discipline).
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_note_classifier.py tests/test_vault_sweep_plan.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_topic_dir_for_journal_derives_from_dict passes (asserts ops/journal/{date} for the journal topic)
    - test_is_in_topic_dir_does_not_conflate_ops_subdirs passes
    - The two rewritten vault_sweep_plan tests pass against the new ops/-prefixed destinations
    - No `journal/`-prefixed literal remains as the journal topic's computed path — both journal branches derive from the dict base (verified by the journal test)
    - TopicSlug closed vocabulary unchanged; test_note_classifier.py vocabulary assertions still pass
  </acceptance_criteria>
  <done>PARA reroute + both journal literals + is_in_topic_dir family fix landed together; the two new characterization tests and the two rewritten sweep-plan tests pass; classifier vocabulary unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the carrier-namespace recency allowlist from recall (D-01, D-01a, VAULT-03)</name>
  <files>sentinel-core/app/services/recall.py, sentinel-core/tests/test_recall.py</files>
  <read_first>
    - 44-RESEARCH.md "Code Examples" (Removing the dead carrier-weighting block — before/after) and "Regression Test Surface" (the 3 recall tests to rewrite + the 1 new invariant test)
    - 44-PATTERNS.md recall.py section (D-01 removal pattern; the note that recency_weight() itself must NOT be removed)
    - sentinel-core/app/services/recall.py — _CARRIER_NAMESPACE_PREFIXES, _path_date, the _warm_search warm-tier recency block, recency_weight, _hot_sessions
    - sentinel-core/tests/test_recall.py — test_recency_warm_carrier_journal, test_recency_warm_carrier_topic_dir, test_recency_excludes_self
  </read_first>
  <behavior>
    - test_recency_applies_only_to_session_summaries: a warm result under notes/ or ops/ dated today never outranks an older warm result purely on its date (neither is a carrier; only result.sessions ordering is recency-sensitive). This asserts D-01a positively — recency weighting is never applied to a namespace by omission.
    - The three existing recency-carrier tests are rewritten (not deleted): journal/topic-dir carrier premises are invalidated by D-01/D-03c; restructure the self/session comparison around two ops/sessions-shaped Session summaries so it asserts hot-tier session recency ordering (which survives) rather than warm-carrier weighting (which is gone).
    - _hot_sessions still sorts sessions by recency_weight(session.date) descending — MEM-09 place (a) is preserved and unchanged.
  </behavior>
  <action>
    In recall._warm_search, remove the warm-tier recency reweight loop that gates on the carrier-prefix startswith check: the merged RRF output flows straight through to the deterministic sort unchanged (no per-result recency multiplier in the warm tier). Delete _CARRIER_NAMESPACE_PREFIXES and _path_date entirely — both become dead code with no other caller after this change. Do NOT remove recency_weight itself: it is still used by _hot_sessions for the hot-tier session sort (MEM-09 place a). Leave RecallConfig.exclude_prefixes and _WARM_TIER_EXCLUDE_PREFIXES untouched in this plan (the warm-tier-exclusion-list reconciliation belongs to plan 44-03, which removes the consumer that keeps _WARM_TIER_EXCLUDE_PREFIXES alive — removing it here would break the wave-1 suite).

    Add the new invariant test test_recency_applies_only_to_session_summaries and rewrite the three existing recency-carrier tests per the behavior block, using the same Recall.assemble() fixture-and-assert shape the current tests use. Preserve the T-41-08 principle in the new test: assert positively that only Session-tier items are recency-sensitive, never that "the old tuple is gone."
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_recall.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_recency_applies_only_to_session_summaries passes
    - The three rewritten recency tests pass against the sessions-only model
    - MEM-09 hot-tier session recency ordering still holds (session sort test green)
    - recency_weight remains importable and used by _hot_sessions (no NameError anywhere in the suite)
  </acceptance_criteria>
  <done>Carrier allowlist, reweight loop, and _path_date removed; recency weighting is sessions-only; the positive-invariant test and the three rewritten recency tests pass; recency_weight still used by _hot_sessions.</done>
</task>

<task type="auto">
  <name>Task 3: Establish the v0.6.0 regression ledger and record the D-05 accepted transient</name>
  <files>.planning/v0.6.0-REGRESSION-LEDGER.md</files>
  <read_first>
    - .planning/research/PITFALLS.md Pitfall 1 (establish the ledger in the first v0.6.0 phase) and "Looks Done But Isn't" checklist
    - 44-CONTEXT.md D-05 (accept the 44->47 migration-window recency-weight transient, no compat shim) and the MIG-03 requirement in .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create .planning/v0.6.0-REGRESSION-LEDGER.md as the standing MEM-0x + command-surface regression contract that every v0.6.0 phase boundary is graded against (Pitfall 1 discipline; this is the artifact MIG-03 verifies in Phase 47, established now). Record: (1) the MEM-01..MEM-09 requirement set as the hot gate — each must stay green at every phase boundary; (2) the full-suite baseline (cd sentinel-core && pytest, 471 collected this session) with the rule that a shrinking test count is a phase-gate failure, not a pass; (3) the D-05 accepted behavior change — between Phase 44 and Phase 47, existing top-level journal/ and accomplishments/ notes remain warm-recallable but lose recency weighting immediately because D-01 removed the carrier allowlist; this is deliberate, aligns with MEM-09, self-heals when Phase 47 migrates them under ops/, and is explicitly NOT a silent regression and NOT compensated by any throwaway shim. Keep the ledger concise and append-only in shape so later phases add their boundary check-ins.
  </action>
  <verify>
    <automated>test -f .planning/v0.6.0-REGRESSION-LEDGER.md && grep -q 'MEM-09' .planning/v0.6.0-REGRESSION-LEDGER.md && grep -q 'D-05' .planning/v0.6.0-REGRESSION-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/v0.6.0-REGRESSION-LEDGER.md exists
    - It names the MEM-01..MEM-09 contract and the full-suite baseline as the hot gate
    - It records the D-05 accepted recency-weighting transient as a known, accepted behavior change (no compat shim)
  </acceptance_criteria>
  <done>.planning/v0.6.0-REGRESSION-LEDGER.md exists and records the MEM-01..09 contract, the full-suite baseline gate, and the D-05 accepted transient.</done>
</task>

</tasks>

<artifacts_produced>
New symbols / files this plan produces:
- Test: tests/test_note_classifier.py::test_topic_dir_for_journal_derives_from_dict
- Test: tests/test_vault_sweep_plan.py::test_is_in_topic_dir_does_not_conflate_ops_subdirs
- Test: tests/test_recall.py::test_recency_applies_only_to_session_summaries
- Doc: .planning/v0.6.0-REGRESSION-LEDGER.md
Symbols removed (dead code, D-01): recall._CARRIER_NAMESPACE_PREFIXES, recall._path_date, the warm-tier carrier reweight loop in recall._warm_search.
No new external packages. No new modules.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classifier LLM output -> TOPIC_VAULT_PATH routing | LLM-assigned topic selects a vault destination; closed 7-slug vocabulary bounds the output space (unchanged) |
| vault note content -> recall warm tier | notes read back into a prompt are DATA, never instructions (pre-existing Sentinel principle, unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-01-01 | Tampering | note_classifier journal path routing | low | mitigate | Closed 7-slug vocabulary unchanged (D-03a); routing values are planner-fixed constants, not user-influenced. Both journal literals now derive from the dict so a single reviewed source governs the destination. |
| T-44-01-02 | Information Disclosure | recall recency path | low | accept | Removing carrier weighting is a ranking change only; self/ and ops/ remain excluded from warm recall (exclude_prefixes untouched here). No content becomes newly reachable. |
| T-44-01-SC | Tampering | pip installs | n/a | accept | No package installs in this plan or phase (pure internal refactor; 44-RESEARCH Package Legitimacy Audit = not applicable). |

Block-on: HIGH. No high-severity threats — this plan does not add an external input, an auth surface, or a new write path.
</threat_model>

<verification>
- Per task: run the keyword-scoped pytest command in each task's <verify>.
- Plan boundary (SC-5 hard gate): `cd sentinel-core && pytest` stays green at 471 collected; MEM-01..MEM-09 characterization tests green. A dropped test count is a failure.
- Confirm no `NameError`/import breakage from the carrier/`_path_date` removal by running the full suite.
</verification>

<success_criteria>
- VAULT-02: new learning/reference route to inbox/, journal/accomplishment route under ops/, verified by the classifier + sweep-plan tests
- VAULT-03: warm-tier recency weighting is sessions-only; the positive-invariant test passes
- Pitfall 1 and Pitfall 2 both closed with dedicated tests
- Full suite green (471); MEM-01..09 unaffected
- Regression ledger established with the D-05 transient recorded
</success_criteria>

<output>
Create `.planning/phases/44-vault-namespace-taxonomy-foundation/44-01-SUMMARY.md` when done.
</output>
