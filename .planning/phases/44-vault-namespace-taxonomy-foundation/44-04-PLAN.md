---
phase: 44-vault-namespace-taxonomy-foundation
plan: 04
type: execute
wave: 3
depends_on: [44-03]
files_modified:
  - sentinel-core/app/services/recall.py
  - sentinel-core/tests/test_recall.py
  - sentinel-core/tests/test_message.py
autonomous: true
requirements: [VAULT-01, VAULT-05]
must_haves:
  truths:
    - "When any of self/identity.md, self/methodology.md, self/goals.md, self/relationships.md is missing or empty at self-context gather time, a minimal seeded guiding stub is created via the REST-only read-then-conditionally-write shape (D-04, D-14 lazy-create)"
    - "The existing every-message self/ read (RecallConfig.self_paths) is preserved unchanged — it is reused, not rebuilt (D-04a: VAULT-05 is ~90% already done)"
    - "read_self_context keeps its existing read-only, silently-graceful-on-404 contract — the stub-ensure is wired at the Recall self-context call site, NOT inside read_self_context"
    - "Only the four canonical self/ files are stubbed — ops/reminders.md and self/learning-areas.md (also in self_paths) are NOT auto-created"
    - "Stub content is planner-authored, token-bounded boilerplate (read every message); it is treated as untrusted DATA on read, never as instructions"
    - "cd sentinel-core && pytest stays green at 471 collected"
  artifacts:
    - "sentinel-core/app/services/recall.py — build_self_stub(path) pure builder + self-context stub-ensure wiring"
    - "tests/test_recall.py::(self-stub creation-on-miss + no-overwrite-when-present + canonical-paths-only)"
    - "tests/test_message.py::(first-message self/ stub creation + existing self-read preserved)"
  key_links:
    - "stub-ensure composes read_note + conditional write_note at the self-context gather; read_self_context's contract is untouched"
    - "the write_note PUT is the entire lazy-create mechanism (Obsidian REST creates-if-missing) — no local-fs existence check; empty/falsy read IS the 'missing' signal"
    - "the 4-path allowlist is explicit — never iterate all of self_paths for stub creation"
---

<objective>
Close VAULT-01 (self/ stub auto-creation) and finish VAULT-05 (guaranteed session-start self/ read). VAULT-05 is ~90% already shipped: `RecallConfig.self_paths` already reads identity/methodology/goals/relationships (plus ops/reminders and self/learning-areas) into every message via `read_self_context`. The real delta is guaranteeing the four canonical `self/` files exist so the existing read never hits a missing file — via lazy, seeded, token-bounded stubs created on first read-miss (D-04, the established D-14 REST-only read-then-conditionally-write pattern). Do NOT rebuild the self-read (D-04a) and do NOT change `read_self_context`'s read-only contract.

Purpose: make the always-on self/ context self-healing — a missing canonical self/ file is created as a minimal guiding stub the first time it is read, with zero eager boot-time writes.
Output: a pure stub builder, the stub-ensure wiring at the self-context gather, and unit + message-path tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-CONTEXT.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-RESEARCH.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-PATTERNS.md
@.planning/phases/44-vault-namespace-taxonomy-foundation/44-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_self_stub pure builder + stub-ensure wiring at the self-context gather (D-04, D-04a)</name>
  <files>sentinel-core/app/services/recall.py, sentinel-core/tests/test_recall.py</files>
  <read_first>
    - 44-PATTERNS.md "D-14 lazy-create-if-missing via REST PUT" (note_intake.classify_and_apply INBOX_PATH read-then-conditionally-write excerpt; build_initial_inbox as the pure-builder precedent) and the "Where to wire it" guidance (self-context gather, NOT read_self_context)
    - 44-RESEARCH.md Pattern 2 (D-14 lazy stub creation) and the "Important distinction from read_self_context" note
    - 44-CONTEXT.md D-04 and D-04a (do not rebuild the self-read; the delta is guaranteeing the four files exist; token-bounded stub content is Claude's discretion)
    - sentinel-core/app/services/recall.py — RecallConfig.self_paths, _hot_self (the self-context gather), and how self-context is read today
    - sentinel-core/app/services/inbox.py — build_initial_inbox (pure-builder shape to mirror)
    - sentinel-core/app/vault.py — read_self_context (read-only, graceful-404; do NOT modify), write_note (unconditional PUT = the lazy-create primitive)
  </read_first>
  <behavior>
    - build_self_stub("self/identity.md") returns non-empty, token-bounded seeded guiding content (a short header/prompt, not blank, not full prose); likewise for self/methodology.md, self/goals.md, self/relationships.md
    - Stub-ensure creation-on-miss: when read_note(path) returns empty/None for a canonical self/ path, write_note(path, build_self_stub(path)) is called exactly once for that path
    - No-overwrite-when-present: when read_note(path) returns non-empty content, no write_note occurs for that path (existing content is never clobbered)
    - Canonical-paths-only: only self/identity.md, self/methodology.md, self/goals.md, self/relationships.md are stub-ensured; ops/reminders.md and self/learning-areas.md (also in self_paths) are NEVER auto-created
    - The existing every-message self-read still returns the same self-context it does today (reuse, not rebuild)
  </behavior>
  <action>
    Add a module-level pure function build_self_stub(path) in recall.py that returns minimal, token-bounded, seeded guiding content per canonical self/ file (a short header/prompt describing what the file is for — not blank, not verbose prose), mirroring inbox.build_initial_inbox()'s pure-builder shape. Keep it small: these files are read into every message.

    Add a small stub-ensure step at the Recall self-context gather (the _hot_self / self_paths read site): for each of the FOUR canonical self/ paths only, read the note; if the body is empty/falsy, write the stub via write_note (the unconditional PUT is the create-if-missing primitive — no local-fs existence check). Iterate an explicit four-path allowlist, NOT all of self_paths — ops/reminders.md and self/learning-areas.md must never be auto-created. Do NOT touch read_self_context's contract: it stays read-only and graceful-404; the ensure logic composes read_note + conditional write_note at the call site (or a small dedicated helper), keeping read_self_context untouched for any other caller. Do NOT rebuild the existing self-read (D-04a) — reuse it.

    Add unit tests in test_recall.py using the existing in-memory FakeVault fixture convention (patch the injected vault with read_note/write_note async fakes): assert creation-on-miss (write called once with stub body), no-overwrite-when-present (no write when body exists), and canonical-paths-only (ops/reminders.md + self/learning-areas.md are never written).
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_recall.py -k "self_stub or self_paths or self_context" -q</automated>
  </verify>
  <acceptance_criteria>
    - build_self_stub returns non-empty token-bounded content for all four canonical self/ paths
    - Creation-on-miss, no-overwrite-when-present, and canonical-paths-only tests pass
    - read_self_context is unchanged (still read-only, graceful-404)
    - The existing every-message self-read behavior is preserved (no rebuild)
  </acceptance_criteria>
  <done>build_self_stub + four-path stub-ensure wired at the self-context gather; creation-on-miss, no-overwrite, and canonical-paths-only tests pass; read_self_context unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Message-path integration test — first message self-heals missing self/ files (VAULT-05)</name>
  <files>sentinel-core/tests/test_message.py</files>
  <read_first>
    - 44-VALIDATION.md Per-Task Verification Map (VAULT-05 test row) and Manual-Only Verifications (live REST stub creation)
    - sentinel-core/tests/test_message.py — existing message-path fixtures that exercise Recall via the FakeVault seam (reuse the same fixture shape)
    - 44-RESEARCH.md phase_requirements VAULT-05 row (self_paths already reads all four canonical files + 2 extras every message)
  </read_first>
  <behavior>
    - On the first message for a user whose self/ canonical files are missing (empty FakeVault), processing creates the four canonical self/ stubs via the vault seam and still assembles context without error
    - On a subsequent message, the now-present self/ files are read (not re-created / not overwritten)
    - ops/reminders.md and self/learning-areas.md are NOT created by the message path
  </behavior>
  <action>
    Add a message-path integration test in test_message.py that drives a message through the Recall self-context gather against an in-memory FakeVault seeded with NO self/ files, and asserts: the four canonical self/ stubs are written exactly once, context assembly succeeds, ops/reminders.md and self/learning-areas.md are not written, and a second message does not re-write the now-present files. Reuse the existing test_message.py fixture/mocking conventions (FakeVault + patched seams) — do not add a live REST call. This proves VAULT-05's guarantee end-to-end through the production message path, not just at the Recall unit boundary.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_message.py -k "self" -q</automated>
  </verify>
  <acceptance_criteria>
    - First-message self-heal test passes (four canonical stubs created, assembly succeeds)
    - Idempotency on second message (no re-write) asserted
    - ops/reminders.md and self/learning-areas.md not created by the message path
  </acceptance_criteria>
  <done>First-message self-heal integration test passes (four canonical stubs created, assembly succeeds, idempotent on second message, extras not created).</done>
</task>

</tasks>

<artifacts_produced>
New symbols / files this plan produces:
- Function: recall.build_self_stub(path) — pure, token-bounded seeded-stub builder for the four canonical self/ files
- Stub-ensure wiring inside the Recall self-context gather (composes read_note + conditional write_note; four-path allowlist)
- Self/ stub paths auto-created lazily: self/identity.md, self/methodology.md, self/goals.md, self/relationships.md
- Tests: tests/test_recall.py (self-stub creation-on-miss / no-overwrite / canonical-paths-only); tests/test_message.py (first-message self-heal integration)
No new external packages. No new modules (build_self_stub lives in recall.py per Don't-Hand-Roll: no new vault_bootstrap seam).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self/ stub content -> every-message prompt context | Newly-created stub content is read back into every message (new write path this plan opens) |
| self/ namespace -> recall/move guards | self/ stays in PROTECTED_NAMESPACES (move-protected) and RecallConfig.exclude_prefixes (warm-excluded) — unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-04-01 | Tampering | self/ stub write path | low | mitigate | Writes target a fixed four-path allowlist (self/identity|methodology|goals|relationships.md); stub content is planner-authored boilerplate, not user/attacker input. No dynamic path derivation. |
| T-44-04-02 | Information Disclosure | self/relationships.md sensitive content re-injected into context | low | mitigate | self/ remains in PROTECTED_NAMESPACES and exclude_prefixes (never narrowed this phase); stub-ensure only CREATES a missing file, it does not widen where self/ content is surfaced. Existing self-read behavior is reused unchanged (D-04a). |
| T-44-04-03 | Elevation of Privilege (prompt injection) | self/ content read into every prompt | low | mitigate | Vault content (including these stubs) is treated as untrusted DATA, never instructions (pre-existing Sentinel principle). Stub content is Claude/planner-authored, not attacker-influenced. |
| T-44-04-SC | Tampering | pip installs | n/a | accept | No package installs (pure internal refactor). |

Block-on: HIGH. No high-severity threats — the new write path is a fixed allowlist of planner-authored boilerplate; self/ protection and exclusion are unchanged.
</threat_model>

<verification>
- Per task: keyword-scoped pytest in each <verify>.
- Plan boundary (SC-5 hard gate): `cd sentinel-core && pytest` stays green at 471 collected; MEM-01..MEM-09 unaffected.
- Manual-only (deploy-time, non-blocking, per 44-VALIDATION.md): after deploy against the live Obsidian REST vault, confirm self/{identity,methodology,goals,relationships}.md stubs appear where previously missing. The FakeVault unit + message-path tests are the automated gate; the live check is confirmatory.
</verification>

<success_criteria>
- VAULT-01: the four canonical self/ files are auto-created as minimal seeded stubs on first read-miss; templates/ guard (plan 44-02) plus these stubs complete the three-space namespace foundation
- VAULT-05: every message reads the four canonical self/ files at session start, and the read never hits a missing file (stub-ensure guarantees existence)
- read_self_context contract unchanged; self-read reused not rebuilt (D-04a)
- Full suite green (471)
</success_criteria>

<output>
Create `.planning/phases/44-vault-namespace-taxonomy-foundation/44-04-SUMMARY.md` when done.
</output>
