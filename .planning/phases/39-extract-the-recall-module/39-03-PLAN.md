---
phase: 39-extract-the-recall-module
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - sentinel-core/app/routes/status.py
  - sentinel-core/tests/test_status.py
  - sentinel-core/tests/test_message.py
autonomous: true
requirements: [MEM-01]
tags: [recall, memory, endpoint, context-route, refactor, python]

must_haves:
  truths:
    - "GET /context/{user_id} delegates to ctx.recall.assemble() and serializes RecalledContext"
    - "The duplicated inline self_paths + asyncio.gather + get_recent_sessions assembly is deleted from status.py"
    - "GET /context returns user_id and recent_sessions_count (len of recalled.sessions), preserving the existing test assertions"
    - "test_status.py RouteContext fixture supplies a recall so the /context route resolves ctx.recall"
    - "test_message.py lazy test wiring supplies a recall so the message path keeps working"
    - "Full suite stays green (behavior preserved across both shared callers)"
  artifacts:
    - path: "sentinel-core/app/routes/status.py"
      provides: "GET /context delegating to Recall; duplicated assembly removed"
      contains: "ctx.recall.assemble"
    - path: "sentinel-core/tests/test_status.py"
      provides: "RouteContext fixture with recall wired"
      contains: "recall="
    - path: "sentinel-core/tests/test_message.py"
      provides: "lazy test processor/route-ctx with recall wired"
      contains: "recall"
  key_links:
    - from: "sentinel-core/app/routes/status.py::debug_context"
      to: "Recall.assemble"
      via: "await ctx.recall.assemble(fake_req, budget=ctx.context_window)"
      pattern: "ctx\\.recall\\.assemble"
    - from: "sentinel-core/tests/test_status.py::setup_app_state"
      to: "RouteContext.recall"
      via: "recall=Recall(vault=...) in the RouteContext construction"
      pattern: "recall="
---

<objective>
Converge the second caller: make `GET /context/{user_id}` delegate to `ctx.recall.assemble()` and serialize the `RecalledContext`, deleting the duplicated inline hot-tier assembly. After this plan, both the message path (Plan 02) and the `/context` endpoint use the same `Recall` — closing MEM-01's "no duplicated assembly logic" requirement (phase success criterion #2).

This plan also makes the two minimal test-wiring touch-ups required by the `recall` field added in Plan 02: `test_status.py`'s `RouteContext` fixture and `test_message.py`'s lazy test processor/route-context shims must supply a `recall` so the shared callers resolve. These are surgical additions, not test rewrites (Test-Rewrite Ban respected — existing assertions stay).

Purpose: Endpoint convergence (D-05) — `/context` and the message path share one assembler; the duplicated inline assembly is deleted.
Output: `/context` delegating in `status.py`; recall wired into the two affected test fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-extract-the-recall-module/39-CONTEXT.md
@.planning/phases/39-extract-the-recall-module/39-RESEARCH.md
@.planning/phases/39-extract-the-recall-module/39-PATTERNS.md
@.planning/phases/39-extract-the-recall-module/39-VALIDATION.md
@docs/adr/0003-recall-module.md
@.planning/phases/39-extract-the-recall-module/39-01-PLAN.md
@.planning/phases/39-extract-the-recall-module/39-02-PLAN.md
</context>

<artifacts_this_phase_produces>
NEW behavior added by THIS plan (drift pass: newly-created, not pre-existing drift):
- `GET /context/{user_id}` delegates to `ctx.recall.assemble()` (replaces the inline self_paths/gather/get_recent_sessions block)
- `recall` wired into `test_status.py::setup_app_state` RouteContext construction
- `recall` accessor/attribute wired into `test_message.py` lazy test shims (`_LazyTestProcessor` and `_LazyRouteCtx`)
- Reuse of Plan 01/02 symbols: `Recall`, `RecalledContext`, `MessageRequest`, `RouteContext.recall`

NOTE for the drift pass: this plan REMOVES the inline `self_paths` list, the `asyncio.gather(read_self_context...)`, and the `get_recent_sessions` call from `status.py` (relocated behavior now lives in `Recall`). The `/context` response shape changes from `context_files` (dict) to `self_context`/`sessions`/`warm` fields while keeping `user_id` and `recent_sessions_count` — RESEARCH.md Assumption A1 confirms no external caller depends on `context_files`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delegate GET /context/{user_id} to Recall and serialize RecalledContext</name>
  <read_first>
    - sentinel-core/app/routes/status.py (FULL FILE — the /status handler lines 15–33 to mirror, and the duplicated /context handler lines 36–67 to replace; the `import asyncio` line 3 becomes removable if no other use)
    - sentinel-core/app/services/recall.py (Plan 01 — Recall.assemble signature and RecalledContext fields; MessageRequest re-exported here)
    - sentinel-core/app/services/message_processing.py (Plan 02 — MessageRequest definition, for the import the route uses to build a synthetic request)
    - sentinel-core/app/state.py (Plan 02 — RouteContext now has a recall field and context_window)
    - .planning/phases/39-extract-the-recall-module/39-PATTERNS.md (lines 313–355 — the /context delegation handler pattern, import additions)
    - .planning/phases/39-extract-the-recall-module/39-RESEARCH.md (lines 154–210 the duplication map + new serialization target; lines 390–396 Pitfall 7 serialization break, Pitfall 8 empty-content warm)
  </read_first>
  <behavior>
    - GET /context/{user_id} constructs a synthetic MessageRequest with content="" and the route's user_id, then awaits ctx.recall.assemble(req, budget=ctx.context_window)
    - The response JSON contains user_id (the path value) and recent_sessions_count = len(recalled.sessions) (preserves existing test assertions)
    - The response also exposes self_context, sessions, and warm (list of {path, score}) from the RecalledContext
    - Because content="" , Recall._warm_search early-returns [] (no vault.find call), so the route works against fixtures whose vault mock has no find() configured
    - The duplicated inline self_paths/gather/get_recent_sessions assembly is gone from status.py
  </behavior>
  <action>
Edit `sentinel-core/app/routes/status.py`. Replace the body of the `debug_context` handler (lines 41–67) so it:
1. Gets `ctx = get_route_context(request)` (unchanged).
2. Builds a synthetic request: `fake_req = MessageRequest(content="", user_id=user_id, model_name="", context_window=ctx.context_window, stop_sequences=None)`.
3. Calls `recalled = await ctx.recall.assemble(fake_req, budget=ctx.context_window)`.
4. Returns `JSONResponse({"user_id": user_id, "self_context": recalled.self_context, "sessions": recalled.sessions, "warm": [{"path": r.path, "score": r.score} for r in recalled.warm], "recent_sessions_count": len(recalled.sessions)})`.

Keep the `Path(..., pattern=r"^[a-zA-Z0-9_-]+$")` validation on `user_id` unchanged. Add `from app.services.message_processing import MessageRequest` (or `from app.services.recall import MessageRequest` — both re-export it after Plan 02; prefer the recall import to keep the route pointed at the new module). Remove `import asyncio` (line 3) if no other handler in the file uses it. Leave the `/status` handler untouched.

Per RESEARCH.md Pitfall 8 / Assumption A2: passing `content=""` makes warm-tier degrade to `[]` (the route has no user message to drive warm search). This is the documented, behavior-preserving choice — the /context route returns hot-tier-only context (self + sessions) plus an empty warm list. Do not add a `?query=` parameter in this phase (out of scope).
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_status.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ctx.recall.assemble" sentinel-core/app/routes/status.py` returns >= 1 (delegation in place — D-05/MEM-01)
    - `grep -n "self_paths\|get_recent_sessions\|read_self_context" sentinel-core/app/routes/status.py` returns 0 lines (duplicated assembly deleted — phase success criterion #2)
    - `grep -n "import asyncio" sentinel-core/app/routes/status.py` returns 0 lines (no longer needed)
    - `grep -n "recent_sessions_count" sentinel-core/app/routes/status.py` returns >= 1 (response key preserved for test_status.py assertions)
    - `cd sentinel-core && uv run pytest tests/test_status.py -x` exits 0 (note: this also requires the Task 2 fixture update — run Task 2 first or together)
  </acceptance_criteria>
  <done>GET /context/{user_id} delegates to ctx.recall.assemble() and serializes RecalledContext; the inline self_paths/gather/get_recent_sessions block is deleted; response keeps user_id + recent_sessions_count.</done>
</task>

<task type="auto">
  <name>Task 2: Wire recall into the test_status.py and test_message.py fixtures</name>
  <read_first>
    - sentinel-core/tests/test_status.py (FULL FILE — the setup_app_state fixture lines 34–45 constructs RouteContext WITHOUT recall; the mock_obsidian fixture lines 16–22 has read_self_context + get_recent_sessions configured but NO find — which is why content="" empty-warm matters; existing /context assertions lines 95–113 assert only user_id + recent_sessions_count)
    - sentinel-core/tests/test_message.py (lines 66–115 — _LazyTestProcessor.process() constructs MessageProcessor WITHOUT recall (relies on the optional default from Plan 02 Task 1) and _LazyRouteCtx exposes vault/processor/settings/context_window/etc but NO recall property)
    - sentinel-core/app/services/recall.py (Plan 01 — Recall(vault=...) constructor)
    - sentinel-core/app/state.py (Plan 02 — RouteContext.recall is optional with None default)
    - .planning/phases/39-extract-the-recall-module/39-RESEARCH.md (lines 353–359 test wiring impact)
  </read_first>
  <action>
Edit `sentinel-core/tests/test_status.py`:
1. Add `from app.services.recall import Recall` to the imports.
2. In `setup_app_state` (the `RouteContext(...)` at lines 40–45), add `recall=Recall(vault=app.state.vault)` so the `/context` route can resolve `ctx.recall`. The `mock_obsidian` fixture already configures `read_self_context` (AsyncMock returning "context content") and `get_recent_sessions` (AsyncMock returning `["session1"]`), which is exactly what `Recall._hot_self`/`_hot_sessions` call. Because the route passes `content=""`, `Recall._warm_search` early-returns and never calls `find` (which the mock does not configure) — no further fixture change needed.
3. Do NOT alter the existing assertions (`test_context_returns_user_id`, `test_context_includes_recent_sessions_count`, auth tests). They assert `user_id` and `recent_sessions_count`, both preserved by the new serializer. `recent_sessions_count` will be `len(recalled.sessions)`; with `get_recent_sessions` returning one item, it stays an int >= 0 (assertion is `isinstance(..., int)`).

Edit `sentinel-core/tests/test_message.py`:
1. Add `from app.services.recall import Recall` to the imports.
2. In `_LazyTestProcessor.process()` (lines 77–84), pass an explicit `recall=Recall(vault=app.state.vault)` into the `MessageProcessor(...)` construction (so the lazy rebuild mirrors production wiring; even though Plan 02 makes recall default-construct, passing it explicitly is clearer and future-proofs the shim).
3. Add a `recall` property to `_LazyRouteCtx` (lines 88–113) returning `getattr(app.state, "recall", None)` OR a freshly built `Recall(vault=app.state.vault)` — prefer the freshly-built form so any `/context`-style call through this lazy ctx resolves. Place it alongside the other `@property` accessors.

These are additive fixture changes only — no existing test assertion is modified (Test-Rewrite Ban respected).
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "recall=" sentinel-core/tests/test_status.py` returns >= 1 (RouteContext fixture supplies recall)
    - `grep -n "from app.services.recall import Recall" sentinel-core/tests/test_status.py` returns >= 1
    - `grep -n "recall" sentinel-core/tests/test_message.py` returns >= 1 (lazy shims supply recall)
    - `cd sentinel-core && uv run pytest tests/test_status.py tests/test_message.py -x` exits 0
    - `cd sentinel-core && uv run pytest tests/ -x -q` exits 0 (FULL SUITE green — phase-gate regression net across all shared callers)
  </acceptance_criteria>
  <done>test_status.py RouteContext and test_message.py lazy shims supply a recall; existing assertions unchanged; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /context/{user_id} | Existing authenticated debug endpoint (X-Sentinel-Key via APIKeyMiddleware, unchanged). The `user_id` path parameter is the only untrusted input and retains its existing `^[a-zA-Z0-9_-]+$` pattern validation. This plan does not add or relax that boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-06 | Input Validation / Injection | `user_id` path parameter | mitigate | The `Path(..., pattern=r"^[a-zA-Z0-9_-]+$")` regex validation on `user_id` is preserved unchanged on the refactored handler. The synthetic `MessageRequest(content="", user_id=user_id, ...)` carries only the already-validated `user_id`; no new untrusted field is introduced. |
| T-39-07 | Information Disclosure | `/context` response shape | accept | The response now exposes `self_context`/`sessions`/`warm` instead of `context_files`. RESEARCH.md Assumption A1 records no external caller depends on `context_files`; the endpoint is an authenticated debug route. The same self/session content was already returned before — this is a field reshape, not a new disclosure. `warm` is empty here (content=""), so no additional vault content is exposed. |
| T-39-08 | Information Disclosure | warm namespace exclusions | mitigate | Even though warm is empty on this route (content=""), the exclusion policy still lives in `RecallConfig.exclude_prefixes` and would apply if the route ever drives warm search — the `ops/`/`_trash/`/`self/` boundary is centralized and not relaxed by this plan. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md audit: stdlib + existing deps only). No install task → no slopcheck checkpoint required. |
</threat_model>

<verification>
- `cd sentinel-core && uv run pytest tests/ -x -q` exits 0 (FULL SUITE — final phase-gate regression net; both shared callers now use one Recall)
- `grep -n "ctx.recall.assemble" sentinel-core/app/routes/status.py` >= 1 (MEM-01 endpoint convergence)
- `grep -n "self_paths\|get_recent_sessions" sentinel-core/app/routes/status.py` == 0 (duplicated assembly deleted — success criterion #2)
- `app/vault.py` unmodified (`git diff --stat sentinel-core/app/vault.py` empty — ADR-0002/D-06)
</verification>

<success_criteria>
- MEM-01 (endpoint half / phase success criterion #2): `GET /context/{user_id}` returns the same recalled context as the message path by delegating to the shared `Recall.assemble()`; the duplicated inline assembly is deleted.
- Existing `/context` test assertions (`user_id`, `recent_sessions_count`) and the full suite stay green — behavior preserved.
- The two affected test fixtures supply a `recall` via additive changes only (Test-Rewrite Ban respected).
</success_criteria>

<output>
Create `.planning/phases/39-extract-the-recall-module/39-03-SUMMARY.md` when done.
</output>
