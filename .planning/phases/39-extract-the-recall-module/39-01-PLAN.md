---
phase: 39-extract-the-recall-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentinel-core/app/services/recall.py
  - sentinel-core/tests/test_recall.py
autonomous: true
requirements: [MEM-01, MEM-02]
tags: [recall, memory, retrieval, refactor, python]

must_haves:
  truths:
    - "Recall.assemble(request, budget) returns a RecalledContext with self_context, sessions, and warm fields"
    - "RecallConfig exposes relevance_threshold (-200.0), exclude_prefixes (ops/,_trash/,self/), sessions_ratio (0.15), search_ratio (0.10), recent_session_limit (3)"
    - "Warm search excludes notes whose path starts with ops/, _trash/, or self/ and notes scoring below -200.0"
    - "Recall._hot_self reads only the 6 self_paths and never sentinel/persona.md"
    - "tests/test_recall.py drives Recall.assemble() against FakeVault with no MessageProcessor, no AI provider, no InjectionFilter"
  artifacts:
    - path: "sentinel-core/app/services/recall.py"
      provides: "SearchResult, RecalledContext, RecallConfig, Recall, SEARCH_SCORE_THRESHOLD"
      contains: "class Recall"
      min_lines: 120
    - path: "sentinel-core/tests/test_recall.py"
      provides: "Behavioral tests for Recall.assemble against FakeVault"
      contains: "from app.services.recall import"
      min_lines: 80
  key_links:
    - from: "sentinel-core/app/services/recall.py::Recall._warm_search"
      to: "Vault.find / Vault.read_note"
      via: "self._vault.find(query) then asyncio.gather read_note"
      pattern: "self\\._vault\\.(find|read_note)"
    - from: "sentinel-core/tests/test_recall.py"
      to: "app.services.recall.Recall"
      via: "direct construction Recall(vault=FakeVault(...))"
      pattern: "Recall\\(vault="
---

<objective>
Create the new `Recall` module (`app/services/recall.py`) that owns hot-tier and warm-tier retrieval policy as a behavior-preserving extraction from `MessageProcessor`, plus its dedicated unit test (`tests/test_recall.py`) that drives `Recall.assemble()` directly against `FakeVault`.

This is the interface-first plan: every downstream plan (02 extraction/wiring, 03 endpoint convergence) consumes the `Recall`, `RecallConfig`, `RecalledContext`, and `SearchResult` symbols defined here. The constants currently inlined in `message_processing.py` move into `RecallConfig` (MEM-02). The new test file proves the extracted module reproduces existing retrieval behavior without going through `MessageProcessor` or the AI provider (success criterion #4).

Purpose: Retrieval becomes a first-class, independently testable module (D-01, D-02, D-03). The interface becomes the test surface — "given these vault notes + this message, assemble returns these ranked items."
Output: `sentinel-core/app/services/recall.py`, `sentinel-core/tests/test_recall.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-extract-the-recall-module/39-CONTEXT.md
@.planning/phases/39-extract-the-recall-module/39-RESEARCH.md
@.planning/phases/39-extract-the-recall-module/39-PATTERNS.md
@.planning/phases/39-extract-the-recall-module/39-VALIDATION.md
@docs/adr/0003-recall-module.md
@docs/adr/0002-vault-seam-location.md
</context>

<artifacts_this_phase_produces>
This phase (across plans 01–03) creates the following NEW symbols. The source-grounding/drift pass MUST treat these as newly-created (not pre-existing drift):

**New module:** `sentinel-core/app/services/recall.py`
**New test file:** `sentinel-core/tests/test_recall.py`

**New symbols defined in `recall.py` (this plan):**
- `class SearchResult` — frozen dataclass: `path: str`, `score: float`, `body: str`
- `class RecalledContext` — frozen dataclass: `self_context: list[str]`, `sessions: list[str]`, `warm: list[SearchResult]`
- `class RecallConfig` — frozen dataclass with fields: `relevance_threshold`, `exclude_prefixes`, `sessions_ratio`, `search_ratio`, `recent_session_limit`, `self_paths`, `warm_top_n`
- `class Recall` — methods: `__init__(self, vault, *, config=None)`, `async assemble(self, request, budget)`; private `_hot_self`, `_hot_sessions`, `_warm_search`, `_allocate`
- `_ContextBudget` — frozen dataclass (moved from message_processing.py): `sessions_budget: int`, `search_budget: int`
- Module constants moved from `message_processing.py`: `SEARCH_SCORE_THRESHOLD = -200.0`, `_WARM_TIER_EXCLUDE_PREFIXES`, `_SEARCH_STOPWORDS`, `_KEYWORD_SEARCH_THRESHOLD`, and functions `_extract_keywords`, `_best_search_query`
- `MessageRequest` is re-exported from `recall.py` (re-imported from `message_processing`) OR imported directly — see action note

**New symbols added by later plans (listed here so the drift pass excludes them globally):**
- Plan 02: `recall` constructor parameter on `MessageProcessor.__init__`; `recall` field on `AppGraph`; `recall` keyword-only parameter on `build_application`; `recall` field on `RouteContext`
- Plan 03: delegation of `GET /context/{user_id}` to `ctx.recall.assemble()`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Recall module with value types, RecallConfig, and the Recall class</name>
  <read_first>
    - sentinel-core/app/services/message_processing.py (THE extraction source — read lines 1–148 for constants/functions/_ContextBudget/MessageRequest, and lines 195–263 for _append_hot_tier and _append_warm_tier bodies; lines 265–279 _format_search_results stays in message_processing — do NOT move it here)
    - sentinel-core/app/vault.py (the Vault Protocol — confirm read_self_context, get_recent_sessions, find, read_note signatures; do NOT modify this file per D-06/ADR-0002)
    - .planning/phases/39-extract-the-recall-module/39-PATTERNS.md (lines 24–164 — exact import block, frozen-dataclass patterns, _allocate pattern, hot-tier gather pattern, warm-tier filter/top-N pattern)
    - .planning/phases/39-extract-the-recall-module/39-RESEARCH.md (lines 73–148 the extraction map + inline-constant inventory; lines 364–388 Pitfalls 1,2,3,5,6)
    - docs/adr/0003-recall-module.md (lines 36–56 the canonical interface sketch)
  </read_first>
  <behavior>
    - assemble() returns RecalledContext.self_context populated (non-empty strings only) from seeded self notes
    - assemble() returns RecalledContext.sessions populated from seeded session notes (via get_recent_sessions, limit from config.recent_session_limit)
    - _hot_self reads exactly the 6 config.self_paths and never reads sentinel/persona.md
    - _warm_search: notes scoring >= relevance_threshold in non-excluded paths appear in warm; notes with ops/, _trash/, or self/ prefix are excluded; notes scoring below -200.0 are excluded
    - _warm_search returns warm=[] (early, before calling vault.find) when request.content is empty string
    - _allocate uses int() truncation (NOT round) for sessions_budget and search_budget
    - exclude_prefixes is a tuple[str,...] passed directly to str.startswith
  </behavior>
  <action>
Create `sentinel-core/app/services/recall.py`. Use `from __future__ import annotations`, import `asyncio`, `logging`, `dataclass`/`field` from `dataclasses`, and a `TYPE_CHECKING` guard importing `Vault` from `app.vault`. Define `logger = logging.getLogger(__name__)`.

Move these module-level items VERBATIM from `message_processing.py` into `recall.py`: `SEARCH_SCORE_THRESHOLD = -200.0` (keep this exact public name so it can be re-exported), `_WARM_TIER_EXCLUDE_PREFIXES = ("ops/", "_trash/", "self/")`, `_SEARCH_STOPWORDS` (the frozenset), `_KEYWORD_SEARCH_THRESHOLD = 5`, and the functions `_extract_keywords` and `_best_search_query` unchanged.

Move `_ContextBudget` (frozen dataclass: `sessions_budget: int`, `search_budget: int`) from `message_processing.py`. Import `MessageRequest` into `recall.py` from `app.services.message_processing` AND re-export it (so `from app.services.recall import MessageRequest` works for `test_recall.py` and `status.py`); guard against circular import by importing `MessageRequest` lazily inside `assemble` if a top-level import cycles — top-level import is expected to be fine because `recall.py` does not import the `MessageProcessor` class.

Define `@dataclass(frozen=True) class SearchResult` with `path: str`, `score: float`, `body: str`.

Define `@dataclass(frozen=True) class RecalledContext` with `self_context: list[str]`, `sessions: list[str]`, `warm: list[SearchResult]`.

Define `@dataclass(frozen=True) class RecallConfig` with these defaulted fields: `relevance_threshold: float = -200.0`, `exclude_prefixes: tuple[str, ...] = ("ops/", "_trash/", "self/")`, `sessions_ratio: float = 0.15`, `search_ratio: float = 0.10`, `recent_session_limit: int = 3`, `self_paths: list[str] = field(default_factory=lambda: [...])` containing exactly the 6 paths in order: `self/identity.md`, `self/methodology.md`, `self/goals.md`, `self/relationships.md`, `ops/reminders.md`, `self/learning-areas.md` (NOTE per D-02 and Pitfall 2: `ops/reminders.md` stays in self_paths even though `ops/` is in exclude_prefixes — the exclusion applies only to warm search, never to the self_paths allowlist), and `warm_top_n: int = 3`.

Define `class Recall` with `def __init__(self, vault, *, config: RecallConfig | None = None)` setting `self._vault = vault` and `self._config = config or RecallConfig()`.

Implement `def _allocate(self, budget: int) -> _ContextBudget` returning `_ContextBudget(sessions_budget=int(budget * self._config.sessions_ratio), search_budget=int(budget * self._config.search_ratio))`. Use `int()` truncation, NOT `round()` (Pitfall 3 — preserves exact existing budget arithmetic).

Implement `async def _hot_self(self) -> list[str]`: gather `self._vault.read_self_context(p)` over exactly `self._config.self_paths` with `return_exceptions=True`; return `[r for r in results if isinstance(r, str) and r.strip()]`. Do NOT read `sentinel/persona.md` here (Pitfall 1 — persona stays in MessageProcessor per D-04; the persona path must never enter RecalledContext).

Implement `async def _hot_sessions(self, user_id: str) -> list[str]`: return `await self._vault.get_recent_sessions(user_id, limit=self._config.recent_session_limit)`.

Implement `async def _warm_search(self, content: str) -> list[SearchResult]`: if `not content.strip()` return `[]` early (before calling vault.find — Pitfall 8 Option A, keeps the /context empty-content path from touching find()). Otherwise replicate `_append_warm_tier` lines 237–257: choose query via `_best_search_query(content)` when `len(content.split()) > _KEYWORD_SEARCH_THRESHOLD` else use `content`; call `await self._vault.find(query)`; filter to results where `r.get("score", float("-inf")) >= self._config.relevance_threshold and not r.get("filename", "").startswith(self._config.exclude_prefixes)` (exclude_prefixes is a tuple → pass directly to startswith, Pitfall 5); if empty return `[]`; take `relevant[: self._config.warm_top_n]`; gather `self._vault.read_note(path)` over the top paths with `return_exceptions=True`; translate each top dict to `SearchResult(path=r["filename"], score=r["score"], body=<read result if isinstance str else snippet from r.get("matches",[]) first context else "">)`. Raw dicts MUST NOT leak past this method — return `list[SearchResult]` only.

Implement `async def assemble(self, request, budget: int) -> RecalledContext`: gather/await `_hot_self()`, `_hot_sessions(request.user_id)`, and `_warm_search(request.content)`; return `RecalledContext(self_context=..., sessions=..., warm=...)`. The `budget` parameter is accepted for interface stability; per-tier truncation stays in MessageProcessor (D-04), so `assemble` does not truncate — it may call `_allocate(budget)` if useful but must NOT apply TokenBudget here.

Do NOT move `_format_search_results` here — it stays in `message_processing.py` (Pitfall 6, D-04). Do NOT import or call `InjectionFilter` or `TokenBudget` anywhere in this module (security/presentation boundary, D-04).
  </action>
  <verify>
    <automated>cd sentinel-core && uv run python -c "from app.services.recall import Recall, RecallConfig, RecalledContext, SearchResult, SEARCH_SCORE_THRESHOLD, MessageRequest; c=RecallConfig(); assert c.relevance_threshold==-200.0 and c.exclude_prefixes==('ops/','_trash/','self/') and c.sessions_ratio==0.15 and c.search_ratio==0.10 and c.recent_session_limit==3 and 'ops/reminders.md' in c.self_paths and len(c.self_paths)==6; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `sentinel-core/app/services/recall.py` exists and defines `class Recall`, `class RecallConfig`, `class RecalledContext`, `class SearchResult` (grep each returns >= 1)
    - The import smoke command above prints `OK` (all RecallConfig defaults match the moved constants exactly)
    - `grep -n "persona" sentinel-core/app/services/recall.py` returns 0 lines (persona never enters Recall — Pitfall 1)
    - `grep -n "InjectionFilter\|TokenBudget\|wrap_context\|\.truncate(" sentinel-core/app/services/recall.py` returns 0 lines (presentation/defense stays in MessageProcessor — D-04)
    - `grep -n "round(" sentinel-core/app/services/recall.py` returns 0 lines for budget arithmetic (int() truncation preserved — Pitfall 3)
    - `app/vault.py` is unmodified (`git diff --stat sentinel-core/app/vault.py` shows no changes — D-06/ADR-0002)
  </acceptance_criteria>
  <done>`recall.py` contains Recall/RecallConfig/RecalledContext/SearchResult with the verbatim-moved constants in RecallConfig; persona path absent; no InjectionFilter/TokenBudget references; vault.py untouched; import smoke test prints OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests/test_recall.py driving Recall.assemble() against FakeVault</name>
  <read_first>
    - sentinel-core/tests/test_message_processor.py (lines 1–117 — file docstring style, factory-helper pattern, make_request shape, FakeAIProvider-free construction; this is the analog test file)
    - sentinel-core/tests/fakes/vault.py (lines 27–135 — FakeVault constructor kwargs `notes=`/`dirs=`, find() returns score=1.0 for body substring match, get_recent_sessions matches `f"{user_id}-" in filename` under ops/sessions/, read_self_context returns notes.get(path,""))
    - .planning/phases/39-extract-the-recall-module/39-PATTERNS.md (lines 167–255 — test file-level pattern, make_recall/make_request helpers, async auto-mode no decorator, score-below-threshold override pattern)
    - .planning/phases/39-extract-the-recall-module/39-RESEARCH.md (lines 294–308 the 7 key test scenarios; lines 377–384 Pitfall 4 FakeVault find always returns score 1.0)
    - sentinel-core/app/services/recall.py (the module under test — created in Task 1)
  </read_first>
  <behavior>
    - Test: assemble() returns self_context populated from seeded self notes (3 seeded → 3 strings)
    - Test: assemble() returns sessions populated from seeded ops/sessions notes matching user_id
    - Test: warm search returns notes above threshold in non-excluded paths
    - Test: warm search excludes ops/ and self/ prefixed notes even when they match the query
    - Test: warm search excludes a note whose injected score is below -200.0 (override find() to return score=-300.0)
    - Test: empty vault → self_context==[], sessions==[], warm==[] (graceful degrade)
    - Test (config): a custom RecallConfig(relevance_threshold=..., exclude_prefixes=...) is respected
    - Test (empty content): assemble() with content="" returns warm==[] without calling find()
  </behavior>
  <action>
Create `sentinel-core/tests/test_recall.py`. Module docstring describes that tests construct `Recall` against `FakeVault` directly — no MessageProcessor, no AI provider, no InjectionFilter (success criterion #4). Use `from __future__ import annotations`; `import pytest`; `from tests.fakes.vault import FakeVault`; `from app.services.recall import Recall, RecallConfig, RecalledContext, SearchResult, MessageRequest`.

Add a `make_recall(*, notes=None, config=None) -> tuple[Recall, FakeVault]` helper that builds `FakeVault(notes=notes or {})` and `Recall(vault=vault, config=config)`. Add a `make_request(content="hello", budget=8192) -> MessageRequest` helper returning `MessageRequest(content=content, user_id="trekkie", model_name="test-model", context_window=budget, stop_sequences=None)`.

Write the 8 behavioral tests as bare `async def test_*` functions (no `@pytest.mark.asyncio` — `asyncio_mode = "auto"` in pyproject.toml):
1. `test_assemble_returns_self_context` — seed 3 of the 6 self_paths in notes; assert `len(result.self_context) == 3` and each is a non-empty str. Seed at least `self/identity.md`, `self/goals.md`, `ops/reminders.md` to confirm `ops/reminders.md` IS read as self-context (Pitfall 2).
2. `test_assemble_returns_sessions` — seed `ops/sessions/2026-06-11/trekkie-12-00-00.md` = "session body"; assert that body appears in `result.sessions`.
3. `test_warm_includes_above_threshold_non_excluded` — seed `notes/omie.md` = "omie wise synthwave details"; call `make_request("omie wise synthwave")`; assert a SearchResult with `path == "notes/omie.md"` is in `result.warm`.
4. `test_warm_excludes_self_and_ops_prefixes` — seed `self/identity.md` and `ops/reminders.md` containing the query term, plus a non-excluded `notes/topic.md`; query a shared term; assert no warm result path startswith `self/`/`ops/`/`_trash/`.
5. `test_warm_excludes_below_threshold` — monkeypatch the vault's `find` to an async function returning `[{"filename":"notes/target.md","score":-300.0,"matches":[]}]`, seed `notes/target.md` body, assert `result.warm == []` (Pitfall 4).
6. `test_empty_vault_graceful_degrade` — empty notes; assert `result.self_context == []` and `result.sessions == []` and `result.warm == []`.
7. `test_recall_config_respected` — construct `Recall` with `RecallConfig(exclude_prefixes=("blocked/",))`; seed a `blocked/x.md` matching the query and a `notes/y.md` matching; assert `blocked/x.md` is excluded and `notes/y.md` is present (proves config.exclude_prefixes drives the filter, not a hardcoded constant — MEM-02).
8. `test_empty_content_skips_find` — set a sentinel on the vault (e.g. wrap `find` to record calls), call `assemble(make_request(content=""), budget=8192)`, assert `result.warm == []` AND `find` was not called.

All tests await `recall.assemble(make_request(...), budget=8192)` and assert on `RecalledContext` fields. No InjectionFilter, no AI provider, no MessageProcessor anywhere in the file.
  </action>
  <verify>
    <automated>cd sentinel-core && uv run pytest tests/test_recall.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd sentinel-core && uv run pytest tests/test_recall.py -x` exits 0
    - `grep -c "async def test_" sentinel-core/tests/test_recall.py` returns >= 8
    - `grep -n "MessageProcessor\|AIProvider\|ai_provider\|InjectionFilter\|injection_filter" sentinel-core/tests/test_recall.py` returns 0 lines (test surface is Recall-only — success criterion #4)
    - `grep -n "from tests.fakes.vault import FakeVault" sentinel-core/tests/test_recall.py` returns >= 1
    - `grep -n "@pytest.mark.asyncio" sentinel-core/tests/test_recall.py` returns 0 lines (auto mode)
  </acceptance_criteria>
  <done>`tests/test_recall.py` has >= 8 passing behavioral tests driving Recall.assemble() against FakeVault with zero MessageProcessor/AI-provider/InjectionFilter references; `uv run pytest tests/test_recall.py -x` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan introduces no new external surface. `Recall` consumes the existing `Vault` Protocol only; no new endpoint, no new auth boundary, no new untrusted input path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information Disclosure | `Recall._warm_search` namespace exclusions | mitigate | Preserve the `ops/`/`_trash/`/`self/` warm-tier exclusion verbatim in `RecallConfig.exclude_prefixes` and apply it via `str.startswith(tuple)`. This is an information-disclosure boundary (keeps operational session/sweep/self content and trash out of warm-tier recall). Test 4 and Test 7 assert the exclusion holds. |
| T-39-02 | Tampering | recalled vault content reaching the model | accept | `injection_filter.wrap_context()` defense stays in `MessageProcessor` (D-04). `Recall` returns raw content values and never bypasses the filter; the wrapping is applied by the processor before injection. No mitigation needed in this module — Recall is upstream of the defense layer and adds no path around it. |
| T-39-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: no new packages; stdlib + existing deps only). No install task → no slopcheck checkpoint required. |
</threat_model>

<verification>
- `cd sentinel-core && uv run pytest tests/test_recall.py -x` exits 0 (new test surface green)
- `cd sentinel-core && uv run python -c "from app.services.recall import Recall, RecallConfig, RecalledContext, SearchResult, SEARCH_SCORE_THRESHOLD, MessageRequest"` succeeds (all public symbols importable)
- `git diff --stat sentinel-core/app/vault.py` shows no changes (ADR-0002 boundary respected)
- No regression risk to existing suites in this plan (only adds new files); full-suite check happens at the end of Plan 02 after extraction.
</verification>

<success_criteria>
- `Recall`, `RecallConfig`, `RecalledContext`, `SearchResult` exist in `app/services/recall.py` with all MEM-02 constants consolidated into `RecallConfig` (relevance_threshold, exclude_prefixes, sessions_ratio, search_ratio, recent_session_limit).
- `tests/test_recall.py` proves `Recall.assemble()` reproduces hot/warm behavior against `FakeVault` with no `MessageProcessor`, no AI provider, no `InjectionFilter` (phase success criterion #4).
- The persona path never enters `RecalledContext`; namespace exclusion preserved verbatim; budget arithmetic uses `int()` truncation.
</success_criteria>

<output>
Create `.planning/phases/39-extract-the-recall-module/39-01-SUMMARY.md` when done.
</output>
