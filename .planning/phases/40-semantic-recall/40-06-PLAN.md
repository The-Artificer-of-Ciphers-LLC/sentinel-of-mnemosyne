---
phase: 40-semantic-recall
plan: "06"
type: execute
wave: 2
gap_closure: true
depends_on: ["40-04", "40-05"]
files_modified:
  - scripts/uat_phase40_blast_radius.py
autonomous: false
requirements: [MEM-03, MEM-05]
user_setup:
  - service: obsidian-local-rest-api
    why: "Blast-radius audit and UAT re-run require a live Obsidian vault + Local REST API (ports 27123/27124); the FakeVault round-trip is authoritative for the seam but the live .json path acceptance and the real relocated-file scan need the operator's vault."
    env_vars:
      - name: OBSIDIAN_API_URL
        source: "Existing deploy .env on /Volumes/Mini Me (live stack checkout)"
      - name: OBSIDIAN_API_KEY
        source: "Existing deploy .env on /Volumes/Mini Me (live stack checkout)"
    dashboard_config:
      - task: "Confirm Obsidian is running with the Local REST API plugin enabled (HTTP 27123 / HTTPS 27124)"
        location: "Obsidian → Settings → Community plugins → Local REST API"

must_haves:
  truths:
    - "An operator can run one script that scans the live vault for files relocated by the bad sweep (notes carrying relocate provenance frontmatter original_path pointing back into sentinel/, or any original_path move that left a protected namespace) and reports them with their original_path so they can be restored"
    - "After the 40-04/40-05 fixes are redeployed, a PUT then GET of ops/sweeps/embedding-index.json against the live Obsidian Local REST API round-trips a JSON body unchanged (UAT Test 3 re-enabled) OR the documented .md fenced-JSON fallback is triggered and recorded"
    - "After redeploy, a live paraphrase query returns the semantically-matching note in RecalledContext.warm (UAT Test 2 re-run) and sentinel/persona.md is confirmed still in place across a boot"
  artifacts:
    - path: "scripts/uat_phase40_blast_radius.py"
      provides: "operator-runnable blast-radius audit: scans live vault for sweep-relocated files and reports original_path → current_path"
      contains: "original_path"
  key_links:
    - from: "scripts/uat_phase40_blast_radius.py"
      to: "Obsidian Local REST API"
      via: "OBSIDIAN_API_URL/OBSIDIAN_API_KEY vault listing + frontmatter read"
      pattern: "OBSIDIAN_API"
---

<objective>
Close the operator-facing remediations of the Phase-40 UAT blocker:

1. **Blast-radius audit (remediation #4):** The bad first-boot sweep relocated `sentinel/persona.md`
   (operator-confirmed) and the blast radius beyond persona is UNKNOWN — other notes may also have been
   relocated. Deliver an operator-runnable script that scans the live vault for files carrying the
   sweeper's relocation provenance (`original_path` / `topic_moved_at` frontmatter written by
   `ObsidianVault.relocate`) and flags any whose `original_path` pointed into a protected namespace
   (`sentinel/`), plus a full list of all topic-relocations from the incident window so the operator can
   review and restore.

2. **UAT Test 3 re-enable (.json path round-trip, MEM-05/D-07):** Could not run because the incident took
   the system down and it was rolled back to v0.50.3 (no Phase-40 index emission). After the 40-04/40-05
   fixes are redeployed, verify a live PUT then GET of `ops/sweeps/embedding-index.json` returns 2xx and
   an unchanged JSON body. Documented fallback: if the live REST API rejects the `.json` extension, switch
   the index path to a `.md` fenced-JSON note (a one-line constant change to `EMBEDDING_INDEX_PATH` in
   vault_sweeper.py and `RecallConfig.index_path` in recall.py) and record that outcome.

3. **UAT Test 2 re-run (live paraphrase recall, MEM-03):** After redeploy, confirm the original blocker
   scenario now works end-to-end AND that `sentinel/persona.md` survives a boot (the regression that
   started the incident).

Purpose: confirm the fix in the live environment and recover any collateral damage from the original bad
sweep. This plan is operator-facing and gated on a live vault, hence `autonomous: false`.
Output: `scripts/uat_phase40_blast_radius.py` audit script + a human-verify checkpoint capturing the live
round-trip, paraphrase recall, and persona-survives-boot outcomes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-semantic-recall/40-UAT.md
@.planning/phases/40-semantic-recall/40-VERIFICATION.md
@.planning/phases/40-semantic-recall/40-CONTEXT.md
@sentinel-core/app/vault.py
@sentinel-core/scripts/uat_phase37.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the blast-radius audit script</name>
  <read_first>
    - scripts/uat_phase37.py (the existing UAT-script convention: how it reads OBSIDIAN_API_URL/OBSIDIAN_API_KEY, constructs the httpx client, lists/reads vault notes, prints a structured report — mirror this structure and the env-loading pattern exactly)
    - sentinel-core/app/vault.py (`ObsidianVault.relocate` lines 445-488 — the provenance frontmatter it writes: `original_path` and `topic_moved_at`; `move_to_trash` lines 395-443 writes `original_path` + `reason` + `sweep_at` into `_trash/`. The audit must look for BOTH provenance shapes.)
    - sentinel-core/app/markdown_frontmatter.py (`split_frontmatter` — to parse the frontmatter from each note body when scanning)
    - sentinel-core/app/config.py (`protected_namespaces` from 40-05 — reuse the same protected set so the audit flags any original_path that started in a protected namespace)
  </read_first>
  <action>
    Create `scripts/uat_phase40_blast_radius.py` modeled on `scripts/uat_phase37.py`. It connects to the
    live Obsidian Local REST API using `OBSIDIAN_API_URL` and `OBSIDIAN_API_KEY` from the environment
    (same loading pattern as uat_phase37.py), then: (1) walks the vault listing (BFS over directory
    listings, skipping `.obsidian/`), (2) for each `.md` note reads its body and parses frontmatter via
    `split_frontmatter`, (3) collects every note carrying an `original_path` key (relocation OR trash
    provenance), (4) flags as CRITICAL any whose `original_path` starts with a protected namespace
    (import `is_protected_path` from app.vault, or inline the `sentinel/` prefix check if running outside
    the package), (5) also scans `_trash/` for trashed notes whose `original_path` was protected, (6)
    prints a structured report: a CRITICAL section listing `original_path → current_path` for protected
    files needing restore, and an INFO section listing all other relocations in the incident window for
    operator review. The script is READ-ONLY — it MUST NOT write, move, or delete any vault note; it only
    reports. Exit code 1 if any CRITICAL (protected) relocation is found, 0 otherwise, so it can gate a
    redeploy. Include a top-of-file docstring explaining it audits the Phase-40 bad-sweep blast radius and
    how to run it (`OBSIDIAN_API_URL=... OBSIDIAN_API_KEY=... python scripts/uat_phase40_blast_radius.py`).
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/sentinel-of-mnemosyne && python -c "import ast; ast.parse(open('scripts/uat_phase40_blast_radius.py').read())" && grep -c "original_path" scripts/uat_phase40_blast_radius.py</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/uat_phase40_blast_radius.py` exists, parses as valid Python (`ast.parse` succeeds), and contains `original_path` and a protected-namespace check — asserted by `grep -c "original_path" scripts/uat_phase40_blast_radius.py` >= 1 and `grep -c "is_protected_path\|sentinel/" scripts/uat_phase40_blast_radius.py` >= 1.
    - The script is read-only — `grep -c "write_note\|delete_note\|relocate\|move_to_trash\|requests.put\|requests.delete\|\.put(\|\.delete(" scripts/uat_phase40_blast_radius.py` == 0 (no mutating REST calls).
    - The script loads `OBSIDIAN_API_URL` and `OBSIDIAN_API_KEY` from the environment — `grep -c "OBSIDIAN_API" scripts/uat_phase40_blast_radius.py` >= 2.
  </acceptance_criteria>
  <done>An operator can run one read-only script that reports every file the bad sweep relocated/trashed out of a protected namespace, with original_path → current_path, exiting non-zero if any protected file is misplaced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    The 40-04 index-only startup + degradation guard and the 40-05 protected-namespace guard are merged.
    This checkpoint runs the operator-facing live verifications that were blocked by the incident: the
    blast-radius audit, the live `.json` index round-trip (UAT Test 3), and the live paraphrase recall +
    persona-survives-boot (UAT Test 2).
  </what-built>
  <how-to-verify>
    Perform these on the live stack (the `/Volumes/Mini Me` deploy checkout, which has the secrets `.env`),
    with Obsidian running + Local REST API enabled (HTTP 27123 / HTTPS 27124):

    1. **Restore + redeploy:** Confirm `sentinel/persona.md` is back at `sentinel/persona.md` (the incident
       restore). Redeploy the Phase-40 image with the 40-04/40-05 fixes.

    2. **Blast-radius audit:** From the repo root with the live env loaded, run
       `OBSIDIAN_API_URL=... OBSIDIAN_API_KEY=... python scripts/uat_phase40_blast_radius.py`.
       Expected: it lists any protected files the bad sweep moved (CRITICAL section). Restore any it flags
       using their reported `original_path`. Re-run until it exits 0 (no protected file misplaced).

    3. **Cold-start regression (the original crash):** Restart the core service. Expected: boot is clean,
       `"Startup embedding-index rebuild complete"` logs AFTER `"Application startup complete"` (still
       non-blocking), NO `RuntimeError 'sentinel/persona.md missing'`, and after boot
       `sentinel/persona.md` is STILL at `sentinel/persona.md` (boot refreshed the index only, moved
       nothing). If the embedding model is not loaded, expect the graceful WARNING and a skipped embed —
       NOT a relocation.

    4. **UAT Test 3 — live .json round-trip:** PUT a small JSON body to
       `ops/sweeps/embedding-index.json` via the Local REST API, then GET it back. Expected: PUT returns
       2xx and GET returns the same JSON body unchanged. If the API REJECTS the `.json` extension, that is
       the documented fallback trigger: switch `EMBEDDING_INDEX_PATH` (vault_sweeper.py) and
       `RecallConfig.index_path` (recall.py) to a `.md` fenced-JSON path, redeploy, and re-verify.
       Record which path (`.json` accepted, or `.md` fallback) is in effect.

    5. **UAT Test 2 — live paraphrase recall:** Trigger a sweep (admin `POST /vault/sweep/start`) so the
       index is built with the model loaded, then send a Discord message that paraphrases a vault note
       WITHOUT its exact keywords. Expected: the semantically-matching note appears in
       `GET /context/{user_id}` under `warm` (or is reflected in the answer) even though keyword-only BM25
       would miss it. Confirm `sentinel/persona.md` is still in place after the sweep (protected-namespace
       guard held during a real sweep).
  </how-to-verify>
  <resume-signal>
    Type "approved" with: (a) blast-radius audit exit code + any files restored, (b) whether the `.json`
    path round-tripped or the `.md` fallback was triggered, (c) paraphrase-recall result, and (d)
    confirmation that `sentinel/persona.md` survived both boot and a live sweep. Or describe any failure.
  </resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols)

- `scripts/uat_phase40_blast_radius.py` — operator-runnable, read-only live-vault audit reporting sweep-relocated/trashed files (original_path → current_path), exit-1 on any protected-path misplacement.
- A recorded UAT outcome for: live `.json` index round-trip (or `.md` fallback decision), live paraphrase recall, and persona-survives-boot-and-sweep.
- (Conditional) one-line constant change of `EMBEDDING_INDEX_PATH` / `RecallConfig.index_path` to `.md` IF and ONLY IF the live REST API rejects the `.json` extension (documented fallback; captured in the SUMMARY).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script → live vault (REST) | The audit reads the operator's live vault over REST. It must be strictly read-only — it must never relocate, trash, write, or delete while investigating data-loss. |
| live redeploy → operator data | Redeploying the fixed image runs the (now index-only) startup rebuild against real data; the persona must survive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-20 | Tampering / Data-loss | audit script mutating the vault while investigating | high | mitigate | Script is read-only by construction; acceptance criterion greps for zero mutating REST calls (`put`/`delete`/`write_note`/`relocate`/`move_to_trash`). |
| T-40-21 | Information Disclosure | audit dumping note bodies to logs | low | accept | Audit prints paths + original_path provenance only, not full note bodies; operator-local execution on their own vault. |
| T-40-22 | Tampering / Data-loss | redeploy re-triggering a destructive sweep on real data | high | mitigate | Gated on 40-04 (index-only startup) + 40-05 (protected guard) merged first (depends_on). Checkpoint step 3 explicitly verifies persona survives the boot. |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('scripts/uat_phase40_blast_radius.py').read())"` — script is valid Python.
- `grep -c "put(\|delete(\|write_note\|relocate\|move_to_trash" scripts/uat_phase40_blast_radius.py` == 0 — read-only.
- Human checkpoint approved with the four recorded outcomes (audit exit code, .json-vs-.md decision, paraphrase recall, persona survives).
</verification>

<success_criteria>
1. The operator has a read-only audit script that surfaces every file the bad sweep moved out of a protected namespace and exits non-zero until they are restored.
2. The live `.json` index path is confirmed to round-trip (or the `.md` fallback is applied and recorded).
3. A live paraphrase query returns the matching note in `warm`, and `sentinel/persona.md` provably survives both a boot and a live sweep after the fix.
</success_criteria>

<output>
Create `.planning/phases/40-semantic-recall/40-06-SUMMARY.md` when done.
</output>
