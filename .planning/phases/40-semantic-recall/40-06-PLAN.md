---
phase: 40-semantic-recall
plan: "06"
type: execute
wave: 3
gap_closure: true
depends_on: ["40-04", "40-05", "40-07"]
files_modified:
  - scripts/uat_phase40_blast_radius.py
autonomous: false
requirements: [MEM-03, MEM-05]
user_setup:
  - service: obsidian-local-rest-api
    why: "Blast-radius audit and UAT re-run require a live Obsidian vault + Local REST API (ports 27123/27124); the FakeVault round-trip is authoritative for the seam but the live .json path acceptance and the real relocated-file scan need the operator's vault."
    env_vars:
      - name: UAT_OBSIDIAN_URL
        source: "Existing deploy .env on /Volumes/Mini Me (live stack checkout); default http://localhost:27123"
      - name: UAT_OBSIDIAN_KEY
        source: "Existing deploy .env on /Volumes/Mini Me (live stack checkout)"
      - name: LIVE_TEST
        source: "Set LIVE_TEST=1 to arm the live audit (mirrors uat_phase37.py safety gate). WITHOUT it the script is a SILENT NO-OP — it performs NO audit, which is NOT a clean-vault result."
    dashboard_config:
      - task: "Confirm Obsidian is running with the Local REST API plugin enabled (HTTP 27123 / HTTPS 27124)"
        location: "Obsidian → Settings → Community plugins → Local REST API"

must_haves:
  truths:
    - "An operator can run one read-only script with TWO audit modes: (1) provenance scan — notes carrying original_path/topic_moved_at frontmatter whose original_path was in a protected namespace; (2) inventory/namespace-policy scan — compares the live vault against a known-good inventory / namespace policy to catch damage WITHOUT provenance (moved before provenance existed, after the v0.50.3 rollback, or deleted-not-moved)"
    - "The inventory mode's default (manifest-less) namespace-policy scan enumerates EVERY protected namespace's expected/canonical files — not just sentinel/persona.md — and flags any that are MISSING as CRITICAL; --inventory <manifest> is STRONGLY RECOMMENDED in the operator instructions for a complete diff, not framed as an optional nicety"
    - "The inventory mode explicitly scans target namespaces (e.g. learning/persona/) for persona-like or misplaced sentinel/ files and flags them even when no original_path frontmatter exists"
    - "The script supports --dry-run as an AFFIRMATION flag only (the script is read-only REGARDLESS of the flag — it is not a mode switch) and defines the 'incident window' concretely as a --since filter (a timestamp lower bound applied to topic_moved_at/sweep_at), not a vague phrase"
    - "The SUMMARY and the script help/docstring state clearly that running WITHOUT LIVE_TEST=1 is a SILENT NO-OP (no audit performed) — NOT a clean-vault pass — so an operator never mistakes 'did nothing' for 'found nothing'"
    - "After the 40-04/40-05/40-07 fixes are redeployed, a PUT then GET of the active index path round-trips a JSON body unchanged (UAT Test 3 re-enabled); the .json-vs-.md decision was already resolved+tested in 40-07, so this checkpoint only confirms the live round-trip of whichever path 40-07 selected"
    - "After redeploy, a live paraphrase query returns the semantically-matching note in RecalledContext.warm (UAT Test 2 re-run) and sentinel/persona.md is confirmed still in place across a boot AND a live sweep"
  artifacts:
    - path: "scripts/uat_phase40_blast_radius.py"
      provides: "two-mode read-only blast-radius audit (provenance scan + inventory/namespace-policy scan enumerating EVERY protected namespace's canonical files), --inventory (strongly recommended), --dry-run affirmation flag, concrete --since incident-window filter, LIVE_TEST no-op warning in docstring"
      contains: "original_path"
  key_links:
    - from: "scripts/uat_phase40_blast_radius.py"
      to: "Obsidian Local REST API"
      via: "UAT_OBSIDIAN_URL/UAT_OBSIDIAN_KEY vault listing + frontmatter read (read-only)"
      pattern: "UAT_OBSIDIAN"
    - from: "scripts/uat_phase40_blast_radius.py inventory mode"
      to: "config.protected_namespaces (40-05) canonical-file policy"
      via: "enumerate every protected namespace's expected files; flag missing ones CRITICAL"
      pattern: "protected_namespaces|is_protected_path"
---

<objective>
Close the operator-facing remediations of the Phase-40 UAT blocker, strengthened per the SECOND round of
cross-AI review (40-REVIEWS.md, item E — Codex MED on audit completeness, LM Studio LOW on the LIVE_TEST
no-op warning). The `.json`→`.md` fallback (concern 10) has been PULLED OUT of this checkpoint into its own
plan 40-07 (a real, testable, single-constant change), so this plan depends on it and only CONFIRMS the live
round-trip of whatever path 40-07 selected.

1. **Blast-radius audit (remediation #4), now TWO modes, strengthened per round-2 item E:**
   - **Provenance scan** (original): notes carrying the sweeper's relocation/trash provenance
     (`original_path` / `topic_moved_at` / trash `original_path`) whose `original_path` pointed into a
     protected namespace — reported with `original_path → current_path` for restore.
   - **Inventory / namespace-policy scan** (NEW + STRENGTHENED): provenance-only scanning misses files moved
     before provenance was written, after the v0.50.3 rollback (which doesn't write it), or
     deleted-not-moved. The default (manifest-less) namespace-policy scan now enumerates EVERY protected
     namespace's expected/canonical files (not just `sentinel/persona.md`) and flags any MISSING file as
     CRITICAL; it also scans target namespaces (e.g. `learning/persona/`) for persona-like or misplaced
     `sentinel/` files, flagging them even WITHOUT `original_path`. `--inventory <manifest>` is STRONGLY
     RECOMMENDED in the operator instructions for the most complete diff (item E) — not an optional nicety.
   - `--dry-run` is an AFFIRMATION flag only: the script is read-only regardless of it (it is NOT a mode
     switch) — the help text says so explicitly (item E). The incident window is a CONCRETE `--since
     <iso8601>` lower bound applied to `topic_moved_at`/`sweep_at`, replacing the vague phrase.
   - The script docstring AND the SUMMARY state that running WITHOUT `LIVE_TEST=1` is a SILENT NO-OP — no
     audit is performed, which is NOT a clean-vault result (item E / LM Studio).

2. **UAT Test 3 re-enable (live index round-trip, MEM-05/D-07):** After redeploy, verify a live PUT then
   GET of the ACTIVE index path round-trips an unchanged JSON body. The `.json`-accepted-vs-`.md`-fallback
   decision and its regression test now live in 40-07 — this checkpoint only confirms the live round-trip
   of the selected path and records which one 40-07 shipped.

3. **UAT Test 2 re-run (live paraphrase recall, MEM-03):** After redeploy, confirm the original blocker
   scenario works end-to-end AND that `sentinel/persona.md` survives a boot AND a live sweep (the
   protected-namespace guard from 40-05 holding under a real sweep).

Purpose: confirm the fix in the live environment and recover any collateral damage — including damage with
NO provenance, across EVERY protected namespace — from the original bad sweep. Operator-facing and gated on
a live vault, hence `autonomous: false`.
Output: `scripts/uat_phase40_blast_radius.py` (two-mode audit, full namespace-policy enumeration,
LIVE_TEST no-op warning) + a human-verify checkpoint capturing the live round-trip, paraphrase recall, and
persona-survives-boot-and-sweep outcomes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40-semantic-recall/40-UAT.md
@.planning/phases/40-semantic-recall/40-VERIFICATION.md
@.planning/phases/40-semantic-recall/40-CONTEXT.md
@.planning/phases/40-semantic-recall/40-REVIEWS.md
@sentinel-core/app/vault.py
@scripts/uat_phase37.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the two-mode blast-radius audit script (provenance + full-namespace-policy inventory)</name>
  <read_first>
    - scripts/uat_phase37.py (the existing UAT-script convention: it reads UAT_OBSIDIAN_URL/UAT_OBSIDIAN_KEY (NOT OBSIDIAN_API_*), gates on LIVE_TEST=1, builds an httpx client, lists/reads vault notes, prints a structured pass/fail report and uses exit codes 0/1 — mirror this env-loading + httpx + LIVE_TEST gate exactly. NOTE: the original 40-06 draft referenced OBSIDIAN_API_URL/KEY which do NOT match the repo convention — use UAT_OBSIDIAN_URL/KEY.)
    - sentinel-core/app/vault.py (`relocate` lines 445-488 writes `original_path` + `topic_moved_at`; `move_to_trash` lines 395-443 writes `original_path` + `reason` + `sweep_at` into `_trash/`. The provenance scan must look for BOTH shapes.)
    - sentinel-core/app/markdown_frontmatter.py (`split_frontmatter` — parse frontmatter from each note body when scanning)
    - sentinel-core/app/config.py (`protected_namespaces` from 40-05 — reuse the SAME enumerated protected set so the namespace-policy scan covers EVERY protected namespace, not just sentinel/; import `is_protected_path` from app.vault, or inline the prefix check if run outside the package)
  </read_first>
  <action>
    Create `scripts/uat_phase40_blast_radius.py` modeled on `scripts/uat_phase37.py` (UAT_OBSIDIAN_URL /
    UAT_OBSIDIAN_KEY env, LIVE_TEST=1 gate, httpx client, structured report, exit 0/1). It is STRICTLY
    READ-ONLY — it MUST NOT write, move, or delete any vault note. Provide an argparse CLI with
    `--mode {provenance,inventory,both}` (default both), `--since <iso8601>` (the concrete incident-window
    lower bound; default: no lower bound = scan all), `--inventory <path-to-json>` (a known-good manifest;
    STRONGLY RECOMMENDED for a complete diff), and `--dry-run` (read-only is the default regardless;
    --dry-run is an explicit no-op-mutation AFFIRMATION flag for operator confidence — NOT a mode switch).

    SILENT-NO-OP GUARD + DOCSTRING (item E): The top-of-file docstring MUST state, prominently, that running
    WITHOUT `LIVE_TEST=1` performs NO audit and exits as a no-op — this is NOT a clean-vault result. When
    LIVE_TEST is unset, print a clear WARNING to stderr ("LIVE_TEST not set — no audit performed; this is NOT
    a clean-vault result. Set LIVE_TEST=1 to arm.") and exit with a DISTINCT, non-success-looking signal
    (e.g. exit code 2 or an unmistakable "SKIPPED (no audit)" banner) so the operator cannot mistake it for a
    clean pass. The docstring also clarifies --dry-run is an affirmation flag (the script never mutates) and
    explains the --since window semantics and the two modes, with a runnable example
    (`LIVE_TEST=1 UAT_OBSIDIAN_URL=... UAT_OBSIDIAN_KEY=... python scripts/uat_phase40_blast_radius.py --mode both --since 2026-06-10T00:00:00Z --inventory ops/inventory.json`).

    MODE 1 — PROVENANCE SCAN: (1) BFS-walk the vault listing skipping `.obsidian/`, (2) for each `.md` note
    read body + `split_frontmatter`, (3) collect every note carrying `original_path` (relocation OR trash
    provenance), (4) apply the `--since` filter to `topic_moved_at`/`sweep_at` when provided, (5) flag as
    CRITICAL any whose `original_path` starts with a protected namespace (`is_protected_path`), (6) also
    scan `_trash/` for trashed notes whose `original_path` was protected.

    MODE 2 — INVENTORY / NAMESPACE-POLICY SCAN (concern 9 + round-2 item E — full enumeration): catch
    provenance-LESS damage across ALL protected namespaces. (1) PERSONA-LIKE / MISPLACED SCAN: explicitly
    walk target namespaces the bad sweep would relocate INTO — at minimum `learning/persona/` and the topic
    folders `topic_dir_for` can produce — and flag any file there that LOOKS like a relocated operator file:
    a filename matching known protected basenames (e.g. `persona.md`) OR a body containing persona-like
    frontmatter/markers (e.g. frontmatter `source: vault-sweep` combined with persona-shaped content, or a
    `# Persona`/identity heading). (2) FULL NAMESPACE-POLICY PRESENCE CHECK (item E — enumerate EVERY
    protected namespace, not just sentinel/persona.md): for EACH namespace in `protected_namespaces` (40-05),
    assert its expected/canonical files still EXIST and flag MISSING ones as CRITICAL (deleted-not-moved
    detection). Derive the expected canonical files from the `--inventory <manifest>` when provided
    (STRONGLY RECOMMENDED path — gives a complete per-namespace expected-file list); when absent, fall back
    to a built-in per-namespace canonical map that lists the known boot/identity-critical files for EACH
    enumerated protected namespace (at minimum `sentinel/persona.md`, plus the canonical files for the other
    enumerated namespaces such as `self/` and `security/` per 40-05's enumeration) — NOT just
    `sentinel/persona.md` alone. The fallback must iterate every protected namespace; an operator running
    without a manifest must still get a per-namespace presence check, with a printed note that `--inventory`
    yields a more complete diff.

    REPORT: a CRITICAL section (protected files needing restore, plus MISSING protected files across ALL
    namespaces, plus persona-like files found in target namespaces) listing `original_path → current_path`
    where known, and an INFO section listing all other relocations within the `--since` window for operator
    review. Exit code 1 if any CRITICAL finding (in either mode), 0 otherwise (when armed with LIVE_TEST),
    so it can gate a redeploy; exit 2 / SKIPPED banner when LIVE_TEST is unset (see the no-op guard above).
  </action>
  <verify>
    <automated>cd /Users/trekkie/projects/sentinel-of-mnemosyne && python -c "import ast; ast.parse(open('scripts/uat_phase40_blast_radius.py').read())" && python scripts/uat_phase40_blast_radius.py --help</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/uat_phase40_blast_radius.py` exists, parses as valid Python (`ast.parse` succeeds), and `--help` lists `--mode`, `--since`, `--inventory`, and `--dry-run` — asserted by the verify command exiting 0.
    - BOTH modes are present and a protected-namespace check is used in each — `grep -c "original_path" scripts/uat_phase40_blast_radius.py` >= 1 AND `grep -c "is_protected_path\|protected_namespaces\|sentinel/" scripts/uat_phase40_blast_radius.py` >= 1 AND `grep -c "learning/persona\|namespace.policy\|inventory" scripts/uat_phase40_blast_radius.py` >= 1.
    - FULL ENUMERATION (item E): the namespace-policy scan iterates EVERY protected namespace, not just sentinel/ — `grep -c "protected_namespaces\|for .* in .*protected" scripts/uat_phase40_blast_radius.py` >= 1, AND the built-in canonical fallback names files for more than one namespace (manual review of the canonical map covers self// security/ in addition to sentinel/persona.md).
    - LIVE_TEST no-op warning present (item E): `grep -c "LIVE_TEST" scripts/uat_phase40_blast_radius.py` >= 2 AND the docstring/warning text contains "no audit" or "not a clean" wording — `grep -ci "no audit\|not a clean\|no-op\|SKIPPED" scripts/uat_phase40_blast_radius.py` >= 1.
    - --dry-run is documented as an affirmation flag, not a mode switch — `grep -ci "affirmation\|read-only regardless\|not a mode" scripts/uat_phase40_blast_radius.py` >= 1.
    - The script is read-only — `grep -c "write_note\|delete_note\|relocate\|move_to_trash\|\.put(\|\.delete(\|requests.put\|requests.delete" scripts/uat_phase40_blast_radius.py` == 0 (no mutating REST calls).
    - The incident window is a concrete filter, not a phrase — `grep -c "\-\-since\|since" scripts/uat_phase40_blast_radius.py` >= 1 and the `--since` value is applied against `topic_moved_at`/`sweep_at` (visible in the code).
  </acceptance_criteria>
  <done>An operator can run one read-only, two-mode script that surfaces (a) provenance-tracked relocations/trashes out of protected namespaces AND (b) provenance-less damage across EVERY protected namespace (persona-like files in target namespaces, missing canonical files per namespace), filtered by a concrete incident window, with --inventory strongly recommended and a clear LIVE_TEST no-op warning, exiting non-zero on any CRITICAL finding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    The 40-04 index-only startup + per-move fail-closed safe-to-mutate guard, the 40-05 protected-namespace
    guard (source + destination), and the 40-07 single-constant .json/.md index-path decision (already
    tested) are merged. This checkpoint runs the operator-facing live verifications blocked by the incident:
    the two-mode blast-radius audit (with full namespace-policy enumeration and the LIVE_TEST no-op warning),
    the live index round-trip (UAT Test 3, using the path 40-07 selected), and the live paraphrase recall +
    persona-survives-boot-and-sweep (UAT Test 2).
  </what-built>
  <how-to-verify>
    Perform these on the live stack (the `/Volumes/Mini Me` deploy checkout, which has the secrets `.env`),
    with Obsidian running + Local REST API enabled (HTTP 27123 / HTTPS 27124):

    1. **Restore + redeploy:** Confirm `sentinel/persona.md` is back at `sentinel/persona.md`. Redeploy the
       Phase-40 image with the 40-04 / 40-05 / 40-07 fixes.

    2. **Blast-radius audit (both modes, with manifest):** From the repo root with the live env loaded, run
       `LIVE_TEST=1 UAT_OBSIDIAN_URL=... UAT_OBSIDIAN_KEY=... python scripts/uat_phase40_blast_radius.py --mode both --since <incident-start-iso> --inventory <known-good-manifest>`.
       (Running WITHOUT LIVE_TEST=1 performs NO audit — it is a no-op, NOT a clean result.) `--inventory` is
       strongly recommended for a complete per-namespace diff; without it you still get the built-in
       per-namespace presence check. Expected: the CRITICAL section lists any protected files the bad sweep
       moved (provenance mode), any persona-like / misplaced files in target namespaces like
       `learning/persona/`, and any MISSING protected files across EVERY protected namespace (inventory
       mode). Restore anything it flags using the reported `original_path`. Re-run until it exits 0.

    3. **Cold-start regression (the original crash):** Restart the core service. Expected: boot is clean,
       `"Startup embedding-index rebuild complete"` logs AFTER `"Application startup complete"` (still
       non-blocking), NO `RuntimeError 'sentinel/persona.md missing'`, and after boot `sentinel/persona.md`
       is STILL at `sentinel/persona.md` (boot refreshed the index only). If a model is not loaded, expect
       the graceful WARNING and a skipped embed — NOT a relocation.

    4. **UAT Test 3 — live index round-trip:** PUT a small JSON body to the ACTIVE index path (whatever
       40-07 selected — `.json` or the `.md` fenced-JSON path) via the Local REST API, then GET it back.
       Expected: PUT returns 2xx and GET returns the same JSON body unchanged. Record which path is in
       effect (this should match 40-07's tested decision; flag any divergence).

    5. **UAT Test 2 — live paraphrase recall:** Trigger a sweep (admin `POST /vault/sweep/start`, which now
       passes the per-move fail-closed safe-to-mutate probe) so the index is built with the models loaded,
       then send a Discord message that paraphrases a vault note WITHOUT its exact keywords. Expected: the
       semantically-matching note appears in `GET /context/{user_id}` under `warm` even though keyword-only
       BM25 would miss it. Confirm `sentinel/persona.md` is still in place after the sweep (protected-
       namespace guard held during a real sweep).
  </how-to-verify>
  <resume-signal>
    Type "approved" with: (a) blast-radius audit exit code + which modes flagged what + any files restored
    + whether you ran with --inventory, (b) confirmation the active index path (40-07's choice) round-tripped,
    (c) paraphrase-recall result, and (d) confirmation that `sentinel/persona.md` survived both boot and a
    live sweep. Or describe any failure.
  </resume-signal>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (NEW symbols)

- `scripts/uat_phase40_blast_radius.py` — operator-runnable, read-only, TWO-MODE live-vault audit:
  provenance scan (original_path → current_path for protected relocations/trashes) AND inventory/
  namespace-policy scan (provenance-less damage: persona-like files in target namespaces, plus a per-
  namespace canonical-file presence check across EVERY protected namespace, not just sentinel/persona.md),
  with `--mode`, `--since` incident-window filter, `--inventory` (strongly recommended manifest), and
  `--dry-run` affirmation flag; a LIVE_TEST no-op guard (distinct exit / banner when unarmed) and a docstring
  stating that an unarmed run is NOT a clean-vault result; exit-1 on any CRITICAL finding when armed.
- A recorded UAT outcome for: live index round-trip (path per 40-07), live paraphrase recall, and
  persona-survives-boot-and-sweep.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit script → live vault (REST) | The audit reads the operator's live vault over REST. It must be strictly read-only — it must never relocate, trash, write, or delete while investigating data-loss. |
| operator interpretation → audit output | An unarmed (no LIVE_TEST) run does nothing; the operator must not mistake "did nothing" for "found nothing". A distinct no-op signal + docstring warning prevents a false clean reading. |
| live redeploy → operator data | Redeploying the fixed image runs the (now index-only) startup rebuild against real data; the persona must survive boot and a live sweep. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-20 | Tampering / Data-loss | audit script mutating the vault while investigating | high | mitigate | Script is read-only by construction; acceptance criterion greps for zero mutating REST calls (`put`/`delete`/`write_note`/`relocate`/`move_to_trash`); `--dry-run` affirms no-mutation (it is an affirmation flag, not a mode switch). |
| T-40-21 | Information Disclosure | audit dumping note bodies to logs | low | accept | Audit prints paths + provenance + a persona-like flag, not full note bodies; operator-local execution on their own vault. |
| T-40-22 | Tampering / Data-loss | redeploy re-triggering a destructive sweep on real data | high | mitigate | Gated on 40-04 (index-only startup + per-move fail-closed probe) + 40-05 (protected guard) merged first (depends_on); checkpoint step 3 + 5 verify persona survives boot AND a live sweep. |
| T-40-27 | Integrity / Forensics gap | provenance-less damage going undetected | medium | mitigate | Inventory/namespace-policy scan (concern 9 + round-2 item E) catches files moved without provenance AND enumerates EVERY protected namespace's canonical files for a missing-file presence check; `--inventory` strongly recommended; checkpoint step 2 runs `--mode both`. |
| T-40-32 | Integrity / Operator misread | unarmed run mistaken for a clean-vault pass | medium | mitigate | LIVE_TEST no-op guard: a distinct non-success exit/banner + stderr warning + docstring statement that an unarmed run performs NO audit and is NOT a clean result (round-2 item E / LM Studio). Acceptance: LIVE_TEST + "no audit / not a clean" wording greps present. |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('scripts/uat_phase40_blast_radius.py').read())"` — script is valid Python.
- `python scripts/uat_phase40_blast_radius.py --help` lists `--mode`, `--since`, `--inventory`, `--dry-run`.
- `grep -c "put(\|delete(\|write_note\|relocate\|move_to_trash" scripts/uat_phase40_blast_radius.py` == 0 — read-only.
- `grep -c "protected_namespaces\|learning/persona\|inventory\|namespace" scripts/uat_phase40_blast_radius.py` >= 1 — inventory mode present + full enumeration.
- `grep -ci "no audit\|not a clean\|no-op\|SKIPPED" scripts/uat_phase40_blast_radius.py` >= 1 — LIVE_TEST no-op warning present.
- Human checkpoint approved with the recorded outcomes (audit exit code + mode findings + --inventory used, index round-trip path, paraphrase recall, persona survives boot+sweep).
</verification>

<success_criteria>
1. The operator has a read-only, two-mode audit script that surfaces both provenance-tracked AND
   provenance-less blast radius (persona-like files in target namespaces; missing canonical files across
   EVERY protected namespace, not just sentinel/persona.md), filtered by a concrete incident window, with
   `--inventory` strongly recommended and a clear LIVE_TEST no-op warning, exiting non-zero until clean
   (concern 9 + round-2 item E).
2. The live index path (per 40-07) is confirmed to round-trip a JSON body unchanged.
3. A live paraphrase query returns the matching note in `warm`, and `sentinel/persona.md` provably survives
   both a boot and a live sweep after the fix.
</success_criteria>

<output>
Create `.planning/phases/40-semantic-recall/40-06-SUMMARY.md` when done.
</output>
