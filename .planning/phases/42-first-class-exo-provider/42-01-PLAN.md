---
phase: 42-first-class-exo-provider
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentinel-core/app/config.py
  - sentinel-core/tests/test_config.py
  - sentinel-core/app/services/provider_router.py
  - sentinel-core/tests/test_provider_router.py
  - sentinel-core/app/state.py
  - sentinel-core/app/composition.py
autonomous: true
requirements: [SC-1, SC-3, SC-6]
must_haves:
  truths:
    - "exo_base_url / exo_model / exo_api_key load from EXO_* env independently of lmstudio_* (SC-1, D-03)"
    - "ai_provider accepts 'exo'; ai_fallback_provider accepts any configured provider name, not just claude/none (D-04, D-05)"
    - "ProviderRouter falls back on litellm.NotFoundError alongside ConnectError/TimeoutException (SC-3, D-06)"
    - "RouteContext exposes the ProviderRouter itself (ai_provider) so a narrow completion route can reach it (D-09 prerequisite)"
  artifacts:
    - sentinel-core/app/config.py (exo_* Settings fields + secret_map entry)
    - sentinel-core/app/services/provider_router.py (_FALLBACK_TRIGGERS + litellm.NotFoundError)
    - sentinel-core/app/state.py (RouteContext.ai_provider field)
  key_links:
    - "litellm.NotFoundError goes ONLY into _FALLBACK_TRIGGERS, never into litellm_provider.py _RETRYABLE"
    - "RouteContext.ai_provider pinned in initialize_startup() from the already-built graph.ai_provider"
---

<objective>
Lay the three leaf prerequisites that unblock both Wave-2 workstreams: (1) dedicated exo configuration so exo and LM Studio coexist (SC-1, D-03/D-04); (2) generalized fallback semantics — `litellm.NotFoundError`/404 as a fallback trigger and `ai_fallback_provider` accepting any configured provider (SC-3, D-05/D-06); (3) expose the constructed `ProviderRouter` on `RouteContext` so the Wave-2 completion endpoint can reach it (D-09 prerequisite).

Purpose: These are the foundation edits every later plan consumes. Nothing here changes provider *selection* logic (that is 42-02) — only configuration surface, the router's error-trigger tuple, and route-context exposure.
Output: exo_* Settings fields, the NotFoundError fallback trigger, and RouteContext.ai_provider.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-first-class-exo-provider/42-CONTEXT.md
@.planning/phases/42-first-class-exo-provider/42-RESEARCH.md
@.planning/phases/42-first-class-exo-provider/42-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated exo Settings fields + fallback-config generalization (SC-1, D-03/D-04/D-05)</name>
  <files>sentinel-core/app/config.py, sentinel-core/tests/test_config.py</files>
  <read_first>
    - sentinel-core/app/config.py — the `Settings` class; the `ollama_base_url`/`ollama_model` and `llamacpp_base_url`/`llamacpp_model` triplets (PATTERNS.md lines 31-56 cite these at ~lines 60-69), the `ai_provider`/`ai_fallback_provider` fields (~lines 53-54), and `load_secrets()`/`secret_map` (~lines 89-98)
    - sentinel-core/tests/test_config.py — existing Settings field-parse test shape to extend
    - .planning/phases/42-first-class-exo-provider/42-PATTERNS.md — section "sentinel-core/app/config.py — new exo_* fields"
  </read_first>
  <behavior>
    - Setting EXO_BASE_URL / EXO_MODEL / EXO_API_KEY in env populates settings.exo_base_url / exo_model / exo_api_key without touching lmstudio_base_url or model_name.
    - exo_model default is the empty string (blank = auto-discover via /state in 42-02); there is NO hardcoded exo model id anywhere (regression guard for the debug-time default).
    - AI_PROVIDER=exo parses (its comment enum lists exo); AI_FALLBACK_PROVIDER=lmstudio parses as a valid fallback provider name (not restricted to claude/none).
    - If exo_api_key is Docker-secret-backed, load_secrets() reads it the same way it reads lmstudio_api_key/anthropic_api_key.
  </behavior>
  <action>
    Add three fields to `Settings` following the exact ollama/llamacpp triplet shape: `exo_base_url` (str, default `http://host.docker.internal:52415/v1`), `exo_model` (str, default empty string — blank means auto-discover via GET /state; explicitly NOT a hardcoded model id, per D-07/D-08), and `exo_api_key` (str, default empty string). Update the `ai_provider` field comment to list `exo` as a valid value (per D-04). Update the `ai_fallback_provider` field comment from `claude | none` to reflect that any configured provider name is accepted (per D-05) — no code change to the field type is required if it is already a plain str; the generalization of how the value is consumed happens in 42-02's composition change. Add `"exo_api_key": "exo_api_key"` to the `secret_map` dict in `load_secrets()` so exo's key follows the existing Docker-secrets pattern (`_read_secret`). Do NOT log or echo the api_key value. Extend `tests/test_config.py` with exo-field parse cases (env → field) plus an assertion that `exo_model` default is empty (no hardcoded model).
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_config.py -k exo -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd sentinel-core && pytest tests/test_config.py -k exo -x` passes.
    - Source assertion: `grep -n "exo_base_url\|exo_model\|exo_api_key" sentinel-core/app/config.py` shows all three fields defined.
    - Source assertion: the exo_model default is an empty string literal (no model id such as a `mlx-community/...` string in config.py).
    - Source assertion: `grep -n "exo_api_key" sentinel-core/app/config.py` shows the field appears in the `secret_map` used by `load_secrets()`.
  </acceptance_criteria>
  <done>exo_base_url/exo_model/exo_api_key parse from EXO_* env independently of lmstudio_*; exo_model has no hardcoded default; exo_api_key is Docker-secret-eligible; config comments reflect exo as an ai_provider value and any-provider fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add litellm.NotFoundError as a ProviderRouter fallback trigger (SC-3, D-06)</name>
  <files>sentinel-core/app/services/provider_router.py, sentinel-core/tests/test_provider_router.py</files>
  <read_first>
    - sentinel-core/app/services/provider_router.py — the `_FALLBACK_TRIGGERS` tuple (~lines 14-21) and `ProviderRouter.complete()`'s `except _FALLBACK_TRIGGERS` block (~lines 60-71)
    - sentinel-core/app/clients/litellm_provider.py — the `_RETRYABLE` tuple (~lines 28-33) and the existing `from litellm import BadRequestError` top-level import convention; NotFoundError must NOT be added here
    - sentinel-core/tests/test_provider_router.py — existing `test_falls_back_on_connect_error` / `test_falls_back_on_timeout` and `test_raises_unavailable_with_no_fallback`
    - .planning/phases/42-first-class-exo-provider/42-RESEARCH.md — Code Examples section 1 (verified litellm.NotFoundError identity/MRO); .planning/phases/42-first-class-exo-provider/42-PATTERNS.md — provider_router.py section
  </read_first>
  <behavior>
    - When the primary provider raises litellm.NotFoundError, ProviderRouter.complete() invokes the fallback and returns its result (mirrors the existing ConnectError/TimeoutException behavior).
    - When the primary raises litellm.NotFoundError and no fallback is configured, ProviderRouter raises ProviderUnavailableError (mirrors test_raises_unavailable_with_no_fallback).
    - litellm.NotFoundError is a fallback trigger only — it is NOT added to litellm_provider.py's retryable set (a 404 is not transient).
  </behavior>
  <action>
    Import litellm at the top of `provider_router.py` (top-level `import litellm`, matching the module's existing style precedent `from litellm import BadRequestError` in litellm_provider.py). Add `litellm.NotFoundError` as a third member of the `_FALLBACK_TRIGGERS` tuple alongside `httpx.ConnectError` and `httpx.TimeoutException`, per D-06 (exo's real failure mode is a 404, so ConnectError-only fallback would never fire for it). Update the module/tuple docstring to say "connectivity failures + model-not-served". Make NO change to `litellm_provider.py`'s retryable tuple — the 404 exception class is deliberately not retryable and must stay out of that file. Extend `tests/test_provider_router.py` with a `test_falls_back_on_not_found_error` case (primary raises `litellm.NotFoundError`, assert fallback returned + fallback awaited once) and a "not-found + no fallback raises ProviderUnavailableError" case, both following the existing ConnectError/no-fallback test structure.
    <!-- planner-discipline-allow: NotFoundError -->
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_provider_router.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd sentinel-core && pytest tests/test_provider_router.py -x` passes, including a NotFoundError fallback case and a NotFoundError-no-fallback-raises case.
    - Source assertion: `grep -n "NotFoundError" sentinel-core/app/services/provider_router.py` shows it inside `_FALLBACK_TRIGGERS`.
    - Source assertion (comment-filtered so a stray comment cannot self-invalidate): `grep -vn '^\s*#' sentinel-core/app/clients/litellm_provider.py | grep -c "NotFoundError"` returns 0 (the 404 class is not added to the retryable tuple).
  </acceptance_criteria>
  <done>ProviderRouter falls back on litellm.NotFoundError and raises ProviderUnavailableError when no fallback exists; the trigger is never treated as retryable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose the ProviderRouter on RouteContext (D-09 endpoint prerequisite)</name>
  <files>sentinel-core/app/state.py, sentinel-core/app/composition.py, sentinel-core/tests/test_composition.py</files>
  <read_first>
    - sentinel-core/app/state.py — the `RouteContext` frozen dataclass and its existing `ai_provider_name: str | None = None` field (~line 59)
    - sentinel-core/app/composition.py — `initialize_startup()` (~lines 401-413) where RouteContext fields are pinned, and the line where `graph.ai_provider` (the ProviderRouter) already exists (~line 381)
    - .planning/phases/42-first-class-exo-provider/42-PATTERNS.md — the `sentinel-core/app/state.py — RouteContext.ai_provider field` section
  </read_first>
  <behavior>
    - RouteContext has a new optional field `ai_provider: "ProviderRouter | None" = None` distinct from the existing `ai_provider_name` string.
    - After initialize_startup() runs, the RouteContext seen by route handlers has ai_provider set to the same ProviderRouter instance as graph.ai_provider.
    - Existing RouteContext consumers are unaffected (the field is additive with a None default; the frozen dataclass still constructs from prior call sites).
  </behavior>
  <action>
    Add an `ai_provider: "ProviderRouter | None" = None` field to the `RouteContext` frozen dataclass in `state.py`, mirroring the existing `ai_provider_name` field pattern (forward-referenced type string to avoid an import cycle). In `composition.py`'s `initialize_startup()`, pin the new field from the already-constructed `graph.ai_provider` (the ProviderRouter built at ~composition.py:381) alongside the other RouteContext fields — this is a one-line addition that references an existing object and does NOT depend on any exo/registry change. Extend `tests/test_composition.py` (or the appropriate existing startup/state test) with an assertion that the RouteContext produced by `initialize_startup()` has `ai_provider` set to the built router instance. Do not touch `build_provider_router()` in this plan — that function is generalized separately in 42-02.
  </action>
  <verify>
    <automated>cd sentinel-core && pytest tests/test_composition.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd sentinel-core && pytest tests/test_composition.py -x` passes.
    - Source assertion: `grep -n "ai_provider:" sentinel-core/app/state.py` shows the new `ai_provider: "ProviderRouter | None"` field.
    - Source assertion: `grep -n "ai_provider" sentinel-core/app/composition.py` shows it pinned in `initialize_startup()`.
    - Behavior assertion: a test proves `RouteContext.ai_provider is graph.ai_provider` after startup.
  </acceptance_criteria>
  <done>RouteContext exposes the ProviderRouter instance (not just its name), pinned from graph.ai_provider in initialize_startup(); existing consumers unchanged.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 42-01)

New symbols / surfaces introduced by this plan:
- `Settings.exo_base_url`, `Settings.exo_model`, `Settings.exo_api_key` (sentinel-core/app/config.py)
- `exo_api_key` entry in `load_secrets()` `secret_map`
- `litellm.NotFoundError` added to `_FALLBACK_TRIGGERS` (sentinel-core/app/services/provider_router.py)
- `RouteContext.ai_provider: ProviderRouter | None` field (sentinel-core/app/state.py) + its pin in `initialize_startup()`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env / Docker secrets → Settings | `exo_api_key` / `lmstudio_api_key` loaded via `_read_secret` (`/run/secrets/*`) |
| sentinel-core → provider fallback path | `ProviderRouter` decides when to route primary→fallback based on `_FALLBACK_TRIGGERS` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Information Disclosure | `Settings.exo_api_key` handling | medium | mitigate | Load via `secret_map`/`_read_secret` (Docker secrets), same as anthropic_api_key; never log/echo the key value; do not add it to any debug/repr output |
| T-42-02 | Tampering | `_FALLBACK_TRIGGERS` vs `_RETRYABLE` misplacement | low | mitigate | NotFoundError added ONLY to `_FALLBACK_TRIGGERS`; test asserts fallback (not retry) fires; `grep -c NotFoundError litellm_provider.py == 0` |
| T-42-SC | Tampering | pip installs | low | accept | No new packages this phase (RESEARCH.md Package Legitimacy Audit: N/A — litellm/httpx/tenacity already pinned); no install task exists |
</threat_model>

<verification>
- `cd sentinel-core && pytest tests/test_config.py tests/test_provider_router.py tests/test_composition.py -x` all green.
- Full sentinel-core suite unaffected: `cd sentinel-core && pytest -q`.
</verification>

<success_criteria>
- SC-1: exo configured through EXO_* env vars, independent of LMSTUDIO_*.
- SC-3 (partial): NotFoundError is now a fallback trigger; ai_fallback_provider config accepts any provider (selection wiring completes in 42-02).
- D-06 satisfied; D-09 prerequisite (RouteContext.ai_provider) in place.
</success_criteria>

<output>
Create `.planning/phases/42-first-class-exo-provider/42-01-SUMMARY.md` when done.
</output>
