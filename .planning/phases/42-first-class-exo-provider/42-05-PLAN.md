---
phase: 42-first-class-exo-provider
plan: 05
type: execute
wave: 4
depends_on: [42-04]
files_modified:
  - modules/pathfinder/app/foundry.py
  - modules/pathfinder/app/pf_npc_extract.py
  - modules/pathfinder/app/config.py
  - modules/pathfinder/tests/test_foundry.py
  - modules/pathfinder/tests/test_pf_npc_extract.py
autonomous: true
requirements: [SC-5, SC-6]
must_haves:
  truths:
    - "foundry.py roll-narration and pf_npc_extract.py archive-import chat calls reach the LLM via core_client.complete(), not litellm directly (SC-6, D-09)"
    - "pf2e's chat-only litellm config (litellm_model + chat model overrides) is removed; no module hardcodes a chat endpoint or model id (SC-6)"
    - "the embeddings/rules-index path is untouched: litellm_api_base and rules_embedding_model retained; it still degrades gracefully (503) where exo lacks /v1/embeddings (SC-5, Phase 43 scope)"
    - "both sentinel-core and pf2e full suites pass — including the LM Studio construction regression pin (D-02) and the embeddings graceful-503 path"
  artifacts:
    - modules/pathfinder/app/foundry.py (migrated roll-narration call site)
    - modules/pathfinder/app/pf_npc_extract.py (migrated archive-import call site)
    - modules/pathfinder/app/config.py (chat-only litellm fields removed; embeddings config retained)
  key_links:
    - "config removal happens only after ALL chat call sites (llm.py in 42-04 + foundry/pf_npc_extract here) are migrated"
    - "litellm_api_base is referenced by the embeddings/rules-model config (config.py lines 34/40) — it is RETAINED for Phase 43"
---

<objective>
Finish the pf2e chat handoff (D-09) and close the phase: migrate the last chat/completion call sites (`foundry.py` roll-narration, `pf_npc_extract.py` archive-import), remove pf2e's now-dead chat-only litellm configuration so no module hardcodes a chat endpoint/model (SC-6), and run the phase-wide regression gate proving the LM Studio path (D-02) and the embeddings graceful-503 path (SC-5) are intact.

Purpose: This is the finish-line plan. After 42-04 moved llm.py, only foundry.py and pf_npc_extract.py remain on direct litellm for chat. With those migrated, pf2e's chat model config (`litellm_model` and the per-task chat overrides) is dead and is removed — but the embeddings config (`litellm_api_base`, `rules_embedding_model`) stays because embeddings is Phase 43. The gate ensures the whole two-service system is green.
Output: the last migrated call sites, the config cleanup, and a green phase regression gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-first-class-exo-provider/42-CONTEXT.md
@.planning/phases/42-first-class-exo-provider/42-RESEARCH.md
@.planning/phases/42-first-class-exo-provider/42-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate foundry.py + pf_npc_extract.py chat call sites to core_client.complete() (SC-6, D-09)</name>
  <files>modules/pathfinder/app/foundry.py, modules/pathfinder/app/pf_npc_extract.py, modules/pathfinder/tests/test_foundry.py, modules/pathfinder/tests/test_pf_npc_extract.py</files>
  <read_first>
    - modules/pathfinder/app/foundry.py — the roll-narration composer chat call site(s) (grep shows 3 `acompletion_with_profile`/`litellm.acompletion` hits: import + ~2 calls) and the existing short-lived `async with httpx.AsyncClient(timeout=5.0)` pattern (line 115)
    - modules/pathfinder/app/pf_npc_extract.py — the archive-import NPC-extraction chat call site(s) (3 grep hits: import + ~2 calls); its existing JSON-parse/salvage
    - modules/pathfinder/app/llm.py — the SentinelCoreClient wiring convention established in 42-04 (reuse the same client-construction + httpx pattern)
    - shared/sentinel_client.py — `SentinelCoreClient.complete(messages, client, stop, temperature) -> dict`
    - modules/pathfinder/tests/test_foundry.py, modules/pathfinder/tests/test_pf_npc_extract.py — existing test shapes to extend (patch the core client call)
  </read_first>
  <behavior>
    - foundry.py's roll-narration composer and pf_npc_extract.py's archive-import extraction obtain their completion from core_client.complete(messages=..., client=..., stop=..., temperature=...) and read result["content"], passing no model/api_base.
    - Each site's surrounding logic (roll narration formatting; NPC-extraction JSON salvage) is preserved; only the LLM-invocation + content-extraction lines change.
    - The direct-litellm chat import (`acompletion_with_profile`) is removed from both files once their chat sites are migrated.
    - complete() raising on error still triggers each site's existing error handling.
  </behavior>
  <action>
    Migrate foundry.py's roll-narration chat call site(s) and pf_npc_extract.py's archive-import extraction call site(s) from `acompletion_with_profile(model=..., api_base=..., messages=[...], ...)` to `result = await core_client.complete(messages=[...], client=<client>, stop=..., temperature=...)` then `content = result["content"]`, reusing the exact SentinelCoreClient construction + httpx.AsyncClient convention 42-04 established in llm.py (client from settings.sentinel_core_url + sentinel_api_key; foundry.py already has the short-lived-client pattern at line 115). Drop `model=`/`api_base=` at these sites. Preserve all surrounding formatting/salvage. Remove the `acompletion_with_profile` import from both files once no chat call site remains in each. Extend test_foundry.py and test_pf_npc_extract.py to patch `SentinelCoreClient.complete` and assert the migrated sites call it and consume result["content"], plus a complete()-raises case per file proving existing error handling fires.
    <!-- planner-discipline-allow: acompletion_with_profile -->
  </action>
  <verify>
    <automated>cd modules/pathfinder && pytest tests/test_foundry.py tests/test_pf_npc_extract.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `cd modules/pathfinder && pytest tests/test_foundry.py tests/test_pf_npc_extract.py -x` passes.
    - Negative gate (comment-filtered): `grep -vn '^\s*#' modules/pathfinder/app/foundry.py modules/pathfinder/app/pf_npc_extract.py | grep -c 'acompletion_with_profile('` returns 0.
    - Source assertion: `grep -n "core_client.complete(" modules/pathfinder/app/foundry.py modules/pathfinder/app/pf_npc_extract.py` shows the migrated calls.
    - Phase-wide gate: `grep -rvn '^\s*#' modules/pathfinder/app/ | grep -c 'acompletion_with_profile('` returns 0 (ALL ~13 pf2e chat sites — llm.py + foundry.py + pf_npc_extract.py — now migrated).
  </acceptance_criteria>
  <done>The last pf2e chat call sites reach the LLM through core; no pf2e app module retains a direct-litellm chat call.</done>
</task>

<task type="auto">
  <name>Task 2: Remove pf2e chat-only litellm config + phase-wide regression gate (SC-5, SC-6, D-02)</name>
  <files>modules/pathfinder/app/config.py, modules/pathfinder/tests/test_pf_npc_extract.py</files>
  <read_first>
    - modules/pathfinder/app/config.py — the chat-only fields to remove: `litellm_model` (line 24), `litellm_model_chat`/`litellm_model_structured`/`litellm_model_fast` (lines 29-31), `session_recap_model` (line 49), `foundry_narration_model` (line 52); AND the embeddings-adjacent fields to RETAIN: `litellm_api_base` (line 25 — referenced by the embeddings/rules-model config, see the comments at lines 34/40) and `rules_embedding_model` / any embedding config
    - modules/pathfinder/app/model_selector.py / resolve_model.py — pf2e's own chat model selection (drift); confirm no non-embedding code still reads the removed chat fields before deleting them
    - .planning/phases/42-first-class-exo-provider/42-CONTEXT.md — D-09 (remove pf2e's direct chat litellm config + hardcoded model default) and the explicit embeddings-out-of-scope boundary
    - .planning/phases/42-first-class-exo-provider/42-RESEARCH.md — State of the Art "Deprecated/outdated" note (litellm_model/litellm_api_base chat fields removed; rules_embedding_model + embedding config UNCHANGED)
  </read_first>
  <behavior>
    - After removal, config.py has no `litellm_model` chat default and no chat-task model-override fields; the embeddings config (`litellm_api_base`, `rules_embedding_model`) remains so the rules-index/embeddings path is unchanged.
    - No remaining pf2e code references a removed field (verified before deletion); the embeddings/rules-index path still builds-or-degrades gracefully (503) exactly as before.
    - The full pf2e suite and the full sentinel-core suite both pass — including the LM Studio construction regression pin from 42-02 (D-02) and the embeddings graceful-503 tests (SC-5).
  </behavior>
  <action>
    Remove the chat-only litellm config from pf2e config.py — `litellm_model`, `litellm_model_chat`, `litellm_model_structured`, `litellm_model_fast`, `session_recap_model`, `foundry_narration_model` — ONLY after grep-confirming no remaining non-embedding code reads each (chat sites were migrated in 42-04 + Task 1). RETAIN `litellm_api_base`, `rules_embedding_model`, and all embedding-related config (Phase 43): the embeddings path still reads `litellm_api_base` per the config comments at lines 34/40 — do NOT remove it, and do NOT touch `_build_rules_index_safely()` or `embed_texts`. If any removed field is still referenced by pf2e's `model_selector.py`/`resolve_model.py` for chat, delete that dead chat-resolution path too; if referenced by embeddings, retain it. Update any config-reading test that asserted the removed fields. Then run the phase-wide regression gate: full sentinel-core suite AND full pf2e suite green, confirming the LM Studio construction pin (D-02, 42-02) and the embeddings graceful-503 path (SC-5) are intact.
  </action>
  <verify>
    <automated>cd modules/pathfinder && pytest -q && cd ../../sentinel-core && pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd modules/pathfinder && pytest -q` and `cd sentinel-core && pytest -q` both pass (full suites, both services).
    - Source assertion: `grep -c 'litellm_model\b' modules/pathfinder/app/config.py` returns 0 for the chat model default (the `litellm_model` field is gone).
    - Source assertion: `grep -c 'litellm_api_base' modules/pathfinder/app/config.py` is >= 1 (embeddings config retained) AND `grep -c 'rules_embedding_model' modules/pathfinder/app/config.py` is >= 1.
    - Behavior assertion: the embeddings/rules-index graceful-503 test(s) remain green (SC-5 unchanged).
    - Regression: sentinel-core `test_composition.py` LM Studio construction-args pin still green (D-02).
  </acceptance_criteria>
  <done>pf2e's chat-only litellm config is gone; embeddings config retained; both full suites green including the LM Studio regression pin and the embeddings graceful-503 path.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 42-05)

New symbols / surfaces + removals introduced by this plan:
- Migrated `core_client.complete()` call sites in `modules/pathfinder/app/foundry.py` and `modules/pathfinder/app/pf_npc_extract.py`
- REMOVED from `modules/pathfinder/app/config.py`: `litellm_model`, `litellm_model_chat`, `litellm_model_structured`, `litellm_model_fast`, `session_recap_model`, `foundry_narration_model` (chat-only)
- RETAINED (Phase 43): `litellm_api_base`, `rules_embedding_model`, embeddings config
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pf2e-module foundry.py / pf_npc_extract.py → sentinel-core `POST /provider/complete` | Cross-service HTTP via SentinelCoreClient; authenticated with `X-Sentinel-Key` |
| operator env → pf2e Settings | Chat model config removed; embeddings config (`litellm_api_base`) retained for Phase 43 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-11 | Tampering | removing `litellm_api_base` would break the embeddings/rules-index path | medium | mitigate | Explicitly RETAIN `litellm_api_base` + `rules_embedding_model` (embeddings reads them, config.py lines 34/40); full pf2e suite (incl. embeddings graceful-503) green gates the removal |
| T-42-12 | Spoofing | foundry/pf_npc_extract trusting a malformed core response | medium | mitigate | SentinelCoreClient.complete() raises on error (post_to_module posture); migrated sites read result["content"] and route failures through existing per-site handling |
| T-42-SC | Tampering | pip installs | low | accept | No new packages this phase (RESEARCH.md Package Legitimacy Audit: N/A); no install task exists |
</threat_model>

<verification>
- Task 1: `cd modules/pathfinder && pytest tests/test_foundry.py tests/test_pf_npc_extract.py -x` green; phase-wide `acompletion_with_profile(` count == 0 across modules/pathfinder/app/ (all ~13 chat sites migrated).
- Task 2 (phase gate): `cd modules/pathfinder && pytest -q` AND `cd sentinel-core && pytest -q` both green — LM Studio construction pin (D-02) + embeddings graceful-503 (SC-5) intact.
</verification>

<success_criteria>
- SC-5: embeddings-dependent paths still degrade gracefully; the exo/openai path uses the openai/ prefix (verified in 42-02).
- SC-6: both sentinel-core and pf2e resolve provider/model through unified config — no module hardcodes a chat endpoint or model id.
- D-09 complete: ALL ~13 pf2e chat/completion call sites migrated; embeddings deferred to Phase 43.
</success_criteria>

<output>
Create `.planning/phases/42-first-class-exo-provider/42-05-SUMMARY.md` when done.
</output>
