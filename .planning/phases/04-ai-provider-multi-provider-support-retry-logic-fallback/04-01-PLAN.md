---
phase: 04-ai-provider-multi-provider-support-retry-logic-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sentinel-core/pyproject.toml
  - sentinel-core/models-seed.json
  - sentinel-core/app/config.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02

must_haves:
  truths:
    - "All provider URLs and API keys are loaded from env vars — no hardcoded endpoints"
    - "pyproject.toml lists litellm>=1.83.0, tenacity, anthropic as dependencies"
    - "models-seed.json exists with Qwen 2.5 14B, Claude Haiku, and Claude Sonnet entries"
    - "Settings class has AI_PROVIDER, AI_FALLBACK_PROVIDER, and all provider-specific env vars"
  artifacts:
    - path: "sentinel-core/models-seed.json"
      provides: "Static model registry seed — context window sizes and capability flags"
      contains: "qwen, claude-haiku, claude-sonnet"
    - path: "sentinel-core/app/config.py"
      provides: "Extended Settings with all provider env vars"
      exports: ["settings"]
  key_links:
    - from: "sentinel-core/app/config.py"
      to: "sentinel-core/pyproject.toml"
      via: "litellm, tenacity, anthropic must be importable at runtime"
      pattern: "litellm>=1.83.0"
---

<objective>
Add AI provider dependencies and extend Settings with all provider configuration env vars. Create the models-seed.json seed file for the model registry.

Purpose: Every subsequent plan in this phase depends on the new env vars being defined in Settings and the libraries being installed. This is the foundation.

Output: Updated pyproject.toml (new deps), extended config.py (new Settings fields), new models-seed.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ai-provider-multi-provider-support-retry-logic-fallback/04-CONTEXT.md
</context>

<interfaces>
<!-- Current Settings class signature (from sentinel-core/app/config.py) -->
```python
class Settings(BaseSettings):
    lmstudio_base_url: str = "http://host.docker.internal:1234/v1"
    pi_harness_url: str = "http://pi-harness:3000"
    sentinel_api_key: str  # Required — no default
    model_name: str = "local-model"
    log_level: str = "INFO"
    obsidian_api_url: str = "http://host.docker.internal:27123"
    obsidian_api_key: str = ""
    lmstudio_num_ctx: int = 8192

    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}

settings = Settings()
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add provider dependencies to pyproject.toml</name>
  <files>sentinel-core/pyproject.toml</files>
  <read_first>
    - sentinel-core/pyproject.toml (current deps to understand what already exists)
  </read_first>
  <action>
Add three new dependencies to the `dependencies` list in pyproject.toml:

```
"litellm>=1.83.0,<2.0",
"tenacity>=8.2.0,<10.0",
"anthropic>=0.93.0,<1.0",
```

CRITICAL — version constraint rationale (must be documented in a comment in pyproject.toml):
- litellm: versions 1.82.7 and 1.82.8 were malicious supply-chain releases (March 2026). Pin >=1.83.0 to skip both bad versions and all prior 1.82.x releases. Upper bound <2.0 prevents breaking changes.
- anthropic: >=0.93.0 for Messages API v2 compatibility. <1.0 prevents breaking changes.
- tenacity: >=8.2.0 for async retry support. <10.0 for stability.

Add the comment block immediately above the three new entries:
```
# AI provider libraries — pinned per supply-chain incident (LiteLLM 1.82.7-1.82.8 were malicious, March 2026)
```

Do NOT add these to [project.optional-dependencies] dev — they are runtime dependencies.
  </action>
  <verify>
    <automated>grep -E "litellm>=1.83.0|tenacity>=8.2.0|anthropic>=0.93.0" sentinel-core/pyproject.toml | wc -l | grep -q "^3$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sentinel-core/pyproject.toml contains `"litellm>=1.83.0,<2.0",`
    - sentinel-core/pyproject.toml contains `"tenacity>=8.2.0,<10.0",`
    - sentinel-core/pyproject.toml contains `"anthropic>=0.93.0,<1.0",`
    - sentinel-core/pyproject.toml contains the supply-chain incident comment
    - All three are under `[project]` `dependencies`, not under `[project.optional-dependencies]`
  </acceptance_criteria>
  <done>pyproject.toml has all three new runtime dependencies with correct version bounds and the supply-chain comment.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend Settings with provider env vars and create models-seed.json</name>
  <files>
    sentinel-core/app/config.py
    sentinel-core/models-seed.json
  </files>
  <read_first>
    - sentinel-core/app/config.py (current Settings — must preserve all existing fields exactly)
  </read_first>
  <action>
**Part A — Update sentinel-core/app/config.py:**

Add the following fields to the Settings class, after the existing `lmstudio_num_ctx` field. Preserve all existing fields verbatim — do not remove or rename anything.

```python
# AI provider selection (PROV-01, PROV-02)
ai_provider: str = "lmstudio"  # lmstudio | claude | ollama | llamacpp
ai_fallback_provider: str = "none"  # claude | none

# Claude / Anthropic (PROV-02)
anthropic_api_key: str = ""  # blank = Claude provider disabled
claude_model: str = "claude-haiku-4-5"  # runtime configurable

# Ollama (stub — Linux workstation LAN)
ollama_base_url: str = "http://localhost:11434"
ollama_model: str = "qwen2.5:14b"

# llama.cpp (stub — OpenAI-compatible server)
llamacpp_base_url: str = "http://localhost:8080"
llamacpp_model: str = "local-model"
```

Note: `lmstudio_base_url` and `model_name` already exist in Settings — they serve as the LM Studio config. Do not duplicate them.

**Part B — Create sentinel-core/models-seed.json:**

Create this file with exactly this content (no trailing newline issues — valid JSON):

```json
{
  "models": [
    {
      "id": "qwen2.5:14b",
      "provider": "ollama",
      "context_window": 32768,
      "capabilities": {
        "chat": true,
        "function_calling": false,
        "vision": false
      },
      "notes": "Qwen 2.5 14B Q4_K_M — ~10-11GB VRAM, Nvidia A2000 workstation"
    },
    {
      "id": "claude-haiku-4-5",
      "provider": "claude",
      "context_window": 200000,
      "capabilities": {
        "chat": true,
        "function_calling": true,
        "vision": true
      },
      "notes": "Claude Haiku 4.5 — fast and cheap, default cloud provider"
    },
    {
      "id": "claude-sonnet-4-5",
      "provider": "claude",
      "context_window": 200000,
      "capabilities": {
        "chat": true,
        "function_calling": true,
        "vision": true
      },
      "notes": "Claude Sonnet 4.5 — stronger reasoning, higher cost"
    },
    {
      "id": "claude-sonnet-4-6",
      "provider": "claude",
      "context_window": 200000,
      "capabilities": {
        "chat": true,
        "function_calling": true,
        "vision": true
      },
      "notes": "Claude Sonnet 4.6 — strongest reasoning in haiku/sonnet tier"
    },
    {
      "id": "local-model",
      "provider": "lmstudio",
      "context_window": 8192,
      "capabilities": {
        "chat": true,
        "function_calling": false,
        "vision": false
      },
      "notes": "LM Studio default model — actual context window fetched at startup"
    }
  ]
}
```
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('sentinel-core/models-seed.json')); ids=[m['id'] for m in d['models']]; assert 'qwen2.5:14b' in ids and 'claude-haiku-4-5' in ids and 'claude-sonnet-4-5' in ids, 'missing model'; print('PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - sentinel-core/app/config.py contains `ai_provider: str = "lmstudio"`
    - sentinel-core/app/config.py contains `ai_fallback_provider: str = "none"`
    - sentinel-core/app/config.py contains `anthropic_api_key: str = ""`
    - sentinel-core/app/config.py contains `claude_model: str = "claude-haiku-4-5"`
    - sentinel-core/app/config.py contains `ollama_base_url: str`
    - sentinel-core/app/config.py contains `llamacpp_base_url: str`
    - All existing fields (lmstudio_base_url, pi_harness_url, sentinel_api_key, model_name, etc.) are unchanged
    - sentinel-core/models-seed.json is valid JSON (python3 -c "import json; json.load(open('sentinel-core/models-seed.json'))" exits 0)
    - models-seed.json contains entries for qwen2.5:14b, claude-haiku-4-5, claude-sonnet-4-5, claude-sonnet-4-6, local-model
    - Each model entry has: id, provider, context_window, capabilities object, notes
  </acceptance_criteria>
  <done>Settings has all provider env vars with correct defaults. models-seed.json is valid JSON with 5 model entries covering all supported providers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → config | Provider API keys (ANTHROPIC_API_KEY) enter from environment — must never be logged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | settings.anthropic_api_key | mitigate | Never log Settings object directly; anthropic_api_key has empty default so startup doesn't fail without it; no repr/str of key values |
| T-04-02 | Tampering | models-seed.json | accept | Static file in repo, no runtime write path, low-value target for this personal tool |
| T-04-03 | Supply Chain | litellm dependency | mitigate | Version pinned >=1.83.0 skipping known-malicious 1.82.7-1.82.8 releases; upper bound <2.0 prevents silent breaking upgrades |
</threat_model>

<verification>
After both tasks:
1. `python3 -c "import json; json.load(open('sentinel-core/models-seed.json')); print('JSON valid')"` — exits 0
2. `grep -c "ai_provider\|ai_fallback_provider\|anthropic_api_key\|claude_model\|ollama_base_url\|llamacpp_base_url" sentinel-core/app/config.py` — outputs 6
3. `grep -E "litellm>=1.83.0|tenacity>=8.2.0|anthropic>=0.93.0" sentinel-core/pyproject.toml | wc -l` — outputs 3
</verification>

<success_criteria>
- pyproject.toml has litellm, tenacity, anthropic with correct version pins and supply-chain comment
- Settings has ai_provider, ai_fallback_provider, anthropic_api_key, claude_model, ollama_base_url, ollama_model, llamacpp_base_url, llamacpp_model — all with safe defaults
- models-seed.json is valid JSON with 5 model entries
- No existing Settings fields removed or renamed
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-provider-multi-provider-support-retry-logic-fallback/04-01-SUMMARY.md`
</output>
